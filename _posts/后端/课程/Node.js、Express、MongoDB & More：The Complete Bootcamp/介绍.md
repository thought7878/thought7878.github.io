# What you'll learn  你会学到什么

- Master the entire modern back-end stack: Node, Express, MongoDB and Mongoose (MongoDB JS driver)  
掌握整个现代后端开发技术栈：Node.js、Express、MongoDB 以及 Mongoose（MongoDB 的 JavaScript 驱动程序）。

- Build a complete, beautiful & real-world application from start to finish (API and server-side rendered website)  
从头到尾完整地构建出一个完整、美观且符合实际需求的应用程序（包括 API 接口和服务器端渲染的网站）。

- Build a fast, scalable, feature-rich RESTful API (includes filters, sorts, pagination, and much more)  
请构建一个快速、可扩展且功能丰富的 RESTful API。该 API 包括过滤、排序、分页等功能，非常适合各种应用场景。

- Learn how Node really works behind the scenes: event loop, blocking vs non-blocking code, streams, modules, etc.  
了解 Node 在幕后是如何工作的：事件循环、阻塞与非阻塞代码、流处理、模块等等。

- CRUD operations with MongoDB and Mongoose  
使用 MongoDB 和 Mongoose 进行 CRUD 操作

- Deep dive into mongoose (including all advanced features)  
深入了解猫狗兽（包括所有高级功能）的相关信息

- How to work with data in NoSQL databases (including geospatial data)  
如何操作 NoSQL 数据库中的数据（包括地理空间数据）

- Advanced authentication and authorization (including password reset)  
高级认证与授权功能（包括密码重置）

- Security: encryption, sanitization, rate limiting, etc.  
安全性：加密处理、数据清理、速率限制等机制。

- Server-side website rendering with Pug templates  
使用 Pug 模板进行服务器端网站渲染

- Credit card payments with Stripe  
使用 Stripe 进行信用卡支付

- Sending emails & uploading files  
发送电子邮件并上传文件

- Deploy the final application to production (including a Git crash-course)  
将最终版本的应用程序部署到生产环境（包括相关的 Git 故障排除教程）

- Downloadable videos, code and design assets for projects  
可下载的视频、代码和设计素材，适用于各种项目使用。


# This course includes:  本课程包含以下内容：

- 42 hours on-demand video  42 小时的点播视频播放时间
- 3 articles  3 篇文章

# 目录
15 sections • 229 lectures • 42h 12m total length  
15 个章节 • 229 次讲座 • 总时长 42 小时 12 分钟

##   1.欢迎，欢迎，欢迎！  
3 次讲座 • 每次 13 分钟

### 课程结构与项目安排
Course Structure and Projects  
6:45

Build modern back-end applications with Node.js, Express, and MongoDB through a four-part bootcamp, featuring a Natours project with RESTful API and server-side rendered website, authentication, and deployment.  
通过为期四部分的培训课程，使用 Node.js、Express 和 MongoDB 构建现代化的后端应用程序。课程内容包括一个具有 RESTful API 的 Natours 项目实践、服务器端渲染的网站、身份验证机制以及应用程序的部署。

### 在开始之前请务必阅读这部分内容！
READ BEFORE YOU START!  0:35

### 安装 Node.js
Let's Install Node.js  让我们来安装 Node.js 吧。
5:24

Install node.js on macOS, Windows, and Linux by downloading from nodejs.org, choose the lts version (10 in this course) or the current version (12), and verify with node -v.  
可以通过访问 nodejs.org 下载适用于 macOS、Windows 和 Linux 的 Node.js 安装程序。选择 LTS 版本（本教程中使用的是 10 版本）或当前版本（12 版本）。然后使用 node -v 命令来验证安装是否成功。

##  2.**Node.js 和 NPM 简介**  
20 次讲座 • 3 小时 20 分钟

### 章节介绍
Section Intro  0:54

Kick off the Node.js bootcamp by writing Node.js code quickly, using core modules to build a small, fun project, with later sections exploring deeper concepts.  
让我们快速开始 Node.js 编程训练吧！首先使用核心模块编写一个小型有趣的项目。在后续章节中，我们将深入探讨更深的概念。

### **什么是 Node.js？为什么应该使用它？**  
What Is Node.js and Why Use It?  
7:38

Discover what Node.js is, a JavaScript runtime built on the V8 engine for server-side use. Explore its event-driven, non-blocking I/O model for fast, scalable APIs and back-end apps with MongoDB.  
了解什么是 Node.js 吧，它是一种基于 V8 引擎的 JavaScript 运行环境，适用于服务器端应用。其事件驱动、非阻塞的 I/O 模型使得它能够构建出快速且可扩展的 API 和后端应用程序，尤其是与 MongoDB 结合使用时。

### 在浏览器外部运行 JavaScript 代码
Running Javascript Outside the Browser  
6:27

Learn to run JavaScript outside the browser by using Node.js in the terminal, start the Node REPL, and explore ES6 syntax, Node modules, and handy REPL shortcuts in VS Code.  
学习如何在终端中使用 Node.js 在浏览器之外运行 JavaScript 代码。启动 Node REPL，然后探索 ES6 语法、Node 模块，以及 VS Code 中方便使用的 REPL 快捷键。

### 使用模块 1：核心模块
Using Modules 1: Core Modules  
5:59

Learn to run your first Node.js script, require core modules like fs, and explore Node.js documentation to read files from disk using read file sync in future lessons.  
学习如何运行你的第一个 Node.js 脚本，了解如何使用核心模块如 fs，并在后续的课程中学习如何利用 Node.js 文档来读取磁盘上的文件，以及如何在需要时将文件同步到磁盘。

### 读取和写入文件
Reading and Writing Files  
7:28

Learn to read from input.txt and write to output.txt in node.js using the fs module, with readFileSync and writeFileSync, utf8 encoding, and template strings.  
使用 Node.js 中的 fs 模块，学习如何从 input.txt 中读取数据并写入到 output.txt 中。需要使用 readFileSync 和 writeFileSync 函数来实现这一操作。同时，需要注意使用 utf8 编码方式来处理文本数据，并且可以利用模板字符串来格式化输出结果。

### **阻塞与非阻塞**：Node.js 的异步特性
Blocking and Non-Blocking: Asynchronous Nature of Node.js  
10:13

Explore the asynchronous nature of Node.js, contrasting synchronous blocking code with non-blocking asynchronous code using callbacks and readFile. Understand the single-thread model and how to avoid callback hell.  
了解 Node.js 的异步特性，对比同步阻塞代码与利用回调函数的非阻塞异步代码。理解单线程模型，以及如何避免回调地狱现象。

### 异步读取和写入文件
Reading and Writing Files Asynchronously  
14:16

Learn to read and write files asynchronously in Node.js using the fs module, callbacks, and the error-first pattern, including reading start.txt, read this.txt, and append.txt, then writing a final file.  
学习在 Node.js 中使用 fs 模块异步读取和写入文件。使用回调机制，并采用以错误为优先的处理方式。包括读取 start.txt、read_this.txt 和 append.txt 文件，然后写入一个最终的文件。

### 创建一个简单的 **Web 服务器**
Creating a Simple Web Server  
  12:49

Create a simple web server in Node.js using the http module's createServer, handling requests and sending responses with res.end, and listen on port 8000.  
使用 Node.js 中的 http 模块，创建一个简单的 Web 服务器。通过 createServer 方法来处理请求，并使用 res.end 方法发送响应。然后监听 8000 端口。

### **路由**
Routing  
15:18

Develop a basic Node.js routing pattern by inspecting req.url, mapping paths like /overview and /product to specific responses, handling root and unknown routes with 404 status and content-type headers.  
通过检查 req.url 来开发一个基本的 Node.js 路由模式。将诸如 /overview 和 /product 这样的路径映射到特定的响应，同时处理根路径以及未知路径，对于未知路径则使用 404 状态码和相应的内容类型头。

### 构建一个非常**简单的 API**
Building a (Very) Simple API  
14:28

Build a simple web API in Node.js that serves product data from a JSON file at /api. Load the data once at startup with a synchronous read and return application/json.  
在 Node.js 中构建一个简单的 Web API，该 API 可以在/api 路径提供 JSON 格式的产品数据。通过同步读取数据来初始化应用程序，并返回 application/json 格式的数据。

### HTML 模板制作：构建模板
HTML Templating: Building the Templates  
14:03

design and implement templates for product detail and overview pages, using json data to populate names, images, origin, nutrients, quantity, price, and description, with inlined css and dynamic product cards.  
设计和实现产品详情页和概述页的模板，使用 JSON 数据来填充名称、图片、来源、营养成分、数量、价格以及描述等信息。同时，会加入内嵌的 CSS 代码和动态化的产品卡片设计。

### HTML 模板：**填充模板内容**
HTML Templating: Filling the Templates  
20:45

Preload HTML templates into memory and replace placeholders with product data to render the overview page with dynamic product cards.  
将 HTML 模板预加载到内存中，用产品数据替换占位符，从而以动态的产品卡片形式呈现概述页面。

### **从 URL 中解析变量**
Parsing Variables from URLs  
10:23

Learn to parse variables from the URL using the Node.js URL module, convert the query to an object, extract id and pathname, and render a dynamic product page.  
学习如何*使用 Node.js 的 `URL 模块`从 URL 中解析变量，将查询参数转换为对象，提取 id 和路径名，然后动态生成产品页面*。

### 使用模块 2：我们自己的模块
Using Modules 2: Our Own Modules  
6:34

Create a custom node module by exporting a function with module.exports in modules/replaceTemplate.js, then import it in index.js with require, demonstrating that Node.js treats each file as a module.  
可以通过在 modules/replaceTemplate.js 文件中导出函数，并将该函数赋值给 module.exports 来创建一个自定义节点模块。然后，在 index.js 文件中通过 require 语句导入该模块，这样就能展示出 *Node.js 是将每个文件都视为一个模块*的特性。

### 关于 NPM 和 package.json 文件的介绍
Introduction to NPM and the package.json File  
5:17

Learn how npm, the node package manager, handles installing and managing open-source packages from the npm registry, and how the package.json file configures your Node.js project.  
了解 `npm`（Node.js 包管理器）是*如何处理从 npm 注册表中安装和管理开源包的*，以及 *package.json 文件是如何配置你的 Node.js 项目的*。

### Packages类型与安装方式
Types of Packages and Installs  
13:39

Learn to distinguish regular dependencies and development dependencies, install them locally or globally with npm, use npm scripts, and see how nodemon and slugify populate package.json and enable global tools.  
学会区分*常规依赖项*和*开发依赖项*，通过 npm 进行*本地或全局安装*它们，使用 npm 脚本，并了解 nodemon 和 slugify 如何填充 package.json 文件，以及*如何实现全局工具的使用*。

### 使用第三方模块 3：第三方模块
Using Modules 3: 3rd Party Modules  
7:55

Learn how to require third-party modules from the npm registry, install and use slugify to generate url slugs, and integrate a new package as a project dependency.  
了解如何从 npm 仓库中引入第三方模块，安装并使用 slugify 函数来生成 URL 路径，以及如何将新的包作为项目的依赖项进行集成。

### 软件包版本控制与更新
Package Versioning and Updating  
11:45

Master semantic versioning in npm, including major, minor, and patch changes and their backward compatibility. Learn to update, install, and uninstall packages, and share code with package.json and package-lock.json.  
掌握 npm 中的`语义版本控制`，包括*主版本、次版本和补丁版本*的变更及其向后兼容性。学习如何更新、安装和卸载包，以及通过 package.json 和 package-lock.json 共享代码。

### 在 VS Code 中设置 Prettier
Setting up Prettier in VS Code  
11:27

Install and configure the Prettier extension in VS Code, enable format on save, and use a prettierrc file to enforce single quotes and consistent formatting across a team.  
在 VS Code 中安装并配置 Prettier 扩展程序，启用“保存时格式化”功能，并使用一个 prettierrc 文件来强制使用单引号标记字符，并*确保整个团队中的格式保持一致*。

### **回顾与后续计划**
Recap and What's Next  
2:57

Review the section's progress by recalling file operations, module imports, and building a web server with routing and templating; look ahead to backend theory, Node.js internals, and Express next.  
通过**回顾文件操作、模块导入过程**，以及**构建支持路由和模板渲染的 Web 服务器**来了解一下该部分的进展。*接下来将探讨后端开发原理、Node.js 的内部机制，以及 Express 框架的相关内容*。

## 3.**后端开发的整体理解**
后端网页开发入门  
5 次讲座 • 35 分钟

### 章节介绍
Section Intro  
0:46

Learn back end development fundamentals that underpin Node.js, Express, and databases. Understand how the web works, the request-response model, HTTP and TCP/IP, static versus dynamic websites, and APIs.  
**学习支撑 Node.js、Express、数据库技术的后端开发基础知识。了解网页的工作原理、请求与响应模型、HTTP 和 TCP/IP 协议、静态网站与动态网站的区别、API 的相关知识**。

### 关于**网络工作原理**的概述
An Overview of How the Web Works  
13:28

Explore how the browser sends HTTP requests, uses DNS to resolve domains, and retrieves pages. See how the initial HTML triggers loading of assets and page rendering.  
**了解浏览器是如何发送 HTTP 请求的，如何利用 DNS 解析域名，如何获取网页内容的**。同时，**了解初始的 HTML 代码是如何触发资源加载和页面渲染的**。

### HTTP 实践
HTTP in Action  
5:29

Explore http in action by inspecting browser dev tools' network tab: observe requests and responses, disable cache, and review headers, cookies, and assets like css, images, and js.  
通过查看浏览器*开发工具中的网络选项卡*，可以了解 http 服务的实际运作情况：*观察请求的发送和响应的接收过程*，禁用缓存，并*查看头部信息、Cookie 以及 CSS、图片、JavaScript 等资产文件*。

### **前端开发与后端开发**
Front-End vs. Back-End Web Development  
6:02

Explore front-end and back-end distinctions in web development and the client-server workflow. See how HTML, CSS, and JavaScript power the front end, while Node.js and MongoDB drive the back end.  
了解网页开发中的*前端和后端区别*，以及*客户端-服务器工作流程*。了解 *HTML、CSS 和 JavaScript 如何驱动前端开发*，而 *Node.js 和 MongoDB 则负责后端处理*。

### **静态网站 vs 动态网站 vs API网站**
Static vs Dynamic vs API  
9:15

Clarify static versus dynamic websites and API-powered sites, contrast server-side rendering with client-side rendering, and show how Node.js powers APIs delivering JSON data across clients.  
明确区分*静态网站与动态网站的区别*，以及*基于 API 的网站类型*。同时，*对比服务器端渲染和客户端渲染的优缺点*。此外，*说明 Node.js 如何驱动 API 工作，使 API 能够向客户端发送 JSON 数据*。

## 4.**Node.js 的工作原理**  
11 次讲座 • 1 小时 46 分钟

### 章节介绍
Section Intro  
1:24

Explore how Node.js works behind the scenes, covering architecture, event loop, streams, and modules. Apply theory with code to gain a competitive edge as a Node developer.  
**了解 Node.js 在幕后是如何工作的**，包括**其架构、事件循环、流处理、模块机制**等方面。通过实践代码来应用相关理论，从而成为具有竞争力的 Node.js 开发者。

### Node, V8, Libuv and C++
3:47

Explore Node architecture by examining core dependencies such as the V8 engine and libuv, and how C++ and JavaScript enable the event loop and thread pool for server-side code.  
通过研究 **`核心依赖组件`，如 V8 引擎和 libuv** 等，来**了解 `Node 的架构设计`**。同时，**了解 C++和 JavaScript 如何共同实现事件循环和线程池**，从而*为服务器端代码提供高效的支持*。

[[2.Node, V8, Libuv and C++]]

### 进程、线程和线程池
Processes, Threads and the Thread Pool  
4:21

Explore how Node runs on a single thread and how the libuv provided thread pool offloads heavy tasks—files, cryptography, compression, and DNS lookups—keeping the event loop responsive.  
**了解 Node 是如何在单线程上运行的**，以及 **libuv 提供的线程池如何分担那些耗时的任务**——比如*文件处理、密码学运算、压缩操作、DNS 查询*等任务——**从而保持事件循环的高效运转**。

[[3.进程、线程和线程池]]

### Node.js 的事件循环
The Node.js Event Loop  
11:33

The event loop is the heart of the Node.js architecture, orchestrating callback-driven work and offloading tasks to the thread pool. It manages timers, I/O, and microtask queues to prevent blocking.  
`事件循环`是 **Node.js 架构的核心部分，它负责协调基于回调的工作流程，并将任务分配到线程池中处理**。它还管理**计时器、I/O 操作、微任务队列，*以避免出现阻塞现象***。

[[4.Node.js 的事件循环]]

### 实践中的事件循环
The Event Loop in Practice  
18:50

Explore the event loop in practice by coding setTimeout, setImmediate, and file I/O to observe execution order, process.nextTick, microtasks, and thread-pool effects.  
通过编写代码来实际体验事件循环的工作原理，例如使用 setTimeout、setImmediate 来处理异步任务，以及利用文件 I/O 来观察执行顺序。同时，还可以了解 process.nextTick、微任务以及线程池机制的作用。

### 事件与事件驱动架构
Events and Event-Driven Architecture  
4:18

Explore Node.js event-driven architecture with event emitters and listeners, using the HTTP module to handle requests, and apply the observer pattern for decoupled, multi-listener reactions.  
探索使用 Node.js 的事件驱动架构，了解事件发射器和监听器的工作原理。利用 HTTP 模块来处理请求，同时应用观察者模式来实现模块化、多监听器的反应机制。

### 实践中的事件
Events in Practice  
13:54

Master Node.js event emitters and listeners through practical examples, including newSale events and multiple listeners. Extend EventEmitter with ES6 classes and observe event-driven http server behavior.  
通过实际案例来讲解 Node.js 中事件发射器和监听器的使用，包括 newSale 事件以及多个监听器的作用。利用 ES6 的类扩展 EventEmitter，从而观察事件驱动的 HTTP 服务器的行为。

###  流简介
Introduction to Streams 
6:17

Explore how streams process data in chunks to improve memory and speed, covering readable, writable, duplex, and transform streams, with practical piping and event handling in Node.js.  
了解流式处理如何将数据分块处理，以优化内存使用效率和处理速度。内容包括可读取、可写入的流类型，双工流以及转换流等概念。还会介绍在 Node.js 中使用管道技术以及事件处理的实践方法。

### 实践中的流
Streams in Practice    
16:06

Explore streaming large files in node by comparing readFile, readStream with res.write, and the pipe operator to handle backpressure.  
通过对比`readFile`和`readStream`函数，以及`res.write`方法，还可以使用管道操作符来处理背压问题，从而了解在 Node 中如何流式处理大文件。

### 那些需要模块的功能究竟是如何运作的？
How Requiring Modules Really Works  
9:39

Explore how Node.js resolves and loads modules using core, developer, and npm modules, and how require wraps, exports, and caches module code.  
了解 Node.js 是如何解析和加载模块的，包括使用核心模块、开发者模块以及 npm 模块的方式。同时，也会介绍 require 函数如何包装、导出并缓存模块代码。

### 实践中所需的模块
Requiring Modules in Practice  
15:51

Explore how Node.js wraps module code in a wrapper function, export and import patterns using module.exports and exports, and how caching affects repeated requires.  
探索 Node.js 如何将模块代码包装在一个包装函数中，使用 module.exports 和 exports 的导出和导入模式，以及缓存如何影响重复的 requires。

## 5.[可选] 异步 JavaScript：Promise 与异步/等待机制  
7 次讲座 • 1 小时 2 分钟

### 章节介绍
Section Intro  
0:57

Learn how modern Node.js handles asynchronous code with promises and async/await, moving beyond callbacks, and get up to speed on these essential tools for this course.  
了解现代 Node.js 是*如何通过 Promise 和 async/await 来处理异步代码的，这种方式比使用回调方式更为高效*。*掌握这些对于开发来说至关重要的工具*，是本课程的核心内容。

### 回调问题：回调地狱
The Problem with Callbacks: Callback Hell  
12:46

Explore how excessive callbacks lead to callback hell in Node.js, illustrated by nested readfile and http requests. Learn how promises and async/await improve readability and maintainability.  
了解在 Node.js 中，*过多的回调是如何导致“回调地狱”现象的*，通过嵌套的 readfile 操作以及 HTTP 请求来加以说明。**了解 Promise 和 async/await 如何提升代码的可读性和可维护性**。

### 从“回调地狱”到Promise
From Callback Hell to Promises  
6:42

Learn to escape callback hell by using promises with the super agent library get method. Understand pending, fulfilled, and rejected states, and master then and catch for clean, chained code.  
学会如何利用承诺机制，结合 super agent 库的 get 方法，*摆脱回调地狱的困扰*。*理解 pending、fulfilled 和 rejected 三种状态*，掌握如何编写干净、链式化的代码，避免回调的混乱。

### Building Promises
建立承诺
14:42

Learn how to promisify read file and write file, return promises, and chain then and catch to manage async flow with ES6 promises.  
了解如何*使用 ES6 的 Promise 功能来改进读取文件和写入文件的操作，实现异步操作的链式处理*，以及如何在异步流程中处理异常。

### Consuming Promises with Async/Await  
使用异步/等待机制来执行承诺
7:48

Master async/await to replace promise then-handlers and write cleaner promise-based code. Use async functions, await expressions, and try-catch error handling for robust API calls and file operations.  
*掌握异步/等待技术，以替代传统的 Promise 处理方式，从而编写更简洁的基于 Promise 的代码*。使用异步函数、等待表达式以及 *try-catch 错误处理机制*，可以构建更强大的 API 调用和文件操作功能。

### 异步函数的返回值
Returning Values from Async Functions  
13:03

Explore how async functions return promises, how to retrieve their values with await or then, and improve error handling using try/catch and IIFE patterns in Node.js.  
了解异步函数如何返回 Promise 对象，如何使用 await 或 then 来获取它们的返回值，以及如何通过 try/catch 机制以及 Node.js 中的 `IIFE` 模式来改进错误处理。

### 同时等待多个 Promise
Waiting for Multiple Promises Simultaneously  
6:11

Learn to run multiple promises in parallel with Promise.all to fetch three random dog images, map responses to messages, and assemble a final output.  
学习如何使用 `Promise.all` 并行运行多个 Promise 来获取三个随机狗图片，将响应映射为消息，并组装最终输出。

## 6.Express（构建 Natours API）
22 场讲座 • 3 小时 24 分钟

### 章节介绍
Section Intro  
0:56

Start building your API with Express in Node.js, the popular framework that speeds development and simplifies writing Node.js code for your complete bootcamp project.  
*开始使用 Node.js 中的 Express 框架*来构建你的 API 吧。这是一个*非常流行的框架*，**能够加速开发过程，并简化 Node.js 代码的编写，从而让你的完整项目开发更加高效**。

### **什么是 Express？**
What is Express?  
2:01

Explore Express, a minimal node.js framework built on top of node.js, offering complex routing, request and response handling, middleware, server-side rendering, and MVC-friendly structure to speed up node.js development.  
`Express` 是**一个基于 Node.js 构建的轻量级框架**，它**提供了复杂的路由处理、请求与响应处理、中间件功能、服务器端渲染**等功能。该框架**采用了适合 MVC 架构的结构设计，能够显著提升 Node.js 开发的效率**。

![[_posts/后端/课程/Node.js、Express、MongoDB & More：The Complete Bootcamp/media/2f598e5a05e5ce2008dac069ff7c139c_MD5.webp]]


### [实践] 安装 Postman
Installing Postman
3:25

Install Postman to test APIs and explore http methods like get, post, put, and patch. Save requests, create collections, run tests, and manage environments for api development.  
*安装 Postman 来测试 API*，了解 GET、POST、PUT 和 PATCH 等 HTTP 方法的使用。*保存请求、创建集合、运行测试*，以及*管理 API 开发所需的环境设置*。

### 配置 Express 基本路由功能
Setting up Express and Basic Routing  
14:47

Set up express and create a simple server to learn basic routing for the natours project, using app.get and post, with json responses and rest-style api testing.  
请*设置一个快速响应的简单服务器*，以*学习 Natours 项目中的基本路由处理技术*。通过*使用 app.get 和 post 方法*，结合 JSON 响应格式，以及 REST 风格的 API 测试，来掌握相关技能。

### **API 和 RESTful 设计**
APIs and RESTful API Design  
16:07

Learn RESTful API design by structuring resources, using resource-based URLs, and applying HTTP methods (GET, POST, PUT, PATCH, DELETE) for CRUD, with JSON and JSEND, in stateless APIs.  
**学习 RESTful API 设计方法**，包括*如何构建资源结构、使用基于资源的 URL 地址、运用 HTTP 方法*（GET、POST、PUT、PATCH、DELETE）*来实现 CRUD 操作*，以及*如何在无状态 API 中使用 JSON 和 JSEND 进行通信*。

[[1.API 和 RESTful 设计]]

![[_posts/后端/课程/Node.js、Express、MongoDB & More：The Complete Bootcamp/media/6f67a54cb505b5c465f612d17141e345_MD5.webp]]

### [实践] 启动我们的 API：
#### 处理 GET 请求
Starting Our API: Handling GET Requests  
12:19

Start building the API by creating a GET endpoint for tours, reading data from a json file, and returning a JSend-formatted response with data and results.  
开始构建 API 吧！先*创建一个 GET 端点，用于获取旅游相关信息*，从 JSON 文件中读取数据，然后返回包含数据和结果的 JSend 格式响应。

#### 处理 POST 请求
Handling POST Requests
15:21

Implement a post route with express.json middleware to parse req.body, create a new id from last tour via Object.assign, merge it with body, and save to json file with 201.  
*实现一个 POST 路由，创建行程*，在中间件中*使用 express.json() 中间件来解析请求体的数据*。通过 Object.assign 方法从上次旅行中获取新的 ID，将其与请求体数据合并，然后*以 201 状态码将结果保存为 JSON 文件*。

#### 响应 URL 参数
Responding to URL Parameters  
11:48

Learn how to define route parameters in Express URLs, read them with req.params, and return a single tour by id or a 404 error when the id is invalid.  
了解*如何在 Express 的 URL 中定义路由参数（路径中的变量），如何通过 `req.params` 获取这些参数值*，以及*当参数无效时如何返回相应的旅游信息或提示 404 错误*。

#### 处理 PATCH 请求
Handling PATCH Requests  
7:05

Explore handling patch requests in an Express app to update only changed fields via api/v1/tours/:id, compare patch with put, and return appropriate 200, 404 responses.  
在 Express 应用中*处理patch请求*，仅通过 api/v1/tours/:id 来更新已更改的字段。*将补丁与 put 请求进行比较*，并返回相应的 200 或 404 响应。

#### 处理 DELETE 请求
Handling DELETE Requests  
2:40

Change the route to handle delete with an id, return 204 no content with null data, and verify via Postman that the response shows no content.  
请修改路由设置，以便能够*使用 ID 来处理删除操作*。当发生删除操作时，*应返回 204 状态码表示无内容*，并且通过 Postman 验证确认响应中确实显示无内容。

#### 重构路由
Refactoring Our Routes  
6:47

Refactor routes by exporting handler functions and grouping routes for better readability. Use app.route with chained get, post, patch, and delete to preserve functionality while keeping a clear structure.  
*通过导出处理函数并将路由进行分组，来重构路由结构*，**以提高代码的可读性**。*使用`app.route`结合链式调用的`get`、`post`、`patch`和`delete`功能*，可以在*保持结构清晰*的同时保留原有的功能。

### **Express 原理/中间件/请求-响应循环：**
#### **中间件**、**请求-响应循环**
Middleware and the Request-Response Cycle  
4:48

Explore how Express uses a middleware stack to handle the request-response cycle. Middleware processes request and response objects in code-defined order, advancing with next until the route sends the response.  
了解 Express 是**如何利用中间件堆栈来处理请求与响应的循环的**。`中间件`**按照代码中定义的顺序处理请求和响应对象，直到路由模块发送响应为止，才会继续处理下一个请求**。

[[2.中间件、请求-响应循环]]

#### 创建自己的中间件
Creating Our Own Middleware  
9:05

Create and test custom middleware in Express using app.use, access req, res, and next, and add request time to the request object, while understanding how middleware order affects route handling.  
在 Express 中*创建并测试自定义中间件*。**使用 app.use 函数来注册中间件**，然后*通过 req、res 和 next 参数来处理请求*。同时需要注意，**中间件的执行顺序会影响路由的处理方式**。

[[3.创建自己的中间件]]

#### 使用第三方中间件
Using 3rd-Party Middleware  
9:35

install and configure morgan, a popular logging middleware, to log request data in the console using the dev preset, and add it as the first middleware in express.  
*安装并配置流行的`日志中间件 morgan`*，使用 dev 预设*将请求数据记录到控制台*，并将其*作为 express 中的第一个中间件*添加。

### [实践] 实施“用户”路由功能
Implementing the "Users" Routes  
6:57

Implement the users resource routes under /api/v1/users, including get all users, get user, create user, update user, and delete with placeholders. Plan to separate routes into controllers.  
实现/api/v1/Users 下的用户资源相关路由功能，包括*查询所有用户、获取特定用户信息、创建用户、更新用户信息、删除用户*等操作。建议*将相关路由分配到不同的控制器中处理*。

### **组织多个路由**

#### 创建并挂载多个路由器
Creating and Mounting Multiple Routers  
8:04

Learn to create and mount multiple express routers, separating routes and handlers into resource-specific files, and attach tour and user routers as middleware.  
学习如何*创建和挂载多个 express 路由器*，**将路由和处理器分离到特定资源的文件中**，并*将 tour 和 user 路由作为中间件附加*。

#### **更合理的文件结构**
A Better File Structure  
18:20

Refactor your Node.js app by modularizing routers and controllers, embrace the model-view-controller pattern, and wire routers via app.js, then start the server with server.js and npm start.  
**通过模块化、路由器、控制器来重构**你的 Node.js 应用程序，**采用模型-视图-控制器模式**。*将路由逻辑整合到 app.js 文件中*，然后使用 server.js 和 npm start 命令启动服务器。

### 参数中间件
Param Middleware  
10:54

Explore param middleware that runs for a specific url parameter, add a checkID middleware to validate ids, and streamline routes by placing validation in the Express middleware stack.  
探索那些*针对特定 URL 参数运行的参数中间件*；添加 checkID 中间件来验证 ID 信息；通过将验证逻辑放在 Express 中间件栈中，从而**简化路由配置**。

### 链接多个中间件功能
Chaining Multiple Middleware Functions  
7:34

Chain multiple middleware functions for a route by adding a checkBody middleware before createTour to validate name and price; return 400 bad request if missing, otherwise proceed to createTour.  
为了对某个路由路径应用多个中间件函数，可以在 createTour 之前添加 checkBody 中间件来验证名称和价格字段。如果缺少这些字段，则返回 400 错误请求；否则，将继续执行 createTour 的操作。

### 提供静态文件服务
Serving Static Files  
5:27

Learn to serve static files with express using the express.static middleware and a public folder, enabling access to overview.html, images, css, and javascript via the browser.  
通过使用 express 的 static 中间件，可以学习如何提供静态文件服务。此外，通过设置公共文件夹，就能通过浏览器访问 overview.html、图片、CSS 和 JavaScript 等文件。

### 环境变量
Environment Variables  
16:39

Explore how environment variables define development and production contexts, configure via .env and dotenv, and control behavior such as login, debugging, and database selection in node.js and express apps.  
了解环境变量如何定义开发和生产环境配置，如何通过 .env 和 dotenv 文件进行配置。同时，了解如何在 Node.js 和 Express 应用中控制登录、调试以及数据库选择等行为。

### 在 VS Code 中配置 ESLint 和 Prettier 工具
Setting up ESLint + Prettier in VS Code  
13:33

Learn to set up eslint and prettier in VS Code to improve code quality, integrate the Airbnb style guide, and configure plugins and rules for Node.js and Express.  
学习如何在 VS Code 中配置 eslint 和 prettier，以提高代码质量。同时，了解 Airbnb 的风格指南，并配置适用于 Node.js 和 Express 的插件和规则。

## 7.MongoDB 简介  
13 次讲座 • 1 小时 21 分钟

### 章节介绍
Section Intro  
0:48

Explore the popular MongoDB database system by installing MongoDB, creating databases, inserting data, and querying data in multiple ways in this section of the complete bootcamp.  
在完整的培训课程中，你将通过安装 MongoDB、创建数据库、插入数据以及多种方式查询数据等方式，深入了解这款流行的 MongoDB 数据库系统。

### 什么是 MongoDB？
What is MongoDB?  
8:44

Explore MongoDB, a NoSQL document database that offers scalability and flexibility, storing data in collections of JSON-like documents with embedded data and de-normalization, contrasting with relational databases.  
探索 MongoDB，一种 NoSQL 文档数据库，它具有出色的可扩展性和灵活性。该数据库将数据存储在类似 JSON 的文档集合中，并支持嵌入式数据和非规范化存储方式，这与关系型数据库有所不同。

### 无需在本地安装 MongoDB 数据库
No Need to Install MongoDB Locally  
0:20

### [可选] 在 macOS 上安装 MongoDB
[OPTIONAL] Installing MongoDB on macOS  
6:34

Install MongoDB on macOS by downloading and extracting the community server, copying binaries to /usr/local/bin, starting mongod, and connecting with the mongo shell, noting Atlas cloud options.  
在 macOS 上安装 MongoDB，步骤如下：首先下载并解压社区版服务器，将二进制文件复制到 /usr/local/bin 目录下，然后启动 mongod 服务器，最后通过 mongo shell 连接服务器，同时注意设置 Atlas 云环境的选项。

### [可选] 在 Windows 操作系统上安装 MongoDB
[OPTIONAL] Installing MongoDB on Windows  
10:02

Learn how to install MongoDB on Windows, including the MongoDB Community Server. Install MongoDB Compass, create data and db directories, run mongod, and add MongoDB to the system path.  
了解如何在 Windows 操作系统上安装 MongoDB，包括使用 MongoDB 社区版服务器。安装 MongoDB Compass 工具，创建数据文件和数据库目录，运行 mongod 命令行工具，以及将 MongoDB 添加到系统路径中。

### [可选] 创建本地数据库
[OPTIONAL] Creating a Local Database  
7:39

Create your first local MongoDB database with the Mongo Shell, switch databases with use, and insert documents into a tours collection using insertOne or insertMany.  
使用 Mongo Shell 创建你的第一个本地 MongoDB 数据库。可以通过使用`use`命令切换数据库，然后使用`insertOne`或`insertMany`方法将文档插入到`tours`集合中。

### [可选] CRUD：创建文档
[OPTIONAL] CRUD: Creating Documents  
4:17

Learn how to create multiple MongoDB documents with insertMany, adding fields like name, price, rating, and optional difficulty, and verify with find in the tours collection.  
了解如何使用 insertMany 方法创建多个 MongoDB 文档，添加诸如名称、价格、评分以及可选的难度等字段，然后通过 tours 集合中的 find 操作来验证这些文档的信息是否正确。

### [可选] CRUD：查询（读取）文档
[OPTIONAL] CRUD: Querying (Reading) Documents  
  12:26

Learn to query documents in MongoDB with find, using filters and projection on fields like price and rating. Apply operators such as lt, lte, and, or to compose precise conditions.  
学习如何使用 MongoDB 中的 find 语句来查询文档，包括使用过滤条件以及针对价格、评分等字段进行筛选。还可以运用诸如 lt、lte、and、or 等运算符来构建精确的条件。

### [可选] CRUD：更新文档
[OPTIONAL] CRUD: Updating Documents  
7:50

Update documents in MongoDB using updateOne and updateMany by applying a filter and the $set operator to adjust fields like price and add a premium flag.  
通过应用过滤器以及$set 运算符来更新 MongoDB 中的文档。例如，可以修改价格字段，并添加一种“高级版”标志。这些操作可以通过 updateOne 和 updateMany 方法来实现。

### [可选] CRUD：删除文档
[OPTIONAL] CRUD: Deleting Documents  
2:47

Learn how to delete documents in MongoDB using deleteOne and deleteMany, including deleting by a rating condition and the need for backups before deleting all documents with an empty query.  
了解如何在 MongoDB 中使用 deleteOne 和 deleteMany 方法删除文档。包括根据评分条件进行删除，以及删除所有文档之前是否需要备份。

### 使用 Compass 应用程序进行CRUD操作
Using Compass App for CRUD Operations  
7:10

Learn to use MongoDB Compass for crud operations, connect to a local database, run queries with lt and lte, edit and delete documents, and explore aggregations, schemas, and indexes.  
学习如何使用 MongoDB Compass 进行基础操作，连接本地数据库，使用`lt`和`lte`语法运行查询，编辑和删除文档，以及了解聚合、模式和索引的相关内容。

### 使用 Atlas 创建托管数据库
Creating a Hosted Database with Atlas  
4:27

Create a remote MongoDB Atlas database, a database-as-a-service, with a free M0 cluster (512 MB, 100 connections) for cloud development.  
创建一个远程 MongoDB Atlas 数据库，这是一个数据库即服务，使用免费的 M0 集群（512 MB，100 个连接）进行云开发。

### 正在连接到我们的托管数据库
Connecting to Our Hosted Database  
7:35

Connect a remote Atlas cluster to Compass and the Mongo shell, whitelisting your IP and creating a user, then verify natours and tours with the connection string.  
将一台远程的 Atlas 集群连接到 Compass 和 Mongo shell 上，将你的 IP 地址列入白名单，并创建一个用户。然后使用连接字符串来验证 natours 和 tours 的功能。

## 8.使用 MongoDB 和 Mongoose 技术  
28 次讲座 • 5 小时 10 分钟

### 章节介绍
Section Intro  
0:55

Connect a MongoDB database with our application using Mongoose, enabling real data from a real database. We'll explore data models, API features, data validation, and the advanced aggregation pipeline.  
通过 Mongoose 工具将 MongoDB 数据库与我们的应用程序连接在一起，从而实现真实数据库中的数据操作。我们将探讨数据模型、API 功能、数据验证以及高级聚合操作流程。

### 将我们的数据库与 Express 应用程序连接起来
Connecting Our Database with the Express App  
12:24

Connect the MongoDB atlas database to the express app by obtaining the connection string, storing it as an environment variable, and configuring mongoose with deprecation options to verify the connection.  
通过获取连接字符串、将其存储为环境变量，以及配置 mongoose 以启用弃用选项，可以将 MongoDB Atlas 数据库与 Express 应用程序连接起来，从而验证连接是否正常。

### 什么是 Mongoose？
What Is Mongoose?  
1:53

Mongoose provides schemas, validation, a simple query API, and middleware as an object data modeling library for MongoDB and Node.js, simplifying data modeling and document operations.  
Mongoose 提供了一个架构、验证功能、简单的查询接口，以及用于 MongoDB 和 Node.js 的对象数据建模中间件。这些功能简化了数据建模和文档操作的过程。

### 创建简单的游览模型
Creating a Simple Tour Model  
7:01

Create a simple Mongoose schema and model to manage tour documents, including required name and price, default rating, and basic schema type options for validation.  
创建一个简单的 Mongoose schema 和 model 来管理旅游文档，包括必填的名称和价格、默认评分以及用于验证的基本 schema 类型选项。

### 创建文档并测试模型
Creating Documents and Testing the Model  
9:02

Create a testTour document from the tour model, save it to MongoDB with testTour.save, and handle success and errors, including duplicate key and price validation, with _id shown in Compass.  
从 tour 模型中创建一个 testTour 文档，将其保存到 MongoDB 中，使用 testTour.save 方法进行保存。同时处理成功和失败的情况，包括重复键和价格验证问题。在 Compass 中显示_id 信息。

### 后端架构入门：MVC 模式、逻辑类型等
Intro to Back-End Architecture: MVC, Types of Logic, and More  
6:54

Explore the model-view-controller architecture with model, view, and controller layers, distinguish business logic from application logic, and learn how routers, controllers, and models create modular, scalable back-end applications.  
了解模型-视图-控制器架构，包括模型、视图和控制器层的工作原理。区分业务逻辑与应用程序逻辑。学习路由器、控制器和模型如何共同构建模块化、可扩展的后端应用。

### 基于 MVC 模式的重构工作
Refactoring for MVC  
6:31

Refactor for MVC by moving the tour schema to a model, exporting it, and wiring it into the tour controller to prepare for getting, creating, updating, and deleting tours.  
通过将旅游模式移至模型、导出并连接到旅游控制器中，重构为 MVC 模式，以准备获取、创建、更新和删除旅游。

### Another Way of Creating Documents  
创建文档的另一种方法13:53

Create tour documents using Tour.create with data from req.body, replacing the older new Tour().save approach. Apply async/await with try/catch for error handling and return clear bad request messages.  
使用 Tour.create 创建旅游文档，并使用 req.body 中的数据，从而替代之前使用 new Tour().save 方法的做法。采用 async/await 机制来处理错误，并避免出现错误的请求消息。

### Reading Documents  阅读文档8:35

Learn to read documents with Mongoose by fetching all tours with find and fetching a single tour with findById via req.params.id, using async/await and try/catch for error handling.  
学习使用 Mongoose 读取文档，通过 find 获取所有旅游项目，通过 findById 获取单个旅游项目（使用 req.params.id），并使用 async/await 和 try/catch 进行错误处理。

### Updating Documents  更新文档10:51

Update documents with mongoose by finding by id and applying changes from the request body via a patch update, returning the updated document with new: true and runValidators: true.  
通过 ID 进行查找，然后应用来自请求体的更改来更新文档。更新后，返回更新的文档，同时设置 new: true 和 runValidators: true。使用 mongoose 来实现这一操作。

### Deleting Documents  删除文档4:09

Delete documents using findByIdAndDelete in a RESTful API, returning a 204 no-content status, and practice this CRUD operation alongside update in Node.js, Express, and MongoDB.  
通过 RESTful API 中的 findByIdAndDelete 方法来删除文档，此时会返回 204 状态码，表示没有内容可返回。请在实际开发中练习这种 CRUD 操作，同时结合 Node.js、Express 和 MongoDB 来实施更新操作。

### Modelling the Tours  对旅行团进行建模18:13

Model and validate tour data using a richer Mongoose schema, adding fields such as name, duration, maxGroupSize, difficulty, ratingsAverage, ratingsQuantity, price, priceDiscount, imageCover, images, startDates, and createdAt.  
通过更丰富的 Mongoose 模式来建模和验证旅游数据。添加诸如名称、持续时间、最大团队规模、难度等级、平均评分、评分数量、价格、折扣价、图片链接、开始日期以及创建时间等字段。

### Importing Development Data  
导入开发数据15:40

Create a standalone Node.js script to import tour data from a JSON file into MongoDB using mongoose, and support command-line options to import or delete data, exiting cleanly.  
创建一个独立的 Node.js 脚本，该脚本能够使用 mongoose 将旅游数据从 JSON 文件导入到 MongoDB 中。同时支持命令行选项，用于导入或删除数据，并且能够优雅地退出程序。

### Making the API Better: Filtering  
改进 API 的功能：实现过滤功能18:45

Enhance your api with filtering using a query string, access req.query in Express, and implement Mongoose filters via objects or chained methods, while excluding paging, sort, limit, and fields.  
通过查询字符串来实现过滤功能，从而增强你的 API 性能。在 Express 中处理访问 req.query 的问题，并通过对象或链式方法实现 Mongoose 过滤功能。同时，请排除分页、排序、限制以及字段选择这些功能。

### Making the API Better: Advanced Filtering  
改进 API 功能：高级过滤功能12:04

Implement advanced filtering by supporting greater than, greater or equal than, less than, and less or equal than operators, converting them to MongoDB dollar operators for precise filtering.  
通过支持“大于”、“大于等于”、“小于”和“小于等于”运算符，实现高级过滤功能。这些运算符在应用后会转换为 MongoDB 中的美元运算符，从而实现更精确的过滤效果。

### Making the API Better: Sorting  
改进 API 的功能：排序功能10:59

Learn to implement api sorting by price, ascending or descending, using the query string and Mongoose, with multi-field support (price, ratings average) and a default sort by created at descending.  
学习如何使用查询字符串和 Mongoose 来实现按价格排序的功能，支持升序或降序排序。同时，该排序方式还支持多字段排序（包括价格、评分平均值等）。此外，系统默认排序方式为按创建日期降序排列。

### Making the API Better: Limiting Fields  
改进 API 的功能：限制输入字段的数量6:18

Limit API payloads with field limiting and projection; specify fields via a fields query (name, duration, difficulty, price) and use minus to exclude others; hide sensitive data in schema.  
通过字段限制和投影功能来限制 API 的返回数据量；可以通过字段查询来指定需要返回的字段（如名称、持续时间、难度等级、价格等），并使用“减去”操作来排除某些不需要的字段；同时，应将模式中的敏感数据隐藏起来。

### Making the API Better: Pagination  
改进 API 功能：分页处理  14:59

Implement pagination in your API with page and limit, compute skip as page minus one times limit, apply Mongoose skip and limit, and validate with countDocuments to handle unavailable pages.  
在您的 API 中实现分页，使用 page 和 limit 参数，计算 skip 为 page 减一乘以 limit，应用 Mongoose 的 skip 和 limit，并通过 countDocuments 进行验证以处理不可用页面。

### Making the API Better: Aliasing  
改进 API 的功能：使用别名处理8:56

Add an alias route for top five cheap tours by using a middleware that presets the query with limit five, sort by ratings average and price, and select key fields.  
为排名前五的便宜旅行线路添加别名路由，使用中间件在查询中预设第五个选项，按评分平均值和价格进行排序，并选择相关的关键字段。

### Refactoring API Features  重构 API 功能15:38

Refactor API features into a reusable APIFeatures class to clean up code and enable modular, chainable operations (filter, sort, limit, paginate). Move to a utilities module for cross-resource reuse.  
将 API 功能重构为可复用的 API。通过专门的特性类来整理代码，实现模块化、可链式操作的功能（如过滤、排序、限制、分页等）。将相关功能移入工具模块，以便在不同资源之间复用。

### Aggregation Pipeline: Matching and Grouping  
聚合管道：匹配与分组20:37

Explore the MongoDB aggregation pipeline, a multi-stage framework to transform documents through match and group stages to compute ratings average, average price, min and max prices across tours.  
了解 MongoDB 的聚合管道机制，这是一个多阶段框架，通过匹配和分组操作来转换文档，从而计算出不同旅游线路的评分平均值、平均价格以及最低和最高价格。

### Aggregation Pipeline: Unwinding and Projecting  
聚合管道：展开与投影19:28

Master the aggregation pipeline to unwind start dates, group by month, project results, and build a get monthly plan function to identify the busiest month of a year.  
掌握聚合流程的运作机制，能够解析开始日期、按月份分组数据、查看项目结果等。同时，还需要开发一个每月获取计划的功能，以便识别出一年中最为繁忙的月份。

### Virtual Properties  虚拟财产6:23

Define Mongoose virtual properties to derive fields like duration weeks from days on the tour schema, using a getter. Enable virtuals in JSON outputs; they are not persisted or queryable.  
在 Tour schema 中，定义 Mongoose 虚拟属性，以便从 Day 类型的字段中提取出如 Duration（周）这样的字段，并提供一个获取器方法来获取这些字段的值。同时，允许在 JSON 输出中显示这些虚拟属性；这些属性不会被持久化，也无法进行查询。

### Document Middleware  文档中间件14:29

Learn document middleware in mongoose, a pre and post hook around save and create, including slug generation, with limits like not triggering on insertMany or findByIdAndUpdate, and query middleware.  
了解在 mongoose 中使用的文档中间件功能，包括在保存和创建操作前后触发的中间件函数。这些中间件可以处理 slug 的生成，以及限制在某些特定情况下不触发这些操作的情况，比如当执行 insertMany 或 findByIdAndUpdate 操作时。此外，还包括查询中间件的相关内容。

### Query Middleware  查询中间件13:54

Explore query middleware in mongoose: create pre-find and post-find hooks, filter secret tours by secretTour, apply to all find variants with a regex, and log query duration.  
探索使用 mongoose 中的查询中间件功能：创建预查找和后查找的钩子函数，通过 secretTour 过滤秘密之旅数据，将正则表达式应用于所有查找操作，以及记录查询耗时。

### Aggregation Middleware  聚合中间件7:42

Learn aggregation middleware in Node.js with MongoDB, using the aggregate hook to modify the pipeline by unshifting a match stage that excludes secretTour true, ensuring only non-secret tours are aggregated.  
在 Node.js 中使用 MongoDB 学习聚合中间件。通过修改聚合流程，使用聚合钩子来排除 secretTour 条件，这样只有非秘密旅行线路才会被纳入聚合结果中。

### Data Validation: Built-In Validators  
数据验证：内置验证工具10:06

Learn how to validate data in mongoose with built-in validators such as required, max/min length, min/max for numbers, and enum values, and apply runValidators for updates.  
了解如何使用 mongoose 中的内置验证器来校验数据，例如要求字段必须填写、长度限制（如最大/最小长度）、数字类型的范围限制，以及枚举值的约束。同时，也能学习如何应用“runValidators”函数来执行验证操作。

### Data Validation: Custom Validators  
数据验证：自定义验证器14:02

Build custom validators in mongoose to ensure the price discount is lower than the price, with a clear message. Note this keyword limitation and use the validator library isAlpha.  
在 mongoose 中构建自定义的验证器，以确保价格折扣确实低于原价，同时还会显示明确的提示信息。请注意这个关键词的限制，并使用 validator 库来实现 isAlpha 验证功能。


## 9.使用 Express 进行错误处理  
14 次讲座 • 2 小时 10 分钟

### Section Intro  章节介绍0:41

Build a robust error-handling workflow for backend applications using Express and environment variables. Discover how this section demonstrates applying a better error-handling strategy to future projects.  
使用 Express 和环境变量，为后端应用程序构建一个可靠的错误处理流程。看看这一部分是如何展示如何在未来的项目中采用更好的错误处理策略的。

### Debugging Node.js with ndb  
使用 ndb 调试 Node.js20:29

Learn to debug Node.js with the ndb tool, install and run it, set breakpoints, inspect variables, and fix issues in an Express app.  
学习如何使用 ndb 工具来调试 Node.js 应用程序。包括安装并运行该工具、设置断点、检查变量值以及修复 Express 应用程序中的错误。

### Handling Unhandled Routes  
处理未处理的路由7:50

Define a catch-all route with app.all('*') after all routers to handle unhandled routes, returning a 404 JSON with the requested URL (req.originalUrl) to improve API friendliness.  
在所有路由之后，定义一个通用的处理路由的路由，使用`app.all('*')`来表示可以处理所有未处理的路由。当遇到未处理的路由时，返回一个包含请求 URL（`req.originalUrl`）的 404 JSON 响应，以此来提高 API 的友好性。

### An Overview of Error Handling  
错误处理概述3:36

Explore the distinction between operational and programming errors in Express, and implement a global error handling middleware to centralize error responses.  
了解 Express 中操作错误与编程错误之间的区别，并实现一个全局错误处理中间件，以便集中处理错误响应。

### Implementing a Global Error Handling Middleware  
实现全局错误处理中间件8:59

Implement a global error handling middleware in Express with an error-first function of four arguments. Read error status and message, default to 500, and centralize responses for 404 errors.  
在 Express 中实现一个全局错误处理中间件，使用一个具有四个参数的错误优先函数。读取错误状态和消息，默认为 500，并将 404 错误的响应集中处理。

### Better Errors and Refactoring  
更好的错误处理与重构工作11:12

Create a robust AppError class that extends Error, sets message and statusCode, and marks isOperational. Capture the stack trace and wire a global error handler with an error controller.  
创建一个名为 AppError 的类，该类继承自 Error 类，需要设置错误消息和状态码，并标记为“操作正常”。同时，还需要捕获堆栈跟踪信息，并为一个全局错误处理器配备错误控制器功能。

### Catching Errors in Async Functions  
发现异步函数中的错误14:53

Refactor async error handling by introducing a catchAsync wrapper that returns a function accepting (req, res, next), removing per-function try-catch blocks, and routing errors to global middleware.  
通过引入一个`catchAsync`包装器来重构异步错误处理，该包装器返回一个接受`req, res, next`参数的函数；同时移除每个函数中的`try-catch`块，并将错误传递给全局的中间件来处理。

### Adding 404 Not Found Errors  
增加了 404 未找到的错误9:06

Implement 404 not found errors using the AppError class, catchAsync, and targeted handling in tour routes for getTour, updateTour, and delete, with proper returns to global error handling.  
使用 AppError 类在 404 未找到错误时实施相应的处理机制。在 getTour、updateTour 和 delete 方法中采用针对性处理，同时确保正确的返回逻辑能够触发全局的错误处理机制。

### Errors During Development vs Production  
开发阶段与生产过程中的错误10:30

Distinguish development vs production error handling in a node app by exposing detailed errors in development and sending safe messages in production, using operational errors and a centralized error controller.  
在节点应用中区分开发阶段与生产环境中的错误处理机制。在开发阶段展示详细的错误信息，而在生产环境中则发送安全的信息。利用操作错误和集中的错误控制器来实现这一功能。

### Handling Invalid Database IDs  
处理无效数据库 ID9:42

Learn to turn Mongoose errors into operational ones by handling CastError, duplicate key, and ValidationError, creating friendly AppError messages and producing meaningful 400 responses in production.  
学会如何将 Mongoose 中的错误转化为可操作的错误，例如处理 CastError、重复键问题以及 ValidationError。同时，要学会编写友好的 AppError 消息，并在生产环境中给出有意义的 400 响应。

### Handling Duplicate Database Fields  
处理重复数据库字段6:04

Handle duplicate field errors in MongoDB by matching error code 11000, extracting the conflicting value with a regular expression, and responding with a clear 400 bad request via AppError.  
通过匹配错误代码 11000 来处理 MongoDB 中重复的字段错误，使用正则表达式提取冲突的数据，并通过 AppError 返回明确的 400 错误响应。

### Handling Mongoose Validation Errors  
处理 Mongoose 验证错误8:24

Learn to handle Mongoose validation errors by extracting multiple field messages from error.errors, building a combined, user-friendly message, and returning a robust operational error in production.  
学会如何处理 Mongoose 验证错误，方法是从 error.errors 中提取多个字段的信息，构建一份简洁易懂的错误消息，并在实际运行中返回可靠的错误处理信息。

### Errors Outside Express: Unhandled Rejections  
Express 之外的错误：未处理的拒绝8:49

Explore handling unhandled promise rejections outside express by using process.on('unhandledRejection') to log errors and shut down gracefully with server.close.  
通过使用`process.on('unhandledRejection')`来记录错误，并借助`server.close()`方法优雅地关闭进程，可以处理在 Express 环境中无法处理的 Promise 拒绝情况。

### Catching Uncaught Exceptions  
捕获未捕获的异常9:16

Discover how to catch uncaught exceptions in Node apps using the uncaughtException handler, log the full error, crash safely, and prepare automatic restarts for production.  
了解如何在 Node 应用中使用 uncaughtException 处理程序来捕获未捕获的异常，记录完整的错误信息，安全地崩溃应用程序，并为实现生产环境的自动重启做好准备。


## 10.身份验证、授权和安全  
23 次讲座 • 5 小时 29 分钟

### Section Intro  章节介绍0:58

Learn to implement authentication and authorization with JSON web tokens to protect access to app features. Build secure API interactions by verifying users as they sign up and log in.  
学习如何使用 JSON Web 令牌来实现身份验证和授权功能，从而保护对应用程序功能的访问。通过用户在注册和登录时进行的验证，构建安全的 API 交互机制。

### Modelling Users  对用户进行建模分析10:00

Model a user with mongoose for authentication and authorization, including name, email, photo, password, and passwordConfirm, with email validation, uniqueness, and lowercase normalization, then export the user model.  
使用 mongoose 来建模一个用户模型，包括姓名、电子邮件、照片、密码以及密码确认信息。同时需要考虑电子邮件的验证、唯一性检查，以及小写形式的密码规范化处理。最后，将这个用户模型导出。

### Creating New Users  创建新用户11:19

Create new users via an auth-focused signup controller that uses the User model to create and return the user, with a post /signup route and catchAsync-based async error handling.  
通过一个以认证为中心的注册控制器来创建新用户。该控制器使用 User 模型来创建用户并返回用户信息。注册路径为/post/signup，并且采用 catchAsync 方式处理异步错误。

### Managing Passwords  管理密码18:24

Validate password equals confirm password with a custom validator on create and save; hash passwords with bcrypt with cost 12 in a pre-save middleware and remove the confirm field.  
在创建和保存时，使用自定义验证器来确保密码与确认密码一致；在预保存中间件中，使用成本 12 的 bcrypt 算法对密码进行哈希处理，并移除确认密码的字段。

### How Authentication with JWT Works  
如何使用 JWT 进行身份验证8:46

Explore how json web tokens enable stateless authentication for restful APIs by issuing a token at login, storing it on the client, and using it to access protected resources.  
了解如何通过 JSON Web 令牌来实现 RESTful API 的无状态认证机制。在登录时颁发令牌，将其存储在客户端设备上，然后利用该令牌访问受保护的资源。

### Signing up Users  注册用户16:01

Implement a real signup with automatic login by signing and verifying json web tokens using a secret, restricting signup data to name, email, and password, and enforcing token expiration.  
实现一种真正的注册功能，用户注册后可以通过自动登录进入系统。注册过程中需要使用密钥来验证 json Web 令牌的有效性。注册数据仅包括姓名、电子邮件和密码，同时还需要确保令牌有有效期。

### Logging in Users  登录用户26:35

Validate login by reading email and password, verify the user exists and the password matches, then sign and return a jwt. Use bcrypt for password comparison and error handling.  
通过读取电子邮件和密码来验证登录信息是否正确。确认用户存在且密码匹配后，进行签名操作，并返回一个 JWT 令牌。使用 bcrypt 算法来进行密码比较和错误处理。

### Protecting Tour Routes ### Part 1  
保护旅游路线——第一部分14:57

protect tour routes by adding a protect middleware that reads a bearer token from the authorization header, validates it, and allows access to getAllTours only for logged-in users.  
通过添加一个保护中间件来保护旅游路线，该中间件从授权头中读取一个 bearer token，验证它，并且只允许登录用户访问 getAllTours。

### Protecting Tour Routes ### Part 2  
保护旅游路线——第二部分35:14

Implement a jwt protected middleware using promisified jwt.verify, handling JsonWebTokenError and TokenExpiredError with AppError, validating user existence and password changes, and attaching currentUser to req for protected routes.  
实现一种基于 jwt 保护的 middleware，使用 promisified 的 jwt.verify 方法来处理错误。当遇到 JsonWebTokenError 和 TokenExpiredError 时，应使用 AppError 进行错误处理。同时，需要验证用户的存在性和密码是否发生变化。此外，还需要将 currentUser 附加到经过保护的路由请求对象上。

### Advanced Postman Setup  高级 Postman 设置11:35

Learn to set up advanced Postman environments with development and production URLs, manage variables like URL and JWT, and automate bearer token handling for protected routes.  
学习如何配置高级的 PostMan 环境，包括开发环境和生产环境的 URL 地址。同时，掌握管理诸如 URL 地址和 JWT 令牌等变量的技巧。此外，还能实现针对受保护路由的 bearer 令牌自动处理功能。

### Authorization: User Roles and Permissions  
授权：用户角色与权限15:54

Implement authorization by adding a role-based restrictTo middleware, protect routes with authentication, and enforce admin and lead guide permissions to delete tours.  
通过添加基于角色的 restrictTo 中间件来实现授权，用身份验证保护路由，并强制执行管理员和领导指南权限以删除旅游路线。

### Password Reset Functionality: Reset Token  
密码重置功能：重置令牌17:03

Implement password reset in a Node.js app by handling forgot password requests, generating a random token (not JWT), encrypting it, storing it with a 10-minute expiry, and emailing it.  
在 Node.js 应用中实现密码重置功能。当收到密码遗忘的请求时，生成一个随机令牌（不是 JWT 格式），将其加密后存储起来，并设置有效期为 10 分钟，最后通过电子邮件将令牌发送给用户。

### Sending Emails with Nodemailer  
使用 Nodemailer 发送电子邮件23:04

Learn to send password reset emails using nodemailer by building a mail transporter, configuring options, and handling errors with async/await while testing with mailtrap.  
通过构建邮件传输工具、配置各项选项，以及利用 async/await 处理错误，学习如何发送密码重置邮件。在使用 mailtrap 进行测试时，也要注意处理相关错误。

### Password Reset Functionality: Setting New Password  
密码重置功能：设置新密码21:33

Master the password reset flow: validate the reset token, set the new password with validators, clear reset fields, and issue a JWT while updating passwordChangedAt.  
掌握密码重置的流程：验证重置令牌，使用验证工具设置新密码，清空重置字段，并在更新密码变更时间的同时签发一个 JWT 令牌。

### Updating the Current User: Password  
更新当前用户的密码信息：16:36

Learn to let a logged in user update their password by verifying the current password, saving with validation, and reissuing a token via a dedicated update password flow.  
学会让登录的用户能够通过验证当前密码来更新密码，在验证通过后保存新密码，并通过专门的密码更新流程重新发送令牌。

### Updating the Current User: Data  
更新当前用户的数据：20:11

Update the currently authenticated user's data with the updateMe route, enforcing password update restrictions and using findByIdAndUpdate with a filtered body containing only name and email, protected by token-based authentication.  
使用 updateMe 路由来更新当前已认证用户的数据。在更新密码时需遵守相关限制条件；同时，使用 findByIdAndUpdate 方法进行数据更新，且过滤条件仅包含用户名和电子邮件信息，这些信息受到基于令牌的认证保护。

### Deleting the Current User  
删除当前用户10:28

Learn to delete the current user by marking the user as inactive with an active boolean, using deleteMe to set active to false and return a 204 response.  
学会如何删除当前用户，方法是将该用户标记为不活跃，使用`deleteMe`方法来将`active`属性设置为`false`，然后返回状态码 204。

### Security Best Practices  安全最佳实践10:13

Apply bcrypt-based password protection, use http-only cookies for jwt tokens, implement rate limiting and denial-of-service defenses, sanitize inputs, set secure headers, enforce https, and adopt mongoose-based NoSQL injection defenses.  
应用基于 bcrypt 的密码保护，使用 http-only cookie 存储 jwt 令牌，实现速率限制和拒绝服务防御，清理输入，设置安全头部，强制使用 https，并采用基于 mongoose 的 NoSQL 注入防御。

### Sending JWT via Cookie  通过 Cookie 发送 JWT 令牌9:46

Send the JSON web token as a secure httpOnly cookie with res.cookie, set an expires date (90 days) and apply production secure flag for HTTPS.  
通过 res.cookie 将 JSON Web 令牌作为安全的 httpOnly cookie 发送出去。同时设置有效期为 90 天，并启用 HTTPS 上的生产环境安全标志。

### Implementing Rate Limiting  
实施速率限制7:04

Implement a rate limiting middleware with express-rate-limit to cap IP requests at 100 per hour for /api route, preventing denial of service and brute force attacks, exposing RateLimit-Limit and RateLimit-Remaining.  
使用 express-rate-limit 实现一个速率限制中间件，对 /api 路径的 IP 请求每小时限制为 100 次，以防止服务拒绝和暴力破解攻击，并暴露 RateLimit-Limit 和 RateLimit-Remaining。

### Setting Security HTTP Headers  
设置安全 HTTP 头信息5:19

Use the helmet npm package to configure security http headers in an express app, placing it early in the middleware stack and enabling xss protection and strict transport security.  
使用 helmet npm 包在 express 应用中配置安全 http 头部，将其放在中间件堆栈的早期位置，并启用 xss 保护以及严格传输安全。

### Data Sanitization  数据清理9:15

Apply data sanitization to defend against NoSQL query injection and cross-site scripting by using express-mongo-sanitize and XSS_clean middleware that cleanses request data.  
采用数据清洗机制来防止 NoSQL 查询注入和跨站脚本攻击，可以通过使用 express-mongo-sanitize 和 XSS_clean 中间件来实现对请求数据的清洗处理。

### Preventing Parameter Pollution  
防止参数污染8:20

Learn to prevent parameter pollution in a Node.js, Express API by using the HPP middleware to eliminate duplicate query parameters, with examples on sorting and whitelisting.  
学习如何在 Node.js 和 Express API 中预防参数污染问题，通过使用 HPP 中间件来消除重复的查询参数。同时，还会介绍一些关于排序和白名单管理的技巧。

## 11.建模数据与先进的 Mongoose 技术  
27 次讲座 • 5 小时 33 分钟

### Section Intro  章节介绍1:02

Explore MongoDB data modeling and advanced Mongoose features to design robust backend data schemas, establish relationships, and implement a fully functioning API with routes, resources, authentication, and authorization.  
探索 MongoDB 数据建模和高级 Mongoose 功能，以设计强大的后端数据模式，建立关系，并实现一个具有路由、资源、认证和授权的完整功能 API。

### MongoDB Data Modelling  MongoDB 数据建模24:39

Explore MongoDB data modeling, identifying relationships and choosing embedding or referencing using a three-criteria framework, with examples of one-to-one, one-to-many, and many-to-many.  
探索 MongoDB 的数据建模方式，识别数据之间的关系，并根据三个标准来选择使用嵌入或引用策略。同时，通过一些示例来说明一对一、一对多和多对多的关系类型。

### Designing Our Data Model  设计我们的数据模型10:57

Design a robust data model for the Natours app by modeling tours, users, reviews, locations, and bookings, and exploring one-to-many, many-to-many relationships, and embedding versus referencing with Mongoose.  
为 Natours 应用程序设计一个健壮的数据模型。需要建模旅游线路、用户、评价、地点以及预订信息。同时，要处理一对多、多对多关系，并利用 Mongoose 框架实现嵌入与引用关系。

### Modelling Locations (Geospatial Data)  
建模位置信息（地理空间数据）12:25

Model geospatial data in MongoDB by embedding startLocation and locations in tours using GeoJSON, with coordinates as [longitude, latitude] and a point default. Learn to store and query embedded locations.  
通过在 MongoDB 中嵌入 GeoJSON 数据，可以将旅行中的地点信息作为模型数据存储。GeoJSON 中的坐标格式为[经度，纬度]，每个地点都包含一个点。了解如何存储和查询这些嵌入式的位置数据。

### Modelling Tour Guides: Embedding  
建模导游：嵌入技术7:48

Embed tour guide documents into tour records using a pre-save middleware, resolving user IDs with Promise.all, and understand embedding drawbacks that lead to preferring referencing.  
使用预保存的中间件将导游文档嵌入到旅行记录中，通过 Promise.all 来解析用户 ID。同时，需要认识到嵌入方式的缺点，因此更倾向于采用引用方式来处理这些文档。

### Modelling Tour Guides: Child Referencing  
建模导游：儿童参考机制4:56

Switch from embedding to referencing to keep tours and users as separate entities, storing only IDs on tours. Use mongoose.Schema.objectId with ref: 'User' to enable populating and show user data.  
从使用嵌入方式改为引用方式，将游客和用户作为独立的实体来处理。在游客对象上只存储 ID。利用 mongoose.Schema.objectId 并结合 ref: 'User'的方式，可以方便地对用户数据进行填充和展示。

### Populating Tour Guides  人口分布导游10:42

Learn how to use populate in Mongoose to replace tour guide references with actual data across queries and tailor fields to exclude v and passwordChangedAt.  
了解如何在 Mongoose 中使用 populate 函数，将导游的引用替换为实际的数据，并自定义字段以排除 v 和 passwordChangedAt 字段。

### Modelling Reviews: Parent Referencing  
建模评论：家长参考意见8:30

Create the review model in ReviewModel.js with fields for review text, rating (1–5), createdAt, and required parent references to tour and user, plus virtuals in json outputs.  
在 ReviewModel.js 文件中创建评论模型，包含以下字段：评论文本、评分（1-5 分）、创建时间，以及与旅行记录和用户相关的必要父引用信息。此外，还支持在 JSON 输出中显示虚拟评论。

### Creating and Getting Reviews  
创建并获取评价17:03

Implement endpoints to get all reviews and create new reviews, build the reviews controller and routes, and protect access for regular users on api/v1/reviews.  
实现端点的功能，以获取所有评论，并创建新的评论。同时构建评论控制器和相应的路由规则。还需要保护普通用户在 api/v1/reviews 接口上的访问权限。

### Populating Reviews  填充评论6:06

Populate reviews by auto-populating related tour and user data in queries, using two populate calls to fetch tour name and user name plus photo, while omitting emails.  
通过自动填充相关旅游行程和用户数据来生成评论。具体方法是使用两个填充函数，分别获取旅游行程名称、用户名称以及用户照片，同时忽略电子邮件信息。

### Virtual Populate: Tours and Reviews  
虚拟人口：旅游与评价12:02

Master virtual populate in Mongoose to access all reviews for a tour without storing review IDs on the tour, enabling efficient, targeted populates instead of heavy cross-populates.  
在 Mongoose 中管理虚拟用户，以便无需存储评论的 ID 即可获取某个旅行中的所有评论信息。这种处理方式能够实现高效、有针对性的用户填充，而无需进行大量的交叉填充操作。

### Implementing Simple Nested Routes  
实现简单的嵌套路由13:29

Learn how to implement nested routes in Express by attaching reviews to tours, using the tour ID in the URL and the logged-in user's ID for creation.  
学习如何在 Express 中实现嵌套路由，通过将评论附加到旅游路线，使用 URL 中的旅游路线 ID 和登录用户的 ID 进行创建。

### Nested Routes with Express  
使用 Express 的嵌套路由机制8:00

Learn to implement nested routes in Express by mounting routers and using mergeParams to pass the parent tour id to the review router, enabling cleaner, decoupled code.  
学习如何在 Express 中实现嵌套路由。通过挂载路由器并使用 mergeParams 方法将父路由的 ID 传递给子路由，从而实现更简洁、更独立的代码结构。

### Adding a Nested GET Endpoint  
添加嵌套 GET 终端点5:06

Implement a nested get endpoint to fetch reviews for a specific tour by tourId, updating the getAllReviews handler to apply a tour filter when tourId is present.  
实现一个嵌套的获取端点，根据特定的旅游景点 ID 来获取相关评论。同时，更新 getAllReviews 处理程序，在收到旅游景点 ID 时加入过滤条件，以筛选出与该旅游景点相关的评论。

### Building Handler Factory Functions: Delete  
构建处理程序工厂函数：删除14:21

Build a reusable delete handler with a factory that accepts a model and returns a deleteOne function, reducing duplicate code across tours, reviews, and users.  
构建一个可复用的删除处理程序，包含一个工厂函数，该函数接受一个模型作为参数，并返回一个删除功能的实现。这样就能避免在不同轮次、评论以及用户之间重复编写相同的代码。

### Factory Functions: Update and Create  
工厂函数：更新和创建13:22

Learn how to build and apply factory functions for updating and creating resources, applying them to tours, users, and reviews, wiring routes, and testing with Postman.  
了解如何构建和应用工厂函数，以更新和创建资源，将这些函数应用于路线、用户和评论的创建过程，以及使用 PostMan 进行路由配置和测试。

### Factory Functions: Reading  
工厂函数：读取14:41

Implement factory functions for reading documents with optional populate options, enabling getOne and getAll with API features like filtering, sorting, and pagination.  
实现工厂函数以读取文档，并支持可选的填充功能。通过 API 特性如过滤、排序和分页功能，可以方便地获取所需的数据。

### Adding a /me Endpoint  添加了一个“我”端点。4:42

Add a /me endpoint to fetch the current user's data by using middleware that assigns request.params.id from request.user.id, protecting the route and reusing the getOne factory to return the user.  
添加了一个/me 端点，用于获取当前用户的数据。通过使用中间件来解析 request.params.id 的值，从 request.user.id 中获取数据。同时，该端点保护了路由的安全性，并使用了 getOne 工厂函数来返回用户信息。

### Adding Missing Authentication and Authorization  
添加了缺失的身份验证和授权信息14:44

Protect all routes with authcontroller.protect and restrictto admin or lead guide, applying router.use to guard tours, reviews, and user endpoints, with bearer tokens and postman validation.  
使用 authcontroller.protect 和 restrict 来保护所有路由路径。同时，通过 router.use 函数来保护旅行路线、审核流程以及用户端点的访问。此外，还需要使用带有承载令牌的验证方式，以及 Postman 进行验证。

### Importing Review and User Data  
导入审核和用户数据7:26

import dev data for users and reviews by duplicating the tours script, delete current data, disable validation and password encryption, and verify admin access and data in compass.  
通过复制 tours 脚本的方式导入用户和评论的数据；删除当前的数据；禁用验证功能和密码加密功能；确认管理员访问权限是否正常，以及数据在 Compass 中的存储情况是否无误。

### Improving Read Performance with Indexes  
通过索引提升读取性能16:11

Learn how MongoDB indexes improve read performance using explain to compare scans and slug queries. Build a compound index on price and ratings average to optimize queries.  
学习如何使用 MongoDB 索引提高读取性能，使用 explain 比较扫描和 slug 查询。在价格和平均评分上构建复合索引以优化查询。

### Calculating Average Rating on Tours ### Part 1  
计算旅游平均评分 ### 第一部分22:25

Calculate and store the average rating and rating count for each tour using a Mongoose static method and aggregation, triggered by post save when reviews are added, updated, or deleted.  
使用 Mongoose 的静态方法以及聚合功能，来计算并存储每个旅行团的平均评分和评分数量。该操作在保存后、评论被添加、更新或删除时触发。

### Calculating Average Rating on Tours ### Part 2  
计算旅游平均评分 ### 第二部分15:56

Learn to compute tour review statistics for updates and deletions using Mongoose query middleware, pre and post hooks, and a robust calcAverageRatings workflow that updates averages in real time.  
学习如何使用 Mongoose 查询中间件、前后置处理函数，以及高效的计算平均评分的工作流程，来统计更新和删除操作后的用户评价统计数据。这些工具能够实时更新平均评分数值。

### Preventing Duplicate Reviews  
防止重复评论9:29

Prevent duplicate reviews by enforcing a compound unique index on user and tour in the review model. Use a mongoose setter to round the average rating on updates.  
通过在对用户和行程进行评分时实施复合唯一索引，以防止重复评分的情况发生。在更新评分时，使用 mongoose 的 setter 函数来调整平均评分。

### Geospatial Queries: Finding Tours Within Radius  
地理空间查询：查找半径内的旅游  29:52

Master geospatial queries to build a tours search within a chosen distance from a center point, using center coordinates, miles or kilometers, geo within, and a 2d sphere index.  
掌握地理空间查询技巧，能够根据中心点所处的距离范围，使用中心坐标、英里或公里单位，以及 2D 球体索引，来构建旅游路线搜索结果。

### Geospatial Aggregation: Calculating Distances  
地理空间聚合：计算距离16:28

Explore geospatial aggregation in MongoDB to calculate distances from a point to all tours using geoNear, project concise results, and convert meters to kilometers or miles.  
在 MongoDB 中探索地理空间聚合功能，利用 geoNear 算法计算某个点到所有路径的距离，并筛选出简洁的结果。同时，可以将距离单位从米转换为千米或英里。

### Creating API Documentation Using Postman  
使用 Postman 创建 API 文档10:37

Create clear api documentation directly in Postman, using environment variables, request descriptions, and markdown to describe endpoints and authentication, then publish the documentation for public access.  
在 PostMan 中直接创建清晰的 API 文档，利用环境变量、请求描述以及 Markdown 格式来详细说明端点信息和认证方式。完成后，将文档发布以供公众访问。

## 12.基于 Pug 模板的服务器端渲染  
24 次讲座 • 5 小时 51 分钟

### Section Intro  章节介绍0:56

Transition from a JSON API to server-side rendered websites by leveraging existing authentication, data models, and API, then render HTML and CSS UI to bring the app to life.  
通过利用现有的身份验证、数据模型和 API 接口，将 JSON API 过渡为服务器端渲染的网站形式。然后结合 HTML 和 CSS 来呈现用户界面，使应用程序具有真实的生命力。

### Recap: Server-Side vs Client-Side Rendering  
回顾：服务器端渲染与客户端渲染5:16

Recap the differences between server-side and client-side rendering, and show building a server-rendered Natours site with templates, database data, and API-driven content.  
回顾一下服务器端渲染和客户端渲染之间的区别，并通过示例展示如何利用模板、数据库数据以及 API 驱动的内容来构建一个服务器端渲染的 Natours 网站。

### Setting up Pug in Express  
在 Express 环境中配置 Pug 模块10:07

Set up the Pug template engine in Express and configure the views folder. Render a dynamic page using base.pug with tours, users, and reviews data.  
在 Express 中配置 Pug 模板引擎，并设置视图文件夹。使用 base.pug 文件来渲染包含参观信息、用户数据和评价内容的动态页面。

### First Steps with Pug  与帕格相处的第一步14:37

Learn the fundamentals of Pug templates, using indentation to build HTML documents with doctype, head, and body, pass locals, and render dynamic content with interpolation.  
了解 Pug 模板的基础知识，包括如何使用缩进来构建 HTML 文档，包括 doctype、head 和 body 部分的处理，如何传递局部变量，以及如何通过插值来渲染动态内容。

### Creating Our Base Template  
创建我们的基础模板15:24

Create a reusable base template by converting the starter HTML into a Pug layout, including a header and footer, so dynamic content can be injected later.  
通过将初始的 HTML 模板转换为 Pug 布局格式，可以创建一个可重复使用的基础模板。这种布局包含了一个头部和尾部，这样之后就可以轻松地插入动态内容了。

### Including Files into Pug Templates  
将文件包含到 Pug 模板中4:45

Learn how to include header and footer partials in Pug using underscores like _header and _footer. Use a Pug beautify extension and VS Code commands to fix indentation.  
了解如何使用 underscores 标签（如 _header 和 _footer）在 Pug 模板中添加页眉和页脚部分。利用 Pug 的格式化扩展功能以及 VS Code 的语法高亮功能来优化代码的缩进。

### Extending Our Base Template with Blocks  
扩展我们的基础模板，加入多个模块10:06

Extend the base Pug template with blocks to render page-specific content for the overview and tour detail pages. Define routes and pass dynamic titles, overriding the content block.  
在基础 Pug 模板中添加模块，以渲染概览页面和参观详情页面上的特定内容。定义路由路径，并传递动态标题，从而覆盖内容模块中的默认标题。

### Setting up the Project Structure  
设置项目结构5:57

Refactor and set up the project structure with a view router and views controller in the nvc architecture, mount routes, and prepare the overview page.  
进行重构工作，建立项目结构，在 NVC 架构中配置视图路由器和视图控制器。同时，配置路由规则，并准备概述页面。

### Building the Tour Overview ### Part 1  
构建旅游概览——第一部分11:37

Build and render a dynamic tour overview page in a Node.js, Express app by fetching tours, wiring a pug template, and looping over the data to create cards.  
在 Node.js 和 Express 应用程序中构建并渲染一个动态的导游概览页面。通过获取导游信息、使用 Pug 模板语法，以及遍历数据来生成卡片界面。

### Building the Tour Overview ### Part 2  
构建旅游概览——第二部分  15:52

Build dynamic tour cards from a database using pug templates, interpolating tour data (name, image cover, location, dates, price, rating) and linking to slug-based detail pages.  
从数据库中提取旅游信息，使用 Pug 模板生成动态的旅游卡片。这些卡片包含旅游信息（如名称、图片、地点、日期、价格、评分等），并链接到包含详细信息的页面。

### Building the Tour Page ### Part 1  
构建旅游页面——第一部分20:28

Build the tour detail page using Pug with conditionals and mixins, create a slug-based route and controller, fetch reviews and guides, and render dynamic tour data.  
使用 Pug 框架构建旅游详情页面，利用条件语句和混入功能来实现动态渲染。创建基于 slug 的路由和控制器，获取评论和指南信息，然后渲染动态的旅游数据。

### Building the Tour Page ### Part 2  
构建旅游页面 ### 第二部分25:49

Build a dynamic tour detail page using Pug templates and mixins to loop guides, render description paragraphs, display images, and show reviews with star ratings from populated user data.  
使用 Pug 模板和混合器构建一个动态的导游详情页面。可以循环显示指南内容、渲染描述段落、展示图片，并根据用户数据展示带有星级评分的评论信息。

### Including a Map with Mapbox ### Part 1  
包含使用 Mapbox 的地图功能——第一部分11:33

Fix the tour title bug and implement a client-side Mapbox map by exposing tour locations in HTML data attributes, creating a Mapbox.js file, and loading scripts correctly in templates.  
修复了旅游路线标题的错误，并通过在 HTML 数据属性中标记旅游地点、创建 Mapbox.js 文件，以及正确加载模板中的脚本，实现了对 Mapbox 地图的渲染。

### Including a Map with Mapbox ### Part 2  
包含使用 Mapbox 的地图功能——第二部分27:51

Learn to integrate Mapbox in a tour page, obtain a per-project token, add markers with popups, fit bounds with padding, and disable scroll zoom.  
学习如何在旅游页面中集成 Mapbox 功能，为每个项目获取令牌，添加带有弹出窗口的标记点，调整边界范围并添加填充效果，同时禁用滚动缩放功能。

### Building the Login Screen  
构建登录界面6:30

Render the login screen at /login with a get login form controller and login.pug titled 'log into your account', showcasing a static login form and validation visuals.  
在/login 路径下，使用 get login form 控制器来生成登录页面。该页面的标题为"登录您的账户"，会展示一个静态的登录表单以及验证相关的视觉元素。

### Logging in Users with Our API ### Part 1  
使用我们的 API 登录用户——第一部分20:53

Learn to implement a login flow with a login API, exchange HTTP requests for a cookie-based JWT authentication, and protect routes in a Node.js, Express, MongoDB bootcamp.  
学习如何使用登录 API 实现登录流程，配置 HTTP 请求以进行基于 cookie 的 JWT 认证，并在 Node.js、Express 和 MongoDB 环境中保护路由安全。

### Logging in Users with Our API ### Part 2  
使用我们的 API 登录用户——第二部分18:39

Learn to implement an isLoggedIn middleware in Express to read a jwt cookie, expose the user to pug templates via res.locals, and render login or user navigation across all routes.  
学习在 Express 中实现`isLoggedIn`中间件，以读取 jwt cookie 值；通过`res.locals`将用户信息传递给 Pug 模板；并在所有路由中显示登录状态或用户相关导航信息。

### Logging in Users with Our API ### Part 3  
使用我们的 API 登录用户——第三部分28:15

Finish implementing the login flow and alert system for the API. Build modular front-end code using parcel bundler and axios.  
完成登录流程的搭建，并配置好 API 的警报系统。使用 parcel bundler 和 axios 来构建模块化的前端代码。

### Logging out Users  注销用户13:56

Securely log out by overwriting the http only jwt cookie with a dummy value and a brief expiry, then trigger a logout route from the client.  
通过用虚拟值替换 http-only jwt cookie 中的数据，并设定较短的有效期，从而安全地注销会话。之后，在客户端触发一个注销操作的路由接口。

### Rendering Error Pages  渲染错误页面21:45

Learn to render error pages in a Node.js app by distinguishing API and rendered website errors, implementing operational vs unknown errors, and using a template-driven production strategy.  
学习如何在 Node.js 应用中处理错误页面，通过区分 API 错误与网站渲染过程中的错误来进行处理；实现对可识别错误与未知错误的区分处理；采用基于模板的生产策略来优化应用程序的性能。

### Building the User Account Page  
构建用户账户页面16:05

Build the user account page using a pug template and reusable nav items, protect the /me route, and display current user data (name, email, photo) with admin menu when applicable.  
使用 Pug 模板构建用户账户页面，并利用可复用的导航项设计界面。保护“/me”路由的访问权限，并在适当情况下，通过管理员菜单显示当前用户的数据（姓名、电子邮件、照片）。

### Updating User Data  更新用户数据17:07

Update user data on the account page via a traditional html form post with action and name attributes, urlencoded data; compare this to the upcoming api method with route protection.  
通过传统的 HTML 表单提交用户数据到账户页面，包含动作属性和名称属性，数据采用 url 编码方式传递。这与即将推出的带有路由保护的 API 方法有所不同。

### Updating User Data with Our API  
通过我们的 API 更新用户数据12:34

Update user data via api by implementing an updateData function that calls a patch request with axios, handles errors with alerts, and updates name and email from the front end.  
通过实现`updateData`函数来更新用户数据。该函数使用 axios 发送请求，处理错误时使用警告机制，并从前端部分更新用户的姓名和电子邮件信息。

### Updating User Password with Our API  
通过我们的 API 更新用户密码14:41

Learn to update user data or password via a unified update settings function, call data or password endpoints, handle password fields, and provide UI feedback while awaiting API responses.  
学习如何通过统一的更新设置功能来更新用户数据或密码。调用数据或密码相关端点，处理密码字段，并在等待 API 响应时提供用户界面反馈。

## 13.高级功能：支付功能、电子邮件通讯、文件上传  
20 次讲座 • 4 小时 4 分钟

### Section Intro  章节介绍0:32

Enhance your website API by implementing uploads, image processing, email sending, and credit card payments with Stripe. Explore these advanced features within a Node.js, Express, MongoDB bootcamp.  
通过集成 Stripe 的功能，您可以增强网站的 API 功能，包括文件上传、图像处理、邮件发送以及信用卡支付等。在 Node.js、Express 和 MongoDB 的培训课程中，您可以了解这些高级功能。

### Image Uploads Using Multer: Users  
使用 Multer 进行图片上传：用户指南10:12

Upload user photos with multer by configuring the destination public/image/users, creating a single-file upload middleware, and storing images on disk with a database link.  
通过使用 Multer 工具，可以通过配置目标目录为 public/image/users，创建单文件上传中间件，并将图片存储在磁盘上，同时利用数据库链接来保存图片，从而上传用户照片。

### Configuring Multer  配置 Multer13:45

Move multer code to controller, configure diskStorage to produce unique filenames using user id and timestamp, extract the extension from mimetype, apply image-only filter, export uploadUserPhoto, and test with postman.  
将 multer 代码移动到控制器中，配置 diskStorage 以使用用户 ID 和时间戳来生成唯一的文件名，从 mimetype 中提取扩展名，应用仅处理图像的过滤器，导出 uploadUserPhoto 功能，然后使用 Postman 进行测试。

### Saving Image Name to Database  
将图像名称保存到数据库中5:32

Learn how to save an uploaded image's filename to a user document in a Node.js app, set a default photo, and update user profiles with the photo field.  
学习如何在 Node.js 应用中将上传图像的文件名保存到用户文档中，设置默认照片，以及使用照片字段更新用户资料。

### Resizing Images  调整图像大小13:19

Learn to resize images with node js using sharp, convert to jpeg at 90% quality, and store in memory to create square 500x500 avatars for user photos.  
学习如何使用 Node.js 中的 sharp 库来调整图像大小，将其转换为 90%质量的 JPEG 图像，并存储在内存中，从而生成大小为 500x500 的方形头像，用于用户照片的展示。

### Adding Image Uploads to Form  
在表单中添加图片上传功能11:33

Add image uploads to user forms by implementing a file input, multipart form data, and an API call using form data and multer in a Node.js backend.  
通过在 Node.js 后端实现文件输入、多部分表单数据处理，以及利用 FormData 和 Multer 进行 API 调用，可以将图片上传功能集成到用户表单中。

### Uploading Multiple Images: Tours  
上传多张图片：旅游相关行程12:02

Learn to upload multiple tour images with a single image cover and an images array using multer fields, then test via Postman and prepare for in-memory processing and resizing.  
学习如何使用 Multer 工具将多张旅游图片上传到网站，每个图片都可以作为封面使用。同时，利用数组形式存储这些图片。之后通过 Postman 进行测试，为后续的内存处理与尺寸调整做好准备。

### Processing Multiple Images  
处理多张图片18:11

In the second part, fix the user controller by awaiting asynchronous image processing. Resize the cover to 2000x1333, then process and name tour images, updating the tour with filenames.  
在第二部分中，通过等待异步图像处理来完成用户控制器的设置。将封面尺寸调整为 2000x1333，然后处理并命名旅游图片，同时更新旅游图片的文件名。

### Building a Complex Email Handler  
构建复杂的电子邮件处理系统19:57

Build a robust email handler in Node.js using pug templates, SendGrid for production, Mailtrap for development, and a reusable Email class with sendWelcome and password reset emails.  
在 Node.js 中使用 Pug 模板构建一个强大的电子邮件处理工具。生产环境则使用 SendGrid 服务，而开发环境则使用 Mailtrap 框架。此外，还提供了一个可复用的电子邮件类，该类能够执行欢迎邮件和密码重置邮件的发送操作。

### Email Templates with Pug: Welcome Emails  
带有帕吉犬图案的电子邮件模板：欢迎邮件15:08

Build and send a welcome email with a Pug template and inline CSS. Dynamically populate first name, URL, and subject in the signup flow and test with Mailtrap.  
制作一封欢迎邮件，使用 Pug 模板并加入内嵌的 CSS 代码。在注册流程中自动填充姓名、URL 和主题信息，然后使用 Mailtrap 进行测试。

### Sending Password Reset Emails  
正在发送密码重置邮件7:07

Implement password reset emails by creating a passwordReset template, adding a sendPasswordReset method, integrating it with the auth controller, and testing the flow with Postman using a token link.  
通过创建 passwordReset 模板、添加 sendPasswordReset 方法、将其集成到认证控制器中，然后使用 Postman 通过令牌链接来测试整个流程，从而实现密码重置邮件的发送功能。

### Using Sendgrid for "Real" Emails  
使用 Sendgrid 来发送“真实”的电子邮件9:32

Learn to send real emails using SendGrid with nodemailer via SMTP relay, configure API keys, and test in production for reliable inbox delivery.  
通过使用 SendGrid 服务，借助 nodemailer 的 SMTP 中继功能，您可以学习如何发送真实的电子邮件。同时，您可以配置 API 密钥，并在实际环境中进行测试，以确保电子邮件能够成功送达收件人邮箱。

### Credit Card Payments with Stripe  
使用 Stripe 进行信用卡支付10:08

Learn to implement credit card payments with Stripe by creating a server-side Stripe Checkout Session, using public and secret keys, client requests, and webhooks for bookings.  
通过创建服务器端 Stripe Checkout 会话，使用公共密钥和秘密密钥，以及客户端请求和 Web 钩子功能，来学习如何实施信用卡支付功能。

### Integrating Stripe into the Back-End  
将 Stripe 集成到后端系统中  22:34

Learn to integrate Stripe into the back-end by building a protected checkout session API, using tour data to configure Stripe checkout, and returning the session to the client.  
学习如何将 Stripe 集成到后端系统中。具体包括：创建一种受保护的结账会话 API，利用旅游数据来配置 Stripe 结账流程，以及将会话返回给客户端。

### Processing Payments on the Front-End  
在前端处理支付事务23:04

Configure a front-end payments flow with stripe, including a login-aware book tour button, data-tour-id attributes, axios-based checkout session, and redirect to stripe checkout.  
配置一个使用 Stripe 的前端支付流程，包括一个支持登录的图书巡展按钮、data-tour-id 属性、基于 Axios 的结账会话，以及重定向到 Stripe 结账页面的功能。

### Modelling the Bookings  对预订情况进行建模6:23

Create a booking model using Mongoose, referencing both tour and user, include price and createdAt, and default paid to true, with automatic population of just the tour name on queries.  
使用 Mongoose 创建预订模型，同时引用游客和用户的信息。该模型应包含价格以及创建时间，并将默认值为“true”，这样在查询时就能自动填充游客的名称。

### Creating New Bookings on Checkout Success  
在结账成功的情况下创建新的预订订单19:07

Create a new booking on checkout success using a temporary, insecure workaround by passing tour, user, and price in the query string, then redirect to the homepage.  
在结账成功时，通过一种临时且不安全的解决方法来创建新的预订。在查询字符串中传入导游信息、用户信息和价格，然后重定向到主页。

### Rendering a User's Booked Tours  
渲染用户已预订的旅行行程11:16

Render the my-tours page by fetching the logged-in user's bookings, extracting tour IDs, querying tours with an in operator, and reusing the overview template to display booked tours.  
通过获取已登录用户的预订信息，提取旅游活动的 ID，然后使用`in`操作符查询有预订活动的旅游项目，再利用现有的概览模板来展示已预订的旅游活动，从而生成“我的旅行”页面。

### Finishing the Bookings API  
完成预订 API 的接口操作7:12

Finish the bookings API by implementing all CRUD operations using a factory, securing routes with protect and restrictTo admin and lead guide, and testing with Postman.  
通过实现工厂模式，来完成所有 CRUD 操作的实现，确保路由的安全性，使用 protect 和 restrictTo 来限制访问权限，同时针对管理员和引导员进行测试，使用 Postman 工具进行最后的测试。

### Final Considerations  最终思考7:28

Explore final considerations for enhancing the API and website, including booking logic, date availability, authentication upgrades, reviews, favorites, admin pages, and deployment readiness.  
探讨完善 API 和网站功能的最后细节，包括预订逻辑、日期可用性、身份验证升级、用户评价、收藏功能、管理页面设计以及部署准备等方面。

## 14.配置 Git 以及进行部署  
10 次讲座 • 每次 2 小时 11 分钟

### Section Intro  章节介绍0:40

Set up Git and GitHub, prepare the application for deployment to production, and deploy to Heroku to share your project with the world.  
安装好 Git 和 GitHub，准备将应用程序部署到生产环境，然后将其部署到 Heroku 上，这样就能让全世界的人都能看到你的项目了。

### Setting Up Git and GitHub  
配置 Git 和 GitHub4:38

Install git, set up a GitHub account, and learn that git is a version control system that saves code via commits and branches in a repository using the command line.  
安装 Git，创建一个 GitHub 账户。了解 Git 是一种版本控制系统，它可以通过命令行将代码保存在仓库中，利用分支和提交来实现代码的保存和管理。

### Git Fundamentals  Git 基础知识10:33

Learn how to initialize a git repository, create a .gitignore to exclude node_modules and .env files, and stage and commit changes to the master branch.  
了解如何初始化一个 Git 仓库，如何创建.gitignore 文件来排除 node_modules 和.env 文件，以及如何将更改暂存并提交到 master 分支。

### Pushing to GitHub  将项目推送到 GitHub 上10:27

Push your local code to a new GitHub repository, connect it as origin, and push master while managing commits, a readme.md, and ignored files like node_modules.  
将你的本地代码推送到新的 GitHub 仓库中，将其链接为 origin 源，并在管理提交的同时推送 master 分支。同时准备一个 readme.md 文件，并忽略诸如 node_modules 这样的文件。

### Preparing Our App for Deployment  
准备我们的应用程序以进行部署10:55

Learn to prepare a Node.js app for deployment by adding compression middleware, cleaning production logs, switching to relative API URLs, building a final bundle, and committing for deployment.  
学习如何准备一个 Node.js 应用程序以进行部署，包括添加压缩中间件、清理生产日志、使用相对 API 地址、构建最终的软件包，以及进行提交操作。

### Deploying Our App to Heroku  
将我们的应用程序部署到 Heroku 上26:30

Deploy and scale a Node.js app on Heroku by installing the platform, configuring production settings, pushing to a Heroku remote, and verifying with logs, environment variables, and production URLs.  
通过安装相关平台、配置生产环境设置、将代码推送到 Heroku 远程服务器，以及通过日志、环境变量和生产 URL 进行验证，可以在 Heroku 上部署和扩展 Node.js 应用程序。

### Testing for Secure HTTPS Connections  
测试安全的 HTTPS 连接5:52

Learn to test for secure https connections on Heroku by checking req.secure or the x-forwarded-proto header, and configure express to trust proxies to ensure secure cookies.  
学习如何在 Heroku 上通过检查`req.secure`或`x-forwarded-proto`头部的值来判断连接是否安全，并配置 Express 以信任代理服务器，从而确保 cookie 的安全。

### Responding to a SIGTERM Signal  
响应 SIGTERM 信号7:21

Learn to respond to a sigterm signal from Heroku by listening for the event and gracefully closing the server to finish pending requests, verified by dyno restarts and logs.  
学习如何响应 Heroku 发送的 sigterm 信号，通过监听事件并优雅地关闭服务器以完成挂起的请求，验证方式为 dyno 重启和日志。

### Implementing CORS  实现 CORS 功能15:53

Implement cross-origin resource sharing (CORS) for your API using the npm package to enable cross-origin requests. Explain simple vs non-simple requests and preflight options to return access-control headers.  
为您的 API 实现跨源资源共享功能，使用 npm 包来支持跨源请求。需要区分简单请求与复杂请求，并了解如何返回访问控制头部信息。

### Finishing Payments with Stripe Webhooks  
通过 Stripe Webhooks 完成支付流程38:08

Finish a secure Stripe integration by implementing webhook checkout, validating signatures, and creating bookings from session data, with live deployment and user feedback alerts.  
通过实现 Webhook 验证、签名验证，以及基于会话数据进行预订功能，来完成 Stripe 接口的整合工作。同时，要确保系统的实时部署，并收集用户反馈以优化系统性能。

## 15.结束语 
就是这样，各位！
2 次讲座 • 每次 5 分钟

### Where to Go from Here  
接下来该去哪里呢3:39

Put your Node.js skills to work, tackle challenges, and build new projects such as a quiz app, a forum, restaurant management software, or a social networking app.  
发挥你的 Node.js 技能，挑战难题，打造新的项目，比如测验应用、论坛、餐厅管理软件或社交网络应用。

### My Other Courses + Updates  
我的其他课程与更新信息

# 要求/条件
Requirements  

### Absolutely NO understanding of Node or back-end development is required! I take you from beginner to advanced developer!  
完全不需要对 Node 或后端开发有任何了解！我会带你从初学者成长为高级开发者！

### Basic understanding of JavaScript is required (the course contains a section about asynchronous JavaScript with promises and async/await in case you need to get up to speed)  
需要具备基本的 JavaScript 知识（课程中会包含关于异步 JavaScript 的内容，涉及 Promise 和 async/await 技术，因此如果你需要掌握这些知识的话，那么具备一定的 JavaScript 基础是必须的）。

### Basic understanding of HTML is a plus (only for final part of the course), but NOT a must  
如果具备基本的 HTML 知识的话，那当然很好了（不过这仅适用于课程的最后部分内容），但并没有硬性要求。

### Any computer and OS will work — Windows, macOS or Linux  
任何计算机和操作系统都能胜任这项任务——无论是 Windows、macOS 还是 Linux 系统。


# 描述
Description  

Do you want to build fast and powerful back-end applications with JavaScript? Would you like to become a more complete and in-demand developer?  
你想用 JavaScript 构建快速且强大的后端应用程序吗？你想成为更优秀、更受需求的开发者吗？

Then Node.js is the hot technology for you to learn right now, and you came to the right place to do it!  
那么，Node.js 正是你现在应该学习的热门技术，你来到这里真是太对了！

Welcome to the Complete Node.js, Express, and MongoDB Bootcamp, your fast track to modern back-end development.  
欢迎来到完整的 Node.js、Express 和 MongoDB 培训课程，这是通往现代后端开发的捷径。

**This course is the perfect all-in-one package that will take you from a complete beginner to an advanced, highly-skilled Node.js developer.  
这门课程是一个完美的全方位学习方案，能帮助你从完全初学者成长为一名经验丰富的 Node.js 开发专家。**

Like all my other courses, this one is completely project-based! And not just any project: it's a complete, beautiful, and feature-rich application, containing both a RESTful API and a server-side rendered website. It's the most fantastic and complete project that you will find in any Node.js course on the internet!  
就像我所有的课程一样，这个课程也是完全以项目为基础进行教学的！而且，这个项目并非普通的项目——它是一款功能齐全、美观且富有特色的应用程序，包含了一个 RESTful API 和一个服务器端渲染的网站。这是互联网上所有 Node.js 课程中最为精彩且完整的项目了！

**By building this huge project, you will learn all the skills that you need in order to plan, build, and deploy your own modern back-end applications with Node.js and related technologies.  
通过参与这个大型项目，你将掌握所有必要的技能，从而能够使用 Node.js 及相关技术来规划、构建和部署自己的现代后端应用程序。**

_(If you feel like exploring the project, you can do so at www[.]natours[.]dev. And this is only a small part of the project! Log in with "laura@example.com" and password "test1234")  
（如果您想了解该项目的更多内容，可以访问 www.natours.dev 网站进行探索。而这仅仅只是该项目的一小部分而已！请使用“laura@example.com”作为用户名，密码为“test1234”，然后登录即可。）_

  

**After finishing this course, you will:  
完成本课程后，你将能够：**

**1)** Be building your own fast, scalable, and powerful Node.js RESTful APIs or web applications;  
1) 正在构建属于自己的快速、可扩展且强大的 Node.js RESTful API 或 Web 应用程序；

**2)** Truly understand how Node.js works behind the scenes;  
2) 真正理解 Node.js 在幕后是如何工作的；

**3)** Be able to work with NoSQL data and model data in real-world situations (a hugely important skill);  
3) 能够处理 NoSQL 数据，并在实际场景中构建数据模型（这是一项非常重要的技能）；

**4)** Know how modern back-end development works, and how all the different technologies fit together (hard to understand from scattered tutorials and videos);  
4) 了解现代后端开发的工作原理，以及各种技术是如何相互结合的（这些知识很难通过零散的教程和视频来理解）；

**5)** Have experience in professionally used tools and libraries like Express, Mongoose, Stripe, Sendgrid, Atlas, Compass, Git, Heroku, and many more;  
5) 拥有使用过相关工具的经验，例如 Express、Mongoose、Stripe、Sendgrid、Atlas、Compass、Git、Heroku 等工具；

**6)** Have built a complete application, which is a perfect starting point for your own applications in the future.  
6) 已经完成了一个完整的应用程序开发，这将是你未来自己开发其他应用程序的完美起点。

Please note that this course is **NOT** for absolute web development beginners, so you should already be familiar with basic JavaScript. **NO** back-end experience required though!  
请注意，本课程并不适合完全没有网页开发经验的初学者。因此，你们应该已经掌握了基本的 JavaScript 知识。不过，无需具备任何后端开发经验即可参加本课程！

  

**It's an absolutely full-packed, deep-dive course with over 40 hours of content!  
这是一门内容丰富的课程，总共包含超过 40 小时的学习时长！**

Since this is the "Complete Node.js Bootcamp", the course is crammed with tons of different technologies, techniques, and tools, so that you walk away from the course as a complete Node.js developer.  
由于这是《完整的 Node.js 培训课程》，因此课程中包含了大量的各种技术、技巧和工具。通过这门课程的学习，你将会成为一个全面的 Node.js 开发者。

That's why the course turned out to be over 40 hours long. But if that sounds like too much for you, don't worry, there are videos or entire sections that you can safely skip.  
这就是为什么整个课程的总时长达到了 40 多个小时。不过，如果你觉得这个时间太长，不用担心，有一些视频或章节是可以直接跳过的。

Here is exactly what you're gonna learn:  
这就是你将要学习的内容：

### Fundamentals of Node.js, core modules, and NPM (Node Package Manager)  
Node.js 的基础知识、核心模块以及 NPM（Node 包管理器）

### How Node.js works behind the scenes: event loop, blocking vs non-blocking code, event-driven architecture, streams, modules, etc.  
Node.js 在幕后是如何工作的：事件循环、阻塞代码与非阻塞代码、事件驱动架构、流处理、模块系统等。

### Fundamentals of Express (Node.js framework): routing, middleware, sending responses, etc.  
Express 的基础知识（Node.js 框架）：路由处理、中间件、发送响应等。

### RESTful API design and development with advanced features: filtering, sorting, aliasing, pagination  
具有高级功能的 RESTful API 设计与开发：过滤、排序、别名生成、分页等功能。

### Server-side website rendering (HTML) with Pug templates  
基于 Pug 模板的服务器端网站渲染（HTML 格式）

### CRUD operations with MongoDB database locally and on the Atlas platform (in the cloud)  
在本地使用 MongoDB 数据库进行 CRUD 操作，同时也可以在云端的 Atlas 平台上进行此类操作。

### Advanced MongoDB: geospatial queries, aggregation pipeline, and operators  
高级 MongoDB：地理空间查询、聚合管道操作以及运算符

### Fundamentals of Mongoose (MongoDB JS driver): Data models, CRUD operations, data validation, and middleware  
MongoDB 的基础知识（MongoDB JS 驱动程序）：数据模型、CRUD 操作、数据验证以及中间件机制

### Advanced Mongoose features: modeling geospatial data, populates, virtual populates, indexes, etc.  
高级 Mongoose 的功能包括：对地理空间数据进行建模、批量插入数据、虚拟插入数据、索引处理等。

### Using the MVC (Model-View-Controller) architecture  
采用 MVC（模型-视图-控制器）架构

### How to work with data in NoSQL databases  
如何在 NoSQL 数据库中处理数据

### Advanced data modelling: relationships between data, embedding, referencing, and more  
高级数据建模：数据之间的关系处理、嵌入策略、引用机制等

### Complete modern authentication with JWT: user sign up, log in, password reset, secure cookies, etc.  
通过 JWT 实现完整的现代身份验证功能：用户注册、登录、密码重置、安全 Cookie 存储等。

### Authorization (user roles)  
授权（用户角色）

### Security: best practices, encryption, sanitization, rate limiting, etc.  
安全性：最佳实践、加密技术、数据清理、速率限制等。

### Accepting credit card payments with Stripe: Complete integration on the back-end and front-end  
使用 Stripe 接受信用卡支付：在后端和前端都完成了整合工作。

### Uploading files and image processing  
上传文件和进行图像处理

### Sending emails with Mailtrap and Sendgrid  
使用 Mailtrap 和 Sendgrid 发送电子邮件

### Advanced error handling workflows  
高级的错误处理工作流程

### Deploying Node.js application to production with Heroku  
使用 Heroku 将 Node.js 应用程序部署到生产环境

### Git and GitHub crash course  
Git 和 GitHub 基础教程

### And so much more!  还有更多的内容！


  

**Why should you learn Node.js and take this course?  
为什么你应该学习 Node.js 并参加这门课程呢？**

If you want to learn Node.js and modern back-end development, then there is no doubt that this course is for you!  
如果你想要学习 Node.js 和现代后端开发技术，那么毫无疑问，这门课程非常适合你！

It's the biggest Node.js course on the internet, it has by far the most complete course project, and offers the most in-depth explanations of all topics included.  
这是互联网上最大的 Node.js 课程，其课程项目也是最为完整的。对于所有涉及的主题，课程都提供了最为深入的讲解。

And even if you already know some Node.js, you should still take this course, because it contains subjects that are not covered anywhere else, or not in the same depth!  
即使你已经掌握了一定的 Node.js 知识，你仍然应该参加这门课程。因为课程中的内容在其他地方是无法找到的，而且讲解的深度也远远超过了其他课程！

_But maybe you're not yet convinced that Node.js really is the right technology for you to learn right now?  
不过，也许你仍然不确定 Node.js 是否真的是你目前应该学习的合适技术呢？_

Well, first, Node.js will allow you to use your JavaScript skills to build applications on the back-end. That itself is a huge gain, which makes your full-stack development process so much easier and faster.  
嗯，首先，Node.js 将允许你使用你的 JavaScript 技能来构建后端应用程序。这本身就是一个巨大的收获，这使得你的全栈开发过程变得更加容易和快速。

Plus, popularity and opportunities for Node.js are off the charts. It's a modern, proven, and reliable technology, used by tech giants (and 6-figure-salary-paying companies) like Netflix, PayPal, Uber, and many more.  
此外，Node.js 的受欢迎程度和就业机会也非常高。这是一种现代化、成熟且可靠的技术，被诸如 Netflix、PayPal、Uber 等科技巨头以及那些薪资高达六位数的公司所采用。

Node.js really is what you should invest your time in, instead of outdated technology like PHP.  
Node.js 确实是你应该投入时间发展的技术，而不是像 PHP 这样的过时技术。

In summary, if you already know JavaScript, learning Node is the logical next step for you! **_It will make you a better, more versatile, and more complete developer, which will ultimately boost your opportunities in the job market!_**  
总结来说，如果你已经掌握了 JavaScript 知识，那么学习 Node.js 无疑是你下一步合理的选择！这将使你成为更优秀、更全能、更全面的开发者，从而提升你在就业市场上的机会！

_And I created this course to help you do exactly that! It really is the course I wish I had when I was first learning back-end development with Node.js and all related technologies.  
我创建这个课程就是为了帮助你们实现这一目标！这真是我当年学习 Node.js 及相关技术时想要拥有的课程啊。_

  

**And this is what you get by signing up today:  
这就是您今天注册后所获得的内容：**

### Lifetime access to 40+ hours of HD quality videos. No monthly subscription. Learn at your own pace, whenever you want;  
终身访问超过 40 小时的高清视频内容。无需订阅费用。随时按照自己的节奏学习，随心所欲地观看视频。

### Friendly and fast support in the course Q&A whenever you have questions or get stuck;  
无论您遇到什么问题或遇到困难时，都会提供友善且迅速的帮助和支持；

### English closed captions (not the auto-generated ones provided by Udemy);  
英语字幕（不是 Udemy 提供的自动生成字幕）；

### Course slides in PDF format;  
以 PDF 格式提供的课程幻灯片；

### Downloadable assets, starter code, and final code for each section;  
可下载的素材、示例代码以及每个部分的最终代码；

### Lots of small challenges are included in the videos so you can track your progress.  
这些视频中包含了许多小挑战，这样你就可以随时跟踪自己的进度。


  

**And now, I hope to welcome you as a new student in my course! So click that "Enroll" button right now, and join me in this adventure today!  
现在，我希望能欢迎你成为我的课程的新学生！所以请立即点击“注册”按钮，和我一起开始这一天的学习之旅吧！**

But if you're not 100% sure yet, just go ahead and watch the promo video to take a look at the course project. I promise you will be amazed :)  
不过，如果你还不太确定，那就直接观看宣传视频吧，这样就能了解该课程的项目了。我保证你会感到惊讶的 :)

**See you in the course!  
我们在课程中相见吧！**

# 这门课程适合谁
Who this course is for:  

### Take this course if you want to build amazingly fast and scalable back-end applications using the JavaScript skills you already have. Node is the perfect tool for you!  
如果你希望利用已有的 JavaScript 技能来构建快速且可扩展的后端应用程序，那么请选择这门课程。Node.js 正是实现这一目标的理想工具！
### Take this course if you're a front-end developer looking to go into back-end development using the most complete course on the market.  
如果你是一名前端开发者，希望转向后端开发领域，那么请选择这门市场上最完整的课程。
### Take this course if you have taken other Node courses but: 1) still don't feel confident to code real-world apps, or 2) still feel like you need more back-end skills. This course is perfect for you!  
如果你已经修过其他 Node.js 课程，但仍然存在以下情况，那么请选择这门课程：1) 你仍然不自信地编写实际的应用程序；2) 你认为自己还需要更多的后端技能。这门课程非常适合你！
### Take this course if you're an experienced Node developer who wants to add new skills missing in other courses: How Node works behind the scenes, advanced data modelling, geospatial data, complete and secure authentication, stripe payments, and more.  
如果你是一名经验丰富的 Node.js 开发者，希望获得其他课程中缺乏的新技能，那么请选择这门课程。课程内容包括：Node.js 背后的工作原理、高级数据建模、地理空间数据处理、完整且安全的身份验证机制、Stripe 支付处理等。