Build, deploy, and scale an E-Commerce app using Microservices built with Node, React, Docker and Kubernetes  
利用基于 Node、React、Docker 和 Kubernetes 构建的微服务来开发、部署和扩展电子商务应用程序。

# What you'll learn  你将学到什么

- Architect large, scalable apps using a collection of microservices  
利用一系列微服务来构建大型、可扩展的应用程序。

- Deploy a multi-service app to the cloud with Docker and Kubernetes  
利用 Docker 和 Kubernetes 将多服务应用程序部署到云端。

- Solve concurrency issues in a distributed systems environment  
解决分布式系统环境中的并发问题

- Leverage your Javascript skills to build a complex web app  
利用你的 JavaScript 技能来打造复杂的网页应用吧。

- Build a Server-Side Rendered React App to render data from your microservices  
构建一个服务器端渲染的 React 应用，用于展示来自微服务的数据。

- Understand how enterprise companies design their infrastructure  
了解企业是如何设计其基础设施的

- Share reusable code between multiple Express servers using custom NPM packages  
利用自定义的 NPM 包，在多个 Express 服务器之间共享可重复使用的代码。

- Write comprehensive tests to ensure each service works as designed  
编写全面的测试，以确保每个服务按设计工作

- Communicate data between services using a lightning-fast event bus  
利用极快的事件总线，在各个服务之间传输数据。

- Write nothing but production-level code. No cutting corners!  
只编写符合生产级标准的代码。绝不要偷工减料！


# This course includes:  本课程包含以下内容：

- 54 hours on-demand video  54 小时的点播视频时间

- 70 articles  70 篇文章

- 348 downloadable resources  
348 种可下载的资源

# 目录

26 sections • 654 lectures • 54h 22m total length  
26 个章节 • 654 次讲座 • 总时长 54 小时 22 分钟

  展开所有部分

##   
关于微服务的核心概念  9 次讲座 • 每次 46 分钟

### How to Get Help  如何获得帮助  预览版0:56

Learn how to get help during this microservices course: use Udemy discussion boards, contact the instructor on Udemy or Twitter, and receive timely answers during weekdays.  
在这门关于微服务的课程中，你将学会如何在需要帮助时获得帮助：可以使用 Udemy 的论坛进行讨论，或者联系讲师在 Udemy 或 Twitter 上的账号，这样就能在工作日内获得及时的回复。

### Course Resources  课程资源0:38

### What Is a Microservice?  什么是微服务？  预览版3:19

Compare monolithic servers with microservices. Learn how a microservice contains routing, middleware, business logic, and database access to implement a single feature.  
比较单体服务器与微服务。了解微服务如何包含路由、中间件、业务逻辑和数据库访问来实现单个功能。

### Data in Microservices  微服务中的数据  预览版7:34

Discover data management in microservices via the database per service pattern, where each service has its own database and never accesses another's data, improving independence and uptime.  
通过每个服务一个数据库的模式发现微服务中的数据管理，每个服务拥有自己的数据库，从不访问其他服务的数据，从而提高独立性和正常运行时间。

### Quiz ### Data in Microservices  
问答环节 ### 微服务中的数据

### Big Problems with Data  数据方面的问题相当严重5:08

Examine data management challenges of a database-per-service approach and compare monolith versus microservices for ecommerce. Explain why services cannot access each other's databases and preview two methodologies for service dx.  
探讨数据库服务化架构在数据管理方面所面临的挑战，并对比了传统单体架构与微服务架构在电子商务领域的优缺点。解释为什么各个服务无法访问彼此的数据库，同时介绍两种服务设计的方法论。

### Sync Communication Between Services  
服务之间的同步通信6:53

Compare synchronous and asynchronous communication strategies in microservices. Learn how direct service requests affect dependencies, latency, and database per service in a scalable e-commerce context.  
比较微服务中同步和异步通信策略。了解直接服务请求如何影响依赖关系、延迟情况以及可扩展电子商务环境中的各个服务的数据库性能。

### Event-Based Communication  
基于事件的通信5:19

Explore asynchronous communication via an event bus to emit and receive events across services, examine the downsides of a single event bus and first method, and preview a second approach.  
探索通过事件总线进行异步通信，以发射和接收跨服务的事件，研究单一事件总线的缺点和第一种方法，并预览第二种方法。

### A Crazy Way of Storing Data  
一种疯狂的数据存储方式9:48

Discover asynchronous cross-service communication by building Service dx that uses an event bus to capture user, product, and order events across services A, B, and C, updating a user-product database.  
发现异步跨服务通信，通过构建 Service dx 使用事件总线捕获服务 A、B 和 C 中的用户、产品和订单事件，并更新用户-产品数据库。

### Pros and Cons of Async Communication  
异步通信的优缺点6:15

Explore the pros and cons of asynchronous communication in microservices, focusing on services with zero direct dependencies, data duplication challenges, and the overhead of event-driven patterns.  
探讨在微服务中采用异步通信方式的优缺点，重点关注那些没有直接依赖关系的服务、数据重复问题，以及事件驱动模式所带来的额外开销。


##   一款小型微服务应用程序  
43 场讲座 • 3 小时 35 分钟

### Important ### Optional Boilerplate  
重要——可选的模板内容0:31

### App Overview  应用概述5:44

Explore building a microservices demo app with two resources, posts and comments. Learn how post creation and comment linking work and how asynchronous patterns enable cross-service communication.  
尝试构建一款基于微服务的演示应用，该应用涉及两种资源：帖子与评论。了解帖子的创建过程以及评论之间的链接机制，同时了解异步模式如何实现跨服务的通信。

### Project Setup  项目设置4:57

Set up a microservices workflow with a React client communicating to Express-based post and comments services, using in-memory data and a three-project structure (client, posts, comments).  
构建一个基于微服务的工作流程，其中使用 React 客户端与基于 Express 的帖子及评论服务进行通信。数据采用内存存储方式处理，系统结构分为三个部分：客户端、帖子模块和评论模块。

### Posts Service Creation  帖子服务创建8:18

Create an Express post service with two routes: get all posts and create a post, storing posts in memory, generating IDs, parsing JSON bodies, and running on port 4000.  
创建一个 Express 的 POST 服务，包含两个路由：获取所有帖子、创建新帖子。帖子数据应存储在内存中，同时需要生成 ID、解析 JSON 体数据，并在 4000 端口上进行处理。

### Testing the Posts Service  
测试帖子服务4:04

Manually test the posts service with Postman by posting JSON to /posts and verifying a 201 Created response with id and title, then retrieve all posts with GET /posts.  
可以通过使用 Postman 手动测试帖子服务，方法是向/articles 路径发送 JSON 数据，并验证返回是否包含状态码为 201 的创建响应，同时还需要获取带有 ID 和标题的帖子信息。之后，可以使用 GET /posts 接口来获取所有帖子内容。

### Implementing a Comments Service  
实施评论服务8:22

Implement a comments service with routes to create and list comments by post ID, storing in memory and generating random IDs from a content body, via Express.  
实现一个评论服务，该服务可以通过帖子 ID 来创建和列出评论。评论数据会存储在内存中，同时服务还会从内容体中生成随机的评论 ID。整个系统将通过 Express 框架来实现。

### Quick Comments Test  快速评论测试3:57

Test the two express services on ports 4000 and 4001 with postman, create and fetch comments via /posts/{id}/comments, and prepare to solve issues with a microservices style React app.  
使用 Postman 测试端口 4000 和 4001 上的两种快速服务接口。通过/posts/{id}/comments 接口创建和获取评论。同时，要准备好解决基于微服务架构的 React 应用程序中可能遇到的各种问题。

### Note on the React App  
关于 React 应用程序的说明0:20

### Addressing Default Export and ReactDom.render Warnings  
处理默认导出和 ReactDom.render 警告0:39

### React Project Setup  React 项目设置5:00

Design and set up React app for the microservices project, implementing an app component with post create, post list, comment list, and comment create components, wiring Axios to Express API.  
为微服务项目设计和构建 React 应用程序。包括创建一个应用程序组件，该组件具备创建记录、显示列表、评论列表以及创建评论的功能。同时，将 Axios 库与 Express API 进行集成。

### Building Post Submission  提交邮件后，请等待邮件处理完成。9:54

Create a post by submitting a simple form that captures a blog post title, sends it to the post service on port 4000 /posts via axios, and resets the input.  
通过提交一个简单的表单来创建一篇帖子，该表单包含博客文章的标题。然后使用 axios 将标题发送到端口 4000 的 post 服务接口，之后重置输入的表单数据。

### Handling CORS Errors  处理 CORS 错误4:08

Configure cross-origin resource sharing (CORS) on the post and comments services by installing and wiring the cors middleware in Express, allowing browser requests between localhost:3000 and localhost:4000.  
通过在 Express 中安装并连接 cors 中间件来配置帖子和服务评论服务的跨域资源共享 (CORS)，允许 localhost:3000 和 localhost:4000 之间的浏览器请求。

### Fetching and Rendering Posts  
获取并渲染帖子10:04

Fetch and render posts in a React app by wiring the post list to the post service with axios and useEffect, and display post titles as cards.  
在 React 应用中通过 axios 和 useEffect 将帖子列表连接到帖子服务来获取和渲染帖子，并将帖子标题显示为卡片。

### Creating Comments  创建评论8:09

Create a React comment creation component that captures user input, posts it to the comments service for a given post ID, and displays under each post in the list.  
创建一个 React 的评论创建组件，该组件可以捕获用户输入，将其发布到与给定帖子 ID 相关的评论服务中，并展示在列表中的每个帖子的下方。

### Displaying Comments  显示评论7:23

Fetches comments for a post from the comments microservice using the post id passed from the post list. Renders each comment content in a list by mapping fetched data.  
根据从帖子列表传递过来的帖子 ID，从评论微服务中获取评论内容。通过映射获取的数据，将每条评论的内容以列表形式展示出来。

### Completed React App  完成的 React 应用程序0:13

### Request Minimization Strategies  
请求最小化策略4:54

Explore request minimization strategies in microservices with Node.js and React, comparing synchronous and asynchronous communication to reduce multiple post and comment service fetches.  
探索在基于 Node.js 和 React 的微服务中减少请求次数的策略。比较同步和异步通信方式，以减少多次调用服务以获取数据的情况。

### An Async Solution  一种异步解决方案7:16

Explore asynchronous communication with an event broker and a query service to build fast, decoupled microservices that emit and process post and comment events, while weighing data duplication and complexity.  
探索使用事件代理和查询服务进行异步通信，以构建快速、解耦的微服务，这些微服务会发出和处理帖子和评论事件，同时权衡数据重复和复杂性。

### Common Questions Around Async Events  
关于异步事件的常见问题4:14

Explore common questions about asynchronous event architecture, including when to join data across resources without extra services, why independent services boost reliability, and handling corner cases with an event bus.  
探索关于异步事件架构的常见问题，包括如何在无需额外服务的情况下跨资源合并数据，为什么独立服务可以提高可靠性，以及如何使用事件总线处理边缘情况。

### Event Bus Overview  事件总线概述5:14

Build a simple, from-scratch event bus with Express in Node.js to publish events between services and learn the event flow behind a production-grade event bus.  
在 Node.js 中使用 Express 构建一个简单的事件总线，用于在服务之间发布事件，并了解生产级事件总线背后的事件处理流程。

### Important Note about Node and Unhandled Promise Rejections  
关于节点和未处理的 Promise 拒绝的重要说明0:21

### A Basic Event Bus Implementation  
一种基本的事件总线实现方式5:22

Set up a basic event bus with Express and Axios, handling post /events and forwarding events to services on localhost ports 4001 and 4002, then listen on 4005.  
使用 Express 和 Axios 构建一个基本的事件总线，处理 POST 请求/事件，并将事件转发到本地主机上的 4001 和 4002 端口对应的服务中。然后，在 4005 端口上监听这些事件。

### Emitting Events  触发事件5:18

Emit post created events to the event bus whenever a new post is added, including a type and data payload that the bus echoes to other services.  
每当有新的帖子被添加时，系统会向事件总线发布相应的事件。这些事件包含类型和数据 payload 信息，而事件总线会将这些信息传递给其他服务。

### Emitting Comment Creation Events  
发布评论的创建事件3:45

Emit a comment created event from the comments service to the event bus, including the comment data and post id, and observe how other services receive and handle these events.  
从评论服务中生成一个评论，并将其发送到事件总线中，包括评论数据和帖子 ID。然后观察其他服务如何接收和处理这些事件。

### Receiving Events  接收事件4:32

Implement and test event handlers for the post and common services by receiving events from the event bus, logging event types, and preparing data for the upcoming query service.  
实现并测试针对 post 服务和通用服务的事件处理程序。通过接收来自事件总线的事件信息，记录事件类型，并准备相关数据以用于即将到来的查询服务。

### Creating the Data Query Service  
创建数据查询服务4:42

Create a node express query service that aggregates posts and comments from post created and comment created events, exposes get /posts for listing, and accepts events at post /events.  
创建一个节点 Express 查询服务，能够汇总来自帖子创建事件和评论创建事件的帖子和评论。该服务可以提供用于列表展示的“get /posts”接口，并且能够接收来自“post /events”接口的事件数据。

### Parsing Incoming Events  解析传入的事件信息7:02

Explore how the query service parses post created and comment created events to populate a post object with id, title, and a comments array.  
了解查询服务是如何解析创建后的帖子以及评论事件，并将这些信息用于填充具有 ID、标题以及评论数组的帖子对象。

### Using the Query Service  使用查询服务7:09

Learn how a React app uses the query service on port 4002 to fetch posts with embedded comments, replacing direct calls to post and common services, demonstrating independent services.  
了解如何使用 React 应用程序通过端口 4002 调用查询服务来获取包含嵌入评论的帖子，从而替代对 post 和常见服务的直接调用，从而展示出服务之间的独立性。

### Adding a Simple Feature  添加简单特征4:46

Add a simple content moderation feature by creating a separate moderation service to flag comments containing the word orange, with statuses approved, rejected, or pending, avoiding hardcoded filters.  
通过创建一个独立的审核服务，可以添加一个简单的内容审核功能。该服务可以标记包含“orange”这个词的评论，并给出审核通过、驳回或待处理等多种状态，从而避免使用硬编码的过滤规则。

### Issues with Comment Filtering  
关于评论过滤的问题7:43

Explore how a moderation service filters comments in a microservice architecture. Trace the event-driven flow from comment creation to moderation and query persistence, and examine latency and user experience implications.  
了解在微服务架构中，审核服务是如何过滤评论的。追踪从评论创建到审核以及评论存储过程中的整个流程，并考虑这一过程对延迟和用户体验的影响。

### A Second Approach  第二种方法5:37

This second option emits comment created events to the moderation and query services, exposing the comment with a pending status immediately and updating it to approved or rejected after moderation.  
这个第二个选项会向审核和查询服务发送带有待处理状态的评论，这些评论会在审核过程中被标记为已批准或被拒绝。

### How to Handle Resource Updates  
如何处理资源更新问题4:45

Describe a two-event pattern for resource updates in microservices: domain-specific comment moderated events handled by the comments service, and generic comment updated events consumed by the query service.  
描述微服务中资源更新的两事件模式：由评论服务处理的特定于领域的评论审核事件，以及被查询服务消费的通用评论更新事件。

### Creating the Moderation Service  
创建审核服务4:52

Create a moderation service boilerplate, wire up an express API to listen for comment created events, and emit a comment moderated event on port 4003.  
创建一个审核服务模板，连接一个 express API 来监听评论创建事件，并在端口 4003 上发出评论审核事件。

### Adding Comment Moderation  
添加评论审核功能4:26

Add a pending status to new comments, emit a comment created event to update moderation and query services, and let moderation set approved or rejected based on orange presence.  
在新的评论中添加待处理状态，当评论创建完成后会触发相应的事件，以便更新审核系统并查询相关服务。同时，让审核人员根据用户的在线状态来决定是否批准该评论。

### Reminder about Error Catching  
关于错误捕获的提醒0:11

### Handling Moderation  处理适度性5:29

Build moderation: consume comment created events, check content for orange to approve or reject, emit comment moderated event with id, post id, status, content, and route events via event bus.  
构建审核机制：处理已创建的评论事件，检查内容是否达到通过或拒绝的标准。在审核完成后，通过事件总线发布审核完成的事件，其中包含事件 ID、帖子 ID、状态、内容等信息。

### Updating Comment Content  更新评论内容4:58

Listen for the comment moderated event, locate and update the matching comment by id to approved or rejected, then emit a comment updated event to the event bus.  
请注意监听相关的事件信息，找到与 ID 对应的评论，将其状态更新为“已批准”或“已拒绝”，然后向事件总线发布该评论已更新的事件。

### A Quick Test  快速测试5:57

Update the query service to handle 'Comment Updated' events, locate the target comment in memory, and refresh its content and status, then test the moderation flow in the React UI.  
请更新查询服务，以处理“评论更新”事件。在内存中找到目标评论，刷新其内容和状态。然后测试 React 界面中的审核流程。

### Rendering Comments by Status  
状态下的渲染评论3:26

Render comments based on status in a react app, showing approved content, or 'awaiting moderation' and 'this comment has been rejected' messages; explore moderation service downtime and event bus synchronization.  
根据 React 应用程序中的状态来显示评论的审核结果，例如“已批准”的内容，或者显示“正在审核”和“该评论被驳回”的消息。同时，还需要考虑审核服务的停机情况以及事件总线之间的同步问题。

### Dealing with Missing Events  
处理缺失事件10:23

Learn how to handle missing events in a microservices architecture by storing all emitted events on the event bus and replaying them to services brought online later.  
了解如何在微服务架构中处理丢失的事件。通过将所有发生的事件存储到事件总线上，然后将这些事件重新传输给后续上线服务的应用程序来处理这些事件。

### Required Error Handling Update for Query Service  
查询服务所需的错误处理更新0:17

### Implementing Event Sync  实现事件同步6:11

Store all incoming events in an array on the event bus and expose an endpoint to retrieve them, then have the query service fetch and process them on startup.  
将所有传入的事件存储在一个事件总线上的数组中，并提供一个端点以便检索这些事件。然后让查询服务在启动时提取并处理这些事件。

### Event Syncing in Action  事件同步进行中4:16

Explore how event syncing restores state after service outages by testing post and comment events, catching up via an event bus, and resyncing the query service.  
通过测试帖子与评论的事件处理过程，利用事件总线进行数据同步，从而确保服务中断后状态能够恢复正常。同时，还会重新同步查询服务的操作。


##   
使用 Docker 运行服务  8 次讲座 • 每次 30 分钟

### Deployment Issues  部署问题6:55

Explore deploying a microservices app from local to online, address port mapping and event bus coordination across multiple servers, and consider Docker and Kubernetes for scalable deployment.  
了解如何将微服务应用程序从本地部署扩展到在线环境。处理端口映射问题，以及多服务器之间的事件总线协调问题。考虑使用 Docker 和 Kubernetes 来实现可扩展的部署。

### Why Docker?  为什么选择 Docker？2:55

Docker creates isolated containers that wrap dependencies like node and npm, letting each service run independently with simple start-up commands, enabling 1-to-1 pairing and smoother production deployments.  
Docker 创建的是隔离的容器，这些容器能够封装诸如 Node 和 npm 之类的依赖项。这样一来，每个服务都可以独立运行，只需简单的启动命令即可。这种架构能够实现一对一的匹配关系，从而让生产环境的部署更加顺畅。

### Why Kubernetes?  为什么选择 Kubernetes？5:47

Kubernetes runs containers across a node cluster managed by a master, uses configuration files to deploy and scale two copies of post service and streamline communication via a common channel.  
Kubernetes 能够在由主节点管理的节点集群上运行容器。它通过配置文件来部署和扩展两个后端的服务副本，并通过共享通道来简化通信过程。

### Don't Know Docker? Watch This.  
你了解 Docker 吗？那就看看这个吧。1:17

Explore Docker and Kubernetes basics at your own pace, with an appendix that covers Docker essentials for beginners and upcoming videos that walk through Kubernetes basics for the microservices course.  
您可以按照自己的节奏学习 Docker 和 Kubernetes 的基础知识。书中附有附录，介绍了 Docker 的基本内容，适合初学者学习；此外，还有视频教程，讲解 Kubernetes 的基础知识，适用于微服务课程的学习。

### Note About Docker Build Output and Buildkit  
关于 Docker 构建输出和构建工具的说明0:53

### Dockerizing the Posts Service  
将 Posts 服务迁移到 Docker 环境中4:02

Dockerize the posts service by creating a dockerfile with a node alpine base, building an image, installing dependencies, copying source, and starting with npm start.  
通过创建包含 Node.js Alpine 环境的 Docker 文件，将 posts 服务容器化。具体步骤包括：构建镜像、安装依赖项、复制源代码，最后使用 npm start 命令启动服务。

### Review Some Basic Commands  
回顾一些基本命令5:23

Master essential docker commands: build -t, run, ps, exec -it, and logs. Build tagged images, run containers, access shells, and view logs to inspect services.  
掌握以下重要的 Docker 命令：build -t、run、ps、exec -it 以及 logs。使用这些命令可以构建带有标签的镜像、运行容器、访问 shell 界面，以及查看日志以了解服务的运行状况。

### Dockerizing Other Services  
将其他服务迁移到 Docker 环境中3:01

Reuse a single dockerfile and dockerignore to dockerize all node-based services, build and run images with npm start, and prepare the project for Kubernetes deployment.  
通过重复使用同一个 dockerfile 和 dockerignore 文件，可以将所有基于 Node 节点的服务进行容器化封装。使用 npm start 命令可以构建并运行镜像，同时让项目准备好进行 Kubernetes 部署。


##   
利用 Kubernetes 来协调各种服务之间的协作  
43 次讲座 • 3 小时 25 分钟

### Installing Kubernetes  正在安装 Kubernetes3:19

Learn to set up Kubernetes locally for microservices, choosing an easy Mac/Windows path or a more involved Mini Cube route for Docker Toolbox or Linux, and enable container communication.  
学习如何在本地环境中配置 Kubernetes，以适用于微服务的需求。可以选择简单的 Mac/Windows 平台，或者更复杂的 Mini Cube 平台来使用 Docker Toolbox 或 Linux 系统。同时，还需要实现容器之间的通信功能。

### IMPORTANT Note for Minikube and MicroK8s Users  
重要通知：适用于 Minikube 和 MicroK8s 用户1:21

### A Kubernetes Tour  Kubernetes 之旅9:45

Verify Kubernetes is running with kubectl version. Follow how a post image becomes two pods managed by a deployment and exposed by a service across a cluster.  
使用 `kubectl version` 来验证 Kubernetes 是否已运行。请观察一个镜像如何被分解为两个容器，这两个容器由某个部署管理，同时又被某个服务所提供。

### Important Kubernetes Terminology  
重要的 Kubernetes 术语2:51

Explore essential Kubernetes terminology, including clusters, nodes, pods, deployments, and services, and clarify how Kubernetes services differ from application services for microservices.  
了解 Kubernetes 相关的核心术语，如集群、节点、Pod、部署和服务等等。同时明确 Kubernetes 服务与用于微服务的应用服务之间的区别。

### Notes on Config Files  关于配置文件的说明3:11

Learn how yaml config files define Kubernetes objects (deployments, pods, and services), commit them to Git as documentation, and avoid creating objects directly in the terminal.  
了解 YAML 配置文件如何定义 Kubernetes 对象（如部署、Pod 和服务），以及如何将这些配置提交到 Git 仓库作为文档。同时，也可以避免直接在终端中创建这些对象。

### Creating a Pod  创建 Pod6:41

Create a single pod by writing a Kubernetes yaml file for one container using the posts image, then apply it with kubectl to run it.  
通过编写包含单个容器的 Kubernetes YAML 文件来创建一个容器实例，然后使用 kubectl 来部署该容器。

### ErrImagePull, ErrImageNeverPull and ImagePullBackoff Errors  
错误类型：ErrImagePull、ErrImageNeverPull 以及 ImagePullBackoff 错误0:35

### Understanding a Pod Spec  理解 Pod 规范5:15

Explore the pod spec in Kubernetes, detailing apiVersion, kind, metadata, and spec with containers and image settings, including versioning to avoid fetching from Docker Hub.  
了解 Kubernetes 中的 Pod 规范，包括 apiVersion、kind、元数据以及容器和镜像设置的相关信息。还涵盖了版本管理功能，以避免从 Docker Hub 直接获取资源。

### Common Kubectl Commands  常见的 Kubectl 命令4:44

Learn how Docker commands map to kubectl and manage pods with apply -f, get pods, and describe pods. Run commands inside containers with kubectl exec, fetch logs, and read events.  
了解 Docker 命令如何映射到 kubectl 接口，以及如何使用 apply -f 命令来管理 Pod；还可以获取 Pod 的信息以及描述 Pod 的状态。使用 kubectl exec 可以在容器中执行命令，同时还可以获取日志和事件信息。

### A Time-Saving Alias  一种节省时间的别名2:26

Discover how to save time by using a shell alias, such as K for kubectl and DPS for docker ps, with setup tips for zsh and bash rc files.  
了解如何通过使用 shell 别名来节省时间，例如使用 K 作为 kubectl 的别名，使用 DPS 作为 docker ps 的别名。同时，也会介绍一些关于使用 zsh 和 bash 脚本文件的设置技巧。

### Introducing Deployments  介绍部署方式3:27

Learn how deployments manage pods in Kubernetes, ensuring a chosen number of identical pods, automatically replacing failed ones and rolling out new versions.  
了解在 Kubernetes 环境中如何管理 Pod 的部署情况，如何确保一定数量的相同 Pod 始终存在，如何自动替换失败的 Pod，以及如何逐步推出新的版本。

### Creating a Deployment  创建部署方案6:11

Create and apply a deployment config that runs a single pod with the posts image version 001, using selector labels to manage pods.  
创建并应用一个部署配置，该配置会运行一个包含 posts 镜像版本 001 的 Pod，并使用选择器标签来管理这些 Pod。

### Common Commands Around Deployments  
常见的部署相关命令4:35

Explore common commands around deployments, including listing deployments and pods, describing deployments, applying configuration, and deleting deployments to manage and repair workloads.  
了解与部署相关的常见命令，包括列出部署和容器、描述部署、应用配置以及删除部署，以便管理和修复工作负载。

### Updating Deployments  更新部署状态6:02

Discover two methods to update a deployment's image in Kubernetes, including rebuilding a new docker image version and applying config changes with kubectl.  
在 Kubernetes 中，有两种方法可以更新部署的镜像。一种是重新构建一个新的 Docker 镜像版本，另一种是通过 kubectl 应用配置变更来更新镜像。

### Preferred Method for Updating Deployments  
优选的更新部署方式5:42

Use latest image tag for deployments to avoid version errors in the config file. Rebuild and push the image to Docker Hub, then restart the deployment and verify logs.  
在部署过程中，请使用最新的镜像标签，以避免配置文件中出现版本错误。重新构建镜像并将其推送到 Docker Hub，然后重新启动部署过程，最后检查日志是否正常运行。

### Networking With Services  通过服务进行网络连接4:15

Learn how Kubernetes services enable pod-to-pod and external access by using Cluster IP, Node Port, and Load Balancer, while noting External Name exists but is not used.  
了解 Kubernetes 服务如何支持跨容器之间的通信以及通过使用集群 IP 地址、节点端口和负载均衡器来实现外部访问。同时注意到虽然存在外部名称参数，但实际上并未被使用。

### Creating a NodePort Service  
创建节点端口服务7:52

Create a node port service in Kubernetes by writing a config file and applying it to the cluster, exposing pods labeled app posts to the outside world for development purposes.  
通过编写配置文件并将其应用到集群中，在 Kubernetes 中创建一个节点端口服务。然后将带有“app posts”标签的 Pod 暴露给外部世界，以便于开发使用。

### Accessing NodePort Services  
访问节点端口服务5:09

Run kubectl apply to create a node port service; inspect services, understand the random node port, and access the posts app via mini cube ip or localhost for development.  
使用 `kubectl apply` 命令来创建节点上的服务；查看服务信息，了解随机分配的节点端口号；在开发过程中，可以通过迷你立方体的 IP 地址或本地主机来访问帖子应用。

### Setting Up Cluster IP Services  
配置集群 IP 服务3:12

Deploy the event bus and configure cluster IP services for posts and the event bus to enable pod-to-pod communication within the cluster, despite pods having random IPs.  
部署事件总线，并配置集群内的节点相关 IP 服务以及事件总线接口，以实现集群内各个节点之间的通信，即使这些节点的 IP 是随机分配的。

### Building a Deployment for the Event Bus  
为事件总线构建部署方案5:35

Build the event bus image, push to Docker Hub, create a deployment and pod, then configure cluster IP services for event bus and posts to enable in-cluster communication.  
构建事件总线镜像，推送到 Docker Hub，创建部署和 Pod，然后为事件总线和消息配置集群 IP 服务以启用集群内通信。

### Adding ClusterIP Services  
添加 ClusterIP 服务7:37

Co-locate deployment and cluster IP services to expose post and event bus pods. Configure cluster IP services in YAML, set selectors, and map ports 4000 and 4005.  
将联合部署和集群 IP 服务部署在一起以暴露消息和事件总线 Pod。在 YAML 中配置集群 IP 服务，设置选择器，并映射端口 4000 和 4005。

### How to Communicate Between Services  
如何在不同服务之间进行通信4:09

Replace localhost URLs with the event bus and posts service names to enable pod-to-pod communication via cluster IP services and ports 4005 and 4000.  
将本地 URL 替换为事件总线相关的名称，并使用这些名称来调用帖子服务。这样就能通过集群的 IP 服务以及端口 4005 和 4000 实现节点间的通信了。

### Updating Service Addresses  
更新服务地址6:27

Update the post and event bus services to use the event bus service name and updated URLs. Rebuild and push images, restart deployments, and test event exchanges.  
更新帖子以及事件总线服务，使用正确的事件总线服务名称和已更新的 URL 地址。重新构建并推送图片，重新启动部署流程，同时测试事件交换功能是否正常运行。

### Verifying Communication  验证通信是否正常5:00

Verify inter-service communication in a Kubernetes cluster by sending a post request via postman and confirming the post pod emits a post created event to the event bus, via logs.  
通过使用 Postman 发送 POST 请求，并在 Kubernetes 集群中验证服务间通信，并确认 POST Pod 通过日志向事件总线发出 POST 创建事件。

### Adding Query, Moderation and Comments  
添加查询、审核和评论功能9:57

Update comments, query, and moderation to post events to the event bus CRV, replacing localhost. Build and push images, deploy cluster IP services, and apply configs for testing.  
更新评论、查询和审核，以将事件发布到事件总线 CRV，替换 localhost。构建并推送镜像，部署集群 IP 服务，并应用配置进行测试。

### Testing Communication  测试通信功能5:29

Update the event bus to reach cluster IP services for comments, query, and moderation, then rebuild, push to docker hub, and test events from event bus with postman and logs.  
更新事件总线，使其能够访问集群的 IP 服务，以便进行评论、查询和审核操作。之后重新构建应用程序，将其推送到 Docker Hub。最后，使用 Postman 和日志来测试来自事件总线的事件。

### Load Balancer Services  负载均衡服务5:13

Integrate the React development server inside a Kubernetes pod and expose a load balancer service as the single entry point, routing browser requests to the appropriate cluster IP pods.  
将 React 开发服务器集成到 Kubernetes Pod 中，并将一个负载均衡服务作为唯一的入口点，将浏览器请求路由到相应的集群 IP Pod 上。

### Load Balancers and Ingress  
负载均衡器与入口配置6:38

Learn how a load balancer service provisions external traffic into a Kubernetes cluster, and how an ingress controller routes requests to pods using path-based rules.  
了解负载均衡服务如何分配外部流量到 Kubernetes 集群中，以及如何通过基于路径的规则将请求路由到相应的 Pod。

### Important ### DO NOT SKIP ### Ingress Nginx Installation Info  
重要提示——请不要跳过——请仔细阅读 Nginx 入口服务器的安装信息。0:40

### Installing Ingress-Nginx  正在安装 Ingress-Nginx 模块7:57

Install ingress nginx with ingress engine x to deploy a load balancer and an ingress controller in your cluster, using kubectl apply -f and avoiding Kubernetes ingress confusion.  
使用 kubectl apply -f 命令来安装 ingress nginx 和 ingress 引擎 x，从而在你的集群中部署负载均衡器和 ingress 控制器。避免陷入 Kubernetes ingress 相关的困惑之中。

### Ingress v1 API Required Update + pathType Warning  
Ingress v1 API 需要更新，同时路径类型也出现了问题，存在警告。0:52

### Writing Ingress Config Files  
编写 Ingress 配置文件4:47

Create and apply an ingress config to route requests to the post service via a cluster IP service on port 4000, using annotations for the ingress class Engine X.  
创建并应用一个入口配置，通过该配置将请求通过集群 IP 服务在 4000 端口路由到 Post 服务上。同时，使用 Engine X 的入口类标签来标识该配置。

### Important Note About Port 80  
关于 80 端口的重要说明1:12

### Hosts File Tweak  主机文件调整工具6:15

Learn how to use Ingress Engine X to host multiple apps on a Kubernetes cluster, map domains with the host file, and route traffic to local or Minikube IPs.  
了解如何使用 Ingress Engine X 在 Kubernetes 集群上承载多个应用程序。可以通过 host 文件来映射域名，并将流量路由到本地地址或 Minikube 的 IP 上。

### Important Note to Add Environment Variable  
重要提示：添加环境变量0:33

### Deploying the React App  部署 React 应用程序6:09

Update host file for development to route the React app to the post service on the local machine; push a Docker image and configure Kubernetes ingress with cluster IP service.  
更新主机文件，以便将 React 应用程序的路由指向本地机器上的 Post 服务；推送一个 Docker 镜像，并配置 Kubernetes 入口以使用集群 IP 服务。

### Unique Route Paths  独特的路线路径6:55

Define unique route paths in ingress to differentiate post and query requests, updating React and post services to handle /posts/create, then rebuild images and restart deployments.  
在入口处定义唯一的路由路径，以区分 POST 请求和查询请求。同时更新 React 和 POST 服务，使其能够处理"/posts/create"类型的请求。之后重新构建图片文件，并重启相关部署工作。

### Final Route Config  最终路由配置6:32

Configure final route config in Kubernetes ingress to route post creation, posts, and comments to microservice backends, using regex for wildcard paths and Engine X annotations.  
在 Kubernetes 的 Ingress 配置中，完成最终路由配置的工作。在创建后，将文章和评论的路由指向微服务后端服务。使用正则表达式来处理通配路径，并利用 Engine X 的注释功能进行配置。

### Introducing Skaffold  欢迎了解 Skaffold！3:08

Use scaffold to speed updates to running pods in a Kubernetes dev environment, enabling quick code changes, easy object creation and deletion, and seamless project switching.  
使用脚手架工具可以加快在 Kubernetes 开发环境中对运行中的容器进行更新操作的效率，从而实现快速的代码修改、简单的对象创建和删除操作，以及无缝的项目切换。

### Skaffold API version Update  
Skaffold API 版本更新0:43

### Skaffold Setup  Skaffold 设置9:17

Install Scaffold dot HTML file to manage Kubernetes manifests in the infra aids directory. Scaffold watches manifests, applies on start, and deletes related objects on stop.  
在基础设施辅助目录中安装 Scaffold HTML 文件，以管理 Kubernetes 清单。Scaffold 可以监控清单的变化，在启动时执行相关操作，并在停止时删除相关对象。

### First Time Skaffold Startup  
首次创业1:00

Run scaffold dev from the project root to start the scaffold. The first startup rebuilds six images and may show warnings, not found messages, or uncommitted changes as it initializes.  
从项目根目录开始运行 scaffold dev，以启动脚手架环境。首次运行时会重新构建六个镜像，过程中可能会看到一些警告信息，或者遇到找不到某些文件的情况，同时也会有一些未提交的更改被处理。

### A Few Notes on Skaffold  
关于 Skaffold 的一些说明6:30

Learn to use Skaffold to run and debug microservices with node and React, troubleshoot manifest unknown errors, and leverage automatic rebuilds and logs from pods for rapid iteration.  
学习如何使用 Skaffold 来运行和调试基于 Node 和 React 的微服务。能够解决那些难以确定的错误问题，并利用 Pods 中的自动构建过程和日志信息来实现快速迭代。


##   
多服务应用程序的架构  
13 次讲座 • 1 小时 6 分钟

### Big Ticket Items  昂贵的物品15:11

Build large microservices with a shared library, clear event contracts, and async communication, using TypeScript and tests to ensure data consistency and handle out-of-order events.  
通过共享库、明确的事件契约以及异步通信方式，构建大型微服务。使用 TypeScript 进行编码，并通过测试来确保数据的一致性，同时能够处理无序发生的事件。

### App Overview  应用概述8:53

Build a production-grade ticketing app with microservices, featuring 15-minute purchase locks, user authentication, Stripe payments, and an event-driven architecture using Node.js and React.  
请开发一个具有微服务架构的优质票务应用程序，具备 15 分钟购买锁定功能、用户身份验证机制、Stripe 支付功能，以及基于 Node.js 和 React 技术的事件驱动架构。

### Resource Types  资源类型3:23

Explore the four core resources for the app—users, tickets, orders, and charges—detailing their fields, relationships, and Stripe-based payment flow, including order expiry and status transitions.  
了解该应用程序的四大核心资源：用户、订单、订单信息和费用。本文将详细介绍这些资源的字段、相互关系以及基于 Stripe 的支付流程，包括订单的有效期和状态变化等细节。

### Service Types  服务类型3:37

Design five microservices for authentication, tickets, orders, exploration, and payments, each handling its resource domain, and weigh whether one service per resource is best.  
请设计五款用于认证、票据、订单、探索和支付的微服务。每款微服务分别负责处理特定的资源领域事务。需要权衡的是，是否应该为每种资源都单独创建一个服务。

### Events and Architecture Design  
事件与建筑设计3:48

Learn the architecture of a microservices app with Node.js and React, covering events like user created and order expired, plus Next.js SSR and a NATS streaming event bus.  
了解使用 Node.js 和 React 构建微服务应用程序的架构设计，包括用户创建事件、订单过期事件的处理，以及 Next.js 的 SSR 功能和 NATS 流事件总线。

### Note on Typescript  关于 TypeScript 的说明0:20

### Auth Service Setup  认证服务设置4:51

Build an auth service with sign up, sign in, sign out, and current user routes using TypeScript and express, supported by a Node.js development setup.  
构建一个认证服务，包括注册、登录、注销功能，以及使用 TypeScript 和 Express 实现的当前用户路由。该系统基于 Node.js 开发环境构建。

### Auth K8s Setup  认证 K8s 配置8:15

Build a docker image for the auth service from node alpine, using a dockerfile and dockerignore, then deploy a Kubernetes deployment and service on port 3000.  
请创建一个用于运行 node alpine 的 auth 服务的 Docker 镜像，使用 Dockerfile 和 Dockerignore 进行配置。然后，在 3000 端口上部署一个 Kubernetes 应用程序和服务。

### Adding Skaffold  添加 Skaffold5:36

Add and configure a scaffold yaml file to watch the infra directory and sync auth changes to the running container, enabling automatic cluster updates with scaffold dev.  
添加并配置一个 scaffold YAML 文件，用于监控 infra 目录，并将身份验证的变更同步到运行中的容器中。这样就能实现与 scaffold 开发环境的自动集群更新了。

### Note on Code Reloading  关于代码重加载的说明0:08

### Ingress v1 API Required Update  
Ingress v1 API 需要更新0:40

### Ingress-Nginx Setup  Ingress-Nginx 配置7:19

Configure ingress nginx to route api/users to the auth service, enabling manual testing with postman; define host tickets.dev and ticketing.dev and map /api/users to the CRV service on port 3000.  
配置 ingress nginx 将 api/用户路由到认证服务，从而实现 Postman 的手动测试;定义主机 tickets.dev 和 ticketing.dev，并将 /api/users 映射到 3000 端口的 CRV 服务。

### Hosts File and Security Warning  
关于文件和安全方面的警告4:06

Configure the hosts file to route ticketing dev to the local Ingress Engine X server. Resolve self-signed certificate warnings in development and test the authentication flow via the ingress setup.  
配置主机文件，将工单开发路由到本地的 Ingress Engine X 服务器。解决开发中的自签名证书警告，并通过入口设置测试认证流程。


##   
利用云环境进行开发  12 次讲座 • 每次 40 分钟

### Note on Remote Development  
关于远程开发的说明2:51

Learn how to run your development in Google Cloud to avoid lag from multiple pods, with an optional setup and access to $300 in free credits.  
了解如何在 Google Cloud 上运行您的开发工作，以避免由于多个容器之间的交互而导致的延迟。此外，您还可以选择进行设置，并获得 300 美元的免费积分使用。

### Remote Dev with Skaffold  使用 Skaffold 进行远程开发6:35

Set up a remote development environment on Google Cloud using Skaffold to manage Kubernetes pods, deployments, and automatic syncing of code changes.  
在 Google Cloud 上搭建一个远程开发环境，使用 Skaffold 来管理 Kubernetes 容器、部署任务，以及代码更新的自动同步功能。

### Free Google Cloud Trial and Credits  
免费试用谷歌云服务，获取积分吧！0:16

### Google Cloud Initial Setup  
Google Cloud 初始设置2:43

sign up for a google cloud account to obtain $300 in free credits, then create a new project in the google cloud console and configure billing details.  
注册一个谷歌云账户，以获得 300 美元的免费积分。然后在谷歌云控制台中创建一个新项目，并配置好计费信息。

### Kubernetes Cluster Creation with Autopilot  
通过自动模式创建 Kubernetes 集群0:41

### Kubectl Contexts  Kubectl 上下文设置3:49

Learn to switch between kubectl contexts to access different Kubernetes clusters, including your local Docker cluster and a Google Cloud cluster, by using the Google Cloud SDK to auto-manage contexts.  
学习如何使用 Google Cloud SDK 在 kubectl 上下文之间切换，以访问不同的 Kubernetes 集群，包括本地的 Docker 集群和 Google Cloud 集群。通过这种方式，可以自动化地管理上下文。

### Initializing the GCloud SDK  
正在初始化 GCloud SDK1:23

### Installing the GCloud Context  
正在安装 GCloud 环境4:17

learn to use gcloud to fetch credentials for your Google Cloud Kubernetes cluster and switch kubectl contexts between Docker Desktop and the ticketing-dev cluster.  
学习如何使用 gcloud 来获取您的 Google Cloud Kubernetes 集群的凭证，并可以在 Docker Desktop 和 ticketing-dev 集群之间切换 kubectl 的上下文环境。

### Updating the Skaffold Config  
更新 Skaffold 配置4:34

Enable Google Cloud Build to build images from source, update the Skaffold configuration, set up Ingress Engine X on the Google Cloud cluster, and update the host file before restarting scaffold.  
启用 Google Cloud Build 功能，以便能够从源代码构建镜像；更新 Skaffold 配置；在 Google Cloud 集群上配置 Ingress Engine X；在重启 scaffold 之前，更新主机文件。

### More Skaffold Updates  更多 Skaffold 更新信息1:06

Update the scaffold html to keep only one build option, local or google cloud. Update the deployment to use the new image name by applying the scaffold's image name.  
更新脚手架的 HTML 文件，确保只保留一种构建选项：要么使用本地环境，要么使用谷歌云环境。同时，更新部署配置，通过应用脚手架中定义的镜像名称，使部署能够使用新的镜像名称。

### Creating a Load Balancer  创建负载均衡器5:12

Learn how to install Ingress Engine X on a Google Cloud cluster, expose a load balancer, and update host file to route traffic to the deployed back end server.  
了解如何在 Google Cloud 集群上安装 Ingress Engine X，如何暴露负载均衡器，以及如何更新主机文件以将流量导向已部署的后端服务器。

### Final Config and Test  最终配置和测试6:25

Restart scaffold, build a remote image with google cloud build, and deploy deployment, service, and ingress to a remote cluster; verify build in cloud build history and test domain access.  
重新启动脚手架工具，在谷歌云上构建远程镜像，然后将服务、部署和入口配置部署到远程集群中；在云构建历史中验证构建结果，并测试域名访问是否正常。


##   
响应规范化策略  
21 次讲座 • 1 小时 58 分钟

### Creating Route Handlers  创建路由处理器5:40

Organize authentication by creating four route handler files (sign up, sign in, sign out, current user) as express routers in a roots directory and wire them into index with app.use.  
通过创建四个路由处理器文件（分别用于注册、登录、注销以及当前用户操作），并将它们作为 Express 路由器存储在 roots 目录下。然后将这些文件通过 app.use 函数连接到索引路由中。这样就能实现认证功能的组织与配置。

### Scaffolding Routes  脚手架通道4:05

Scaffold and wire up authentication routes by duplicating a root handler across sign in, sign out, and sign up files, and attach routers to the Express app.  
通过复制根处理程序来设置认证路由，从而在登录、注销和注册文件中实现这一功能。同时，将路由器附加到 Express 应用程序上。

### Adding Validation  添加验证功能8:38

Add validation to the sign up route by using express-validator to require a valid email and a trimmed password, via middleware that checks the request body and provides error messages.  
通过使用 express-validator 来校验注册过程中的输入数据，确保用户提供的电子邮件地址是有效的，且密码长度符合要求。这种验证方式可以通过中间件来实现，该中间件会检查请求体中的数据，并在发现错误时输出相应的错误信息。

### Handling Validation Errors  
处理验证错误6:18

Use express validator and validation results to detect email or password errors in signup requests, returning a 400 with the errors array, then proceed when validation passes.  
使用快速验证器以及验证结果来检测注册请求中的电子邮件或密码错误。如果检测到错误，则返回 400 错误状态码，并附带错误列表。只有当验证通过后，才继续处理注册流程。

### Postman HTTPS Issues  邮递员处理 HTTPS 问题0:11

### Surprising Complexity Around Errors  
关于错误的惊人复杂性6:06

In a microservices architecture, this lecture explains the importance of consistent error responses across services and plans a shared library to unify validation error formats for the React frontend.  
在微服务架构中，本讲座阐述了各服务之间保持一致的错误响应的重要性，并规划了一个共享库，以统一 React 前端部分的验证错误格式。

### Other Sources of Errors  其他错误来源4:17

Establish a single, consistent error response structure across all services, from Express Validator failures to database outages, so the React app can parse errors uniformly.  
在所有服务中建立统一一致的错误响应机制，无论是来自 Express 验证器的错误，还是数据库故障等状况，都能让 React 应用程序以相同的方式处理错误。

### Solution for Error Handling  
错误处理方案4:50

Implement a consistent error handling strategy with an Express middleware, capturing errors from synchronous and asynchronous routes via next for a unified response.  
采用统一的错误处理策略，通过 Express 中间件来捕获同步和异步路由中的错误，从而生成统一的响应结果。

### Building an Error Handling Middleware  
构建错误处理中间件7:38

Create an error handling middleware in a Node.js and Express project with a four-argument signature and TypeScript annotations, delivering a consistently structured error response.  
在 Node.js 和 Express 项目中创建一个错误处理中间件，该中间件需要使用四参数签名，并带有 TypeScript 注释，以输出结构清晰、一致的错误响应。

### Communicating More Info to the Error Handler  
向错误处理人员提供更多信息5:23

Improve error handling by attaching detailed validation results to the error object and delivering a well-structured response that tells users exactly what went wrong. Emphasize TypeScript-style handling over plain JavaScript.  
通过为错误对象附加详细的验证结果来改善错误处理机制，从而提供结构清晰的响应，明确告知用户究竟出现了什么问题。建议使用 TypeScript 风格的错误处理方式，而非纯 JavaScript 的方式。

### Encoding More Information In an Error  
在错误情况下编码更多信息4:35

Subclass the built-in error with TypeScript to create request validation error and database connection error, adding custom properties like reasons to convey rich information to the error handling middleware.  
使用 TypeScript 对内置错误进行子类化，以生成请求验证错误和数据库连接错误。同时，可以添加自定义属性如“原因”，以便向错误处理中间件提供更详细的错误信息。

### Subclassing for Custom Errors  
针对自定义错误进行子类划分8:17

Create custom error subclasses for Node.js microservices, including request validation error and database connection error, and apply them in a signup route with express validator and centralized handling.  
为 Node.js 微服务创建自定义的错误子类，例如请求验证错误和数据库连接错误。将这些错误类应用于注册页面的路由中，通过 Express 验证工具来实现集中式处理。

### Determining Error Type  确定错误类型3:10

Implement error type detection in the error handling middleware by distinguishing request validation errors and database connection errors, logging each case, and building a reason-based response for the client.  
在错误处理中间件中实施错误类型检测功能，能够区分请求验证错误和数据库连接错误，并记录每种情况。同时，为客户端提供基于原因的响应。

### Property 'param' does not exist on type 'AlternativeValidationError'  
类型“AlternativeValidationError”中不存在名为“param”的属性。0:23

### Converting Errors to Responses  
将错误转换为响应信息10:12

Map every error to a common response structure using a central error handling middleware, producing an errors array of messages and optional fields for validation and database errors.  
将所有错误映射到一个通用的响应结构中，通过中央错误处理中间件来处理这些错误。同时会生成一个包含错误消息的数组，以及可选的字段用于验证和数据库错误处理。

### Moving Logic Into Errors  将逻辑错误移入错误之中8:36

Refactor error handling by adding serialize errors and status codes to request validation and database connection errors, letting the error handler produce a unified response and prevent middleware bloat.  
通过向请求验证和数据库连接操作中添加序列化错误和状态码处理，可以改进错误处理机制。这样，错误处理程序能够生成统一的响应，从而避免中间件代码过于臃肿的问题。

### serializeErrors' not assignable to the same property in base type 'CustomError'  
序列化错误无法归属于基础类型“CustomError”中的任何属性。0:21

### Verifying Our Custom Errors  
验证我们的自定义错误8:34

Improve error handling middleware by enforcing a consistent serializeErrors output for custom errors. Compare using an interface versus an abstract class to ensure status codes and structure across all errors.  
改进错误处理中间件的功能，通过强制使用统一的 serializeErrors 输出格式来处理自定义错误。比较使用接口与抽象类的方式，以确定哪种方式能更好地确保所有错误处理的代码结构和状态码的一致性。

### Final Error Related Code  最终错误相关代码10:19

Create a custom abstract error class that enforces a status code and serializeErrors, have request validation and database errors extend it, and unify the error handling middleware for consistent responses.  
创建一个自定义的抽象错误类，该类需要强制使用特定的状态码；同时让 request validation 和 database_errors 类继承该类；此外，还需要让所有的错误处理中间件具有一致性，以确保响应结果的一致性。

### How to Define New Custom Errors  
如何定义新的自定义错误5:01

Define a new custom air to handle route not found, export a Not Found Air class, set a 404 status, implement serialize errors, and rely on Express middleware to send the response.  
定义一个新的自定义 Air 来处理未找到的路由，导出一个 Not Found Air 类，设置 404 状态，实现 serialize 错误，并依赖 Express 中间件发送响应。

### Uh Oh... Async Error Handling  
呃哦……异步错误处理问题出现了5:49

discover how async error handling in express works, including the async keyword returning a promise and using next(error); install and import express-async-errors to auto-catch errors in async route handlers.  
了解 Express 中异步错误处理的工作原理，包括 async 关键字返回 Promise 以及使用 next(error)；安装并导入 express-async-errors 以在异步路由处理程序中自动捕获错误。


##   
数据库管理与建模  
16 次讲座 • 1 小时 27 分钟

### Creating Databases in Kubernetes  
在 Kubernetes 中创建数据库8:25

Install mongoose in the service and create a private MongoDB instance in Kubernetes. Configure a deployment and a cluster IP service to expose MongoDB on port 27017 for signup.  
在服务端安装 mongoose 模块，并在 Kubernetes 中创建一个私有的 MongoDB 实例。配置部署模式，同时设置集群 IP 服务，以便在端口 27017 上暴露 MongoDB，以便用户进行注册操作。

### Connecting to MongoDB  正在连接至 MongoDB 数据库7:53

Connect the auth service to MongoDB with mongoose using async/await, handling startup with a start function and logging success or errors before listening for traffic.  
使用 async/await 技术将认证服务与 MongoDB 以及 mongoose 连接起来。在启动过程中通过启动函数来处理相关逻辑，并在处理流量之前记录成功或失败的日志。

### Understanding the Signup Flow  
理解注册流程4:25

Explore the signup flow: validate unique emails using a mongoose user model in MongoDB, hash passwords, create the user, and return a token or cookie for sign-in.  
请探索注册流程的设计：使用 MongoDB 中的 mongoose 用户模型来验证唯一的电子邮件地址，对密码进行哈希处理，创建用户账户，并返回用于登录的令牌或 cookie。

### Getting TypeScript and Mongoose to Cooperate  
让 TypeScript 和 Mongoose 能够协同工作5:21

Explore how TypeScript and Mongoose cooperate in a microservices project using a Mongoose user model and a user document, and tackle two big issues around constructor properties and document fields.  
了解 TypeScript 和 Mongoose 如何在微服务项目中协同工作，以使用 Mongoose 的用户模型和用户文档来实现功能。同时，探讨在构造函数属性和文档字段方面存在的两个常见问题。

### Creating the User Model  创建用户模型4:54

Create and export a Mongoose user model by defining a schema with email and password, then expose a model to access MongoDB data, while highlighting TypeScript type gaps.  
通过定义包含电子邮件和密码的模式，创建并导出 Mongoose 用户模型。然后，将该模型暴露出来以访问 MongoDB 数据，同时需要注意 TypeScript 类型上的差异。

### Type Checking User Properties  
检查用户属性6:01

Learn to enforce TypeScript type checking with Mongoose by introducing a user adders interface and a buildUser function, ensuring emails and passwords are correctly typed when creating users.  
通过引入用户添加接口和 buildUser 函数，学习如何使用 TypeScript 进行 Mongoose 的类型检查。确保在创建用户时，电子邮件和密码输入是正确的。

### Adding Static Properties to a Model  
向模型添加静态属性6:00

Refactor the user model to include a static build method via schema statics, enabling user built with email and a password, and address TypeScript typing for mongoose models.  
对用户模型进行重构，加入一个静态构建方法，通过模式静态特性来实现。使用电子邮件和密码创建用户，并为 mongoose 模型添加 TypeScript 类型注解。

### Defining Extra Document Properties  
定义额外文档属性4:53

Define a user document interface to align create user properties with Mongoose document fields, include created at and updated at as needed, and replace any with user doc in model.  
定义一个用户文档接口，以便将用户属性与 Mongoose 文档字段进行关联。根据需要，可以包含创建时间和更新时间等信息。此外，还可以将相关字段替换为模型中的用户文档字段。

### What's That Angle Bracket For?  
那个角码是用来做什么的？3:52

Explore how TypeScript generics use angle bracket syntax to customize function types. See how the model function returns the type defined by the second generic argument.  
了解 TypeScript 的泛型是如何使用尖括号语法来定制函数类型的。了解模型函数如何返回由第二个泛型参数所定义的类型。

### User Creation  用户创建6:47

Create a user model and signup flow in the microservices with Node JS and React course, including email duplication checks, password hashing, saving to MongoDB, and Postman testing.  
在 Node JS 和 React 框架下，创建用户模型并实现注册流程。过程中需要包含电子邮件重复检查、密码哈希处理、数据保存到 MongoDB，以及使用 Postman 进行测试等功能。

### Proper Error Handling  正确的错误处理7:25

Learn to implement a custom bad request error for the signup route, return actionable messages when an email is in use, and prepare for password hashing and a JWT cookie.  
学会为注册路由处理自定义的不良请求错误，当电子邮件已被占用时返回可操作的消息，并准备好应对密码哈希处理以及 JWT 令牌和 Cookie 的使用。

### Note on Password Hashing  关于密码哈希处理的说明0:37

### Reminder on Password Hashing  
关于密码哈希处理的提醒4:58

Hash passwords during signup and store only the hashed version in the database, replacing plain-text storage; on sign in, hash the input and compare to verify credentials.  
在注册过程中对密码进行哈希处理，并将哈希后的版本存储在数据库中，而不要存储明文版本；在登录时，再次对输入密码进行哈希处理，以验证身份凭证的有效性。

### Adding Password Hashing  添加密码哈希处理6:57

Implement password hashing during signup by placing hashing logic in a separate services/password class with static hash and compare methods, returning hash and salt joined by a dot.  
在注册过程中实现密码哈希处理，将哈希逻辑放在一个独立的服务/密码类中。该类包含静态的哈希方法和比较方法，返回的是包含点连接的哈希值与盐值的组合结果。

### Comparing Hashed Password  
比较哈希化密码2:54

Implement static compare method to verify a supplied password against the stored hash and salt by splitting stored password to extract hash and salt, then hashing input and comparing.  
实现静态比较方法，将提供的密码与存储的哈希值及盐值进行比对。具体做法是先将存储的密码拆分成哈希值和盐值，然后对新输入的密码进行哈希处理，最后将哈希后的结果与存储的哈希值及盐值进行比较。

### Mongoose Pre-Save Hooks  猫鼬预保存功能：保存链接5:53

Implement a mongoose pre-save hook in the user model to hash passwords automatically. Hash only when modified, using a function keyword and done callback, ensuring plaintext passwords are never stored.  
在用户模型中实现 mongoose 的预保存钩子功能，以便自动对密码进行哈希处理。仅在对密码进行修改时才进行哈希操作，使用 function 关键字指定相关函数，并设置回调函数来确保绝不会存储明文密码。


##   
认证策略与选项  
26 次讲座 • 2 小时 48 分钟

### Fundamental Authentication Strategies  
基本认证策略8:58

Explore authentication strategies for microservices, comparing centralized authentication with JSON web tokens or cookies in a gateway versus independent service-level checks; highlight trade-offs like single points of failure and duplication.  
探讨微服务的认证策略，比较集中式认证与 JSON Web 令牌或 Cookie 在网关中的使用方式，以及独立服务级别检查的优势。同时，需要考虑诸如单点故障和重复性问题所带来的权衡。

### Huge Issues with Authentication Strategies  
认证策略存在严重问题7:32

Examine the drawbacks of option two, where each service authenticates independently, removing the central auth dependency but risking valid tokens after bans and cross-service inconsistency.  
请考虑第二种方案的缺点。在这种方案中，每项服务都独立进行身份验证，这确实消除了对中央认证系统的依赖，但随之而来的问题是，有效令牌可能会在账户被封禁后失效，而且不同服务之间也可能出现不一致的情况。

### So Which Option?  那么，应该选择哪个选项呢？2:58

Compare central authentication versus per-service authentication for microservices; opt for independent services with async communication, while acknowledging a brief window before bans propagate, and plan a hybrid approach.  
比较微服务的集中式认证与每服务认证；选择独立的异步通信服务，同时承认封禁传播前的短暂窗口，并计划混合方法。

### Solving Issues with Option #2  
采用第二种方法来解决问题。8:26

Implement option two by enforcing short-lived JWTs of 15 minutes with optional token refresh, and use event-driven bans and short-lived caches to revoke access across microservices.  
通过实施 15 分钟短期 JWT 并可选的令牌刷新来执行选项二，并使用事件驱动的封禁和短期缓存来跨微服务撤销访问权限。

### Reminder on Cookies vs JWT's  
关于使用 Cookies 与 JWT 服务的区别提醒6:21

Clarify how cookies differ from JSON web tokens and how each supports authentication and authorization, including storage, transport, and usage in a microservices architecture.  
请说明 JSON Web 令牌与 Cookie 之间的区别，以及这两种技术如何在支持身份验证和授权方面发挥作用，包括它们在存储、传输以及微服务架构中的使用方式。

### Microservices Auth Requirements  
微服务认证要求11:00

Assess json web tokens and cookies for authentication, then adopt a jwt approach that stores user details and authorization, encodes expiration, and supports multi-language use without a backing store.  
评估 JSON Web 令牌和 Cookie 在身份验证方面的性能，然后采用 JWT 方式来存储用户信息和权限信息。这种技术能够编码出令牌的有效期，并且支持多语言使用，无需依赖任何后端存储机制。

### Issues with JWT's and Server Side Rendering  
JWT 和服务器端渲染存在的问题9:58

Explore how JSON web tokens are communicated to backend services in server-side rendered React apps, using cookies with Next.js to securely pass authentication on the initial request.  
了解如何在基于 React 的服务器端应用程序中，通过 Next.js 的 Cookie 技术，将 JSON Web 令牌安全地传递给后端服务。这种方式能够在首次请求时完成身份验证的传递。

### Cookies and Encryption  Cookies 与加密技术4:51

Authenticate users with json web tokens stored in cookies, using cookie session to manage token data without a backing data store.  
通过存储在 Cookie 中的 JSON Web 令牌来验证用户身份，利用 Cookie 会话来管理令牌数据，而无需依赖任何持久化的数据存储。

### Adding Session Support  添加会话支持功能3:11

Install and configure cookie session in an express app, including TypeScript typings, enable https-only cookies, trust the proxy, and generate a json web token stored in a cookie.  
在 Express 应用中安装并配置 Cookie 会话功能，同时使用 TypeScript 进行类型注解。支持仅通过 HTTPS 进行通信，允许使用代理服务器，并生成存储在 Cookie 中的 JSON Web 令牌。

### Generating a JWT  生成一个 JSON Web Token8:29

Generate a JSON Web Token, signing a payload with the user's id and email, then store it on the cookie session for authenticated requests and verify the token when needed.  
生成一个 JSON Web Token，使用用户的 id 和电子邮件对负载进行签名，然后将其存储在 cookie 会话中以进行身份验证请求，并在需要时验证该 token。

### JWT Signing Keys  JWT 签名密钥4:56

Learn how a session becomes a JSON web token, verify its signature with a signing key, and inspect the payload while exploring key sharing across services via Docker and Kubernetes.  
学习如何将会话转换为 JSON Web Token，使用签名密钥验证其签名，并在探索通过 Docker 和 Kubernetes 在服务之间共享密钥时检查负载。

### Securely Storing Secrets with Kubernetes  
使用 Kubernetes 安全存储秘密信息2:13

Securely share the JSON web token signing key across microservices by creating a Kubernetes secret and exposing it as environment variables inside each pod.  
通过创建 Kubernetes 密钥并在每个 Pod 内部将其作为环境变量暴露，安全地在微服务之间共享 JSON Web 令牌签名密钥。

### Creating and Accessing Secrets  
创建和访问秘密信息9:18

Create and bind a generic secret in a Kubernetes cluster, expose it as an environment variable via a deployment's secretKeyRef, and debug not-found errors with describe and get pods.  
在 Kubernetes 集群中创建并绑定一个通用密钥，通过部署的 secretKeyRef 将其作为环境变量暴露，并使用 describe 和 get pods 调试未找到错误。

### Accessing Env Variables in a Pod  
在 Pod 中访问环境变量5:18

reference the JWT key from the pod’s environment variable, validate the variable at startup, throw an error if undefined, and use a non-null assertion in TypeScript while avoiding hard-coded secrets.  
从 Pod 的环境变量中获取 JWT 密钥，在启动时验证该变量是否存在；如果变量不存在，则抛出错误。同时，在 TypeScript 中使用非空断言来避免硬编码的秘密信息。

### Common Response Properties  
常见的响应属性4:41

Remove password and __v from signup responses and remap _id to id to ensure consistent front-end data across microservices using Node and Express and MongoDB.  
在注册响应中移除密码和__v 字段，并重新映射_id 字段，以确保通过 Node.js、Express 和 MongoDB 在各个微服务之间共享一致的数据结构。

### Formatting JSON Properties  
格式化 JSON 属性10:38

Customize how user documents turn into JSON by using mongoose's toJSON transform to remap _id to id, remove password, and omit the version key.  
可以通过使用 mongoose 的 toJSON 转换函数来定制用户文档如何被转换为 JSON 格式。具体操作包括：重新映射 _id 为 id 字段，移除密码字段，以及省略版本信息键。

### The Signin Flow  登录流程7:49

Wire up the sign-in router; validate email and password with express-validator; verify the user in MongoDB, issue a json web token in a cookie, and prepare reusable validation middleware.  
将登录路由器连接到位；使用 express-validator 工具验证电子邮件和密码；在 MongoDB 中确认用户身份；将 JSON Web 令牌存储在 cookie 中；准备可复用的验证中间件。

### Common Request Validation Middleware  
常见的请求验证中间件5:33

Extracts validation into a reusable express middleware named validate request, throws request validation errors via validation results, and calls next when valid in signup and signin routes.  
将验证功能提取到一个可复用的 Express 中间件中，该中间件名为 validateRequest。在请求验证过程中，会根据验证结果抛出验证错误；在注册和登录路由中，当请求有效时，会调用 next 函数继续执行后续操作。

### Sign In Logic  登录逻辑6:49

Implement sign in by validating credentials, querying MongoDB for the user by email, and comparing the provided password to stored hash. Then issue the json web token in a cookie.  
通过验证用户凭据、在 MongoDB 中查询该用户的信息，并将提供的密码与存储的哈希值进行比对，来实现登录功能。之后，将 JSON Web 令牌存储在 Cookie 中。

### Quick Sign In Test  快速登录测试1:53

Implement and test sign up and sign in using Postman with a test email and password, validate credentials and cookies, and plan to add current user and logout routes.  
使用 Postman 实现并测试注册和登录流程，使用一个测试用的电子邮件和密码进行验证。同时，需要验证关于凭证和 cookie 的处理是否正确。此外，计划添加针对当前用户的注销功能。

### Current User Handler  当前用户处理程序3:01

Create a current user route handler that lets the React app verify sign-in by checking the session json web token and return the payload as current user when valid.  
创建一个当前的用户路由处理器，该处理器能够让 React 应用程序通过检查会话中的 JSON Web 令牌来验证登录状态，并在令牌有效时返回当前用户的个人信息。

### Returning the Current User  
返回当前用户8:55

Implement a current user route using JSON Web Token and cookie session, decoding and verifying tokens with a try/catch, returning the payload when valid or null when not.  
使用 JSON Web Token 和 Cookie 会话来实现当前用户的路径导航功能。在验证令牌时采用 try/catch 机制进行异常处理。当令牌有效时返回有效数据，无效时返回空数据。

### Signing Out  注销2:48

Learn how to sign out a user by clearing the cookie session with cookie-session, removing the JSON web token, and returning an empty response for subsequent requests.  
了解如何注销用户身份，方法是通过清除 cookie 会话、移除 JSON Web 令牌，并返回空响应以处理后续的请求。

### Creating a Current User Middleware  
创建当前用户的中间件6:55

Create a current user middleware that decodes the json web token payload and attaches user data to request. Pair it with login-check middleware to guard routes and support downstream handlers.  
创建一个当前用户中间件，该中间件负责解码 JSON 格式的 Web 令牌负载，并将用户数据附加到请求中。同时，结合登录检查中间件来保护路由访问，并支持后续的処理流程。

### Augmenting Type Definitions  
扩展类型定义7:46

Augment the express request type to include an optional current user property by defining a user payload interface and declaring augmentation, enabling middleware to attach the json web token payload.  
扩展该快速请求类型，添加了一个可选的当前用户属性。通过定义用户负载接口并声明相关扩展功能，可以使得附加中间件能够绑定 JSON Web 令牌的负载数据。

### Requiring Auth for Route Access  
需要认证才能进行路由访问7:46

Develop a require-auth middleware that rejects unauthenticated requests by checking the current user property, throws a not-authorized error, and enforces access on protected routes.  
请开发一个 require-auth 中间件，该中间件通过检查当前用户的属性来判断请求是否经过认证。如果请求未经过认证，则会产生一个“未授权”的错误，并禁止对受保护的路由进行访问。


##   
测试孤立的微型服务  
22 场讲座 • 1 小时 22 分钟

### Scope of Testing  测试范围4:28

Define the scope of testing in a microservices environment, from unit tests of a middleware to integration tests across components and services.  
明确在微服务环境中的测试范围，从中间件的单元测试到组件与服务之间的集成测试。

### Testing Goals  测试目标4:32

Explore testing goals for microservices by examining basic request handling, model unit tests, and future event emitting and receiving, all run locally with npm run test against Node.js and MongoDB.  
通过检查基本请求处理、模型单元测试，以及未来可能出现的事件发送和接收功能，来了解微服务的测试目标。所有这些测试都将在本地环境中通过 npm run test 命令进行运行，所使用的环境是 Node.js 和 MongoDB。

### Testing Architecture  测试架构7:51

Wire up testing architecture with Gest, in-memory MongoDB, Express, and Super Test to send requests and assert results, exporting the app for testing on an ephemeral port.  
将测试架构与 Gest、内存中的 MongoDB、Express 以及 Super Test 相结合，通过发送请求来验证结果。同时，将应用程序导出后，可以在临时端口上进行测试。

### Index to App Refactor  应用程序重构索引2:54

Refactor the express setup by creating a dedicated app file, exporting the configured app, and importing it into index to wire middleware and mongoose for future in-memory MongoDB testing.  
通过创建专用的应用程序文件、导出已配置的应用程序，然后将其导入到`index`和`wireMiddleware`模块中，来对`express`设置进行重构。这样就能为后续的内存 MongoDB 测试做好准备。

### Replacing --only=prod Install Flag  
替换 --only=prod 安装标志0:12

### A Few Dependencies  一些依赖项3:33

Install development dependencies with npm install --save-dev, including @types/supertest, supertest, and mongodb-memory-server for in-memory multi-service tests, and adjust the Dockerfile to skip dev dependencies during builds.  
使用`npm install --save-dev`来安装开发所需的依赖项，其中包括@types/supertest、supertest 以及 mongodb-memory-server，这些依赖项用于构建内存中的多服务测试。同时，调整 Dockerfile，在构建过程中跳过开发依赖项的安装。

### Required MongoMemoryServer Updates  
需要进行的 MongoMemoryServer 更新操作0:33

### Test Environment Setup  测试环境搭建8:12

Set up a jest-based test environment for a node microservice with TypeScript support, a MongoDB memory server, and setup files to isolate data across tests.  
为支持 TypeScript 的 node 微服务设置基于 jest 的测试环境，包括 MongoDB 内存服务器，以及用于在测试中隔离数据的配置文件。

### Our First Test  我们的第一次测试6:24

Learn to write a basic signup route test for a Node.js Express app using Jest and Supertest, including test file structure, post requests, and jwt key setup.  
学习如何使用 Jest 和 Supertest 来编写 Node.js Express 应用程序的基础注册路由测试，包括测试文件的结构、POST 请求以及 jwt 密钥的设置。

### An Important Note  重要提示1:31

The lecture notes that jest enables TypeScript support, and that tests can fail to reflect changes; restart the test runner and re-run npm test to update results.  
讲义中提到，使用玩笑式的语言风格可以支持 TypeScript 的编程需求。此外，测试结果可能会因变量的变化而失效；此时需要重新启动测试运行器，并再次运行 npm test 以更新测试结果。

### Testing Invalid Input  测试无效的输入情况5:13

Develop and run input validation tests for the sign up route using super test; verify that invalid email, invalid password, or missing fields return a 400 status.  
使用超级测试工具来开发和运行注册流程的输入验证测试。确保对于无效的电子邮件、错误的密码或缺少必填项的情况，系统能够返回 400 错误状态码作为提示。

### Requiring Unique Emails  需要唯一的电子邮件地址1:45

Write a test to prevent duplicate emails by posting the same email and password twice to the sign-up API, expecting 201 on the first and 400 on the second.  
编写一个测试来验证防止重复发送电子邮件的功能是否正常工作。当尝试使用相同的电子邮件和密码两次进行注册时，期望第一次返回 201 状态码，第二次返回 400 状态码。

### Changing Node Env During Tests  
在测试过程中更改节点环境5:43

Develop tests for the signup route to confirm 201 response and a set-cookie header, inspect the response with supertest, and adapt cookie session security to false in the test environment.  
为注册路由开发测试用例，以验证 201 响应以及 set-cookie 头信息的正确设置。使用 supertest 工具检查响应结果，并在测试环境中将 cookie 会话安全性设置为 false。

### Tests Around Sign In Functionality  
关于登录功能的测试6:26

Write tests for the sign in request handler, verifying 400 for non-existent users or incorrect passwords, and 200 with a set-cookie header for valid credentials.  
为登录处理程序编写测试案例，对于不存在的用户或错误的密码，应返回 400 错误；而对于有效的凭据，则应使用 set-cookie 头信息返回 200 成功状态。

### Cookie Request is Possibly Undefined Error  
“Cookie Request”可能是一个未定义的错误。0:27

### Testing Sign Out  测试已注销状态4:31

Develop a test that signs up a user, ensures authentication, then signs out and verifies the set-cookie header clears the session, asserting a valid response.  
开发一个测试程序，该程序能够注册一个用户，验证其身份验证，然后注销用户，并确认设置的 Cookie 头信息确实使会话失效，从而确保返回的是有效的响应。

### Issues with Cookies During Testing  
测试过程中遇到的 cookie 问题5:18

Learn how to test the current user endpoint by signing up and querying authentication, and tackle cookie handling in tests to enable authenticated requests across services.  
了解如何通过注册和查询认证信息来测试当前用户的端点。同时，也要注意在测试中处理 cookie 的问题，以实现跨服务的认证请求。

### No Overload Matches This Call Error with Cookie  
此调用过程中没有出现过载匹配错误，使用 Cookie 也无问题。0:20

### Easy Auth Solution  简单的认证解决方案3:03

Capture the authentication cookie from the initial response and include it in the follow-up request by setting the cookie header. Refactor into a helper to simplify authenticated testing across services.  
从初始响应中捕获身份验证 cookie，并通过设置 cookie 头将其包含在后续请求中。将这一功能重构为辅助函数，以简化跨服务的身份验证测试工作。

### globalThis has no index signature TS Error  
全局：此语句没有索引签名，存在错误。0:28

### Auth Helper Function  认证辅助功能7:12

Create a global async sign-in helper in the test setup to sign up a user, capture the authentication cookie, and perform authenticated follow-up requests in tests.  
在测试环境中创建一个全局的异步登录辅助工具，用于注册用户、捕获认证 Cookie，并在测试中执行经过认证的后续操作。

### Testing Non-Authed Requests  
测试非认证请求1:44

Test non-authenticated requests by sending a get request to the API users current user, without cookies, and confirm the response returns current user as NULL with status 200.  
通过向当前用户发送 GET 请求来测试未认证的请求。同时，在没有使用 cookie 的情况下，确认响应中是否将当前用户作为 NULL 参数返回，并且状态码为 200。


##   
集成服务器端渲染的 React 应用程序  
40 次讲座 • 3 小时 1 分钟

### Starting the React App  启动 React 应用2:11

Develop the authentication portion of the React app with a landing page that shows not signed in, sign up and sign in forms; address server-side rendering and kubernetes deployment tweaks.  
请开发 React 应用程序的身份验证部分，包括一个登陆页面，页面上能够显示未登录的用户信息、注册表单以及登录表单。同时，还需要处理服务器端渲染以及 Kubernetes 部署相关的调整工作。

### Reminder on Server Side Rendering  
关于服务器端渲染的提醒3:38

Use server side rendering with Next.js to render HTML on the server, fetch data from order service and ticketing service, and boost performance and SEO in a microservices app.  
使用 Next.js 的服务器端渲染功能，可以在服务器端渲染 HTML 页面，从订单服务和票务服务中获取数据。这样既能提升微服务架构中的性能表现，又能改善 SEO 效果。

### Suggestion Regarding a Default Export Warning  
关于默认导出警告的建议0:30

### Basics of Next JS  Next JS 的基础知识5:24

Set up a Next.js project in a client folder, install React, react-dom, and Next.js, create a pages directory with index and banana routes, and run the dev server on localhost:3000.

### Building a Next Image  构建下一个图像4:28

Learn to build a docker image for a Next.js client project, create a Dockerfile and a .dockerignore, exclude node_modules and .next, and prepare a Kubernetes deployment.  
学习如何为 Next.js 客户端项目构建 Docker 镜像，创建 Docker 文件以及`.dockerignore`文件来排除 node_modules 和.next 目录。同时，准备一个 Kubernetes 部署方案。

### Running Next in Kubernetes  
在 Kubernetes 环境中运行下一个任务10:12

Push the client image to Docker Hub, deploy it on a Kubernetes cluster with a deployment and service, configure artifact syncing and ingress, and test at ticketing.dev.  
将客户端镜像推送到 Docker Hub，然后通过部署和服务在 Kubernetes 集群上实施运行。同时配置工件同步和入口配置，最后在 ticketing.dev 上进行测试。

### Small Update for Custom Webpack Config  
关于自定义 Webpack 配置的简要更新0:28

### Note on File Change Detection  
关于文件变化检测的说明4:26

Apply a next.config.js with webpack dev middleware to set watchOptions.poll to 300 ms, addressing docker-based next.js file change detection and guiding pod restarts when updates fail.  
在 next.config.js 文件中添加 webpack 开发中间件的处理逻辑，将 watchOptions.poll 设置为 300 毫秒。这一设置能够解决基于 Docker 的 next.js 文件检测问题，并在更新失败时能够自动重启 Pod。

### Adding Global CSS  添加全局 CSS 样式5:09

Set up global CSS with bootstrap in a Next.js project by wiring the CSS through the custom _app.js file. Install bootstrap, import its CSS, and apply styles globally.  
在 Next.js 项目中，通过配置 _app.js 文件来引入全局 CSS 样式表。安装 Bootstrap，导入其 CSS 文件，然后全局应用这些样式。

### Adding a Sign Up Form  
添加注册表单3:11

Create a sign up form component in the app, expose it at the auth sign-up route via folder routing, and include email and password inputs with a submit button.  
在应用中创建一个注册表单组件，通过文件夹路由将表单组件映射到认证注册路由上。同时添加电子邮件和密码输入字段，并配备一个提交按钮。

### Handling Email and Password Inputs  
处理电子邮件和密码输入3:42

Wire up email and password inputs to React state with useState, add onChange handlers, and handle form submission with onSubmit and preventDefault, preparing data to send to the backend.  
将电子邮件和密码输入字段与 React 状态进行连接，使用 useState 函数来管理状态。同时，添加 onChange 处理程序，并在表单提交时通过 onSubmit 和 preventDefault 方法来处理表单提交，从而将数据准备发送到后端服务器。

### Successful Account Signup  
成功注册账户5:12

Sign up a new user by posting email and password to the API users sign up route with Axios, then manage cookies and basic email validation in the browser.  
通过向 API 用户注册路线发送电子邮件和密码来注册一个新用户。然后使用 Axios 来完成注册过程。之后，在浏览器中管理 cookie 和基本电子邮件验证功能。

### Handling Validation Errors  
处理验证错误6:53

Wrap the signup request in a try/catch, capture the error data, and store it in state. Map the errors array to display messages in an alert only when errors exist.  
在注册请求中加入一个 try/catch 机制，捕获错误数据并将其存储在状态中。将错误数组映射为显示消息，只有出现错误时才会显示相应的提示信息。

### The useRequest Hook  useRequest 钩子函数6:59

Learn to build a reusable useRequest hook in React that takes url, method, and body, executes requests with Axios, and returns a doRequest function with rendered errors for reuse.  
学会在 React 中构建一个可重复使用的 useRequest 钩子函数。该函数接受 url、method 和 body 作为参数，使用 Axios 执行请求，并返回一个包含渲染后的错误信息的 doRequest 函数，以便后续使用。

### Using the useRequest Hook  
使用 useRequest 钩子函数3:40

Integrate the useRequest hook into the signup component to post email and password to api/users/signup, manage errors, and redirect the user after a successful sign-up.  
将 useRequest 钩子集成到注册组件中，用于将电子邮件和密码发送到 api/users/signup 接口；同时能够处理错误情况，并在注册成功后引导用户进行下一步操作。

### An onSuccess Callback  在成功情况下进行的回调函数5:37

Learn to programmatically redirect users in a Next.js app after a successful sign up using an on success callback, routing to the root landing page with router.push.  
学习如何在 Next.js 应用中程序化地重定向用户在成功注册后，使用 on success 回调，通过 router.push 路由到根着陆页。

### Overview on Server Side Rendering  
服务器端渲染概述6:47

Discover how server-side rendering determines sign-in status by querying the current user API and using getInitialProps to fetch data during Next.js SSR.  
发现服务器端渲染如何通过查询当前用户 API 并使用 getInitialProps 在 Next.js SSR 期间获取数据来确定登录状态。

### A note about ECONNREFUSED errors  
关于 ECONNREFUSED 错误的说明：0:24

### Fetching Data During SSR  在 SSR 期间获取数据5:07

Fetch authentication status during server side rendering with getInitialProps using Axios to determine the current user, highlighting differences between server and browser environments in Next.js within Kubernetes.  
在服务器端渲染过程中通过`getInitialProps`函数获取认证状态，使用 Axios 来识别当前用户。这样可以凸显出在 Next.js 框架下，Kubernetes 环境与浏览器环境之间的差异。

### Why the Error?  为什么会出现这个错误？9:00

Trace how browser and server requests differ when getInitialProps runs. See how container networking to 127.0.0.1:80 creates a nasty error with axios for api users current user.  
观察在调用`getInitialProps`时，浏览器和服务器请求之间的差异。看看通过容器网络将请求发送到 127.0.0.1:80 时，为何会导致 axios 在当前用户身上出现错误。

### Two Possible Solutions  两种可能的解决方案7:04

Identify and solve a mysterious SSR fetch error by configuring axios for browser versus server-side requests and routing via Ingress Engine X, rather than direct service calls, while handling cookies.  
通过配置 axios 以支持浏览器端和服务器端请求，并通过 Ingress Engine X 进行路由处理，而不是直接调用服务接口，来解决一个神秘的 SSR 获取错误。同时，还需要处理与 cookie 相关的问题。

### Cross Namespace Service Communication  
跨命名空间服务通信7:45

Learn cross-namespace service communication in Kubernetes by addressing Ingress Engine X across namespaces with a domain built from service name, namespace, and svc cluster local.  
在 Kubernetes 中了解跨命名空间的服务通信机制，通过利用由服务名称、命名空间和集群本地地址组成的域名，来实现不同命名空间之间的 Ingress 通信。

### When is GetInitialProps Called?  
“GetInitialProps”函数是在什么时候被调用的？6:46

Explain when getInitialProps runs on the server versus the browser and how to set axios base URLs for server side rendering and client requests.  
请解释在服务器和浏览器上执行`getInitialProps`时的区别，以及如何为服务器端渲染和客户端请求设置`axios`的基础 URL。

### On the Server or the Browser  
在服务器上，或者在浏览器中2:01

Determine whether you are on the server or in the browser by checking if the window is undefined, and adjust getInitialProps requests to use server url or browser url.  
通过检查窗口是否为 undefined 来判断当前是在服务器上还是通过浏览器访问。同时，调整 getInitialProps 函数的调用方式，使其使用服务器 URL 还是浏览器 URL。

### Ingress-Nginx Namespace and Service ### Important Update  
Ingress-Nginx 命名空间与服务——重要更新0:19

### Specifying the Host  指定主机信息8:23

Specify the host for server and browser requests using axios, set the host header to route to the ingress service, and handle current user data and cookies for sign-in.  
通过使用 axios 来指定服务器和浏览器请求的宿主地址，设置主机头以指向入口服务，同时处理当前用户的数据和登录时的 cookie 信息。

### Passing Through the Cookies  
穿过饼干4:00

Pass the cookie and request headers from an incoming Next.js request to outbound calls to Ingress Engine X, via the server using rec.headers in getInitialProps.  
请传递一下饼干，并通过在`getInitialProps`中使用`rec.headers`来配置服务器，将 Next.js 请求的请求头信息传递给对 Ingress Engine X 的外部调用。

### A Reusable API Client  可重复使用的 API 客户端7:05

Build a reusable api client by creating a build client function that returns environment-aware axios instances for server or browser. Centralize requests and simplify get initial props data fetching.  
通过创建一个可重复使用的 API 客户端函数来构建该客户端，该函数能够返回适用于服务器或浏览器的、考虑环境因素的 axios 实例。这样可以将请求操作集中化处理，并简化对初始数据获取的过程。

### Content on the Landing Page  
登陆页面上的内容2:01

Implement a landing page that uses the current user prop to display 'you are signed in' or 'you are not signed in' and test sign-out via cookies.  
请实现一个登录页面，该页面能够利用当前用户的属性来显示“您已登录”或“您未登录”的提示信息。同时，请测试通过 Cookie 进行登出操作的功能。

### The Sign In Form  登录表单3:12

Replicate the sign up flow to build a sign in page, updating the label and API URL to sign in, and adjust the button text; then test with cookies.  
复制注册流程以创建登录页面，更新标签和 API URL 以完成登录操作，同时调整按钮文字；然后使用 Cookie 进行测试。

### A Reusable Header  可重复使用的头部组件5:00

Design a reusable header that appears on every page by leveraging the app component in Next.js, centralizing current user data, and passing it as props to header and pages.  
通过利用 Next.js 中的应用组件设计一个可在每一页出现的可重用标题，集中当前用户数据，并将其作为属性传递给标题和页面。

### Moving GetInitialProps  移动 GetInitialProps2:11

Implement get initial props in both the custom app component and landing page to test data fetching with a built Axios client, then remove it from the landing page.  
在自定义应用组件和着陆页中实现获取初始属性，以使用构建的 Axios 客户端测试数据获取，然后从着陆页中移除它。

### Issues with Custom App GetInitialProps  
自定义应用程序在获取初始参数时存在的问题6:41

Explore how getInitialProps differs between a page and a custom app in Next.js, and learn to pass app context through ctcs to build a data-fetching client.  
探索 Next.js 中页面和自定义应用之间 getInitialProps 的区别，并学习通过 ctcs 传递应用上下文来构建数据获取客户端。

### Handling Multiple GetInitialProps  
处理多个`getInitialProps`请求6:07

Discover how to coordinate app-wide and page-specific data by invoking a page's getInitialProps from the app component, and safely distribute props to header and landing pages.  
发现如何通过从应用组件中调用页面的 getInitialProps 来协调全局和特定页面的数据，并安全地将 props 分发给头部和着陆页。

### Passing Props Through  将道具传递过去3:03

Learn to pass data through the app with get initial props and page props, and expose current user information to components in a Next.js setup.  
学习如何通过应用程序传递数据，包括获取初始的道具和页面道具，并将当前用户的信息暴露给 Next.js 中的各个组件。

### Error: Invalid <Link> with <a> child  
错误：无效的<Link>，包含<a>子元素0:16

### Building the Header  构建标题栏5:05

Build a reusable header component for a microservices app, wiring a current user prop to show sign in or sign out links, and use the Next.js Link component for navigation.  
为微服务应用程序构建一个可重复使用的头部组件。将当前用户的属性与登录或注销链接关联起来，并使用 Next.js 的 Link 组件来实现导航功能。

### Conditionally Showing Links  
有条件地显示链接5:58

Create a dynamic header that conditionally renders sign up, sign in, and sign out links using a single links array, filtering by current user state.  
创建一个动态化的头部布局，能够根据当前用户的状态，有条件地显示注册、登录和注销链接。可以使用一个链接数组来实现这一功能。

### Signing Out  注销4:36

Implement a client-side sign-out flow triggered from a component using a use request hook to post to api users sign out, then redirect to landing page after cookies are cleared.  
实现一个客户端注销流程，该流程由某个组件触发。通过使用 use 请求钩子，将用户注销请求发送到 API 服务器。之后，在清除 cookie 后，将用户重定向到登录页面。

### React App Catchup & Checkpoint  
React 应用更新与检查点0:51


##   
各服务之间的代码共享与重用  11 场讲座 • 每场 52 分钟

### Shared Logic Between Services  
各服务之间的共享逻辑4:34

Extract shared logic from OTT service into a library and reuse it across services to empower the ticket service to list, show, create, and edit tickets with require-auth and JWT.  
从 OTT 服务中提取共享逻辑，将其整理到库中，并在各个服务中复用这些逻辑。这样就能让票务服务能够支持列表、显示、创建和编辑票务信息的功能，同时支持需要认证的场景以及使用 JWT 令牌进行身份验证。

### Options for Code Sharing  代码共享的方案4:46

Explore three ways to share code across services, including direct copy paste, git sub module, and publishing an npm package, with versioning, automation, and tradeoffs highlighted.  
了解三种在多个服务之间共享代码的方法：包括直接复制粘贴代码、使用 Git 子模块，以及发布带有版本控制的 npm 包。同时会介绍这些方法的优点和缺点，以便您做出明智的选择。

### NPM Organizations  NPM 组织4:27

Publish your common library as an npm package and choose an access level: public registry, organization, or private registry, to balance visibility and security.  
将你的公共库作为 npm 包进行发布，并选择适当的访问级别：公共注册表、组织专用注册表，还是私有注册表，以平衡可访问性与安全性。

### Publishing NPM Modules  发布 NPM 模块4:06

Publish a shared common library to your new organization by creating a Common package, setting its npm name with the organization scope, and publishing with npm publish --access public.  
通过创建一个公共包来将共享库发布到新组织上。同时，设置该包的名称以包含组织的范围信息，然后使用 npm 的 publish 命令进行发布，命令参数为--access public。

### Project Setup  项目设置7:34

Set up a common library by converting TypeScript to JavaScript for sharing middleware and error handling, and install TypeScript and Dell CLI as dev dependencies with build and clean scripts.  
通过将 TypeScript 转换为 JavaScript 来创建一个共享的库，以便共享中间件和处理错误的代码。同时，需要将 TypeScript 和 Dell CLI 作为开发依赖项进行安装，并配置好构建和清理脚本。

### Typo in package.json "files" Field ### Do Not Skip  
package.json 文件中的“files”字段有拼写错误——不要跳过该字段。0:12

### An Easy Publish Command  一个简单的发布命令7:26

Configure package.json main entry, types, and files for a microservice, then publish to npm with an automated script that commits, bumps a patch version, builds, and publishes.  
为微服务配置 package.json 的 main 入口、types 和文件，然后使用自动脚本提交、提升补丁版本、构建并发布到 npm。

### Relocating Shared Code  共享代码的迁移7:29

Move shared code from the service into the common module, relocating the air's directory and middleware folders, then configure imports via an indexed file to simplify usage across services.  
将共享代码从服务中移至公共模块，重新定位 air 目录和中间件文件夹，然后通过索引文件配置导入，以简化跨服务的使用。

### Updating Import Statements  
更新进口申报表5:16

Install the common module and methodically update all import statements across the auth project, consolidating related imports, rebuilding dependencies, and validating the project runs in the browser.  
安装通用模块后，需要系统地更新整个认证项目中的所有导入语句，整合相关的导入内容，重新构建依赖关系，并确保在浏览器中项目能够正常运行。

### NPM Update Command  NPM 更新命令0:14

### Updating the Common Module  
更新通用模块5:34

Update the common module and publish a new version with npm run pub, then update the auth project and verify the latest version is running in the container.  
更新公共模块，并通过 npm run pub 发布新版本。之后更新 auth 项目，确保最新版本能够在容器中正常运行。


##   
创建-读取-更新-删除服务器配置  
26 场讲座 • 2 小时 28 分钟

### Ticketing Service Overview  
票务服务概述3:03

Design and implement a tickets service with routes to list all tickets, fetch by id, create, and update, storing title, price as string, and owner id in a dedicated mongodb.  
设计并实现一个票务服务，该服务包含以下功能：列出所有票务信息、根据 ID 获取票务信息、创建和更新票务信息。同时，该系统需要存储票务的标题、价格，以及票务的所有者 ID，这些信息都将存储在专门的 MongoDB 数据库中。

### Project Setup  项目设置5:24

Set up the tickets service by copying config, renaming packages to tickets, installing dependencies with npm, and building and pushing a Docker image to Docker Hub, preparing for Kubernetes deployment.  
通过复制配置文件、将包文件夹重命名为“tickets”、使用 npm 安装依赖模块，以及构建并将 Docker 镜像推送到 Docker Hub 来设置票证服务。现在已经准备好进行 Kubernetes 部署了。

### Running the Ticket Service  
运行票务服务6:05

Deploy the tickets microservice by duplicating the auth deployment, renaming to tickets, wiring the JWT key, and updating the MongoDB and scaffold YAML configurations for Kubernetes.  
通过复制 auth 部署、将其重命名为“tickets”，关联 JWT 密钥，以及更新 MongoDB 和 Kubernetes 的 scaffold YAML 配置，来部署 tickets 微服务。

### Mongo Connection URI  Mongo 连接 URI8:13

Learn to configure the Mongo URI as an environment variable in deployment files for the tickets and auth services, update code to read that URI, and verify proper MongoDB connections.  
学习如何将 Mongo URI 作为环境变量配置在部署文件中，用于票证和认证服务。同时，更新代码以读取该 URI，并确保 MongoDB 连接正确。

### Quick Auth Update  快速认证更新1:47

update the service to read the mongo uri from an environment variable in the deployment file, wire process.env.mongo_uri to connect, and prep for route handlers and tests.  
更新服务以从部署文件中的环境变量中获取 MongoDB 的 URI 地址，并将 wire-process.env.mongo_uri 变量用于连接操作。同时，为路由处理程序和测试做好准备。

### Test-First Approach  测试优先法4:19

Practice test-driven development by designing and testing a new ticket route handler. Validate title and price, enforce authentication, and implement a post /api/tickets workflow with tests.  
通过设计和测试新的票据处理程序来实施练习驱动的开发方式。验证标题和价格信息，实施身份验证机制，并配合测试来构建/api/tickets 工作流程。

### Creating the Router  创建路由器4:56

Create a post route for API/tickets and return a non-404 status to satisfy tests, wiring the create ticket router into the Express app.  
为 API/tickets 接口创建一个处理 POST 请求的方法，并返回一个非 404 状态的响应以满足测试需求。同时，将创建票证的路由逻辑整合到 Express 应用中。

### Adding Auth Protection  添加身份验证保护8:04

Implement authentication protection by wiring current user and require auth middleware to routes, enforce 401 for unauthenticated requests, and craft tests for signed-in versus not-signed-in scenarios.  
通过为当前用户启用身份验证保护功能来实现安全保护。同时，要求认证中间件能够处理未经认证的请求，并响应 401 状态码。此外，还需要编写测试案例来对比用户已登录与未登录时的不同场景。

### Faking Authentication During Tests  
在测试过程中伪造认证信息7:09

Learn how to fake user authentication in tests for a ticket service by constructing a base64-encoded cookie containing a JWT, avoiding inter-service requests and keeping tests self-contained.  
了解如何在测试中实现用户身份验证的伪造操作，以处理票证服务的相关功能。具体方法是通过构造一个包含 JWT 的 Base64 编码 cookie 来实现，同时避免服务之间的相互调用，使测试更加独立且可靠。

### A Required Session Fix and a Global Signin Reminder  
一个必要的会话固定策略，以及一个关于全局登录提醒的提示信息0:15

### Building a Session  构建会话5:39

Build a session by creating a user payload, generating a JSON web token with the JWT library’s sign method, and base64-encoding the resulting session JSON.  
要创建一个会话，需要以下步骤：首先使用 JWT 库中的签名方法生成 JSON Web 令牌；然后对该令牌进行 Base64 编码，最终得到会话的 JSON 数据。

### Testing Request Validation  
测试请求验证4:12

Test request validation by sending empty or missing titles and invalid or missing prices, expecting a 400 error. Use the common request validation middleware to generate errors.  
通过发送空的标题、无效的标题、错误的价格等信息来测试请求验证功能，从而预期会收到 400 错误。使用常见的请求验证中间件来生成错误。

### Validating Title and Price  
验证标题和价格4:09

Wire up the body function from express-validator as middleware after authentication to validate title and price, with title not empty and price as a decimal greater than zero.  
在身份验证之后，将“body function”从 express-validator 中作为中间件使用。该中间件用于验证标题和价格是否符合要求：标题不能为空，且价格必须是一个大于零的小数。

### Reminder on Mongoose with TypeScript  
关于使用 TypeScript 的 Mongoose 的提醒7:12

Define a mongoose-based ticket model in TypeScript, include interfaces and a build method, save tickets to MongoDB, and validate that a POST creates a ticket with 201.  
在 TypeScript 中定义一个基于 mongoose 的票务模型，包括相关接口和构建方法。将票务数据保存到 MongoDB 中，并验证 POST 请求是否成功创建了票务记录，返回 201 状态码表示成功。

### Defining the Ticket Model  
定义票据模型3:48

Define a Mongoose ticket schema with title, price, and userId as required fields, implement a toJSON transform mapping _id to id, and add a static build method.  
定义一个 Mongoose 票据的架构，包含标题、价格以及 userId 等必要字段。实现 toJSON 转换函数，将_id 字段映射为 id 字段。此外，还需要添加一个静态构建方法。

### Creation via Route Handler  
通过路由处理程序进行创建8:21

Create and persist a ticket via a route handler, using authentication, extracting title and price from the request, and saving to MongoDB with a 201 response.  
通过路由处理器创建并保存一个工单。在过程中使用身份验证机制，从请求中提取标题和价格信息，然后以 201 响应将数据保存到 MongoDB 中。

### Testing Show Routes  测试显示路线6:52

Implement a show route that queries api tickets by id, returning 200 with the ticket data or 404 if not found, using a test driven development approach with tests.  
实现一个展示路由功能，该功能通过 ID 查询 API 接口中的票据数据。如果找到票据数据，则返回 200 状态码的结果；如果找不到票据数据，则返回 404 状态码的错误信息。整个实现过程采用测试驱动的开发方式，通过测试来验证代码的正确性。

### Unexpected Failure!  意外故障！5:29

Builds an Express show route for tickets, fetches by id from the database, throws a not found air when missing, and returns the ticket when found, guided by tests.  
为这些票据构建了一个快速展示的路由路径；通过数据库中的 ID 来获取票据信息，如果找不到对应的票据，则会抛出“未找到”的异常；当找到相应的票据时，会返回该票据的信息。整个流程都是基于测试来进行的。

### What's that Error?!  那个错误是什么？！8:57

Debug and diagnose error handling in the common module’s air handler, uncovering a bad id causing a 400 instead of 404, and plan enhanced logging for unknown errors.  
调试并诊断通用模块中的错误处理问题，找出导致错误代码为 400 而非 404 的原因。同时规划对未知错误的改进性日志记录机制。

### Better Error Logging  更好的错误日志记录机制5:11

Improve error visibility by enhancing the common module's error logger, rebuilding and publishing updates, and using mongoose to generate valid object IDs for reliable tests.  
通过增强通用模块的错误记录功能、重新构建并发布更新内容，以及使用 mongoose 生成有效的对象 ID，来提高错误可见性，从而确保测试的可靠性。

### Complete Index Route Implementation  
完整索引路径实施6:31

Implement the index route to fetch all tickets via tests, create three tickets, and return them as a 200 response array from the /api/tickets endpoint.  
实现索引路由，通过测试获取所有票据，创建三个票据，并将它们作为 200 响应数组返回到/api/tickets 端点。

### Ticket Updating  门票更新6:15

Update tickets via put requests for a title and price, enforcing authentication and ownership, while preparing tests for not found, unauthorized, and invalid input scenarios.  
通过发送请求来更新商品信息，包括标题和价格，同时确保身份验证和所有权信息的准确性。同时，还需要准备针对无效输入、未经授权的输入以及找不到商品等情况的测试方案。

### Handling Updates  处理更新信息4:15

Create an express update ticket handler with body validation, authentication, and not-found checks, wiring a put route for api tickets/:id and ensuring tests pass.  
请创建一个快速更新票证处理程序，该处理程序包含主体验证、身份验证以及不存在资源的检查功能。同时，为 api 票证/id 路径配置 put 路由，并确保测试能够通过。

### Permission Checking  权限检查6:49

Learn to enforce permission checks in a microservices setup by creating and updating tickets with user-specific cookies, validating authorization and returning 401 for unauthorized edits.  
学会在微服务架构中实施权限检查机制。通过创建和更新包含用户特定 cookie 的工单，来验证授权操作，对于未经授权的编辑操作则返回 401 错误。

### Final Update Changes  最终更新内容变更9:04

Demonstrates validating and updating a ticket via a put request, using cookies for authentication, with server-side validation for title and price, and persisting updates with Mongoose save.  
该操作展示了如何通过 PUT 请求来验证和更新票据。验证过程中使用了 Cookie 进行身份验证，而标题和价格等字段则经过了服务器端验证。更新后的数据会被保存在 Mongoose 中。

### Manual Testing  手动测试5:36

Test the ticket service routes with Postman, expose the api/tickets path via ingress, authenticate with sign in or sign up, then create, fetch, and update tickets by title and price.  
使用 Postman 测试票服务接口，通过入口路径访问 API 中的票信息部分，通过登录或注册进行身份验证。然后，可以根据标题和价格来创建、获取和更新票信息。


##   
NATS 流媒体服务器——一种事件总线实现方式  
23 次讲座 • 2 小时 57 分钟

### What Now?  现在该怎么办？2:50

Explore implementing the Nat streaming server based event bus to connect the ticket and order services, understand event-driven behavior, and prepare for cross-service workflows.  
尝试实现基于 Nat 流服务器的事件总线，以连接票务和订单服务，了解事件驱动型的行为模式，并为跨服务的工作流程做好准备。

### NATS Server Status ### IMPORTANT NOTE  
NATS 服务器状态 ### 重要提示0:40

### Three Important Items  三件重要的事情4:34

Deploy the Nats streaming server in a Kubernetes cluster using a config file and consult Docker Hub for the image and its command line options.  
使用配置文件在 Kubernetes 集群中部署 Nats 流媒体服务器。同时，可以查看 Docker Hub 上的镜像及其命令行选项。

### Creating a NATS Streaming Deployment  
创建 NATS 流媒体传输部署方案6:25

Deploy a nats streaming server to a kubernetes cluster by creating a deployment and a cluster ip service, using image 0.17.0 and exposing client 4222 and monitoring 8222.  
通过创建部署和集群 IP 服务，并使用 0.17.0 版本的图像，将 nats 流服务器部署到 Kubernetes 集群中。同时，将 4222 端口暴露给客户端，将 8222 端口暴露给监控服务。

### Big Notes on NATS Streaming  
关于 NATS 流媒体的深度解析9:53

Compare nat streaming server to a custom event bus, showing how to publish and subscribe to topics using the Node nat streaming library, with in-memory and persisted event storage options.  
将 Nat 流媒体服务器与自定义事件总线进行比较，了解如何使用 Node 的 Nat 流媒体库来发布和订阅主题，同时提供内存存储和持久化存储选项。

### Building a NATS Test Project  
构建 NATS 测试项目7:31

Build a TypeScript subproject to use node-nats streaming, create publisher and listener scripts to emit and listen for events, and plan connecting from outside a Kubernetes cluster.  
创建一个使用 Node.js 流处理的 TypeScript 子项目，编写发布者和监听器脚本以发送和接收事件。同时，规划如何将项目连接到 Kubernetes 集群之外的地方。

### Port-Forwarding with Kubectl  
使用 Kubectl 进行端口转发4:40

Port forward a pod with kubectl to access the Nats streaming server inside the cluster from your local machine, enabling development-time testing of publisher and listener connections.  
使用 kubectl 将 Pod 转发到集群内的 Nats 流媒体服务器，从而可以从本地机器访问该服务器，进而在开发过程中对发布者和监听器连接进行测试。

### Publishing Events  发布事件6:20

Publish a ticket created event to the nat streaming server by converting the data to json and sending it on the ticket created subject.  
将生成的事件数据转换为 JSON 格式，然后通过该 JSON 数据将事件信息发送到自然流服务器。

### Small Required Command Change  
较小的必要命令变更0:22

### Listening For Data  正在监听数据信号6:55

Implement a listener that subscribes to a stan channel, publishes and receives ticket: created events, and logs messages to verify data flow between publisher and subscriber.  
实现一个监听器，该监听器需要订阅一个 Stan 通道，发布数据并接收相关事件，同时记录消息以验证发布者与订阅者之间的数据流动是否正常。

### Accessing Event Data  访问事件数据6:56

Annotate the message as a type, use get data, get sequence, and get subject to access event data and channel; handle string or buffer, json parse, and log the event.  
将消息标记为某种类型，使用 get 数据、获取序列号以及获取主题来访问事件数据和通道信息；处理字符串或缓冲区数据，解析 JSON 格式的数据，并记录事件日志。

### Client ID Generation  客户端 ID 生成4:48

Learn to scale the order service in Kubernetes by running multiple clients with unique IDs and generating random IDs to avoid duplicate client ID errors on the Nats streaming server.  
通过运行多个具有唯一 ID 的客户端，并生成随机 ID，可以学会如何在 Kubernetes 中扩展订单服务。这样就能避免 Nats 流服务器上因客户端 ID 重复而产生的错误。

### Queue Groups  队列组7:28

Learn to use queue groups in a streaming server to route each event to a single service instance among multiple listeners, avoiding duplicate processing and enabling scalable microservices.  
学会在流服务器中使用队列组，将每个事件分配给多个监听器中的单个服务实例来处理，从而避免重复处理，并使得微服务能够具备可扩展性。

### Manual Ack Mode  手动确认模式9:51

Enable manual ack mode for subscriptions to control when an event is acknowledged and retried after failure. Configure options to ensure processing and enable re-delivery after timeouts.  
为订阅项启用手动确认模式，以控制事件何时被确认，以及在出现故障时如何重试。配置相关选项以确保能够正确处理事件，并在超时后重新发送请求。

### Client Health Checks  客户健康检查9:45

Explore how client health checks work on the NATS streaming server, using the monitoring page and heartbeat settings to diagnose missing events in subscriptions and channels.  
了解在 NATS 流服务器上，如何通过监控页面和心跳设置来诊断订阅和通道中缺失的事件。

### Graceful Client Shutdown  优雅的客户端关闭操作6:42

Implement graceful shutdown by intercepting close events and signaling the Nats server to stop sending messages. Handle process signals and add Stan close calls to prevent missed messages.  
通过拦截关闭事件，通知 Nats 服务器停止发送消息，从而实现优雅的关闭过程。同时处理进程信号，并添加 Stan 的关闭调用，以避免消息被遗漏。

### Core Concurrency Issues  核心并发问题12:56

Investigate core concurrency issues in microservices, using an asynchronous banking example with deposits and withdrawals to reveal how listener failures, delays, and out-of-order events can cause catastrophic errors.  
通过异步银行交易中的存款和取款案例，来研究微服务中的核心并发问题。可以发现，监听器故障、延迟以及事件顺序混乱等问题，都可能导致灾难性的错误发生。

### Common Questions  常见问题8:08

Explain that async event-based microservices face similar concurrency issues as monoliths, and that running multiple service copies helps scalability, while some concurrency problems cannot be solved with code alone.  
请说明，异步的、基于事件的微服务同样面临着与单体系统类似的并发问题。运行多个服务副本有助于提升可扩展性，而某些并发问题则无法仅通过代码来解决。

### [Optional] More Possible Concurrency Solutions  
[可选] 更多可能的并发解决方案16:42

Analyze three extra concurrency ideas and why they fall short in microservices: global sequential processing, per-user sequence pools with channel overhead, and publisher-side sequencing with Nats streaming server.  
分析三种额外的并发处理方案，并说明它们为何在微服务架构中存在不足：全局顺序处理、基于用户的顺序池方案以及带有通道开销的发布者端排序方式，还有使用 Nats 流服务器进行的发布者端排序方案。

### Solving Concurrency Issues  
解决并发问题20:05

Redesign the accounting microservice by introducing a transaction service that stores transactions, emits transaction created events to the NATS streaming server, and uses per-user last transaction numbers to resolve concurrency.  
通过引入一个事务服务来重新设计会计微服务。该事务服务负责存储事务记录，向 NATS 流服务器发送事务创建事件，并利用每个用户的最后事务编号来解决并发问题。

### Concurrency Control with the Tickets App  
使用票据进行并发控制9:40

Apply concurrency control in the tickets service with a last transaction number version. See how price updates, event ordering, version checks, and NATS streaming keep the orders service in sync.  
在票务服务中实施并发控制机制，采用基于最后事务编号的版本管理方式。了解如何通过价格更新、事件排序、版本检查以及 NATS 流式处理技术，来确保订单服务的同步性。

### Event Redelivery  活动重新安排4:34

Show how Nat streaming re-delivers missed events when a service comes back online, by storing events in a channel and using deliver all available with subscription options.  
当服务重新上线时，Nat 流媒体服务能够重新播放那些错过的内容。这是因为该系统将事件存储在频道中，并利用订阅功能来提供所有可用的内容。

### Durable Subscriptions  可持久订阅8:59

Learn how durable subscriptions in nats ensure reliable event delivery by using set durable name, set deliver all available, and a queue group to avoid re delivery.  
了解如何在 NATS 中通过设置持久性名称、选择“传递所有可用数据”功能，以及使用队列组来避免数据重复传递，从而实现可靠的事件传输。这种方式能够确保订阅的可靠性。


##   
在 Node.js 环境中连接到 NATS  
17 次讲座 • 1 小时 22 分钟

### Reusable NATS Listeners  可重复使用的 NATS 监听器4:32

Explore refactoring for reusable nats listeners by building an abstract listener class with a configurable subject, on message, and parse helpers, enabling ticket created and order updated listeners.  
通过构建一个抽象监听器类来探索重构可复用的 NAT 监听器的方法。该抽象监听器类包含可配置的主题、消息处理机制以及解析辅助工具，从而能够实现票证创建和订单更新等监听功能。

### The Listener Abstract Class  
监听器抽象类9:27

Define and implement an abstract listener class for a Node Nats project, including a pre-initialized Stan client, abstract subject and group name, and subscription options with an on message hook.  
为 Node Nats 项目定义一个抽象监听器类，并实现该类。同时，需要包含一个预初始化的 Stan 客户端、抽象的主题对象、组名称，以及带有消息处理函数的订阅选项。

### Extending the Listener  扩展监听器功能5:53

Extend the listener abstract class to build a ticket created listener, defining subject, queue group name, and on message handling to process events and acknowledge or retry failures using Nats.  
扩展监听器抽象类以构建票证创建监听器，定义主题、队列组名称和在消息处理中处理事件，并使用 Nats 确认或重试失败。

### Quick Refactor  快速重构3:17

Refactor the listener architecture by moving the abstract listener to a base listener in a new events folder and extracting the ticket created listener into its own file.  
重构监听器架构，将抽象监听器移动到新事件文件夹中的基础监听器，并将票证创建监听器提取到其自己的文件中。

### Leveraging TypeScript for Listener Validation  
利用 TypeScript 进行监听器验证5:28

Leverage TypeScript to enforce a strongly typed, single structure for event data and its subject in the listener, validating on message data to prevent misnamed channels and incorrect payloads.  
利用 TypeScript 来强制实现事件数据及其主题的严格类型化结构，对消息数据进行验证，以避免使用错误的通道名称或包含不正确的数据内容。

### Subjects Enum  主题枚举3:28

Create a Subjects enum in the events directory to list subjects like ticket created and order updated, and use it to avoid hardcoded strings and prep for data type coupling.  
在 events 目录下创建一个 Subjects 枚举，用于记录诸如创建票据、更新订单等主题信息。这样可以避免使用硬编码的字符串，同时也有助于实现数据类型的耦合。

### Custom Event Interface  自定义事件接口2:26

Create a ticket created event interface that enforces a tight coupling between ticket created subject and its data (id, title, price) using the subjects enum for type checking in listeners.  
创建一个票证创建事件接口，该接口能够确保票证创建的主题与其数据（ID、标题、价格）之间有紧密的关联。同时，该接口还会利用主题枚举来进行类型检查，并在监听器中处理相关逻辑。

### Enforcing Listener Subjects  
强制执行监听器主题7:22

Explore TypeScript generics to enforce a listener’s subject/data contract by defining a generic event interface, aligning subject with ticket created data, and using subjects enum for safety.  
利用 TypeScript 的泛型特性，通过定义通用的事件接口来约束监听器的主题/数据契约。将主题与创建的票据数据保持一致，并使用枚举类型来确保数据的安全性。

### Quick Note: 'readonly' in Typescript  
快速提示：在 TypeScript 中，“readonly”的含义是“只读”状态。0:25

### Enforcing Data Types  强制使用数据类型3:53

Enforce type safety in on message data by using the ticket created event interface, replacing any, and using base listener generics to prevent non-existent properties.  
通过使用票证创建事件接口来强制 on 消息数据中的类型安全，替换 any，并使用基础监听器泛型来防止不存在的属性。

### Where Does this Get Used?  
这种东西会被用到什么地方呢？2:49

We examine a common module that defines subjects, event interfaces, and a base listener so all services share the same event definitions and customize on message logic.  
我们检查一个常见的模块，该模块定义了主题、事件接口和基础监听器，以便所有服务共享相同的事件定义并在消息逻辑上进行定制。

### Custom Publisher  定制发布者8:11

Publish robust events with a type-safe TypeScript publisher, using an abstract base and a ticket created publisher to enforce data shape and valid subjects for nat streaming server.  
通过类型安全的 TypeScript 发布器来发布可靠的事件。利用抽象基类和基于票据创建的发布器，可以确保数据格式的正确性，以及参与事务的实体的有效性，从而确保自然流服务器的正常运行。

### Using the Custom Publisher  
使用自定义发布器3:49

Refine the custom ticket created publisher by converting data to JSON and enforcing TypeScript type checks for id, title, and price when publishing with a Nats client.  
通过将数据转换为 JSON 格式，并在使用 Nats 客户端进行发布时，对 ID、标题和价格等字段进行 TypeScript 类型检查，从而完善由发布者创建的自定义票务信息。

### Awaiting Event Publication  
等待事件公告的发布4:28

Turn the publish call into a promise and use async/await to wait for event publication from the Nat streaming library, handling success with resolve and errors with reject.  
将发布的调用转化为一个承诺，并使用 async/await 来等待来自 Nat 流处理库的事件发布。在事件成功发布时使用 resolve，在出现错误时使用 reject 来处理情况。

### Common Event Definitions Summary  
常见事件定义汇总6:26

Prioritize testing listeners and publishers, and centralize event definitions in a common TypeScript module to ensure consistent subjects and event data across services, with cross-language options discussed.  
优先测试监听器和发布者组件，并将事件定义集中放在一个统一的 TypeScript 模块中，以确保各服务之间主题和事件数据的一致性。同时，还会讨论跨语言的应用场景。

### Updating the Common Module  
更新通用模块7:35

Merge code into the common module by adding an events folder with ticket created and updated events, then publish the module and update the tickets service to use streaming events.  
通过将包含创建和更新事件的事件文件夹添加到公共模块中，然后将该模块发布出去，并更新票务服务以使用流式事件处理机制，从而实现代码整合。

### Restarting NATS  重新启动 NATS2:09

Restart the Nats pod to purge in-memory events and remove trash data from ticket create and update events, then verify a fresh pod is running for clean event handling.  
重新启动 Nats 节点，以清除内存中的事件数据，并删除与创建和更新事件相关的无用数据。确认新的节点能够正常运行，从而确保事件处理的准确性。


##   管理 NATS 客户端  
19 次讲座 • 1 小时 37 分钟

### Publishing Ticket Creation  
发布任务创建3:48

Update the common library, save new tickets to the database, and publish a ticket created event from the tickets service to the event stream using a new nats client.  
更新公共库，将新创建的工单保存到数据库中，然后使用新的 nats 客户端将工单创建事件发布到事件流中。

### More on Publishing  更多关于出版方面的信息3:19

Import the ticket created publisher and publish an event after saving the ticket, sending id, title, price, and user id from the saved ticket.  
导入由发布者创建的事件票证，在保存票证后，根据保存的票证中的信息，发送 ID、标题、价格以及用户 ID 等数据。

### NATS Client Singleton  NATS 客户端单例5:24

Learn how to create a Nats client singleton in a Node.js microservices app, initialize it in the index, and share it safely across files while avoiding circular imports.  
了解如何在 Node.js 微服务应用中创建 Nats 客户端单例对象，如何在索引文件中初始化它，以及如何在不同文件之间安全地共享该单例对象，同时避免循环导入问题。

### Node Nats Streaming Installation  
节点 Nats 流媒体安装0:09

### Remember Mongoose?  还记得 Mongoose 吗？4:57

Develop a NATS wrapper class to initialize a client and export a singleton instance, mirroring Mongoose, so the same pre-initialized object is shared across index and ticket created route handler.  
请开发一个 NATS 封装类，用于初始化客户端并导出单例实例，实现与 Mongoose 的兼容。这样，在索引和创建票证的处理器中，就可以共享相同的预初始化对象。

### TS Error ### Did you forget to include 'void' in your type argument  
TS 错误 ### 你忘记在类型参数中包含'void'关键字了吗？0:17

### Singleton Implementation  单例实现9:04

Implement a singleton Nats client by adding an underscore client and converting the connect callback to an async promise, wiring cluster id, client id, and url for a shared instance.  
通过添加下划线的方式创建单例模式的国家客户端，并将连接回调函数转换为异步承诺函数。同时，需要配置集群 ID、客户端 ID 以及 URL，以创建一个共享实例。

### Accessing the NATS Client  
访问 NATS 客户端4:28

Expose the Nats client from a singleton via a TypeScript getter, guard access before connect, publish events from the new ticket route, and ensure cleanup on shutdown.  
通过 TypeScript 的 getter 方法暴露 Nats 客户端实例，在连接之前进行访问保护，从新的事件路由中发布事件，并在关闭时确保资源被清理。

### Graceful Shutdown  优雅的关闭6:55

explains implementing graceful shutdown in a Node.js and Nats-based app, centralizing exit logic in index, closing the client on interrupt or terminate signals, and testing with a simulated Nats disconnect.  
本文介绍了如何在基于 Node.js 和 Nats 的应用程序中实现优雅的关闭操作。具体做法是将退出逻辑集中放在索引模块中，在收到中断或终止信号时关闭客户端连接。同时，还通过模拟 Nats 连接断开的情况来测试该算法的有效性。

### Successful Listen!  成功的倾听！3:23

Test the end-to-end event flow by publishing and listening for a ticket created event with Nats, using Postman and authentication, and ensure update events are emitted.  
通过使用 Postman 和身份验证功能，发布并监听由 Nats 创建的事件，以测试端到端的事件流程是否正常。同时，要确保更新事件也能被正确触发。

### Ticket Update Publishing  票务信息更新发布4:28

Create a ticket updated publisher by duplicating the ticket created publisher, emit a ticket:updated event (id, title, price, userId) on updates, and verify with a PUT request.  
通过复制已创建的工单来创建一个新的工单。在工单更新时，触发`updated`事件（包含 id、标题、价格、用户 ID 等信息），并通过 PUT 请求进行验证。

### Failed Event Publishing  事件发布失败7:03

Compare ticket creation and update handlers to show how awaiting the event publisher affects error handling and latency, and examine how failed event publishing can threaten cross-service data integrity.  
比较票据创建和更新处理程序的功能，了解等待事件发布者的情况如何影响错误处理和延迟，同时观察事件发布失败如何威胁到跨服务的数据完整性。

### Handling Publish Failures  
处理发布失败的情况6:02

Save the record and its event in the database, then publish the event via a separate process. Use a published flag and consider database transactions for rollback if saves fail.  
将记录及其相关事件保存到数据库中，然后通过一个独立的流程来发布该事件。使用一种“已发布”的标志来区分事件的状态，如果保存失败，可以考虑使用数据库事务来进行回滚操作。

### Fixing a Few Tests  修复一些测试问题3:52

Fix failing tests in the ticketing service by mocking the Nats client in Jest, redirecting imports to a fake initialized client instead of connecting to a real Nats server.  
通过在 Jest 中模拟 NATS 客户端来修复票务服务的失败测试，将导入重定向到假初始化客户端，而不是连接到真实的 NATS 服务器。

### Redirecting Imports  转口进口商品5:35

Learn how to redirect imports in tests by creating __mocks__ files, exporting a fake wrapper, and configuring test files to use mocks, enabling isolated unit testing.  
学习如何在测试中重定向导入，通过创建 __mocks__ 文件，导出一个假封装器，并配置测试文件使用模拟，以实现隔离的单元测试。

### Providing a Mock Implementation  
提供一个模拟实现9:09

Create a fake nats wrapper for tests by implementing a minimal client with a publish function that immediately invokes a callback, used by the ticket route handler and base publisher.  
创建一个用于测试的假 NATS 封装器，通过实现一个具有立即调用回调函数的最小客户端，该客户端被票务路由处理程序和基础发布者使用。

### Test-Suite Wide Mocks  测试套件范围内的模拟对象2:12

This lecture demonstrates centralizing mocks in the test setup using a fake Nats wrapper, updating the path to prevent real NAT server calls, and preparing tests to verify event publishing.  
本次讲座展示了如何在测试环境中将模拟测试集中化处理，通过使用一个假的 Nats 包装器来实现这一目标。同时，讲座还介绍了如何更新路径以避免调用真实的 NAT 服务器，以及如何准备测试来验证事件发布的准确性。

### Ensuring Mock Invocations  
确保模拟调用能够正常工作9:13

Learn how to replace fake publish implementations with a mock function to verify that events are published in create and update flows, using a mock that tracks calls and arguments.  
了解如何用模拟函数替代真实的发布实现，以验证在创建和更新流程中事件是否真的被发布了。这里使用的模拟函数能够追踪调用次数和传递的参数信息。

### NATS Env Variables  NATS 环境变量8:00

Configure NATS connectivity via env vars by adding cluster id, client id, and url in the deployment file; use Kubernetes to derive client id from the pod name for identification.  
通过在部署文件中添加集群 ID、客户端 ID 和 URL 来配置 NATS 连接性；使用 Kubernetes 从 Pod 名称中提取客户端 ID 用于识别。


##   
跨服务数据复制的实际应用  
28 场讲座 • 2 小时 44 分钟

### The Orders Service  订单服务部门5:02

Explore the orders service, which creates, updates, and pays for orders, locks tickets, expires holds, replicates ticket data, listens for ticket service events, and uses versioning to handle out-of-order events.

### Scaffolding the Orders Service  
为“订单服务”系统搭建 scaffolding4:39

Duplicate code from the ticketing service to scaffold the orders service, install dependencies, build a Docker image, and configure a Kubernetes deployment and ingress routing for orders.

### A Touch More Setup  再稍微配置一下吧7:32

Learn to set up the orders service in a microservices stack by creating a Kubernetes deployment, configuring a dedicated MongoDB, and updating image references for deployment across environments.  
学习如何在微服务架构中设置订单处理服务：通过创建 Kubernetes 部署容器、配置专用的 MongoDB 数据库，以及更新图像引用以使其在多种环境中正常运行。

### Ingress Routing Rules  入口路由规则1:53

Configure the ingress service to route API/orders requests to the order service using three path rules, ensure the client service is the fallback, and verify configuration in the scaffold terminal.  
请配置入口服务，使其通过三个路径规则将 API/订单请求路由到订单服务。同时，确保客户端服务作为备选方案。最后，在命令行工具中验证配置是否正确设置。

### Scaffolding a Few Route Handlers  
为几个路由处理器配置支架9:55

scaffolds the orders service by creating index, show, new, and delete route handlers for api/orders and api/orders/:orderId, enforcing authentication and ownership while enabling listing, viewing, creating, and canceling orders.  
通过为 api/orders 和 api/orders/:orderId 创建索引、显示、新建和删除路由处理程序来构建订单服务，同时强制执行身份验证和所有权，并启用订单列表、查看、创建和取消功能。

### Subtle Service Coupling  微妙的服务耦合6:43

Implement a protected create order route that requires a ticket id in the request body, validates its presence and MongoDB id format, and examines subtle coupling between services.  
实现一个受保护的“create order”路由处理程序。该路由需要处理请求体中的票券 ID，验证该 ID 的存在以及其是否符合 MongoDB 的 ID 格式要求。同时，还需要注意各服务之间的紧密耦合关系。

### Associating Orders and Tickets  
关联订单和通知6:49

Use mongoose ref and population to relate orders to tickets across separate collections (orders include user id, status, expires) and enable a ticket reference.  
利用 mongoose 的 ref 功能，可以将订单与票据进行关联，使得订单信息能够指向特定的票据。同时，还可以实现订单与票据之间的相互引用关系。订单包含用户 ID、状态、到期时间等信息，而票据则包含相关的详细信息。

### Order Model Setup  订购模型设置8:52

Set up the order model with Mongoose and TypeScript, define adders and docs interfaces, design a schema with user id, status, and ticket ref, and implement a build method.  
使用 Mongoose 和 TypeScript 来构建订单模型，定义各种接口和文档内容。设计数据库模式，包括用户 ID、状态以及与工单相关的引用字段。最后，实现一个用于构建订单的方法。

### The Need for an Enum  
对枚举的需求5:56

Define a shared order status enum in a common library to ensure all services use the same status strings, such as pending, paid, failed, or expired, for reliable event-driven communication.  
在公共库中定义一种共享的订单状态枚举，以确保所有服务都能使用相同的状态字符串，例如“待处理”、“已付费”、“失败”或“过期”，从而实现可靠的事件驱动式通信。

### Creating an Order Status Enum  
创建订单状态枚举8:31

Create an order status enum with created, cancelled, awaiting payment, and complete. Enforce valid values via TypeScript interfaces and a mongoose enum, with a created default and common module export.  
创建一个订单状态枚举，包含已创建、已取消、等待付款和已完成四种状态。通过 TypeScript 接口和 mongoose 枚举来确保值的有效性，并设定已创建的默认值为"已创建"，同时该值也需要作为模块导出。

### More on Mongoose Refs  更多关于 Mongoose References 的信息2:58

Learn to use mongoose refs to link orders and tickets with populate, and add a ticket model to order service to handle existing and new orders, fetching orders with tickets.  
学习如何使用 mongoose 的 refs 功能来连接订单和工单，并通过 populate 方法将工单模型与订单服务关联起来。同时，为订单服务添加工单模型，以便处理现有的以及新的订单，同时能够获取带有工单的订单信息。

### Defining the Ticket Model  
定义票据模型7:09

Define a ticket model in the tickets service using mongoose, with ticket adders, ticket doc, and ticket model, keeping service-specific schemas and enabling linking tickets to orders.  
使用 mongoose 在票务服务中定义一种票务模型，包括票务添加功能、票务文档以及票务模型。同时保留与服务相关的特定模式，并支持将票务与订单进行关联。

### Order Creation Logic  订单创建逻辑5:45

Prepare the order creation route with the data layer, locate the ticket, validate it isn't reserved, create and save a 15-minute expiring order, and publish an order created event.  
准备包含数据层的订单创建路径，找到相关票据，确认其未被预订，创建一个有效期为 15 分钟的订单，并保存该订单。同时发布一个表示订单已创建的事件。

### Finding Reserved Tickets  寻找预留的门票6:10

Identify whether a fetched ticket is reserved by querying orders for that ticket with a status not canceled, using Mongoose findOne and status in [created, awaiting payment, complete], confirming reservation.  
通过查询那些状态为“未取消”的订单，以确定某个取用的票是否已被预订。使用 Mongoose 的 findOne 方法，并根据订单状态（如[已创建、等待付款、已完成]）来判断是否确实发生了预订行为。

### Convenience Document Methods  
便利文件处理方式7:37

Add an is reserved method on the ticket model to determine if a ticket is already reserved, returning a boolean, using a function keyword and async with this context.  
在票务模型上添加一个 is_reserved 方法来判定票是否已被预订，返回布尔值，使用 function 关键字并在这个上下文中使用 async。

### Order Expiration Times  订单有效期结束时间6:05

Calculate and implement order expiration by creating a date object, advancing it by a configurable window (e.g., 15 minutes), and persisting the expiration with a new order.  
通过创建一个日期对象，将其推进一个可配置的窗口时间（例如 15 分钟），然后以新的订单来保存该过期时间，从而计算并实现订单的过期功能。

### globalThis has no index signature TS Error  
全局：此语句没有索引签名，存在错误。0:25

### Test Suite Setup  测试套件配置2:36

Set up the order service test suite by copying test setup from the tickets service, aligning mocks and sign-in flows, and preparing MongoDB clearance and test directories.  
通过复制来自票据服务的测试配置，调整模拟数据 및 登录流程，以及准备 MongoDB 的权限设置和测试目录，来搭建订单服务的测试套件。

### Small Update for "Value of type 'typeof ObjectId' is not callable"  
关于“类型‘typeof ObjectId’无法调用”这个问题的小更新0:21

### Asserting Tickets Exist  确认票证的存在6:08

Guide writing tests for the new order route, validating authentication and a valid ticket id, and handling non-existent, reserved, and successful reservations with supertest and mongoose IDs.  
指导为新订单路径编写测试，验证认证和有效的工单 ID，处理不存在、预订和成功预订的 supertest 和 mongoose ID。

### Asserting Reserved Tickets  
确认保留的门票5:05

Show how to test microservices with node js and react by ensuring a reserved ticket linked to an order returns a 400 error.  
展示如何用节点 js 测试微服务，并通过确保与订单关联的预留工单返回 400 错误来应对。

### Testing the Success Case  测试成功案例4:09

We validate the reservation flow by creating a free ticket in the database and reserving it, expecting a 201 created order while ensuring authentication with a global sign-in cookie.  
我们通过在数据库中创建一个免费票券并对其进行预订来验证预订流程。同时，我们使用全局登录 cookie 来确保用户身份的真实性。预计会创建一个 201 状态码的订单记录。

### Fetching a User's Orders  获取用户的订单信息5:23

Develop the fetch orders route by authenticating the user, querying orders by user id, and populating each order with its ticket data including title and price, preparing for event publishing.  
开发获取订单信息的路由功能，包括验证用户身份、根据用户 ID 查询订单信息，以及为每个订单填充包含标题和价格等信息的票券数据，为事件发布做准备。

### A Slightly Complicated Test  
一个稍微有点复杂的测试12:23

Write and run an end-to-end test for the orders index route using supertest, creating three tickets and three orders, then verify only user two’s orders appear with embedded tickets.  
使用 supertest 编写并运行订单索引路由的端到端测试，创建三个票证和三个订单，然后验证只有用户二的订单会显示并嵌入票证。

### Fetching Individual Orders  
处理单个订单4:36

Implement a show handler to fetch a specific order by id, ensuring authentication and that users can only access their own orders, and return the order with its associated ticket.  
实现一个显示处理器来通过 id 获取特定订单，确保认证并保证用户只能访问自己的订单，并返回订单及其关联的票证。

### Does Fetching Work?  抓取功能有效吗？7:17

Test the show order route by creating a ticket, building an order, and fetching it to confirm a 200 response and matching id, while verifying not authorized scenarios.  
通过创建一张工单、生成一个订单，然后获取该订单的信息来验证其是否正确。同时，也要检查是否存在未经授权的情形。如果一切正常，则说明测试成功，订单路由设置是正确的。

### Cancelling an Order  取消订单4:32

Learn to cancel an order by updating its status to cancelled rather than deleting it, including authentication, ownership checks, and saving the updated order in a Node.js and React microservice.  
学会如何取消订单，通过将订单状态更新为“已取消”而不是删除它。同时，还需要进行身份验证、确认所有权，并将更新后的订单保存在 Node.js 和 React 微服务中。

### Can We Cancel?  我们可以取消吗？9:12

Develop and run tests for the delete order handler in a microservices setup, creating a ticket, an order, canceling it, and validating the order status.  
在微服务架构中，开发并运行针对“删除订单处理程序”的测试。包括创建订单、取消订单以及验证订单状态等操作。同时，需要生成相关的问题报告。


##   理解事件流程  6 次讲座 • 每次 30 分钟

### Orders Service Events  命令服务事件4:43

Learn how the order service emits order created and order cancelled events to coordinate payment, expiration, and ticket services, detailing the data payload and event interfaces in the common module.  
了解订单处理服务是如何发出“订单创建”和“订单取消”事件的，这些事件用于协调支付、有效期和票务服务的相关操作。文中详细介绍了数据负载以及通用模块中的事件接口。

### Creating the Events  创建事件9:23

Create and publish order created and order canceled events, defining their data with minimal yet usable fields for ticket, payment, and expiration services, and export these events for publication.

### Implementing the Publishers  
实施出版商功能3:13

Create two publishers in the order service—the order created publisher and the order cancelled publisher—extending a publisher base class, using the common subjects, and publishing via the NATS client.

### Publishing the Order Creation  
发布订单创建指令5:03

Publish an order created event in a Node.js microservice using a dedicated publisher and shared NATS client, transmitting id, status, user id, expires at ISO, ticket id, and price.  
在 Node.js 微服务中，使用专用的发布者和共享的 NATS 客户端发布订单创建事件，传递 id、状态、用户 id、ISO 格式的过期时间、票证 id 和价格。

### Publishing Order Cancellation  
发布订单的取消2:33

Implement a delete route that publishes an order cancelled event with the order and its ticket id, using an order cancelled publisher and Nats wrapper, after loading the ticket.  
实现一个删除路由，在加载票证后，使用订单取消发布者和 Nats 包装器发布订单取消事件，并附带订单及其票证 ID。

### Testing Event Publishing  测试事件发布机制5:32

Learn how to test event publishing by verifying that an order created event is published after order creation, using a mocked Nats wrapper and test doubles.  
学习如何通过验证订单创建事件在订单创建后发布来测试事件发布，使用模拟的 Nats 包装器和测试替身。


##   
监听事件并处理并发问题  
48 场讲座 • 4 小时 13 分钟

### Heads Up Regarding Some Mongoose TS Errors  
关于一些 Mongoose 时间戳错误的问题已经得到处理，请放心使用。0:20

### Time for Listeners!  是时候让听众们发言了！2:12

Publishers in the order and ticket services enable listeners for ticket created and ticket updated, then for order created and order canceled, tackling concurrency with a versioning strategy.  
订单和票务服务中的发布者使票创建和票更新的事件监听器生效，然后使订单创建和订单取消的事件监听器生效，通过版本控制策略解决并发问题。

### Reminder on Listeners  关于听众的提醒1:53

Learn how to create a listener by extending the base listener, define the event type and subject, and implement on message logic for ticket created events in the order service.  
学习如何通过扩展基础监听器来创建监听器，定义事件类型和主题，并在订单服务中为票创建事件实现消息逻辑。

### Blueprint for Listeners  倾听者的蓝图3:39

Build a ticket created listener for the orders service by extending the listener base class, wiring the ticket created event, subject, group name, and on message logic.  
通过扩展监听器基类，为 orders 服务创建一个基于创建票证的监听器。需要连接票证创建事件、主题、组名以及处理消息的逻辑部分。

### A Few More Reminders  还有一些提醒需要告知大家5:26

Coordinate ticket created events through the net streaming server using a unique, consistent q group to ensure only one order service processes each event, with typed interfaces and eq acknowledgements.  
该坐标票据通过网络流服务器来协调事件的处理，使用唯一且一致的 Q 组标识，以确保每个事件只由一个订单服务节点来处理。同时，该系统还具备类型化的接口和 EQ 确认机制。

### Simple onMessage Implementation  
简单的 onMessage 实现方式2:17

Pull title and price from the ticket created event, save the ticket to the local tickets collection in the order service, enabling data replication and reducing cross-service calls.  
从已创建的票务事件中提取标题和价格信息，将票务信息保存到订单服务的本地票务集合中。这样可以实现数据复制，减少跨服务的调用次数。

### ID Adjustment  身份调整5:44

Adjusts the ticket build to accept the id and map it to MongoDB's underscore id, ensuring consistent IDs between the ticket and order services during cross-service replication.  
调整票据结构，以接受标识符并将其映射到 MongoDB 的下划线格式标识符，从而在跨服务复制过程中确保票据与订单服务之间标识符的一致性。

### Ticket Updated Listener Implementation  
票证更新监听器实现4:32

Implement a ticket updated listener to handle concurrency issues, locate the ticket by id, update its title and price, save changes, and acknowledge the message in a robust event-driven flow.  
实现一个基于票证的监听器，用于处理并发问题。根据票证 ID 找到该票证，更新其标题和价格，保存这些更改，并在一个高效的事件驱动流程中确认这一消息。

### Initializing the Listeners  
正在初始化监听器2:44

Initialize two listeners with a Nats streaming server client and call listen to start receiving events, using ticket created and updated listeners via the nets wrapper.  
使用 Nats 流服务器客户端初始化两个监听器，然后调用 listen 函数来开始接收事件。这些事件是通过 nets 包装器创建的、可更新的监听器所捕获的。

### A Quick Manual Test  快速手动测试3:16

Manually test ticket creation and updates with Postman, verify authentication, and observe cross-service events (ticket created and ticket updated) to validate publisher and listener flow and concurrency behavior.  
使用 PostMan 工具手动测试票件的创建和更新过程，验证身份验证是否准确，并观察各种服务之间的交互情况（例如票件的创建和更新），以确认发布者和监听器之间的流程以及并发处理机制是否正常工作。

### Clear Concurrency Issues  已解决并发问题12:59

Explore concurrency issues in a microservices setup by simulating parallel ticket creation and price updates, revealing occasional out-of-order events and data inconsistencies between ticket and order databases.  
通过模拟并行创建票务和价格更新的过程，来探索微服务架构中的并发问题。这样可以观察到偶尔会出现的订单处理顺序混乱情况，以及票务数据库和订单数据库之间数据的不一致性问题。

### Reminder on Versioning Records  
关于版本记录的提醒6:41

Understand how to fix concurrency issues with a versioned event flow for tickets, including version numbers and event ordering across services; MongoDB and Mongoose can handle the versioning automatically.  
了解如何针对带有版本号的事件流来解决并发问题，包括版本号的管理以及不同服务之间的事件排序问题；MongoDB 和 Mongoose 可以自动处理版本管理功能。

### Optimistic Concurrency Control  
乐观并发控制5:37

Illustrate how Mongoose and MongoDB apply optimistic concurrency control with a version field during updates by id. Mongoose automatically increments the version on save and rejects mismatched updates.  
请说明在基于 ID 的更新过程中，Mongoose 和 MongoDB 是如何通过版本字段来实现乐观并发控制的。Mongoose 在保存数据时会自动递增版本号，并拒绝那些与版本号不匹配的更新请求。

### Mongoose Update-If-Current  
Mongoose 更新 ### 如果当前有效3:46

Implement concurrency control in the ticket service using the mongoose update-if-current module, switching version tracking from __v to a version field, and reflect version in events and tests.  
在票券服务中实现并发控制，使用 mongoose 的 update-if-current 模块。同时，将版本跟踪从 __v 方式改为使用版本字段进行跟踪，并在事件和测试中体现版本信息。

### Implementing OCC with Mongoose  
使用 Mongoose 实现 OCC 功能4:02

Wire the update-if-current plugin to the ticket model, rename the version key to version, and add tests to verify optimistic concurrency control in the ticket service.  
将更新插件连接到任务模型中，将版本键更名为 version，并添加测试来验证任务服务中的乐观并发控制功能是否正常工作。

### Test functions cannot both take a 'done' callback and return something Error  
测试函数既不能接受“已完成”的回调参数，又必须返回某种错误信息。0:20

### Testing OCC  测试 OCC9:07

Test optimistic concurrency control by creating and saving a ticket, fetching it twice, updating both, and verifying the first saves while the second fails due to an outdated version.  
请通过创建并保存一个任务、再次获取该任务、同时更新这两个任务，然后验证在第二次尝试时由于版本过时而导致操作失败的情况，来测试乐观并发控制机制是否有效。

### One More Test  再测试一次3:45

Explore optimistic concurrency control by testing version increments on every save, validating in-memory changes, and propagating version numbers through the common module to the orders service.  
通过每次保存时检查版本增量，验证内存中的修改内容，并将版本号通过共享模块传递到订单服务中，来探索乐观并发控制机制。

### Who Updates Versions?  谁负责更新版本？6:29

Understand how event versioning prevents out-of-order processing by ensuring only the primary service increments a record's version, while others may include the version.  
了解事件版本控制如何防止处理顺序的混乱，因为它只让主要服务能够更新记录的版本，而其他服务则可能包含额外的版本信息。

### Including Versions in Events  
在事件中包含版本信息2:59

Include a numeric version in the data of all events—order cancelled, order created, ticket created, and ticket updated—and publish the updated common module via npm, with TypeScript enforcing version property.  
在所有事件的数据中，包含数字版本的信息——比如订单取消、订单创建、票据创建以及票据更新等。然后通过 npm 发布更新后的通用模块，同时使用 TypeScript 来确保版本属性的正确性。

### Updating Tickets Event Definitions  
更新事件定义中的票据信息3:55

Ensure every published ticket event includes a version, and update the orders service by adding a version to the ticket model and wiring the Mongoose update if present.  
确保每个发布的票务事件都包含一个版本信息，并通过在票务模型中添加版本信息来更新订单服务。如果系统中存在 Mongoose 更新功能，还需要将其链接到相关代码中。

### Property 'version' is missing TS Errors After Running Skaffold  
在运行 Skaffold 后，某些属性中的“版本”信息缺失，存在技术错误。0:49

### Applying a Version Query  应用版本查询功能7:14

Wire up the update-if-current plugin in the order service to update tickets in the ticket model by id and version minus one for concurrency.  
将更新当前状态的插件连接到订单服务中，通过 ID 和版本减一的方式来更新任务状态，以实现并发处理。

### Did it Work?  成功了吗？4:31

Run a high-concurrency test to verify that version tracking fixes out-of-order events, updating 400 tickets from 5 to 15 and keeping tickets and orders in sync across MongoDB.  
运行一个高并发测试，以验证版本跟踪功能是否能够修复无序事件问题。测试过程中，有 400 个事务被处理，其中事务的数量从 5 个增加到 15 个。同时，需要确保这些事务在 MongoDB 中保持同步。

### Abstracted Query Method  抽象查询方法5:42

Add a model method find by event to fetch by id and previous version, hiding the underscore id and data version minus one to improve concurrency across listeners.  
添加了一个基于事件的模型方法，可以根据 ID 和旧版本来获取数据。这种方法隐藏了下划线的 ID，并使用了数据版本的减一值，从而提升了监听器之间的并发性能。

### [Optional] Versioning Without Update-If-Current  
[可选] 无需更新即可进行版本管理——如果当前版本是最新的话。18:34

Explore versioning without the update-if-current plugin by manually syncing the version field from events and customizing the Mongoose update query to match on both id and version.  
可以通过手动同步事件中的版本字段，并自定义 Mongoose 的更新查询，以同时匹配 ID 和版本，从而了解版本管理功能，而无需使用“更新时保持当前版本”插件。

### Testing Listeners  测试监听器4:55

Develop tests for the ticket created listener by fabricating a data object and a fake message, invoking on message, and verifying ticket creation, database save, and a reusable setup.  
通过创建一个数据对象和一条假消息，来编写针对该票证创建监听器的测试。在测试中，需要调用这些组件，并验证票证创建、数据库保存以及可复用的设置是否正确。

### A Complete Listener Test  全面的倾听能力测试9:25

Demonstrates a complete listener test for a ticket created event using mocks and a NATS wrapper, including fake data with MongoDB IDs and asserting the ticket saves.  
该代码实现了对使用 mock 方法和 NATS 包装器创建的票证创建事件的完整监听器测试，其中包括了带有 MongoDB IDs 的虚拟数据，同时验证了票证是否真的被保存了。

### Testing the Ack Call  测试确认调用功能2:09

Fix failing tests for the ticket created listener and verify the on message flow by setting up data and message, invoking on message, and asserting the act function is called.  
修复了关于创建的监听器的测试失败问题，并通过设置数据和消息、调用“处理消息”的操作，以及确保“执行动作”函数被调用，来验证消息处理流程的正确性。

### Testing the Ticket Updated Listener  
测试已更新的票证监听器8:20

Test the ticket updated listener by creating a ticket in the tickets collection, sending a fake update data object and message, and verifying the ticket is updated and saved.  
要测试已更新的票据监听器，可以在票据集合中创建一个票据，发送一个假的更新数据对象和消息，然后验证该票据是否已更新并保存下来。

### Success Case Testing  成功案例测试4:49

Orchestrates success-case testing for ticket creation and updates by invoking setup, data, and listener. Validates updated title, price, and version after re-fetching the ticket and confirms ack.  
通过调用设置、数据以及监听器函数来组织票证创建和更新的成功案例测试。在重新获取票证后，会验证更新后的标题、价格和版本信息，并确认确认操作已成功执行。

### Out-Of-Order Events  乱序事件4:51

Explore handling out-of-order events in a ticket update listener by testing skipped and future versions to prevent premature act function calls and errors.  
通过测试跳过版本和未来版本，来了解如何在票证更新监听器中处理延迟事件，从而避免过早调用动作函数导致错误。

### The Next Few Videos  接下来的几个视频3:39

Wire the mongoose update-if-current plugin into the orders model to enable version tracking, and ensure tickets in the order service include an id and emit events with a version.  
将 Wire the mongoose 插件连接到 orders 模型中，以实现版本跟踪功能。同时，确保订单服务中的任务具有唯一的 ID，并且能够触发相关事件。

### Fixing a Few Tests  修复一些测试问题6:36

Fix tests in the order service by ensuring ticket builds include a generated id, add version properties to published events, and lay groundwork for enforcing ticket reservations and edit restrictions.  
通过确保测试报告中包含生成的 ID、为发布的事件添加版本属性，以及建立执行票证预订和编辑限制的基础，来修复订单服务中的测试问题。

### Listeners in the Tickets Service  
票务服务中的听众们1:47

Implement two listeners in the ticket service that respond to order created and order canceled events, locking tickets on creation and unlocking on cancellation to prevent edits during purchase.  
在票券服务中实现两个监听器，分别处理订单创建和订单取消的事件。在订单创建时锁定票券，在订单取消时解锁票券，以防止在购买过程中对票券进行编辑。

### Building the Listener  打造优秀的倾听者4:59

Build a listener in the ticket service to react to the order created event, lock the ticket, and leverage shared base classes and TypeScript patterns.  
在票证服务中创建一个监听器，用于响应创建订单的事件，锁定该票证，并利用共享的基础类以及 TypeScript 的编程模式。

### Strategies for Locking a Ticket  
锁定票券的策略5:28

Lock tickets by storing the related order ID on the ticket and treating presence of an order ID as reserved; unlock on order canceled, reusing the order service for status.  
通过将在相关订单 ID 存储到票证中，并将订单 ID 的存在视为已预订状态，来实现票证的锁定功能；当订单被取消时，可以解锁票证，从而重新使用该订单的服务以获取订单状态信息。

### Reserving a Ticket  预订机票3:42

Add an optional order id to the ticket model, update the listener to reserve by setting this id, save the ticket, and ack the message, with tests.  
在票证模型中添加一个可选的订单 ID，更新监听器以通过设置此 ID 来预订资源，保存票证，并确认消息的接收，同时进行相关测试。

### Setup for Testing Reservation  
测试预约的准备工作7:00

Set up and run tests for the order created listener by creating a test file, saving a ticket, and crafting a fake order created event with a mocked message.  
通过创建测试文件、保存工单，以及生成一个带有模拟消息的订单创建事件，来设置并运行针对该订单创建监听器的测试。

### Test Implementation  测试实施4:29

Write and verify tests for the ticket creation listener, ensure the order ID matches the data object and that the listener acts on the message.  
为票证创建监听器的测试编写并验证代码，确保订单 ID 与数据对象一致，并且监听器能够正确地处理消息。

### Missing Update Event  缺失的更新事件6:14

Learn how order created and order canceled events interact with the ticket service listener, and why emitting an update event after every change keeps version data in sync.  
了解如何处理订单创建和订单取消事件与票务服务监听器的交互方式，以及为什么在每次变更后发出更新事件能够确保版本数据的同步。

### Private vs Protected Properties  
私有属性与受保护的属性6:54

Publish ticket updates across services by updating the ticket updated event to include order ID, and convert the Nats client property from private to protected for easier testing and integration.  
通过更新任务更新事件来在各个服务之间共享任务信息，同时将订单 ID 包含在其中。此外，还将 Nats 客户端属性从私有模式转换为受保护的模式，以便于测试和集成工作。

### Publishing While Listening  
发布时监听6:42

Modify the common module to use protected clients in the base listener and publisher, add an optional order id to ticket updated events, and publish events from the listener.  
修改通用模块，在基础监听器和发布器中使用受保护的客户端；在事件更新操作中添加可选的订单 ID；并从监听器中发布事件。

### Mock Function Arguments  模拟函数参数9:49

Learn how to test a Node.js microservice listener by mocking the publish client, validating that a ticket updated event is published with correct data, using TypeScript mocks and JSON payloads.  
了解如何通过模拟发布客户端来测试 Node.js 微服务的监听器功能，验证更新了票证的事件是否包含了正确的数据，同时使用 TypeScript 的模拟技术和 JSON 负载来进行测试。

### Order Cancelled Listener  已取消的订阅者名单6:51

Implement an order cancelled listener in the ticket service that clears the ticket's orderId when an order is cancelled, then publishes a ticket updated event to reflect the release.  
在票证服务中实现一种订单取消监听器，当订单被取消时，该监听器会清除票证的订单 ID，然后发布一个票证更新的事件，以反映订单已释放的情况。

### A Lightning-Quick Test  快速测试7:28

Test the order cancelled listener by setting up a ticket, creating order cancelled event, and verifying the listener processes the message and publishes an update via the Nats wrapper client.  
通过创建事务、生成订单取消事件，并验证监听器是否能够处理该消息，然后通过 Nats 封装客户端发布更新信息，来测试订单取消监听器的功能。

### Don't Forget to Listen!  不要忘记倾听！2:21

Learn how to wire and start order created and order cancelled listeners in a microservices setup using a NATS client, and enforce ticket lock checks on updates with tests.  
了解如何在微服务架构中使用 NATS 客户端来管理和处理已创建、已取消的订单。同时，学习如何通过测试来强制对更新结果进行票证锁定检查。

### Rejecting Edits of Reserved Tickets  
拒绝处理已保留票价的编辑请求6:15

Block edits to tickets with an order ID and throw a bad request error indicating the ticket is reserved, enforcing non-editability of reserved tickets.  
阻止对具有特定订单 ID 的工单进行编辑，同时抛出一条错误提示，表明该工单已被保留，从而确保保留的工单无法被编辑。


##   工人服务  
19 次讲座 • 1 小时 36 分钟

### The Expiration Service  过期服务2:38

Explore the expiration service that listens for order created events, triggers a 15-minute timer, and publishes an expiration complete event while examining several timer implementation options for the order service.  
探索那种能够监听订单创建事件的到期服务。该服务会触发一个 15 分钟的计时器，并在计时结束后发布一个“到期完成”事件。同时，该服务还提供了多种计时器实现方案，以便为订单服务选择合适的方案。

### Expiration Options  到期期权7:55

Explore expiration options for an order service, from in-memory timeouts to Bull JS with Redis, scheduling expiration complete events about 15 minutes after the expires at timestamp.  
探索订单服务的过期选项，包括使用内存超时机制，以及利用 Bull JS 和 Redis 来实现过期处理。可以在过期时间大约 15 分钟前触发相关事件。

### Initial Setup  初始设置6:14

Set up the expiration service by creating its directory, copying ticket service files, trimming dependencies, and installing the job processing manager with types bull. Prepare for Docker and Kubernetes.  
通过创建相关目录、复制票据服务文件、调整依赖关系，以及安装负责处理任务的作业处理管理器，来配置到期处理服务。同时，为 Docker 和 Kubernetes 做好准备。

### Skaffold errors ### Expiration Image Can't be Pulled  
Skaffold 错误——无法获取过期图片0:30

### A Touch of Kubernetes Setup  
一些 Kubernetes 设置的调整7:44

Build and push a Docker image to Docker Hub, then create Kubernetes deployments for the expiration service and a Redis instance, wiring Redis host via an env var.  
构建并推送一个 Docker 镜像到 Docker Hub，然后创建 Kubernetes 部署单元，用于运行到期服务以及 Redis 实例的部署。同时，通过环境变量将 Redis 服务器的地址连接到部署单元中。

### File Sync Setup  文件同步设置2:32

Update the scaffold yaml to enable file sinking for the expiration service and restart scaffold to deploy updates; restart orders, tickets, and expiration pods to restore NAT connectivity.  
更新脚手架的 yaml 文件，以启用对到期服务的文件下沉功能。然后重新启动脚手架以部署这些更新；同时，重新启动订单、工单以及到期相关的组件，以恢复 NAT 连接性。

### Listener Creation  听众创造3:42

Create an order created listener to monitor the order created event, set up bull js, and prepare a publisher for the exploration complete event in the expiration service.  
创建一个订单创建监听器来监控订单创建事件，配置 Bull JS，并为到期服务中的探索完成事件准备一个发布者。

### What's Bull All About?  他们到底在说什么呢？3:56

Explore how the bull library uses queues and jobs to coordinate web and worker servers for processing tasks, and why it's suited for delayed messaging rather than large-scale event streaming.  
了解 Bull 库是如何利用队列和任务来协调 Web 服务器和工作站服务器以处理任务的。同时，也能明白为什么它更适合用于延迟消息传递，而不是大规模的事件流处理。

### Creating a Queue  创建队列9:07

Wire bull library into the exploration service by creating order expiration queue, defining a Payload interface, publishing a job to Reddit server, and processing to emit an expiration complete event.  
通过将 Wire Bull 库融入探索服务中，我们可以创建订单到期队列、定义 Payload 接口、将任务发布到 Reddit 服务器，然后处理以触发到期完成事件。

### Queueing a Job on Event Arrival  
在事件到来时安排任务执行5:10

Queue a job on an order created event using the expiration queue to add a payload with the order ID, and wire the order created listener to start processing.  
在订单创建事件上使用过期队列排定一个任务，以添加包含订单 ID 的有效载荷，并将订单创建监听器连接到开始处理。

### Testing Job Processing  测试作业处理流程3:17

Manually create a ticket and an associated order in Postman, observe the order created and expiration events, and implement a 15 minute delay before expiration processing.  
在 Postman 中手动创建一个工单以及相关的订单。观察订单的创建和到期事件，并在到期前实施 15 分钟的延迟处理。

### Delaying Job Processing  延迟作业处理5:23

Learn to delay job processing in a redis-backed queue using a configurable delay in milliseconds, leveraging expires at and date.getTime to target precise wait times.  
学会在基于 Redis 的队列中延迟任务处理过程，可以通过配置毫秒级的延迟时间来精确控制等待时间。利用 expiresAt 和 date.getTime 函数可以实现精确的目标等待时间。

### Defining the Expiration Complete Event  
定义“到期完成事件”3:50

Define the expiration complete event type in the common module and publish it. Include the order ID as data, prepare a publisher in the expiration service.  
在公共模块中定义“到期完成”事件类型，并对其进行发布。将订单 ID 作为数据包含在其中，同时在到期服务中准备一个发布者组件。

### Publishing an Event on Job Processing  
在作业处理上发布一个事件6:27

Publish an expiration complete event from the exploration service after processing a job, using a publisher and event subject, with the order service marking the related order expired.  
在处理完任务后，通过发布者 và 事件主题，在探索服务中发布一个到期完成事件。同时，让订单服务标记相关订单已到期。

### Handling an Expiration Event  
处理到期事件6:00

Listen for the expiration complete event in the order service, locate the corresponding order by id, update its status to cancelled, and decide whether to clear the ticket reference.  
请注意在订单服务中是否有“过期完成”的事件触发。找到对应的订单 ID，将其状态更新为“已取消”，然后决定是否需要清除该订单的引用信息。

### Emitting the Order Cancelled Event  
发布该取消事件的命令5:37

Publish an order cancelled event in a Node.js and React microservice, update the order status, use isReserved, and notify ticket and payment services via a publisher.  
在 Node.js 和 React 的微服务中发布取消的订单事件，更新订单状态，使用`isReserved`属性，并通过发布者通知票证和支付相关服务。

### Testing the Expiration Complete Listener  
测试 ExpirationComplete 监听器5:51

Test and implement the expiration complete listener in a Node.js microservice, wiring a mocked NATS client, creating and saving ticket and order data, and simulating expiration events.  
在 Node.js 微服务中测试并实现过期事件监听器的功能。需要连接一个模拟的 NATS 客户端，创建并保存票据和订单数据，然后模拟过期事件的发生。

### A Touch More Testing  更多的测试工作7:20

Create three separate tests to verify updating order status to cancelled, emitting an order canceled event via the Nats wrapper, and awaiting listener on message data.  
创建三个独立的测试，以验证将订单状态更新为“已取消”的操作。同时，确保通过 Nats 封装层触发“订单已取消”事件，并等待对消息数据的处理。

### Listening for Expiration  监听到期信号2:20

Implement and wire the expiration complete listener, test with Postman, and observe the order expiring and then being cancelled as the expiration event propagates.  
实现并配置到期监听器，使用 PostMan 进行测试。观察订单何时到期，以及随着到期事件的触发，订单如何被取消。


##   处理付款  
31 次讲座 • 2 小时 40 分钟

### The Payments Service  支付服务2:11

Define the payments service by outlining its events: listen for order created and order cancelled, emit charge created, and address a logic hole before integrating real card processing.

### globalThis has no index signature TS Error  
全局：此语句没有索引签名，存在错误。0:21

### Initial Setup  初始设置8:45

Set up the payments microservice by duplicating ticket service files, updating Docker and package configs, building and pushing its Docker image, and provisioning a MongoDB-backed Kubernetes deployment with Nats.  
通过复制票据服务文件、更新 Docker 和包配置、构建并推送 Docker 镜像，以及配置一个基于 MongoDB 的 Kubernetes 部署环境（包含 Nats 组件），来设置支付微服务。

### Replicated Fields  复制的场域5:57

Learn how the payment service listens for order created and canceled events, replicates essential fields (id, status, version, userId, price) into a replicated orders collection, and validates payments.  
了解该支付服务如何监听订单创建和取消的事件，如何将必要的字段（id、状态、版本、用户 ID、价格等）复制到复制的订单集合中，以及如何验证支付的有效性。

### Another Order Model!  又一个订单模型！7:30

create a new order model in the payment service using mongoose, defining interfaces, a schema with user id, price, and status, and a build method, ready for order events.  
在支付服务中使用 Mongoose，创建新的订单模型，定义接口、包含用户 ID、价格和状态的模式，以及一个构建方法，准备处理订单事件。

### Update-If-Current  更新当前信息1:17

Apply the Mongoose update-if-current plugin to the order schema to ensure events are processed in the correct order, override the version key, and wire the plugin into the payments workflow.  
将 Mongoose 更新-if-current 插件应用到订单模式中，确保事件按正确顺序处理，覆盖版本键，并将插件接入支付工作流程。

### Replicating Orders  复制订单4:07

Build an order created listener in the payments service to replicate orders by constructing and saving an order from the order created event, including id, version, userId, price, and status.  
在支付服务中构建订单创建监听器，通过构建并保存订单创建事件中的订单（包括 ID、版本、用户 ID、价格和状态）来复制订单。

### Testing Order Creation  测试订单创建6:12

Write a quick test for the order created listener, including setup, mocks, and asserting the order price and message handling.  
为订单创建的监听器写一个快速测试，包括设置、模拟、断言订单价格和消息处理。

### Marking an Order as Cancelled  
将订单标记为已取消5:39

Implement an order cancelled listener that updates the order’s status to cancelled and prevents payments for cancelled orders, mirroring the existing order created flow.  
实现一个订单取消监听器，将订单状态更新为取消，并阻止取消订单的付款，镜像现有订单创建流程。

### Cancelled Testing  已取消的测试6:42

Build and test an order cancel test by creating a cancel listener, setting up an order, triggering a cancel event, and verifying the order updates to cancelled.  
通过创建一个取消监听器、设置订单、触发取消事件并验证订单更新为取消，来构建和测试订单取消测试。

### Starting the Listeners  开始为听众们表演吧3:54

Import and instantiate the order listeners, then start listening for order created and order canceled events. Restart NATS streaming server to backfill past events and test by creating an order.  
导入并实例化订单监听器，然后开始监听订单创建和订单取消事件。重新启动 NATS 流服务器以处理过去的事件，然后通过创建订单来进行测试。

### Payments Flow with Stripe  
使用 Stripe 进行支付转账5:10

Explore the payments flow with Stripe, handling credit card billing through the Stripe.js modal, tokenization, and charging via the Stripe API in the payment service, with automated tests.  
使用 Stripe 来处理支付流程，通过 Stripe.js 模态框来处理信用卡账单的计费工作，采用令牌化技术，并通过 Stripe 的支付服务接口进行收费。同时，支持自动化测试。

### Implementing the Create Charge Handler  
实现创建收费处理器的功能8:32

Implement a post request handler in the payment service to process Stripe charges via a token and order ID using the Stripe API, validating the order and amount.  
在支付服务中实现一个 POST 请求处理器，通过 Stripe API 根据令牌和订单 ID 来处理 Stripe 的支付请求，同时验证订单信息和金额是否正确。

### Validating Order Payment  验证订单付款情况4:03

We implement payment order validation by extracting token and order id from the request body, locating the order, confirming ownership and non-cancellation, and setting up automated tests.  
我们通过从请求体中提取令牌和订单 ID 来验证支付订单的有效性，找到相应的订单，确认所有权是否属实且订单未被取消，同时设置自动测试来确保系统的可靠性。

### Testing Order Validation Before Payment  
在付款之前先验证测试订单的有效性6:59

Develop and run tests to validate order handling before payment using a route handler, supertest, and an Express app, covering 404, 401, and 400 scenarios.  
开发并运行测试程序，以在付款之前验证订单处理流程。测试应涵盖针对 404、401 和 400 错误情况的场景。同时，需要使用路由处理程序、超级测试工具以及 Express 应用程序来实施这些测试。

### Testing Same-User Validation  
测试同一用户的验证功能5:10

Test same-user validation by assigning a user id to the order, then verify 400 for purchasing a canceled order and 404 when the order isn't saved, with Stripe API integration.  
通过为订单分配一个用户 ID 来验证用户确认功能是否正常工作。当购买已取消的订单时，应返回 400 错误；当订单未被保存时，则应返回 404 错误。所有操作都通过 Stripe API 集成来实现。

### Stripe Setup  条纹设置3:58

Install the Stripe SDK and obtain your API keys, then use the token from the request to charge the user's credit card via the Stripe API in your payment service.  
安装 Stripe SDK，获取您的 API 密钥，然后使用该令牌通过 Stripe API 将款项扣除到用户的信用卡上。

### Creating a Stripe Secret  创建一条条带秘密密钥3:20

Store your Stripe secret key in a Kubernetes secret and expose it as an environment variable (Stripe_Key) for the payments service, avoiding plaintext and enabling the Stripe SDK.  
将您的 Stripe 秘密密钥存储在一个 Kubernetes 秘密中，并将其作为环境变量 Stripe_Key 进行暴露，以便用于支付服务。这种方式可以避免使用明文存储，同时还能启用 Stripe SDK 的功能。

### Creating a Charge with Stripe  
使用条带创建费用6:08

Instantiate the stripe client in a dedicated module, export it for reuse, and charge a user’s card by calling stripe.charges.create with amount in cents, USD, and a token source.  
在专门的模块中实例化 Stripe 客户端接口，将其导出以便重复使用。通过调用 stripe.charges.create 接口来向用户收费，该接口需要传入金额单位（ cents、USD），以及令牌来源。

### Manual Testing of Payments  
支付的手动测试6:35

Learn to test payments in a Node JS and React microservice by creating tickets, orders, and payments in Stripe test mode using a guaranteed test token, validating success and errors.  
通过创建订单、支付请求等方式，学习如何在 Node.js 和 React 微服务中测试支付功能。在 Stripe 的测试模式下使用有保障的测试令牌来处理这些任务，并验证处理结果的准确性。

### Automated Payment Testing  
自动化支付测试6:27

Implement automated testing for Stripe payments by using a mock Stripe module that mimics charges.create returning a resolved promise, and validate a 204 response from the /api/payments endpoint.  
通过使用一个模拟的 Stripe 模块来实现 Stripe 支付的自动化测试，该模块能够模拟 charge.create 操作，并返回一个已解析的 Promise。同时，还需要验证/api/payments 端点返回 204 状态码的响应。

### Mocked Stripe Client  被嘲笑的条纹客户端4:23

Test the mocked Stripe client to verify the route returns 201 when a payment is created and Stripe charges.create is called with currency USD, amount price*100, and token.  
请测试这个模拟的 Stripe 客户端，确保当创建支付并且调用 Stripe 的 charge.create 方法时，返回的响应状态码为 201，且参数包括美元货币、价格乘以 100 的数值，以及支付令牌。

### A More Realistic Test Setup  
更现实的测试环境设置9:16

Explore a realistic Stripe test setup by removing mocks and using the real Stripe API, including setting Stripe API key in test setup, and validating charges by listing recent charges.  
通过移除模拟数据，使用真实的 Stripe API 来构建逼真的测试环境。这包括在测试设置中配置 Stripe API 密钥，以及通过列出最近的收费记录来验证收费信息的准确性。

### Realistic Test Implementation  
现实的测试实施6:15

Develop a realistic test for creating a Stripe charge by randomizing a dollar amount, converting to cents, and validating a matching charge among the ten most recent charges.  
请设计一个真实的测试场景，通过随机指定金额、将其转换为分，然后验证最近十次交易之间是否存在一致的收费情况。

### Tying an Order and Charge Together  
将命令和指控结合起来7:18

Relate orders to Stripe charges by adding a payments collection in the payment service, modeling a payment with order id and stripe id using a Mongoose schema.  
通过将支付服务中的支付收集功能与 Stripe 的支付流程关联起来，可以使用 Mongoose 数据库模式来创建带有订单 ID 和 Stripe ID 的支付记录。

### Testing Payment Creation  测试支付创建功能6:13

Create a Stripe charge, extract its id, and save a payment record linked to the order ID and Stripe ID; review the route handler test.  
创建一笔分期付款记录，提取其 ID，然后保存与订单 ID 和 Stripe ID 相关的支付记录；请查看路由处理器的测试代码。

### Publishing a Payment Created Event  
发布一个由支付创建的事件6:31

Publish a payment created event after processing a payment, exposing the payment ID, order ID, and Stripe ID so downstream services like orders or tickets can release the paid ticket.  
在处理完付款后，发布一个创建的事件，同时暴露付款 ID、订单 ID 以及 Stripe ID。这样，后续的订单处理或票务管理服务等就可以利用这些信息来确认票款的支付状态。

### More on Publishing  更多关于出版方面的信息3:18

Publish a payment created event after saving a new payment using the payment created publisher and a Nats wrapper client, returning the payment id.  
通过使用“支付创建发布者”和 Nats 封装客户端来保存一个新的支付后，发布一个创建事件，并返回该支付的 ID。

### Marking an Order as Complete  
将订单标记为已完成6:17

A payment created event triggers the order service to mark an order as complete, locate the order, update its status, save it, and acknowledge the message.  
一个支付创建事件会触发订单服务，使其将订单标记为已完成状态，找到该订单，更新其状态，保存该订单信息，并确认收到该消息。

### Important Info About the Next Lecture ### Don't Skip  
关于下一次讲座的重要信息——请勿错过！0:51

### Don't Cancel Completed Orders!  
请不要取消已完成的订单！1:01

Implement a safety check in the exploration complete listener to prevent cancelling paid or completed orders. Return early when the order status is complete to avoid unintended cancellations.  
在探索完成监听器中实现一项安全检查机制，以防止已付费或已完成的订单被取消。当订单状态为“完成”时立即返回，从而避免不必要的订单取消情况发生。


##   回到客户那边吧  
21 次讲座 • 1 小时 43 分钟

### A Few More Pages  再翻几页吧6:15

Build the front-end flow for a tickets app, listing tickets, viewing details, creating orders, and integrating stripe payments with dynamic routes for tickets and orders.  
构建票务应用程序的前端流程：列出票务信息、查看详情、创建订单，并整合 Stripe 支付功能，为票务和订单设置动态路由。

### Reminder on Data Fetching with Next  
关于使用 Next 进行数据获取操作的提醒6:09

Learn how Next.js fetches data for pages via app component and pages, and how to avoid duplicate current user requests by fetching once in app component and passing it down.  
了解 Next.js 如何通过应用程序组件和页面来获取数据。同时，也了解如何避免重复调用当前用户的请求，即只在应用程序组件中一次获取数据，然后再将其传递给其他组件使用。

### Two Quick Fixes  两个简单的解决方案5:43

Fix double fetching of the current user by passing the current user as a prop to all pages and supplying the built client to get initial props during rendering.  
通过将所有页面的参数中包含当前用户的信息，以及提供在渲染过程中用于获取初始属性的内置数据，来解决这个问题，从而避免对当前用户的重复获取。

### Scaffolding a Form  为表单添加 scaffolding 功能3:43

Scaffold the tickets/new route by building a new ticket form with title and price inputs inside a container, preparing later to add state and submit to the backend.  
通过创建一个新的票务表单来为新航线分配票券，该表单包含标题和价格输入字段，这些字段位于一个容器内部。之后，可以准备添加状态信息并将其提交到后端服务器。

### Sanitizing Price Input  消毒成本输入6:29

Manage title and price state with the React useState hook, format the price on blur to the nearest cent, and prepare to submit a new ticket through the tickets service.  
使用 React 的 useState 钩子来管理商品和价格的显示状态。将价格格式化为保留小数点后两位的形式，并准备通过票务服务提交新的票务申请。

### Ticket Creation  票务创建6:44

Learn to create tickets by posting title and price to the tickets API, using the use request hook, handling errors, and responding to success with a callback.  
通过向票务 API 发布标题和价格来创建票务信息。使用使用请求钩子来处理错误，并通过回调函数来响应成功创建票务的结果。

### Listing All Tickets  列出所有票券7:41

Learn to redirect users after ticket creation to the tickets list, fetch all tickets with getInitialProps, and render them in a table showing title and price.  
学习在创建工单后，将用户引导回工单列表页面。通过调用`getInitialProps`函数获取所有工单的信息，然后将这些工单以表格形式展示出来，包括工单标题和价格等信息。

### Reminder on Invalid <Link> with <a> child Errors  
关于使用<Link>标签时可能出现无效情况的提醒，以及与之相关的<a>标签错误情况0:11

### Linking to Wildcard Routes  
链接到通用路由7:07

Implement wildcard routes in a Next.js app by creating a dynamic tickets/[ticketID] page, wiring navigation with next/link and the as prop, and using router.push for programmatic routing.  
在 Next.js 应用中实现通配符路由，可以通过创建类似“dynamic.tickets/[ticketID）”这样的页面来实现。同时，需要将导航功能与“next/link”链接关联起来，并使用“router.push”进行程序化路由配置。

### Creating an Order  创建订单7:25

Load ticket data via get initial props by using the context-supplied ticket id, show its title and price, and post to the orders service with the id, handling errors.  
通过使用的上下文提供的票证 ID 来获取初始权益信息，显示该票证的标题和价格，然后将该信息发送到订单服务中，同时处理可能出现的错误。

### Programmatic Navigation to Wildcard Routes  
针对通配符路由的程序化导航4:16

Programmatically navigate to a dynamic orders page using wildcard routes by pushing to /orders/[orderId], fetch order details, and display a payment countdown timer.  
通过访问"/orders/[orderId]"来实现程序化的导航，直接跳转到动态订单页面；获取订单详细信息；并显示付款倒计时计时器。

### The Expiration Timer  倒计时计时器10:54

Implement a 60-second countdown timer in the order view that updates every second and hides the pay button when it ends, showing expired message using the order expires at timestamp.  
在订单查看页面实现一个 60 秒倒计时器，每秒更新一次，并在倒计时结束时隐藏支付按钮，同时显示过期消息，使用订单过期时间戳。

### Displaying the Expiration  
显示到期日期1:01

Display an expiration message when time left goes negative by initializing time left to zero and using an after effect to render a div that says order expired.  
当剩余时间变为负数时，会显示一条过期提示信息。具体做法是将剩余时间初始化为 0，并使用动画效果来显示一个提示框，告知用户订单已过期。

### Showing a Stripe Payment Form  
正在显示条纹支付表单3:06

Install and integrate the Stripe checkout library to render a pay modal, collect card details, return a token, and post to the payment service using the publishable key.  
安装并集成 Stripe 支付库，以显示支付窗口、收集卡片信息、返回令牌，以及使用发布密钥将数据发送到支付服务。

### Module not found: Can't resolve 'prop-types'  
模块未找到：无法解析“prop-types”模块。0:14

### Configuring Stripe  配置 Stripe4:23

Configure stripe checkout in the order show by importing Stripe checkout from React Stripe Checkout, providing a token callback and the publishable key, and pass the current user's email.  
在订单展示中配置 Stripe 结账功能，需从 React Stripe Checkout 模块导入 Stripe 结账逻辑。同时，需要提供令牌回调参数和发布密钥，并传入当前用户的电子邮件地址。

### Test Credit Card Numbers  测试信用卡号码1:58

Explore how to test the payment flow using Stripe in test mode, using provided test card numbers to generate a token, then send that token id to your payments API.  
了解如何在测试模式下使用 Stripe 来测试支付流程。可以使用提供的测试卡号生成令牌，然后将该令牌 ID 发送到您的支付 API 接口。

### Paying for an Order  支付订单费用9:24

Submit payment to Stripe to receive a token. Use the token and order ID in a post to the API payments endpoint, merging extra props in the request.  
向 Stripe 提交付款申请以获取令牌。使用该令牌和订单 ID 在 API 的付款端点进行请求，并在请求中附加额外的参数。

### Filtering Reserved Tickets  
筛选预留票2:27

Filter the ticket list to return only tickets with no order id, showing only available tickets; add header links to create new tickets and list user orders.  
筛选票列表，只返回那些没有订单 ID 的票证。仅显示可用的票证。添加头部链接以创建新票证，并列出用户拥有的订单。

### Header Links  头部链接1:47

Add conditional header links for logged-in users to sell tickets and view my orders, routing to tickets/new and orders pages, with a header.js implementation and testing.  
为登录用户添加有条件的头部链接，使其能够购买门票和查看订单信息。这些链接将引导用户前往“创建新票券”和“查看订单”页面。同时，需要借助 header.js 插件来实现这一功能，并对其进行测试。

### Rendering a List of Orders  
排列顺序列表6:20

Render a user's orders by fetching from the order service (filtered by user), map them to a list showing ticket titles and statuses, and redirect after payment.  
通过从订单服务中获取数据（该数据经过用户筛选），将订单信息映射到一个列表中，列出各个订单的标题和状态。在付款完成后，将用户引导至相应的页面。


##   
30 场讲座 • 2 小时 17 分钟

### Development Workflow  开发工作流程3:39

Deploy a production style pipeline that handles multi-team changes, from local commits on feature branches through automated tests and pull requests, to automatic deployment on a Kubernetes cluster.  
部署一个符合生产环境的流水线系统，能够处理来自多个团队的变更工作。这些变更包括从特性分支上的本地提交，到自动化测试、拉取请求，最后到在 Kubernetes 集群上的自动部署。

### Git Repository Approaches  
Git 仓库处理方法6:05

Compare mono repo and separate-repo approaches for microservices, then use a single git repository to reduce overhead. Push to GitHub and enable GitHub actions for tests and deployment.  
对于微服务来说，可以同时使用单一仓库的方法与分离各个仓库的方法。不过，为了减少管理成本，建议采用单一 Git 仓库来存放所有代码。将代码推送到 GitHub 上，并启用 GitHub 的自动化操作功能，以便对测试和部署过程进行自动化管理。

### Creating a GitHub Action  创建 GitHub 动作7:11

Create a GitHub action that runs tests on pull requests to master, using a YAML workflow, checkout, install dependencies, and execute service tests with failures surfaced on the pull request.

### Adding a CI Test Script  
添加了一个集成测试脚本2:42

Add a ci test script that runs the test suite once and exits, replacing watcher mode, then commit and push changes to master to test the GitHub workflow end-to-end.  
添加一个 CI 测试脚本，该脚本会运行整个测试套件一次，然后退出。这样就能替代原有的监视模式。之后，将更改提交到主分支，以测试 GitHub 的工作流是否能够正常运行。

### Tests in GitHub Actions Hang ### Jest did not exit  
在 GitHub Actions 的 Hang ### Jest 测试中出现了问题，测试没有成功结束。0:17

### Running Tests on PR Creation  
在 PR 创建上进行测试4:48

Modify a service by adding a startup log, commit to a dev branch, push, and open a pull request; a GitHub action runs tests and shows a green check.  
通过添加启动日志、将代码提交到开发分支、推送代码，然后打开拉取请求来修改服务。此时，GitHub 动作会运行测试，并显示出绿色勾号，表示测试通过。

### Output of Failing Tests  失败测试的结果5:48

Demonstrates how a breaking test change on a dev branch triggers CI tests in the same PR, guiding fixes until all tests pass and you can merge to master.  
展示了如何在开发分支上进行破坏性测试时，触发持续集成测试。在所有的测试都通过之后，就可以将代码合并到主分支中。

### Running Tests in Parallel  
并行运行测试7:28

Create a set of per-service GitHub workflows to run tests for all microservices—orders, payments, tickets, and more—in parallel, instead of in series, to speed up pull requests.  
创建一组针对各个服务的 GitHub 工作流程，以便并行地运行所有微服务的测试——比如订单处理、支付处理、票务处理等任务。这样能够加快提交请求的速度。

### Verifying a Test Run  验证测试运行结果3:01

Push changes on the dev branch, trigger parallel tests across services, review progress in the pull request, and merge to master to prepare deployment.  
在开发分支上提交更改，触发各服务的并行测试，审查拉取请求中的进展，最后合并到 master 分支以准备部署。

### Selective Test Execution  选择性测试执行5:44

Set up service-specific test runs by configuring GitHub workflows to trigger tests only for changed directories on pull requests, reducing cross-service test runs while preserving coverage for modified services.  
通过配置 GitHub 工作流程，仅对拉取请求中发生变化的目录触发测试，从而有针对性地执行服务特定的测试。这样可以减少跨服务的测试次数，同时仍能确保修改过的服务得到充分的测试覆盖。

### Deployment Options  部署选项7:40

Evaluate hosting providers to deploy our application using Kubernetes, comparing Digital Ocean, AWS, Google Cloud, and Azure, while noting costs, credit card requirements, and coupon options.  
我们需要评估几家托管服务提供商，以便选择最适合我们应用部署的平台。我们将比较 Digital Ocean、AWS、Google Cloud 和 Azure 这几家服务商的优劣，同时考虑它们的成本、信用卡要求以及优惠活动等信息。

### Creating a Hosted Cluster  
创建托管集群2:39

Create a hosted Kubernetes cluster on DigitalOcean, choose a default Kubernetes version and nearby data center, provision three $10/month nodes plus a $10/month load balancer, then deploy via GitHub.  
在 DigitalOcean 上创建托管的 Kubernetes 集群，选择默认的 Kubernetes 版本和附近的数据中心，配置三个每月 $10 的节点和一个每月 $10 的负载均衡器，然后通过 GitHub 进行部署。

### Reminder on Kubernetes Context  
关于 Kubernetes 上下文的提醒3:32

Connect to the new cluster via a dedicated context, install and authenticate the Digital Ocean tool, and prepare for debugging and logs.  
通过专用上下文连接到新集群，安装并认证 Digital Ocean 工具，然后准备调试和日志。

### Reminder on Swapping Contexts  
关于交换上下文的提醒3:56

Switch between the DigitalOcean cluster and your local Docker Desktop context using kubeconfig and use-context. List contexts and verify nodes to enable debugging and a GitHub workflow deployment.  
在 DigitalOcean 集群和本地 Docker Desktop 上下文之间使用 kubeconfig 和 use-context 进行切换。列出上下文并验证节点以启用调试和 GitHub 工作流部署。

### The Deployment Plan  部署计划4:04

Set up seven GitHub action workflows that trigger on master merges to deploy each service to a Digital Ocean cluster, building and pushing Docker images to Docker Hub.  
设置七个 GitHub 动作工作流程，这些工作流程在 master 分支发生合并时触发，将每个服务部署到 Digital Ocean 集群中，并构建和推送 Docker 镜像到 Docker Hub。

### Building an Image in an Action  
在动作中构建形象8:40

Build and deploy a Docker image with a GitHub Actions workflow that builds the image and pushes it to Docker Hub, using secrets for login and master path filters.  
构建并部署一个 Docker 镜像，该镜像包含 GitHub Actions 工作流程。工作流程会负责构建镜像并将其推送到 Docker Hub。在构建过程中，会使用 secrets 来进行登录操作，同时还会使用 master 路径过滤器来处理镜像的推送任务。

### Testing the Image Build  测试图像构建结果2:30

Commit the new workflow, build and push the image to Docker Hub on merge, verify the push via actions logs, then update Kubernetes to use the new image.  
在合并时，执行新的工作流程，构建并将镜像推送到 Docker Hub。通过操作日志验证推送是否成功，然后更新 Kubernetes 以使用新的镜像。

### Restarting the Deployment  
重新启动部署过程7:17

Push the new image to Docker Hub, fetch the cluster context with ductile, and run rollout restart deployment auth-devel in the Digital Ocean Kubernetes cluster.  
将新的镜像推送到 Docker Hub，使用 ductile 获取集群上下文信息，然后在 Digital Ocean Kubernetes 集群中执行 rollout restart 部署 auth-devel 任务。

### Applying Kubernetes Manifests  
应用 Kubernetes 清单文件3:27

Apply Kubernetes manifests by deploying infra config files with kubectl, adjust deployment context, and manage ingress routing for development and production domains.  
通过 kubectl 部署基础设施配置文件来应用 KubernetesManifest 文件；调整部署配置；管理开发版和生产版域的入口路由。

### Prod vs Dev Manifest Files  
产品与开发相关的配置文件4:23

Split manifests into infra/dev and infra/prod folders and move the ingress service to dev. Update scaffold and deploy configs to apply dev/prod files and verify secrets before testing.  
将清单文件拆分为基础设施开发版和生产版两个文件夹，并将入口服务部署到开发版环境中。更新脚手架代码并配置配置文件，以应用开发版和生产版的文件，同时验证 secrets 在测试前的正确性。

### Manual Secret Creation  手动创建秘密3:39

Create two Kubernetes secrets, JWT secret and Stripe secret, in a DigitalOcean cluster using kubectl; obtain the Stripe key from the dashboard and set the proper context.  
使用 kubectl 在 DigitalOcean 集群中创建两个 Kubernetes 秘密容器，分别命名为 JWT secret 和 Stripe secret。然后从仪表板中获取 Stripe 密钥，并配置正确的上下文信息。

### Don't Forget Ingress-Nginx!  
别忘了 Ingress-Nginx！5:31

Prepare ingress-nginx in a digital ocean cluster, verify kubectl context, commit and push changes, then deploy manifests via GitHub actions and inspect pods for issues.  
在 Digital Ocean 集群中准备 ingress-nginx 配置，验证 kubectl 上下文设置是否正确，提交并推送更改。然后通过 GitHub Actions 部署相关清单，同时检查容器是否存在问题。

### Testing Automated Deployment  
测试自动化部署流程2:51

Learn to test automated deployment by making code changes, pushing to dev, creating a pull request, and merging to master, triggering a GitHub build and Kubernetes deployment.  
学习如何通过修改代码、将更改推送到开发分支、创建拉取请求、将更改合并到主分支、触发 GitHub 构建以及执行 Kubernetes 部署来测试自动化部署过程。

### Additional Deploy Files  额外的部署文件7:12

Build and push a Docker image to Docker Hub, restart deployments, and verify pod logs for latest service version; create deploy yaml files for client, orders, tickets, payments, and expiration.  
构建并推送一个 Docker 镜像到 Docker Hub，重新启动部署流程，同时验证最新版本的服务日志；为客户端、订单、票据、支付以及有效期等相关功能创建相应的部署配置文件。

### A Successful Deploy!  成功部署！6:07

Make small changes across five services (client, expiration, orders, payments, tickets), commit a PR, and trigger automated deploys in a Kubernetes cluster, then monitor workflows and debug pods as needed.  
在五个服务（客户端、有效期、订单、支付、票务）中进行微小的修改，提交一个 Pull Request，然后触发 Kubernetes 集群中的自动化部署操作。根据需要监控工作流程并调试相关容器。

### Buying a Domain Name  购买域名3:10

Purchase a domain name and point it to the load balancer for your Kubernetes app on Digital Ocean, using a provider such as Namecheap.  
在 Digital Ocean 上购买一个域名，然后使用该域名指向你的 Kubernetes 应用程序的负载均衡器。你可以使用 Namecheap 这样的提供商来完成这个操作。

### Three Important Changes Needed to Deploy ### Do Not Skip!  
部署过程中需要做出三项重要改变——切勿忽视这些改变！1:25

### Configuring the Domain Name  
配置域名5:27

Configure a domain with custom dns, add an A record and a www alias to a load balancer, update the ingress host, and deploy via a GitHub workflow.  
配置一个包含自定义 DNS 信息的域名，为负载均衡器添加 A 记录及 www 别名配置，更新入口主机信息，然后通过 GitHub 工作流程进行部署。

### I Really Hope This Works  
真心希望这个方法能奏效。1:58

Showcases deploying a ticketing app, creating user accounts, listing and purchasing tickets with a test Stripe flow, and updating the app via branch, pull request, and cluster deployment.  
展示了一个票务应用程序的部署，创建用户帐户，使用测试 Stripe 流列出和购买票证，以及通过分支、拉取请求和集群部署更新应用程序。

### Next Steps  接下来的步骤4:36

Wrap up by shutting down the Digital Ocean Kubernetes cluster and load balancer, then explore next steps: https support with cert-manager, an email service, production builds, and a staging cluster.  
通过关闭 Digital Ocean Kubernetes 集群和负载均衡器来结束，然后探索下一步：使用 cert-manager 的 HTTPS 支持、电子邮件服务、生产构建和预发布集群：https


##   
[附录 A] – Docker 基础知识  
46 次讲座 • 3 小时 3 分钟

### Finished Code and Diagrams  
完成的代码和图表0:38

### Why Use Docker?  为什么使用 Docker？3:43

Learn how Docker simplifies installing and running software across any computer or server, demonstrated with a quick Redis setup using a single docker run command.  
了解 Docker 如何简化在任何计算机或服务器上安装和运行软件的过程。通过一个简单的 Redis 配置示例，说明如何使用一个 docker run 命令来实现这一功能。

### What is Docker?  什么是 Docker？2:53

Explore the docker ecosystem, including the docker cli, docker hub, images, and containers, and understand how a container encapsulates a program with isolated resources.  
探索 Docker 生态系统，包括 Docker CLI、Docker Hub、镜像和容器等组件。了解容器如何封装程序，并提供隔离的资源环境。

### Docker for Mac / Windows1:57

Explore how Docker works by installing Docker for Mac or Windows, and use the Docker client (CLI) to interact with the Docker daemon, creating and managing images and containers.  
通过安装适用于 Mac 或 Windows 的 Docker 软件，了解 Docker 的工作原理。然后使用 Docker 客户端（命令行界面）与 Docker 守护进程进行交互，创建和管理镜像及容器。

### Installing Docker on macOS  
在 macOS 上安装 Docker1:10

### Installing Docker with WSL2 on Windows 10/11  
在 Windows 10/11 上通过 WSL2 安装 Docker1:50

### Installing Docker on Linux  
在 Linux 系统中安装 Docker1:55

### Using the Docker Client  使用 Docker 客户端5:03

Run the docker run hello world command to launch a container using the hello world image, illustrating the Docker CLI, Docker server, image cache, and Docker Hub flow.  
运行`docker run hello world`命令，使用`hello world`镜像来启动一个容器。这展示了 Docker 命令行界面、Docker 服务器、镜像缓存以及 Docker Hub 仓库的功能。

### But Really... What's a Container?  
不过，实际上……什么是“容器”呢？8:30

Discover how the kernel uses name spacing and control groups to isolate a process and allocate resources for a container, and how an image becomes that container via startup command.  
了解内核如何利用命名间隔和控制组来隔离进程，并为容器分配资源。同时，了解如何通过启动命令将镜像转换为实际的可运行容器。

### How's Docker Running on Your Computer?  
你的电脑上 Docker 运行得怎么样？2:44

Explore how docker runs on non-linux hosts by using a linux virtual machine, where containers are processes with images, startup commands, and resource isolation via name spacing and control groups.

### Docker Run in Detail  详细讲解 Docker 运行过程1:54

Explore the Docker cli in the terminal and master the docker run command to create and run containers from images, using the Hello World example to illustrate the process.

### Overriding Default Commands  
优先执行默认命令5:12

Override the default startup command in Docker run by supplying an alternate command, using BusyBox to demonstrate echo and ls inside the container and explain the image file system snapshot.  
通过在 Docker 运行时提供替代的命令来覆盖默认的启动命令。使用 BusyBox 在容器中演示“echo”和“ls”命令的效果，并解释镜像文件系统的快照功能。

### Listing Running Containers  
列出正在运行的容器4:09

List running containers with docker ps to view container id, image, status, and ports, and use docker ps -a to see all containers ever created, with their randomly generated names.  
可以使用`docker ps`命令列出运行中的容器，以查看容器的 ID、镜像、状态以及端口。如果想要查看所有曾经创建过的容器及其随机生成的名称，可以使用`docker ps -a`命令。

### Container Lifecycle  容器生命周期5:16

Explore the container lifecycle from creation to startup, comparing docker create, docker start, and docker run, and learn how the dash flag attaches output for real-time insight.  
探索容器从创建到启动的整个生命周期，了解 docker create、docker start 和 docker run 之间的区别。同时，了解如何利用 dash 标志来附加输出信息，从而获得更实时的洞察结果。

### Restarting Stopped Containers  
重启已停止的容器3:43

Explore the lifecycle of a Docker container, restart stopped containers with Docker start, and learn that the default command defined at creation cannot be replaced on restart for effective troubleshooting.  
了解 Docker 容器的生命周期，使用 Docker restart 命令重新启动停止的容器。同时要注意，在重启时，无法更改容器创建时设置的默认命令，因为这对于进行有效的故障排除非常重要。

### Removing Stopped Containers  
删除已停止的容器1:39

Learn how to remove stopped containers and reclaim disk space using Docker system prune, understanding build cache impact and redownload images from Docker Hub.  
了解如何使用 Docker 系统优化功能来删除已停止的容器，并回收磁盘空间。同时，也要掌握对构建缓存的影响，以及如何从 Docker Hub 重新下载镜像。

### Retrieving Output Logs  获取输出日志2:33

Use docker create, docker start, and docker logs to retrieve all output from a container, avoiding reruns and inspecting emitted messages.  
可以使用 docker create、docker start 和 docker logs 命令来获取容器中的所有输出信息，从而避免再次运行容器以及需要手动检查产生的消息。

### Stopping Containers  停止容器运行5:21

Learn to stop a docker container using stop and kill, understand sig term versus sig kill, and why stop waits up to ten seconds when the ping command resists shutdown.  
学习如何使用“stop”和“kill”命令来停止一个容器运行；了解“sig term”与“sig kill”之间的区别；同时理解为什么在使用“ping”命令时，停止操作需要等待长达十秒钟，因为“ping”命令会阻碍容器的关闭过程。

### Multi-Command Containers  多指令容器4:16

Learn how to run Redis locally and in Docker, interact with it via the Redis CLI, and execute commands inside a running container to access the server.  
了解如何在本地或 Docker 环境中运行 Redis，如何通过 Redis CLI 与它进行交互，以及如何在运行中的容器内执行命令以访问服务器。

### Executing Commands in Running Containers  
在运行的容器中执行命令2:53

Execute commands inside running containers using docker exec with the -it flag, interact with the Redis CLI, and observe how input affects container behavior.  
使用`docker exec`命令在运行的容器中执行命令，并带有`-it`标志；同时可以通过 Redis CLI 与容器进行交互，观察输入内容对容器行为的影响。

### The Purpose of the 'it' Flag  
“it”标志的用途4:35

explains how docker exec -it attaches your terminal to a running container, routing input to standard in and output to standard out for a clean interactive session.  
它解释了如何使用 docker exec -it 将终端连接到正在运行的容器中。通过将输入导向标准输入，输出导向标准输出，从而打造出一个干净、交互式的会话环境。

### Getting a Command Prompt in a Container  
在容器中打开命令提示符窗口4:06

Open an interactive shell in a running docker container with docker exec -it <container> sh to run commands, inspect files, and debug.  
使用`docker exec -it <容器> sh`命令在运行的 Docker 容器中打开一个交互式 shell。通过这个 shell 可以执行命令、检查文件以及进行调试操作。

### Starting with a Shell  从外壳开始2:13

Learn how to start a shell inside a Docker container using docker exec -it and docker run -it with busybox, then poke around with shell commands.  
了解如何使用`docker exec -it`和`docker run -it`在 Docker 容器中启动一个 shell 进程，并配合 busbox 进行实验。然后尝试使用 shell 命令进行一些操作吧。

### Container Isolation  容器隔离3:09

Learn how containers maintain separate file systems, ensuring no data sharing unless explicitly connected, demonstrated by multiple busybox containers where a file created in one is not visible in another.  
了解容器如何保持各自的独立文件系统，从而确保除非有明确连接，否则不会共享任何数据。这一点可以通过多个 busybox 容器来举例说明：在一个容器中创建的文件在另一个容器中是看不到的。

### Creating Docker Images  创建 Docker 镜像2:36

Learn to build custom docker images by writing a dockerfile, specifying a base image, adding dependencies, and defining a startup command to boot containers.  
学习通过编写 Dockerfile 来构建自定义 Docker 镜像。包括指定基础镜像、添加依赖项，以及定义启动命令以启动容器。

### Buildkit for Docker Desktop  
Docker Desktop 的构建工具包1:09

### Building a Dockerfile  构建 Dockerfile4:51

Create a Dockerfile to build a Redis image from an Alpine base, install a dependency, and run Redis-server, then build and run the image to start the server.  
创建一个 Dockerfile，从 Alpine 基础镜像开始构建 Redis 镜像。安装必要的依赖项，并运行 Redis 服务器。然后构建并运行该镜像，以启动服务器。

### Dockerfile Teardown  Dockerfile 清理操作2:41

Examine how a dockerfile builds a container image by dissecting the Alpine base, and the run and cmd instructions, with terminal output revealing each configuration step.  
通过解析 Alpine 基础结构、运行指令以及命令部分，来了解 Dockerfile 是如何构建容器镜像的。同时，终端输出会显示每个配置步骤的详细信息。

### What's a Base Image?  什么是基础镜像？5:40

Understand the purpose of a base image in Docker by comparing it to installing an initial operating system, using Alpine to preload Redis with its package manager.  
通过将其与安装初始操作系统进行比较，来理解 Docker 中基础镜像的作用。同时，还可以了解如何使用 Alpine 操作系统及其包管理器来预加载 Redis。

### The Build Process in Detail  
构建过程的详细步骤11:09

Explain how docker build converts a dockerfile into an image using a build context, alpine base, and stepwise container snapshots.  
请解释一下，在给定构建上下文、Alpine 基础镜像以及一系列逐步生成的容器快照的情况下，如何使用 docker build 将 dockerfile 中的指令转化为实际的可运行镜像。

### A Brief Recap  简要回顾3:24

Recap the Docker build flow: start from Alpine, install Redis in a temporary container, snapshot the filesystem, and set Redis as the container command for the final image.  
回顾一下 Docker 的构建流程：从 Alpine 镜像开始，在临时容器中安装 Redis，创建文件系统快照，然后将 Redis 作为最终镜像的容器命令。

### Rebuilds with Cache  使用缓存进行重建7:02

Learn how Docker build cache speeds image creation by reusing cached layers from Alpine and by changing Dockerfile order, which forces a rebuild from the change line down.  
了解 Docker 的构建缓存如何通过重用 Alpine 中的缓存层以及改变 Dockerfile 的排序来加速镜像创建过程。改变 Dockerfile 的排序会强制从修改行开始重新构建镜像。

### Tagging an Image  给图片添加标签4:26

Tag and build docker images using a custom repository name and version, then run containers from the tagged image with docker run, while understanding latest versus explicit tags.  
使用自定义的仓库名称和版本来标记并构建 Docker 镜像。然后，可以使用`docker run`命令从这些标记过的镜像运行容器。同时，需要注意区分`latest`标签和明确的标签。

### Quick Note for Windows Users  
适用于 Windows 用户的快速提示：0:14

### Manual Image Generation with Docker Commit  
使用 Docker 进行手动图像生成5:00

Explore how you can manually create a container from an image, install Redis, then commit a new image from the running container to illustrate the image–container relationship.  
了解如何手动从一个镜像创建容器，安装 Redis，然后将运行中的容器生成的新的镜像提交到仓库，以此来说明镜像与容器之间的关系。

### Project Outline  项目概述2:35

Start a new project to build a tiny Node.js web app, wrap it in a Docker container with a Dockerfile, run locally, and access it from your browser.  
启动一个新项目，构建一个简单的 Node.js Web 应用。将该应用打包到一个 Docker 容器中，使用 Dockerfile 进行构建。然后在本地环境中运行该应用，最后通过浏览器访问它。

### Node Server Setup  节点服务器配置5:03

Set up a simple node server using express, with a root route that returns hi there, configured via package.json and index.js, listening on port 8080 and ready for docker.  
使用 Express 框架搭建一个简单的节点服务器，设置一个根路由来返回“嗨”，该配置通过 package.json 和 index.js 文件进行定义。服务器监听在 8080 端口，并且已经准备好用于 Docker 部署。

### Reminder on Build Kit  关于组装套件的提醒0:27

### A Few Planned Errors  一些经过设计的错误5:12

Assemble a Node.js web app, install dependencies with npm install, and start the server with npm start, then containerize with an Alpine-based Dockerfile to build, run, and explore planned errors.

### Base Image Issues  基础镜像问题7:50

Fix docker npm not found by switching from alpine to a node pre-installed image, and ensure package.json exists before npm install.

### A Few Missing Files  有些文件不见了3:18

Resolve missing files during docker builds by copying index.js and package.json into the node alpine container, ensuring npm install finds dependencies listed in package.json.

### Copying Build Files  复制构建文件4:50

Use the docker copy instruction to ensure package.json is in the container before npm install, then build and tag the image, run it on port 8080, and access localhost:8080.  
使用`docker copy`指令确保在执行`npm install`之前，`package.json`文件已经在容器中。之后，构建并标记该镜像，然后在 8080 端口上运行它。最后，可以在 localhost:8080 访问该镜像。

### Container Port Forwarding  
集装箱港口转运7:26

Learn how to forward localhost requests into a docker container using port mapping with docker run -p, enabling the node app listening on 8080 to respond from the browser.  
了解如何使用端口映射将本地主机的请求转发到 Docker 容器中。通过 Docker 的`docker run -p`命令，可以使得节点应用能够在 8080 端口上响应来自浏览器的请求。

### Specifying a Working Directory  
指定工作目录7:52

Set and apply a working directory in the Dockerfile to isolate project files, preventing root-level conflicts, then rebuild, run, and use docker exec for in-container debugging.  
在 Dockerfile 中设置并应用一个工作目录，以隔离项目文件，从而避免产生根级冲突。之后，重新构建、运行应用程序，并使用 docker exec 进行容器内的调试操作。

### Unnecessary Rebuilds  不必要的重建工作4:16

Showcases how docker snapshots and forces container rebuilds after source changes, triggering npm install, and previews a smarter file-copy approach to avoid reinstalling dependencies.  
展示了如何在源代码发生更改后，通过 Docker 快照来强制重新构建容器，从而避免触发 npm 安装过程。同时，提出了一种更高效的文件复制方法，以无需重新安装依赖项的方式解决问题。

### Minimizing Cache Busting and Rebuilds  
尽量减少缓存的破坏和重建操作4:58

Split the docker build into two copy steps, copying only package.json first and running npm install, then copying the remaining files to preserve cache and minimize rebuilds when code changes.  
将 docker 构建过程分解为两个步骤：首先复制 package.json 文件，然后执行 npm install 命令。之后，再复制剩余的文件，这样可以保留缓存，从而在代码发生变更时最小化重新构建的次数。


##   
[附录 B] – 类型 Script 的基础知识  
75 场讲座 • 5 小时 41 分钟

### How to Get Help  如何获得帮助1:04

Learn how to get help in this course by using the Udemy Q&A, direct messages, or Twitter, with responses typically around nine to eleven MST, Monday through Friday.  
请参考本课程，了解如何获得帮助。你可以使用 Udemy 的问答功能、直接消息，或者通过 Twitter 来寻求帮助。回复通常在周一至周五的上午 9 点至 11 点之间。

### TypeScript Overview  TypeScript 概述6:19

TypeScript adds a type system to JavaScript, using type annotations to catch errors during development. The TypeScript compiler outputs JavaScript for browsers or Node.js, with no runtime performance optimizations.  
TypeScript 为 JavaScript 增加了类型系统，通过类型注解来在开发过程中捕获错误。TypeScript 编译器会生成适用于浏览器或 Node.js 的 JavaScript 代码，不过不会进行任何运行性能优化。

### Environment Setup  环境设置7:59

Install TypeScript and TS Node via npm, verify the compiler, and configure Visual Studio Code with Prettier, format on save, single quotes, two-space indentation, and a solar light theme.  
通过 npm 安装 TypeScript 和 TS Node 模块，确保编译器已正确安装。接下来，在 Visual Studio Code 中配置 Prettier 插件，以便进行自动格式化。同时，设置单引号表示法、两行缩进格式，以及为代码段添加太阳能主题风格。

### Important Update About ts-node and Axios  
关于 ts-node 和 Axios 的重要更新信息0:32

### A First App  首个应用程序4:43

Create a first project that fetches data from a fake API using Axios and TypeScript, printing results to the terminal, and see TypeScript's workflow from setup to installation.

### Executing Typescript Code  
执行 TypeScript 代码5:03

Write a basic TypeScript program that fetches JSON data with Axios and logs the result. Compile to JavaScript and run with Node.js to see the data output.  
编写一个简单的 TypeScript 程序，使用 Axios 来获取 JSON 数据，并将结果输出到日志中。将程序编译成 JavaScript 代码后，在 Node.js 环境中运行，即可看到数据的输出结果。

### One Quick Change  一次快速切换3:35

Extract the to-do's id, title, and completed status, and print a readable multi-line report with a template string in Node, with TypeScript guidance to prevent errors.  
提取待办事项的 ID、标题以及完成状态，然后使用模板字符串在 Node 环境中打印出易于阅读的多行报告。同时使用 TypeScript 代码进行编码，以避免出现错误。

### Catching Errors with TypeScript  
在使用 TypeScript 时捕获错误7:22

TypeScript helps catch typos by defining a to-do interface with id, title, and completed, guiding property names during development.  
TypeScript 通过定义了一个包含 id、title 和 completed 等属性的 todo 接口，从而在开发过程中帮助人们避免拼写错误。这些属性名在编写代码时起到了指导作用。

### Catching More Errors!  犯错的次数变多了！5:15

Refactor a console.log into a log to do helper with an arrow function, then add TypeScript type annotations for id, title, and completed to catch errors early.  
将 console.log 语句重构为日志记录功能，使用箭头函数来实现辅助功能。同时，为 id、title 和 completed 属性添加 TypeScript 类型注解，以便尽早捕获错误。

### Do Not Skip ### Course Overview  
请勿跳过——课程概述3:35

Discover how to learn TypeScript efficiently by balancing syntax and features with design patterns, focusing on writing reusable code through projects.  
了解如何高效地学习 TypeScript 编程，通过平衡语法特性与设计模式，专注于通过项目实践来编写可复用的代码。

### Types  类型5:12

Explore basic types in TypeScript and learn what a type represents. See how values like strings, arrays, objects, and functions gain properties and methods, with practical code examples.  
了解 TypeScript 中的基本类型，了解类型所代表的意义。通过实际的代码示例，了解诸如字符串、数组、对象以及函数等类型如何拥有属性和方法。

### More on Types  更多关于类型的信息5:53

Explore how primitive and object types differ in TypeScript, including strings, numbers, booleans, and dates, and how the compiler analyzes code.  
探索 TypeScript 中原始类型和对象类型的区别，包括字符串、数字、布尔值和日期，以及编译器如何分析代码。

### Examples of Types  类型示例4:48

Explore how TypeScript uses types to define properties and methods, with practical code examples of date objects, plain objects, and a color class, illustrating autocomplete and error checking.  
了解 TypeScript 如何利用类型来定义属性和方法。通过实际的代码示例，包括日期对象、普通对象以及颜色类，来展示自动补全功能和错误检查机制的工作原理。

### Where Do We Use Types?  
我们会在哪里使用类型呢？0:49

Explore where types fit in, noting that in TypeScript every value has a type and the language checks code quality, so types are used everywhere.  
了解各种类型在其中的应用场景。在 TypeScript 中，每个值都有一个类型，而且语言还会检查代码的质量，因此类型在各个方面都得到了广泛应用。

### Type Annotations and Inference  
类型注释与推理2:03

Explore type annotations and type inference in TypeScript, learn how they apply to variables, functions, and objects, and decide when to use each for clearer code.  
探索 TypeScript 中的类型注解和类型推断，了解它们如何应用于变量、函数和对象，并决定何时使用每种方式以编写更清晰的代码。

### Annotations With Variables  
带变量的注释4:53

Explore type annotations in TypeScript by declaring variables with explicit types, like apples: number, and see how assignments enforce type safety and reveal errors. Preview type inference.  
通过声明具有明确类型的变量来探索 TypeScript 中的类型注解，例如使用`apple: number`这种声明方式。了解赋值如何确保类型安全并能够发现错误。同时，体验类型推断的功能。

### Object Literal Annotations  
对象字面量注释6:53

Explore type annotations in TypeScript, including arrays of strings, numbers, booleans, and object literals with x and y properties, and annotate class instances like Car to ensure correct typing.  
探索 TypeScript 中的类型注解，包括由字符串、数字、布尔值组成的数组，以及具有 x 和 y 属性的对象字面量。同时，对像 Car 这样的类实例进行注解，以确保正确的类型匹配。

### Annotations Around Functions  
关于函数的注释说明5:55

Learn how to use type annotations for functions, specifying argument types and a void return, alongside examples of arrays, classes, and object literals, and explore annotation versus inference.  
了解如何为函数使用类型注解，指定参数类型以及是否返回空值。同时，通过示例来掌握数组、类以及对象字面量的使用方式。此外，还会探讨类型注解与自动推断之间的区别。

### Understanding Inference  理解推理过程3:51

Explain TypeScript's type inference, when to rely on it versus adding annotations, and the rules for declaration and initialization on the same line.  
请解释一下 TypeScript 的类型推断机制。何时应该依赖类型推断，何时需要添加注解？另外，关于如何在同一行中进行声明和初始化，有哪些规则需要遵守？

### The Any Type  任意类型7:47

Explore how TypeScript uses type inference and when to add type annotations, focusing on the any type returned by JSON.parse and its impact on safety.  
了解 TypeScript 是如何利用类型推断功能的，以及何时需要添加类型注释。重点讨论由 JSON.parse 返回的 any 类型，以及这一特性对系统安全性的影响。

### Fixing the "Any" Type  修复“任意”类型的问题1:49

Fix the any type by adding explicit type annotations for coordinates (x and y as numbers) and note that json parse returns any, so annotate functions that return any.  
通过为坐标（x 和 y 以数字形式表示）添加明确的类型注释来指定任意类型。需要注意的是，JSON 解析会返回任意类型，因此对于返回任意类型的函数也进行注释说明吧。

### Delayed Initialization  延迟初始化3:05

Explore why type inference breaks when a variable is declared on one line and initialized later, and how to add a type annotation for booleans in TypeScript.  
了解为什么当变量在一行代码中声明但随后才进行初始化时，类型推断会失效。同时，学习如何在 TypeScript 中为布尔值添加类型注释。

### When Inference Doesn't Work  
当推理行不通时4:37

Examine when type inference fails and requires type annotations, using a numbers array and a union boolean | number to handle a variable that may hold either type.  
观察在类型推断失败且需要进行类型注释的情况下的处理方式。此时会使用一个数字数组以及一个布尔值或数字组合来表示可能包含任意类型的变量。

### More on Annotations Around Functions  
关于函数注释的更多信息4:56

Learn how to annotate functions in TypeScript and differentiate type inference for arguments and return values, with an add function that adds two numbers.  
了解如何使用 TypeScript 对函数进行注释，以及如何区分参数和返回值上的类型推断。以一个可以计算两个数字之和的函数为例，演示如何实现这一功能。

### Inference Around Functions  
关于函数的推理6:08

Explore how to annotate function arguments and returns in TypeScript, how the compiler infers return types from the function body, and why return annotations catch mistakes.  
了解如何在 TypeScript 中注释函数的参数和返回值。了解编译器如何从函数体中推断出返回类型，以及为什么返回注释能够捕获一些错误。

### Annotations for Anonymous Functions  
匿名函数的注释说明1:42

Explore annotations for functions across named, anonymous, and arrow forms, showing how to annotate two arguments A and B as numbers and the return type with a colon.  
探索适用于命名、匿名和箭头形式的函数的注释方式。可以看到，如何将两个参数 A 和 B 作为数字进行注释，并且用冒号来标注返回类型的注释方式。

### Void and Never  无效且从未存在2:49

Explore TypeScript void and never return types, implement a logger using console.log, and throw errors to illustrate a function that never returns. See practical examples.  
探索 TypeScript 中的 void 类型，了解“从不返回”类型的用法。使用 console.log 实现日志记录功能，并通过抛出异常来展示一个“从不返回”的函数示例。查看实际的案例。

### Destructuring with Annotations  
使用注释进行结构解析3:35

Annotate a function that logs forecast weather using TypeScript, apply object destructuring to access date and weather, and separate destructuring from the annotation for clarity.  
使用 TypeScript 注释一个记录天气预报的函数，应用对象解构来访问日期和天气，并将解构与注释分开以提高清晰度。

### Annotations Around Objects  
关于对象的注释信息7:05

Explore object modeling in TypeScript, including nested profiles with coordinates, object methods, and advanced destructuring with annotations and inference guidance for when to annotate versus infer types.  
探索 TypeScript 中的对象建模，包括具有坐标的嵌套配置文件、对象方法以及带有注释和推断指导的高级解构，指导何时注释类型与何时推断类型。

### Arrays in TypeScript  TypeScript 中的数组5:05

Explore arrays in TypeScript, including type inference, explicit type annotations, avoiding any type, handling empty arrays, and two-dimensional arrays with practical examples.  
在 TypeScript 中探索数组操作，包括类型推断、显式类型注解、如何处理空数组以及二维数组的处理方法，并通过实际例子来讲解这些概念。

### Why Typed Arrays?  为什么选择使用类型数组？4:30

Shows how TypeScript infers array element types for safe extraction via index or pop, prevents incompatible values in typed arrays, enables map/forEach/reduce autocomplete, and outlines mixed-type arrays with special syntax.  
它展示了 TypeScript 如何推断数组元素的类型，从而通过索引或弹出操作安全地提取元素；同时避免了在类型化的数组中出现不兼容的值。此外，该语言还支持自动完成映射、遍历和 reduce 操作，并使用特殊语法来标识混合类型的数组。

### Multiple Typees in Arrays  
数组中的多种类型2:57

Explore how to store multiple value types in a single array using TypeScript unions, including strings and date objects, with explicit annotations to avoid any and enable safe pushing.  
了解如何使用 TypeScript 中的联合特性，在单个数组中存储多种值类型，包括字符串和日期对象。通过适当的注释说明，可以避免出现错误，并确保数据的安全存储。

### When to Use Typed Arrays  
何时使用类型数组0:54

Learn when to use arrays in TypeScript to represent a collection of records with similar types. Explore how tuples differ from arrays and why you might choose between them.  
了解在 TypeScript 中何时使用数组来表示具有相似类型的记录集合。了解元组与数组之间的区别，以及为什么会在这两种数据类型之间进行选择。

### Tuples in TypeScript  TypeScript 中的元组4:04

Explore tuples in TypeScript by comparing objects and arrays, showing how fixed order and labels preserve a single record’s properties like color, carbonation, and sugar content.  
通过比较对象和数组来了解 TypeScript 中的元组。可以看到，固定顺序和标签能够保留单个记录的属性，比如颜色、碳酸化程度和糖分含量等。

### Tuples in Action  动作中的元组5:28

Compare tuples and arrays in TypeScript by modeling a drink with a fixed order: color, carbonation, and sugar. Use tuple annotations and a type alias to enforce this structure.  
在 TypeScript 中比较元组与数组，通过创建一个包含颜色、碳酸化程度和糖分等属性的饮品模型来实现对比。使用元组注释和类型别名来强制遵循这种数据结构。

### Why Tuples?  为什么选择元组？3:20

Explore why tuples matter in TypeScript, compare tuples with object records for readability, and learn when to use tuples, including a CSV example discussed later in the course.  
了解在 TypeScript 中为什么需要使用元组，比较元组的易读性与对象记录的优势，并学习在何种情况下应该使用元组。此外，还会通过一个 CSV 示例来说明这一点，该示例将在课程后期进行讲解。

### Interfaces  接口1:26

Explore TypeScript interfaces, learn how they define a new custom type with property names and types, and see how interfaces collaborate with classes to enable strong code reuse.

### Long Type Annotations  长类型注释4:42

Create a car object and a print vehicle function with explicit type annotations, then show how inline types clutter the code and lead to introducing an interface.

### Fixing Annotations With Interfaces  
使用接口来修复注释问题4:36

Create a vehicle interface to replace long type annotations, requiring objects to have a string name, a number year, and a boolean broken, and validate calls to print vehicle.  
创建一个车辆接口，以替代冗长的类型注释。该接口要求对象具有一个字符串形式的名称、一个表示生产年份的数字，以及一个布尔值来表示车辆是否损坏。同时，该接口还需要能够验证对“打印车辆”方法的调用。

### Syntax Around Interfaces  接口周围的语法规则3:31

Learn to define interfaces with non-primitive types, such as date, and to include functions like summary that returns a string to describe a vehicle.  
学习定义非原始类型的接口，如日期，并包含像总结这样的函数，返回字符串以描述车辆。

### Functions in Interfaces  在接口中执行的函数4:46

This lecture demonstrates refactoring a TypeScript interface from a vehicle to a generic reportable by requiring only a summary function, showing structural typing and renaming print functions.  
本讲座演示如何将 TypeScript 接口从载体重构为通用报告，只需一个摘要函数，展示结构类型和重命名打印函数。

### Code Reuse with Interfaces  
通过接口实现代码复用4:15

Learn how interfaces describe object shapes and enable code reuse by a generic print summary function, using types like old civic and drink as reportable examples in TypeScript.  
学习界面如何描述对象形状，并使代码通过通用打印摘要函数重用，使用像 old civic 和 drink 这样的类型作为 TypeScript 中的可报告示例。

### General Plan with Interfaces  
带有接口的一般计划3:12

Explore how interfaces act as gatekeepers for functions, requiring values to implement a reportable interface with a summary function to enable reusable TypeScript code.  
了解接口如何充当函数的“守门人”角色，它们要求相关值具备可报告接口的特性，并通过摘要功能来使代码能够重复利用，从而提升 TypeScript 编程的效率。

### Classes  课程/班级3:47

Explore how TypeScript classes act as blueprints for objects, defining methods and fields. Create instances with new, call methods such as drive or honk, and compare TS classes to JavaScript.  
了解 TypeScript 中的类是如何作为对象的蓝图来定义方法和字段的。可以创建实例，并使用如“drive”或“honk”这样的方法。同时，对比一下 TypeScript 类与 JavaScript 类的差异。

### Basic Inheritance  基本继承3:03

Explore basic inheritance in TypeScript by extending a vehicle class with a car, reusing and overriding methods like drive and honk to customize behavior.  
通过扩展一个车辆类，为它添加一辆汽车，然后重定义诸如“驾驶”和“鸣笛”之类的方法，来探索 TypeScript 中的基本继承机制。这样就能自定义行为的表现。

### Class Method Modifiers  类方法修饰符6:41

Explore class modifiers for methods and properties in TypeScript, including public, private, and protected, and see how they restrict access, with examples showing default public behavior and inheritance implications.  
了解 TypeScript 中方法和属性的类修饰符，包括公共的、私有的和受保护的修饰符。了解这些修饰符如何限制访问权限，并通过示例来展示默认的公共行为以及继承对访问权限的影响。

### Fields in Classes  类中的字段6:18

Define and initialize fields in classes by adding a color property, using constructors, and applying access modifiers like public, private, and protected to control visibility.  
通过在类中添加颜色属性、使用构造函数，以及应用公共、私有和受保护的访问修饰符来控制可见性，从而定义和初始化字段。

### Fields with Inheritance  具有继承关系的字段4:18

Explore how fields behave with inheritance in TypeScript classes by examining a car extending vehicle, including constructor behavior, super calls, and passing color through.  
了解在 TypeScript 类中，当类具有继承关系时，各个字段是如何进行操作的。以一辆汽车继承于普通车辆为例，探讨其中的构造函数行为、超类调用方式，以及颜色属性的传递方式。

### Where to Use Classes  这些课程应该用在什么地方呢1:09

Discover why classes matter in TypeScript and how interfaces enable collaboration between classes. See how a typical project organizes one class per file to maximize code reuse and modular design.  
了解为什么在 TypeScript 中类的重要性，以及接口如何促进类之间的协作。看看一个典型的项目是如何将每个类放在一个文件中，从而最大化代码复用性和模块化设计的。

### App Overview  应用概述2:26

Develop a browser-based TypeScript app using design patterns, classes and interfaces to generate a random user and company with locations, then display them as Google map markers via parcel bundler.  
开发一个基于浏览器的 TypeScript 应用程序。使用设计模式、类以及接口来生成随机的用户和公司信息，包括它们的位置。然后，通过 Parcel 工具将这些信息显示为 Google 地图上的标记。

### Updated Parcel Instructions  
更新的地块说明0:16

### Bundling with Parcel  与包裹一起打包4:43

Explore bundling with parcel by turning index.ts into JavaScript, wiring an index.html with a ts script, and serving on localhost 1234 while logging 'hi there' in the console.  
将 index.ts 文件转换为 JavaScript 代码，实现捆绑处理功能。然后，在 index.html 文件中引入该 JavaScript 脚本。最后，在本地主机 1234 上运行应用程序，同时在控制台输出“嗨，你好”。

### Project Structure  项目结构3:19

Define a clean project structure by creating separate files for user, company, and map classes, then import them into a root index to render the map.  
通过创建用于用户、公司和地图类的独立文件，来构建整洁的项目结构。然后将这些文件导入到根索引中，以便渲染地图。

### IMPORTANT Info About Faker Installation  
关于 Faker 安装的重要信息0:25

### Generating Random Data  生成随机数据5:29

Define a user class with name and location, initialize latitude and longitude in the constructor with Faker, and install faker via npm to generate data for mapping on Google Map.  
定义一个用户类，包含名称和位置信息；在构造函数中使用 Faker 库初始化纬度和经度数据；通过 npm 安装 faker 库，以生成用于在 Google 地图上映射的数据。

### Type Definition Files  类型定义文件5:17

Learn how to resolve missing type definition files for JavaScript libraries in TypeScript by installing type definition files from Definitely Typed, such as @types/faker, to enable proper type checking.  
了解如何解决 TypeScript 中 JavaScript 库缺失类型定义文件的问题。只需从 Definitely Typed 下载相关类型定义文件，例如@types/faker，就能实现正确的类型检查功能。

### Using Type Definition Files  
使用类型定义文件6:20

Leverage type definition files for Faker to learn library types and alternative documentation. Initialize a location object in the constructor and convert string latitude and longitude to numbers with parseFloat.  
为 Faker 提供杠杆类型定义文件，以便其了解库的类型以及其他相关文档。在构造函数中初始化一个位置对象，并使用 parseFloat 将字符串形式的经度和纬度转换为数字。

### Export Statements in TypeScript  
TypeScript 中的出口声明5:06

Learn how to export and import in TypeScript with named and default exports. Understand using curly braces in index files and creating instances of user and later a company.  
了解如何使用命名导出和默认导出在 TypeScript 中进行文件的导入和导出。理解如何在索引文件中使用花括号来定义变量，以及如何创建用户对象以及公司对象。

### Defining a Company  定义一家公司4:43

Create a company model using faker to provide a name, catch phrase, and latitude/longitude location; initialize, parse floats where needed, export and import the class in index for testing.  
使用 faker 创建一个公司模型，提供名称、口号和经纬度位置；初始化，在需要的地方解析浮点数，在 index 中导出和导入该类进行测试。

### Important Note About Google Maps Key  
关于 Google Maps Key 的重要说明0:28

### Adding Google Maps Support  
添加了谷歌地图功能支持7:38

Set up a Google Developers project, enable Maps JavaScript API, generate an API key, and load Maps SDK via a script tag in HTML to show user and company markers.  
设置一个 Google Developers 项目，启用 Maps JavaScript API，生成一个 API 密钥，并通过 HTML 中的脚本标签加载 Maps SDK 来显示用户和公司标记。

### Required Update for New @types Library  
新的@types 库需要进行更新操作。0:18

### Google Maps Integration with TypeScript  
使用 TypeScript 实现与 Google 地图的集成4:06

Learn to integrate Google Maps into a TypeScript project by using a script tag, declaring a global Google variable, and installing @types/google-maps for proper typing.  
学习如何通过使用脚本标签、声明一个全局的 Google 变量以及安装 @types/google-maps 来将 Google Maps 集成到 TypeScript 项目中。

### Exploring Type Definition Files  
探索类型定义文件12:46

Learn how TypeScript uses type definition files to understand the Google Maps library, create a map instance, and configure options such as zoom and center.  
了解 TypeScript 是如何利用类型定义文件来理解 Google Maps 库的。学习如何创建地图实例，以及配置诸如缩放和中心位置等选项。

### Hiding Functionality  隐藏功能6:28

Learn to hide complex Google Maps functionality behind a custom map class, exposing only creating a new map and adding markers to prevent engineers from accidentally breaking the app.  
学会将复杂的 Google 地图功能隐藏在自定义的地图类函数中，只暴露用于创建新地图和添加标记的功能，这样就能防止工程师不小心破坏应用程序了。

### Why Use Private Modifiers? Here's Why  
为什么使用私有修饰符？这就是原因。8:25

Create a custom map class that hides the Google map from other engineers by using a private property and a minimal API surface, exposing only essential map operations.  
创建一个自定义的地图类，通过使用私有属性以及有限的 API 接口，将 Google 地图的功能隐藏起来，只让其他工程师能够访问必要的地图操作功能。

### Adding Markers  添加标记点9:18

Implement adding markers to a custom map, starting with a bad two-method approach for user and company markers, then refactor with interfaces. Create markers using map and position options.  
实现了在自定义地图上添加标记的功能。最初采用了两种方法来创建用户和公司的标记，后来通过接口设计进行了重构。使用地图和位置信息来创建标记。

### Duplicate Code  重复代码2:45

Learn to eliminate duplicate marker methods by unifying how we place user and company markers on a Google Maps map, using a single interface that supplies latitude and longitude.  
学会通过统一方式来放置用户标记和公司标记，从而消除重复标记的方法。使用同一个界面来输入经纬度信息，就能实现这一目标。

### One Possible Solution  一种可能的解决方案6:38

Consolidate marker handling by introducing a single add marker method that accepts user or company, examining TypeScript narrowing with the or operator, and flagging scalability limitations.  
通过引入一种统一的添加标记方法来处理合并标记问题，该方法可以接受用户或公司的信息；同时，利用 TypeScript 中的或运算符来进行类型约束；此外，还需要注意标记扩展性的限制。

### Restricting Access with Interfaces  
通过接口限制访问权限5:35

Invert dependencies by using an interface to define how classes can be arguments to add marker, requiring a location with latitude and longitude to qualify.  
通过使用接口来定义类如何可以作为参数的传递方式，从而消除依赖关系。同时，需要提供一个包含经纬度信息的位置信息，以便进行定位。

### Implicit Type Checks  隐式类型检查3:26

Explore how TypeScript implicitly validates that a tappable object has latitude and longitude, enabling a reusable custom map with markers through automatic interface checks.  
了解 TypeScript 如何隐式地验证可点击对象的经度和纬度信息是否合法，从而通过自动的接口检查，实现可重复使用的带有标记的自定义地图功能。

### Showing Popup Windows  显示弹出窗口6:47

Learn to show a popup info window for map markers by creating an info window with HTML content, attaching it to a marker click, and calling open on the map.  
通过创建一个包含 HTML 内容的信息窗口，将其连接到标记的点击事件上，然后调用“打开”函数，就可以学会如何为地图上的标记显示弹出信息窗口了。

### Updating Interface Definitions  
更新接口定义7:11

Update the marker interface to require a marker content function that returns a string, enabling dynamic HTML in the info window for user and company objects when adding markers.  
更新标记接口，要求标记内容函数返回一个字符串。这样在添加标记时，用户和公司对象的信息窗口中就能显示动态生成的 HTML 内容。

### Optional Implements Clauses  
可选实施条款6:06

Export, import the map able interface with a color string and marker method, and implement it on user and company to guide TypeScript and locate errors.  
导出、导入具有颜色字符串和标记方法的地图接口，并在用户和公司上实现它，以指导 TypeScript 并定位错误。

### App Wrapup  应用程序总结8:08

Limit the API surface area in indexes, use interfaces to decouple the custom map from markers, implement interfaces to guide type safety and private access.  
限制索引中的 API 表面区域，使用接口将自定义地图与标记解耦，实现接口以指导类型安全性和私有访问。


##   额外福利！  
1 次讲座 • 1 分钟

### Bonus!  额外福利！

# 要求/条件

- Basic knowledge of Javascript and Express is required  
    需要具备基本的 JavaScript 和 Express 知识。
    
- Knowledge of React is good, but not needed  
    具备 React 的相关知识是很好的，但并非必需。
    
- You must be familiar and comfortable with the command line  
    你必须熟悉并舒适使用命令行
    

# 描述

**Event-Based Architecture?** Covered! **Server side rendering with React?** Yep.  **Scalable, production-ready code?** Its here!  
基于事件的架构？当然支持！使用 React 进行服务器端渲染？没问题。可扩展且适合生产环境的代码？也已经准备好了！

_This course requires you to download Docker Desktop from Docker. If you are a Udemy Business user, please check with your employer before downloading software.  
本课程要求您从 Docker 下载 Docker Desktop。如果您是 Udemy Business 用户，请在下载软件前咨询您的雇主。_

Microservices are the number one solution for building and scaling out apps that are intended to **grow**.  Just one little issue: there are few resources online that delve into the most complex and nasty issues around them!  **I built this course to fix that**.  This course tackles every major issues around microservices head on. From challenges with data replication to confusing unordered event streams, **every major challenge of building microservices is covered.**  
微服务是构建和扩展那些需要持续发展的应用程序的最佳解决方案。不过，网上很少有资料能深入探讨与微服务相关的各种复杂问题。我创建这门课程，正是为了解决这个问题。本课程会逐一解决与微服务相关的所有重大问题，从数据复制方面的挑战，到难以处理的乱序事件流问题，微服务构建过程中遇到的各种难题都将在本课程中得到详细讲解。 

Beyond focusing on the basics of microservices, this course is a perfect introduction to the world of **full-stack development**.  You will work all the way from the frontend, assembling a **React app using Hooks**, to the backend, including database design and deployment strategies.  Every step along the way is covered in tremendous detail, with ample diagrams to ensure every step is crystal clear.  
除了讲解微服务的基础知识外，本课程还堪称全面了解全栈开发领域的绝佳入门指南。你将从前端开始学习，使用 Hooks 来构建 React 应用程序；随后会学习后端开发，包括数据库设计和部署策略。课程中对每个环节都进行了详尽的讲解，并配有大量图表，确保你能够清晰地理解每一个步骤。

Many other resources show only the easiest, simplest apps written with microservices.  This course does the opposite: **we focus on the most challenging aspects of microservices**, challenges that you will likely encounter _every single day._  You will see these difficulties first hand, then **solve them with easy-to-understand strategies.**  
许多其他资源只介绍了那些使用微服务开发的、最简单易用的应用程序。而本课程则恰恰相反：我们专注于微服务中最具挑战性的方面——那些你每天都会遇到的难题。你将亲自面对这些困难，然后通过简单易懂的方法来解决它们。

**_  
How This Course Works  
这门课程是如何运作的_**

This course doesn't focus on using an off-the-shelf microservices framework. Many exist, but they hide the inner workings and challenges of microservices away from you. Instead, we will be using a **minimal number of libraries**, and write as much custom code as possible. This will expose you to challenging problems and clever solutions when handling subjects like async events!  
*这门课程并不侧重于使用现成的微服务框架。虽然有许多这样的框架，但它们都会隐藏微服务的内部运作机制和挑战。相反，我们将使用最少的库，并尽可能编写自定义代码*。这将让你接触到一些具有挑战性的问题，同时也能发现一些巧妙的解决方案，尤其是在处理异步事件这类问题时。

  

**_What Technology You'll Use  
你将使用什么技术？_**

Because we are building a full stack application, we will use a variety of technologies. On the frontend, we'll use **React** and **Next JS** to present content to users. Each service is created using **Node** and **Express**. Data for each service is held in either a **Mongo** database or **Redis**. The entire app is deployed and runs in **Docker** containers executed in a **Kubernetes** cluster. Finally, almost all of the code in this course is written with **Typescript**.  
由于我们正在构建一款全栈应用程序，因此会使用多种技术。在前端方面，我们将使用 React 和 Next JS 来向用户展示内容。每个服务都是用 Node 和 Express 框架构建的。各个服务的数据存储于 Mongo 数据库或 Redis 中。整个应用程序部署在 Kubernetes 集群中的 Docker 容器中运行。此外，本课程中几乎所有的代码都是用 TypeScript 编写的。

This is a scary list of technologies! **Not familiar with some of these? No problem!** The course is built assuming that you _only_ know the basics of Javascript and Express. **No other knowledge is needed** - you will learn everything you need to know.  
这真是一份令人担忧的技术清单！有些技术你可能不太熟悉吧？没关系！这门课程的设计就是基于你仅了解 JavaScript 和 Express 的基础知识为前提的。不需要其他任何相关知识——你将会学到所有需要掌握的内容。

  

**_What You'll Be Able to Do  
你能做到的事情_**

By the time you complete this course, you will be able to:  
当你完成这门课程后，你将能够：

- **Architect** a multi-service application  
    构建多服务应用程序
    
- Determine whether your app is a good fit for a microservices approach  
    确定你的应用程序是否适合采用微服务架构。
    
- Understand and **solve the challenges** in async, event-based communication between services  
    理解并解决服务之间异步、基于事件的通信中所遇到的挑战。
    
- Use Docker and Kubernetes to **deploy a multi-service app to any cloud provider**  
    使用 Docker 和 Kubernetes 将多服务应用程序部署到任何云提供商上。
    
- Organize and enhance the **reusability of code** in large projects  
    在大型项目中，整理并提升代码的可重用性。
    

  

**_What You'll Learn  你将学到什么_**

An absolute incredible number of topics are covered in this course.  Here is a _partial_ list of what you'll do:  
这门课程涵盖了数量惊人的主题。以下是一些你会涉及的内容列表：

- Practice **patterns to create scalable microservices** for a variety of app domains  
    通过实践模式来创建可扩展的微型服务，适用于各种应用领域。
    
- Build a **Server-Side-Rendered React** app using Hooks and Next JS  
    使用 Hooks 和 Next JS 构建 Server-Side-Rendered React 应用
    
- Write a custom implementation of an **event bus**  
    编写一个自定义的事件总线实现
    
- Optionally, run a **development environment through a cloud provider**  
    可选地，通过云服务提供商运行开发环境
    
- Guarantee **consistently structured responses** from your different API's  
    确保来自不同 API 的响应结构始终一致
    
- See **best practices** in communication between different services  
    查看不同服务之间通信的最佳实践方案
    
- Configure and scale your services using **Kubernetes Deployments**  
    使用 Kubernetes 进行服务的配置和扩展。
    
- Document and enforce **structure constraints** on events shared across microservices  
    记录并强制执行对跨微服务共享事件的架构约束。
    
- Limit access to your APIs using **JWT-based authentication**  
    通过基于 JWT 的身份验证来限制对您 API 的访问权限。
    
- And much more!  还有更多内容！
    

This is the course I wish I had when I was learning microservices. A course that focuses on the hardest parts, gives clear explanations, and discusses the pros and cons of different design options. Sign up today and join me in mastering microservices!  
这是我希望自己在学习微服务时能够拥有的课程。这样的课程会专注于最困难的部分，提供清晰的解释，并讨论不同设计方案的优缺点。今天就注册吧，和我一起掌握微服务的精髓吧！

# 这门课程适合谁

- Javascript engineers looking to build large, scalable applications  
    那些希望构建大型、可扩展应用程序的 JavaScript 工程师们
- This course is *not* designed for sysadmins focused on infrastructure deployment  
    这门课程并不是为那些专注于基础设施部署的系统管理员设计的。