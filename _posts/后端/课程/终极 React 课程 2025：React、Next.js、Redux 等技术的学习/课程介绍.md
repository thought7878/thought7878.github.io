
[课程](https://www.udemy.com/course/the-ultimate-react-course/?couponCode=MT260902G1A)


# What you'll learn  你将学到什么

- Become an advanced, confident, and modern React developer from scratch  
从零开始，成为一名高级、自信且精通现代 React 技术的开发者。

- Build 10+ beautiful projects, including two HUGE professional real-world apps  
完成 10 个以上的精美项目，其中包括两个规模巨大的专业级实际用途的应用程序。

- Become job-ready by working with libraries and tools used in professional projects  
通过参与专业项目中的相关库和工具的使用，让自己具备从事相关工作的能力。

- Join my other 1,900,000+ happy students on this journey  
加入我的其他 1,900,000 名满意学员，一起踏上这段旅程吧。

- Think like a senior React engineer with advanced design patterns  
想象一下，你是一名拥有先进设计模式的资深 React 工程师。

- Next..js "App" router with cutting-edge React Server Components + Server Actions  
接下来是使用先进的 React Server Components 和 Server Actions 实现的“App”路由系统。

- State management: thinking about state, where to place, local vs. global, UI vs. remote  
状态管理：思考状态的问题，确定其放置位置，区分局部与全局，处理用户界面与远程操作的问题。

- Build reusable, composable, and versatile components  
构建可复用、可组合且功能多样的组件。

- Master the confusing but important useEffect hook once and for all  
彻底掌握那个复杂但至关重要的 useEffect 钩子函数。

- Build many custom hooks, reusable in your future projects  
构建许多可自定义的钩子函数，这些钩子函数可以在未来的项目中重复使用。

- Build single-page applications with React Router (including v6.4+ with data loading)  
使用 React Router 构建单页应用程序（包括 v6.4 及以上版本，支持数据加载功能）。

- Performance optimization with memo, useMemo, useCallback, and code splitting  
通过 memo、useMemo、useCallback 以及代码分割来实现性能优化

- Advanced React features: useReducer, Context API, cloneElement, portals, etc.  
高级 React 特性：useReducer、Context API、cloneElement、portals 等。

- Advanced state management with Redux, Redux Toolkit, Thunks, React Query  
利用 Redux、Redux Toolkit、Thunks 和 React Query 实现高级的状态管理功能

- Build real-world app features: authentication, data sorting, filtering and pagination, dark mode, charts, etc.  
构建现实世界的应用程序功能：认证机制、数据排序、过滤和分页功能、深色模式、图表展示等。

- Understand how React actually works behind the scenes: virtual DOM, reconciliation, fiber tree, key prop, etc.  
了解 React 在幕后是如何工作的：虚拟 DOM、同步机制、Fiber 树、key 属性等。

- Style your apps with Tailwind CSS, CSS Modules, Styled Components (CSS-in-JS)  
使用 Tailwind CSS、CSS Modules 和 Styled Components（CSS-in-JS）来设计你的应用程序风格。

- Practice your skills with many challenges, exercises, and practice projects  
通过大量的挑战、练习和实战项目来锻炼你的技能。

# This course includes:  本课程包含以下内容：

- 84 hours on-demand video  84 小时的点播视频播放时间
- 9 articles  9 篇文章
- 2 downloadable resources  2 种可下载的资源


# 目录
课程内容

40 sections • 505 lectures • 83h 52m total length  
40 个章节 • 505 次讲座 • 总时长 83 小时 52 分钟


##   欢迎，欢迎，欢迎！
5 次讲座 • 31 分钟

#### Course Roadmap and Projects  
课程路线图与项目安排  预览版2:57

Explore four-part React course content, from fundamentals to advanced topics like data fetching, useEffect, reducers, React Router, Redux Toolkit, and real-world apps with Tailwind and Superbase.  
探索 React 的四部分课程内容，从基础概念到高级主题，例如数据获取、useEffect、reducers、React Router、Redux 工具包，以及使用 Tailwind 和 Superbase 构建实际应用的技巧。

#### Building Our First React App!  
正在构建我们的第一个 React 应用程序！  预览版18:52

Build your first React app in CodeSandbox, creating components with JSX, managing state with useState, fetching advice from an API, and using useEffect and props to pass data.  
在 CodeSandbox 中构建你的第一个 React 应用，使用 JSX 创建组件，使用 useState 管理状态，从 API 获取建议，并使用 useEffect 和 props 传递数据。

#### Watch Before You Start!  在开始之前先观看吧！5:24

This course welcomes learners of all levels, lets you adjust pace, rewatch, and skip sections to build React skills, while coding along and using Q&A for help.  
本课程适合各个水平的学习者。你可以根据个人需求调整学习节奏，重新观看某些章节，也可以跳过部分内容来提升 React 技能。同时，你还可以边学习边进行编程练习，并通过提问来获得帮助。

#### Read Before You Start!  在开始之前先阅读一下吧！0:39

#### Downloading Course Material  
下载课程资料3:34

Download the course starter and final project code from GitHub, unzip, and organize folders for each project, then compare with the final code to fix bugs.  
从 GitHub 上下载课程的启动程序和最终项目代码，解压缩后，为每个项目创建相应的文件夹。然后与最终代码进行比对，修复存在的错误。


##   第一部分：React 基础
[4 个项目]  
2 讲座 • 1 分钟

#### Introduction to Part 1  第一部分简介0:55

Master the React fundamentals by setting up new apps, building components with props and JSX, handling events, and managing state to create dynamic applications.  
通过创建新的应用程序、使用 props 和 JSX 来构建组件、处理事件以及管理状态，从而掌握 React 的基础技能，进而开发出具有动态特性的应用程序。

#### Useful Resources for Part 1  
第一部分的有用资源0:20


### 对 React 的初步了解  
9 次讲座 • 1 小时 15 分钟

#### Section Overview  部分概述0:39

Explore why we need React, what React is, and how it compares to vanilla JavaScript, then review setup options and build your first project with Create-React-App.  
了解为什么我们需要 React，什么是 React，以及它与普通 JavaScript 的区别。然后，学习如何设置 React 项目，并使用 Create-React-App 构建你的第一个项目。

#### Why Do Front-End Frameworks Exist?  
为什么需要前端框架呢？10:27

Front-end frameworks exist to keep the user interface in sync with data and avoid the complexity of vanilla JavaScript by centralizing state and enforcing consistent development patterns.  
前端框架的存在是为了让用户界面与数据保持同步，同时通过集中管理状态并遵循统一的开发模式，来避免纯 JavaScript 编程的复杂性。

#### React vs. Vanilla JavaScript  
React 与纯 JavaScript 的比较  预览版7:08

Compare a React app with a Vanilla JavaScript version side by side to see how React automatically keeps the UI in sync with state, unlike manual DOM updates.  
将 React 应用与纯 JavaScript 版本并排比较，以查看 React 如何自动将 UI 与状态同步，而无需手动更新 DOM。

#### What is React?  什么是 React？9:17

Explore how React, a popular JavaScript library for building user interfaces, uses a component-based, declarative approach with JSX and state to render and update UIs.  
了解著名的 JavaScript 库 React 是如何利用基于组件的声明式方法，结合 JSX 和状态管理，来渲染和更新用户界面的。

#### Setting Up Our Development Environment  
搭建我们的开发环境8:47

Set up your React development environment by installing VS Code, Chrome, and Node.js (v18+), configure ESLint and Prettier, and enable auto-save and format-on-save with starter snippets.  
通过安装 VS Code、Chrome 和 Node.js（v18 及以上版本）来设置你的 React 开发环境。同时，配置 ESLint 和 Prettier 工具，并启用自动保存和基于启动代码片段的自动格式化功能。

#### Pure React  纯React 14:49

Explore pure React by building a simple app inside a regular HTML file without any modern tooling, using React and React-dom, createRoot, and a clock with useState and useEffect.  
通过构建一个简单的应用程序来探索纯 React 的潜力。在普通的 HTML 文件中实现这个应用程序，而不使用任何现代的工具。使用 React 和 React-dom、createRoot 函数，以及 useState 和 useEffect 钩子，来创建一个时钟组件。

#### A Quick Look at React's Official Documentation  
快速了解 React 的官方文档3:16

Navigate React's official documentation at react.dev, focusing on the Learn and Reference sections to grasp fundamentals, hooks like useState, and how these pages support real-world project setup.  
请参考 React 的官方文档，网址为 react.dev。在文档中的“学习”和“参考”部分可以了解基础内容、 useState 这样的钩子函数，以及这些页面如何帮助您完成实际项目的构建。

#### Setting Up a New React Project: The Options  
创建一个新的 React 项目：相关设置选项7:41

Explore setting up a new React project with Create-React-App and Vite, compare preconfigured tooling to manual setup, and choose the right approach for learning and real-world apps.  
了解如何使用 Create-React-App 和 Vite 来创建新的 React 项目；比较预配置的工具与手动设置的方式；选择最适合学习和开发实际应用的方案。

#### Setting Up a Project With Create-React-App  
使用 Create-React-App 来搭建项目12:48

Set up your first create-react-app project using the terminal, navigate folders with cd and ls, install with npx create-react-app@5, and start the app with npm start in VS Code.  
使用终端来创建你的第一个 Create-React-App 项目。通过 `cd` 和 `ls` 命令来浏览各个文件夹。然后使用 `npx create-react-app@5` 命令来安装该项目。最后，在 VS Code 中通过 `npm start` 命令来启动项目。


### [可选] 回顾《React 核心 JavaScript》一书  
15 次讲座 • 1 小时 52 分钟

#### Section Overview  部分概述0:55

Refresh essential JavaScript fundamentals for React, including destructuring, spread operator, template literals, ternaries, promises, and async/await, and study immutable arrays with map, filter, reduce, and sort.  
复习 React 中关键的 JavaScript 基础知识，包括解构赋值、扩展运算符、模板字面量、三元运算符、Promise 以及 async/await 等概念。同时学习如何使用不可变数组进行映射、过滤、减少和排序操作。

#### Destructuring Objects and Arrays  
对象与数组的结构化操作10:58

Master object and array destructuring in JavaScript by extracting book data (title, author, pages, genres) from a simulated API, with VS Code and Quokka demonstrations.  
通过提取来自模拟 API 中的书籍数据（标题、作者、页数、类型），在 JavaScript 中实现对象与数组的解构操作。整个过程使用 VS Code 和 Quokka 进行演示。

#### Rest/Spread Operator  休息/展开运算符9:53

Learn how rest and spread operators work with arrays and objects, using rest for remaining elements and spread to add or override properties in React state updates.  
了解如何使用“rest”操作符来处理数组和对象中的元素，以及如何利用“spread”操作符在 React 状态更新中添加或覆盖属性。

#### Template Literals  模板字面量4:50

explore template literals in ES6 that create dynamic strings with JavaScript variables and expressions using backticks and ${}, as shown with title, pages, author, and publicationDate.  
在 ES6 中，可以使用模板字面量来创建包含 JavaScript 变量和表达式的动态字符串，这些变量和表达式可以通过反引号``和${}来标识。例如，在标题、页面标题、作者以及出版日期等字段中就可以使用这种方式来动态生成内容。

#### Ternaries Instead of if/else Statements  
用三元表达式替代 if/else 语句5:49

Learn how to use the ternary operator in JavaScript to conditionally return values in React, with examples of true/false branches, template literals, and page range logic.  
了解如何在 React 中使用 JavaScript 中的三元运算符来根据条件返回不同的值。通过实例演示了如何处理真/假分支、模板字面量以及页面范围逻辑。

#### Arrow Functions  箭头函数4:59

Explore arrow functions in ES6, compare them to traditional functions, and learn to extract the year from a date in date-month-day format using a concise one-liner.  
了解 ES6 中的箭头函数，将其与传统函数进行比较，并学会如何使用简洁的一行代码从日期-月份-日格式中提取年份。

#### Short-Circuiting And Logical Operators: &&, ||, ??  
短路操作与逻辑运算符：&&、||、??10:32

Explore how short-circuiting works with && and || in JavaScript, using truthy and falsy values, and apply ?? to provide nullish defaults without misinterpreting zero.  
了解在 JavaScript 中，短路求值如何与&&和||运算符协同工作。利用真值和假值，我们可以实现特定的逻辑判断。同时，可以使用??运算符来设定空值默认值，从而避免对零值的误解。

#### Optional Chaining  可选链式绑定6:30

Learn to use optional chaining and the nullish coalescing operator in JavaScript to safely compute total review counts from nested book data, handling missing Goodreads or libraryanything fields.  
学习如何在 JavaScript 中使用可选链式和空值合并运算符，以安全地计算嵌套图书数据中的总评价数量，同时能够处理缺少 Goodreads 或 libraryanything 字段的情况。

#### The Array map Method  Array 映射方法8:35

Master map, filter, and reduce, the three functional array methods, returning new arrays without mutating the original, and learn to extract book titles and essential data.  
掌握映射、过滤和归并这三种数组操作方法，它们都能在不修改原始数据的情况下生成新的数组。同时，学会提取书籍标题和关键数据。

#### The Array filter Method  Array 过滤方法8:36

Apply the array filter method to select books by a condition, chain filters with other array methods, and pair with map to project titles, using includes and adventure genre examples.  
使用数组过滤方法根据特定条件选择书籍；将多个过滤条件与其他数组方法结合使用；结合`map`函数来提取标题；同时结合`includes`和“冒险类”等条件来进行筛选。

#### The Array reduce Method  数组的 reduce 方法7:20

Master the array reduce method to collapse a JavaScript array into a single value by summing numbers, using a callback, an accumulator, and a starter value.  
掌握数组的 reduce 方法，通过使用回调函数、累加器和初始值将 JavaScript 数组折叠成一个单一值，通过求和数字。

#### The Array sort Method  数组排序方法6:38

Sort arrays using JavaScript's sort method, using a and b to achieve ascending or descending order, and copy with slice to avoid mutating the original when sorting objects by pages.  
使用 JavaScript 的 sort 方法对数组进行排序，通过 a 和 b 参数来控制排序的方向，即升序或降序。在按页码对对象进行排序时，使用 slice 方法进行复制，以避免在排序过程中改变原始数据。

#### Working With Immutable Arrays  
处理不可变数组11:06

Learn to work with immutable arrays in React by adding, deleting, and updating elements without mutating the original data, using the spread operator, filter, and map.  
学习如何在 React 中使用不可变数组，通过添加、删除和更新元素来实现操作，同时避免修改原始数据。可以使用展开运算符、过滤操作和映射函数来实现这些操作。

#### Asynchronous JavaScript: Promises  
异步 JavaScript：Promise 机制8:11

Master asynchronous JavaScript by using the fetch API and promises to fetch data from an API, handle fulfilled responses with then and JSON conversion, and display results.  
通过使用 fetch API 和 Promise，实现异步 JavaScript 编程。从 API 获取数据后，利用 then 函数处理已完成的响应，并将数据转换为 JSON 格式，最后展示结果。

#### Asynchronous JavaScript: Async/Await  
异步 JavaScript：异步/等待7:30

Learn to fetch data with async/await, replacing then handlers with an asynchronous function and await for cleaner, synchronous-like code. Explore how promises and React state fit into asynchronous data flows.  
学习如何使用异步/等待机制来获取数据，用异步函数和等待操作替代传统的处理流程，从而打造更简洁、同步化的代码。了解 Promise 和 React 状态如何与异步数据流协同工作。


### 处理组件、属性以及 JSX 的编写  
24 堂课 • 2 小时 54 分钟

#### Section Overview  部分概述0:54

Explore components, props, and JSX as building blocks of React, learn to reuse components, pass data with props, and render lists with conditional rendering in a developer profile card.  
探索组件、属性和 JSX 作为 React 的构建模块，学习如何重用组件，通过属性传递数据，以及在开发者个人资料卡片中通过条件渲染来渲染列表。

#### Rendering the Root Component and Strict Mode  
渲染根组件以及严格模式7:25

Start from scratch by creating index.js, import React and ReactDOM, and render the App into the root with ReactDOM.createRoot in React 18, optionally wrapping in StrictMode to double-render in development.  
从创建 index.js 文件开始吧。需要导入 React 和 ReactDOM，然后使用 ReactDOM.createRoot 将 App 组件渲染到根元素上。在 React 18 中，还可以选择使用 StrictMode 来在开发过程中进行双重渲染。

#### Before We Start Coding: Debugging  
在开始编码之前：调试工作9:46

Develop debugging skills for React apps by ensuring the app runs with npm start, using hard reloads when needed, and inspecting errors via the browser console, ESLint, and dev tools.  
通过确保应用程序使用 npm start 运行、在需要时使用硬重载，并通过浏览器控制台、ESLint 和开发者工具检查错误，来培养 React 应用的调试技能。

#### Components as Building Blocks  
组件作为构建模块4:34

Explore how React builds user interfaces from reusable components, nesting, and props, forming a component tree that links parent and child components to render the UI.  
了解 React 是如何通过可复用的组件、嵌套结构以及属性来构建用户界面的。这些组件共同构成了一个组件树，将父组件与子组件连接起来，从而实现界面的渲染。

#### Creating And Reusing a Component  
创建并复用组件  预览版9:40

Create a brand new React component and render it three times to demonstrate reusability. Use public assets, add alt text for accessibility, and prepare for prop-driven data.  
创建一个新的 React 组件，并将其渲染三次以展示其可重用性。使用公共资源，为可访问性添加替代文本，并准备好基于属性的数据。

#### What is JSX?  什么是 JSX？6:25

Describe how JSX provides a declarative syntax to define a component's appearance from data and logic, and how Babel converts JSX to React.createElement to render the UI.  
请描述一下 JSX 是如何提供一种声明性语法，以便根据数据和逻辑来定义组件的样式的。同时，请说明 Babel 是如何将 JSX 转换为 React.createElement，从而实现 UI 的渲染的。

#### Creating More Components  创建更多组件8:07

Create additional components with JSX for header, menu, and footer, reusing the pizza component to display six pizzas. Compare JSX with React.createElement to see how small components form an app.  
使用 JSX 创建额外的组件，包括头部、菜单和底部组件。可以复用那个披萨组件，从而显示六个披萨的界面。比较一下 JSX 和 React.createElement，了解如何通过小型组件来构建整个应用程序。

#### JavaScript Logic in Components  
组件中的 JavaScript 逻辑实现4:26

Explore how to write JavaScript inside React components, using variables and if-else logic to determine open hours and display results, while understanding render behavior.  
了解如何在 React 组件中编写 JavaScript 代码，利用变量和 if-else 逻辑来确定营业时间，并展示结果。同时，还要理解组件在渲染时的行为。

#### Separation of Concerns  关注点的分离5:57

Discover how React embraces separation of concerns by co-locating data, logic, and ui in single components using jsx that blends html, css, and javascript.  
了解 React 如何通过将数据、逻辑和用户界面集成到同一个组件中来实现职责分离。这种技术利用了 jsx，它将 HTML、CSS 和 JavaScript 结合在一起。

#### Styling React Applications  
React 应用样式12:17

Learn to style React components using inline styles in JSX and external CSS files, import a CSS file with Webpack, and use className for global styles.  
学习如何使用 JSX 中的内联样式和外部 CSS 文件来设置 React 组件的样式。还可以使用 Webpack 将 CSS 文件导入到项目中，并使用 className 来定义全局样式。

#### Passing and Receiving Props  
传递和接收属性9:08

Learn how props enable data flow from a parent to a child component in React, letting you customize each pizza with image, name, ingredients, and price.  
了解如何在 React 中利用道具实现数据从父组件流向子组件的过程，这样你就可以为每个披萨组件添加图片、名称、配料和价格等信息，从而自定义每个披萨的显示效果。

#### Props, Immutability, and One-Way Data Flow  
属性、不可变性和单向数据流7:09

Explore how props pass data from parent to child, why props are immutable, and how one-way data flow in React keeps UI in sync with state and avoids side effects.  
了解如何让道具在父组件和子组件之间传递数据，为什么道具是不可变的，以及如何在 React 中实现单向数据流动，从而让 UI 与状态保持同步，避免副作用的产生。

#### CHALLENGE #1: Profile Card (v1)  
挑战 #1：个人资料卡片（v1）  预览版17:25

Build a developer profile card in React with avatar, name, bio, and a skills list, using props to customize each skill and choosing between Create React App or CodeSandbox.  
使用 React 构建一个开发者个人资料卡片，包含头像、姓名、简介以及技能列表。可以通过 props 来定制每项技能的内容，并且可以选择使用 Create React App 还是 CodeSandbox 来进行项目构建。

#### The Rules of JSX  JSX 的规则3:37

Explore the rules of JSX: JSX works like HTML with a JavaScript mode via curly braces, allows JavaScript expressions, forbids statements, and requires a root element or a React fragment.  
了解 JSX 的规则：JSX 的工作方式类似于 HTML，但它使用了 JavaScript 语法，通过大括号来表示结构。JSX 允许使用 JavaScript 表达式，但禁止使用多个语句。同时，需要有一个根元素或 React 片段作为上下文。

#### Rendering Lists  渲染列表11:24

Render lists in React by mapping an array to create one component per item, using a unique key and passing the item as a prop. Use ul and li markup.  
在 React 中渲染列表，通过将数组映射来为每个项目创建一个组件，使用唯一的 key 并将项目作为 prop 传递。使用 ul 和 li 标记。

#### Conditional Rendering With &&  
带&&的条件渲染9:16

Learn how to conditionally render React components using the && operator and short-circuiting, based on conditions like isOpen or available pizzas.  
了解如何根据诸如“isOpen”或“可提供的披萨数量”等条件，使用&&运算符和短路编程技巧，来有条件地渲染 React 组件。

#### Conditional Rendering With Ternaries  
基于三元组的条件渲染技术4:45

Learn to render conditionally in React using the ternary operator inside JSX, understanding its three parts and why it yields a value in JavaScript.  
学习如何在 React 中使用 JSX 中的三元运算符来实现条件渲染，了解其三个部分的作用，以及为什么在 JavaScript 中它会产生一个具体的值。

#### Conditional Rendering With Multiple Returns  
具有多个返回值的条件渲染5:03

Use multiple returns to conditionally render JSX in React components, enabling early returns, null returns, and alternative JSX based on props such as soldOut.  
在 React 组件中，可以使用多次返回来实现根据条件渲染 JSX 代码的功能。这种方式可以支持提前返回、空返回，以及基于诸如“已售罄”等参数的其他 JSX 结构。

#### Extracting JSX Into a New Component  
将 JSX 代码提取到一个新的组件中4:32

Learn to extract a complex JSX fragment from a footer into a new React component, pass data via props, and render the order component with the correct hours.  
学会从页脚中提取复杂的 JSX 代码，并将其封装到一个新的 React 组件中。通过 props 传递数据，然后以正确的小时数来渲染订单组件。

#### Destructuring Props  构造属性4:52

Learn to simplify React components by destructuring props, receiving a pizza object and other props directly, eliminating repetitive props access, and avoiding common mistakes with curly braces.  
学会通过解构参数来简化 React 组件的逻辑。这样可以直接获取包含多个属性的对象，从而避免重复访问这些属性，同时还能避免使用花括号时常见的错误。

#### React Fragments  React 片段5:36

Learn what a React fragment is and how to render multiple elements without a wrapper, using React fragment when needed and adding keys for lists.  
了解什么是 React 片段，以及如何在不使用包装器的情况下渲染多个元素。在需要的时候使用 React 片段，并为列表添加键。

#### Setting Classes and Text Conditionally  
根据条件设置班级和文本8:00

Learn to conditionally display text and set class names in React using the ternary operator and template literals, illustrating sold out states and price in a pizza app.  
学习如何使用三元运算符和模板字符串在 React 中条件性地显示文本和设置类名，以在披萨应用中展示售罄状态和价格。

#### Section Summary  章节摘要5:33

Explore how React components form a UI using JSX, style props, and props data; render lists with map and apply conditional rendering.  
了解如何使用 JSX、样式属性以及属性数据来构建 React 组件，进而实现 UI 的渲染。还可以学习如何使用 map 方法来处理列表，以及如何实现条件性渲染。

#### CHALLENGE #2: Profile Card (v2)  
挑战#2：个人资料卡（版本 2）8:15

Render a dynamic developer profile card by mapping a skills array to components, using color and level to display beginner, intermediate, or advanced emojis in a React course project.  
通过将技能数组映射到相关组件上，利用颜色与等级来表示初级、中级或高级水平，从而制作出具有动态效果的开发者个人资料卡片。这项任务需要应用于 React 课程项目中。


### 状态、事件和形式：交互式组件  
21 次讲座 • 2 小时 55 分钟

#### Section Overview  部分概述1:02

Explore fundamental React concepts by making components interactive, handling events, and managing state with controlled forms, then summarize props and state through a small flashcards app.  
通过让组件具有交互性、处理事件、管理状态以及使用受控表单来探索 React 的基本概念。然后，通过一个小型的闪卡应用程序来总结关于属性和状态的相关知识。

#### Let's Build a Steps Component  
让我们来构建一个楼梯组件吧。14:48

Learn to build a simple three-step component in React by scaffolding with Create-React-App, rendering static JSX, highlighting the current step, and wiring next/previous buttons for future interactivity.  
学习如何使用 Create-React-App 框架在 React 中构建一个简单的三步组件。包括渲染静态 JSX 代码、高亮显示当前步骤，以及添加上下按钮以实现后续的交互功能。

#### Handling Events the React Way  
以 React 的方式处理事件7:21

Learn to handle events in React with the onClick prop, avoid addEventListener, and pass a function as the handler while using JSX event naming, camel case, and state concepts.  
学会如何使用 React 中的 onClick 属性来处理事件，避免使用 addEventListener 方法，并且在使用 JSX 事件命名、驼峰式命名方式以及状态概念时，将函数作为事件处理程序传递。

#### What is State in React?  
在 React 中，什么是状态？6:08

Understand what state is in React and why it powers interactivity. Learn to manage state with useState, useReducer, context, and Redux, triggering re-renders and keeping the user interface in sync.  
了解 React 中的状态机制，以及为什么需要利用状态来实现交互性。学习如何使用 useState、useReducer、context 和 Redux 来管理状态，从而触发重新渲染，并保持用户界面的一致性。

#### Creating a State Variable With useState  
使用 useState 创建状态变量9:07

Create a dynamic step state with useState, destructure the array, and update it via event handlers for next and previous buttons, while guarding against invalid steps.  
使用 useState 创建动态步骤状态，解构数组，并通过 next 和 previous 按钮的事件处理程序更新它，同时防止无效步骤。

#### Don't Set State Manually!  
不要手动设置状态！5:17

Learn why you should only update React state with the setter function, avoid mutating state, and embrace immutability for reliable re-renders and correct updates.  
了解为什么应该只使用 setter 函数来更新 React 状态，避免对状态进行 mutable 操作，并采用不可变性来实现可靠的重新渲染和正确的更新。

#### The Mechanics of State  国家的运作机制4:51

Explore how state drives rendering in React using the useState hook; updating state on events re-renders the component while preserving state until unmount.  
了解如何使用 useState 钩子来控制 React 中的渲染过程；在事件触发时更新状态会重新渲染组件，而在此之前，状态仍然会被保留，直到组件被卸载为止。

#### Adding Another Piece of State  
再增加一块状态信息8:36

Introduce an isOpen state with useState to toggle content on click. Use conditional rendering and a button to show or hide parts, and illustrate React fragments for multiple elements.  
通过 useState 函数引入一个 isOpen 状态，以便在点击时切换内容的显示状态。使用条件渲染技术，通过一个按钮来显示或隐藏部分内容。同时，利用 React 的片段功能来处理多个元素的情况。

#### React Developer Tools  React 开发工具6:09

Learn to use React dev tools to inspect the component tree and state, view hooks like useState, and toggle values in the console to see UI changes and component behavior.  
学习如何使用 React 的开发工具来查看组件树和状态信息。了解像 useState 这样的视图钩子，并在控制台中切换值以观察 UI 变化以及组件的行为。

#### Updating State Based on Current State  
根据当前状态更新状态5:16

Update state using a callback that receives the current value to ensure safe, correct updates; avoid relying on current state when updating twice.  
通过回调函数来更新状态，该回调函数会接收当前的值，以确保更新操作的安全性和正确性；在更新状态时，避免依赖于当前状态。

#### More Thoughts About State + State Guidelines  
更多关于状态及状态指南的思考6:33

Learn how components isolate state, view the UI as a reflection of state, and apply practical state guidelines, including modal isOpen handling and avoiding unnecessary re-renders.  
了解如何使组件独立处理状态，将用户界面视为状态的反映，并应用实用的状态管理规则，包括处理模态窗口的显示问题，以及避免不必要的重新渲染。

#### A Vanilla JavaScript Implementation  
一个纯 JavaScript 的实现6:34

Compare a vanilla JavaScript implementation with the React version to show how state drives user interface updates, from manual DOM manipulation to declarative rendering.  
将普通的 JavaScript 实现与 React 版本进行比较，从而展示状态如何驱动用户界面的更新，从手动操作 DOM 到声明式渲染。

#### CHALLENGE #1: Date Counter (v1)  
挑战#1：日期计数器（v1 版）13:09

Learn to build a date counter in React using state, with a configurable step to add or subtract days from the current date, and show days from today.  
学习如何使用 React 中的状态功能来构建日期计数器。该计数器可以配置为从当前日期添加或减去天数，并显示距离今天还有多少天。

#### Starting a New Project: The "Far Away" Travel List  
开始一个新项目：所谓的“远方”旅行清单4:08

Create travel-list app with npx create-react-app v5 to add, pack, and clear items, with sorting by input order or description; organize into logo, form, packing list, item, stats, form submissions.  
使用 npx create-react-app v5 创建旅行清单应用程序，能够添加、整理清空项目，并根据输入顺序或描述进行排序；可以将项目组织成不同的类别，如标志、表单、包装清单、项目详情、统计数据以及表单提交记录等。

#### Building the Layout  构建布局图8:54

Build a static layout for a React app by setting up starter CSS, scaffolding app components (logo, packing list, stats), and rendering static items from an array with JSX.  
通过创建初始的 CSS 文件、构建应用程序组件（如 logo、菜单项、统计信息），以及使用 JSX 从数组中渲染静态元素，来为 React 应用程序构建一个静态布局。

#### Rendering the Items List  渲染物品列表8:42

Learn to render a dynamic packing list in React by mapping an array to a ul, passing item props to a component, and styling packed items with a line-through.  
学习如何使用 React 来动态生成包装列表。通过将数组映射到一个 ul 元素，将 item 属性传递给组件，然后利用线穿过样式来设置包装项的样式。

#### Building a Form and Handling Submissions  
构建表单并处理提交数据11:55

Learn to build and handle forms in React using the HTML form element, dynamically generate select options with array.from and map, and prevent page reload with onSubmit.  
学习如何使用 React 中的 HTML 表单元素来构建和处理表单。通过 array.from 和 map 方法动态生成选择选项，并通过 onSubmit 事件防止页面重新加载。

#### Controlled Elements  受控元素18:26

Learn how to implement controlled elements in React forms by keeping state in React, binding inputs and selects, and updating state with onChange, then reset on submit.  
了解如何在 React 表单中实现受控元素。这包括使用 React 来管理状态、绑定输入和选择框的关联，以及通过 onChange 事件来更新状态。同时，还需要在提交时重置这些状态。

#### State vs. Props  国家与财产之争3:30

Explain how state stores internal data and drives interactivity, while props pass external, read-only data from parent to child, triggering re-renders on updates.  
解释状态存储内部数据并驱动交互性，而属性从父组件传递到子组件传递外部只读数据，在更新时触发重新渲染。

#### EXERCISE #1: Flashcards  练习#1：闪卡  预览版13:56

Learn to manage flashcard state in React by building a small app that uses a single useState hook to track the selected question, render questions, and reveal answers on click.  
通过开发一个小型应用程序来学习如何在 React 中管理闪卡状态。该应用程序使用单一的 useState 钩子来跟踪所选问题、显示问题内容，并在点击时揭示答案。

#### CHALLENGE #2: Date Counter (v2)  
挑战#2：日期计数器（版本 2）10:31

Upgrade the date counter with a range slider and a numeric text input, convert values to numbers, make both inputs controlled, and render a reset button only when changes occur.  
通过范围滑块和数字文本输入来更新日期计数器的值，将输入的值转换为数字形式。同时，让这两个输入项都由用户控制，并且只有在值发生变化时才会显示重置按钮。


### 在 React 中思考：状态管理  
17 次讲座 • 2 小时 42 分钟

#### Section Overview  部分概述0:58

Learn to think in React by mastering state management: decide when to create and derive state, and lift state up to communicate between child and parent components.  
通过掌握状态管理技巧来学习如何在 React 中思考：决定何时创建状态，以及如何将状态提升到子组件与父组件之间进行通信。

#### What is "Thinking in React"?  
"React 思维"是什么？4:59

Learn to think in React by breaking the UI into components, mastering state, props, and one-way data flow, and building reusable interfaces with a clear component tree.  
学会通过将 UI 拆分为组件来思考 React，掌握状态、属性和单向数据流，并构建具有清晰组件树的可用界面。

#### Fundamentals of State Management  
状态管理基础9:24

Explore the fundamentals of state management in React, distinguish local and global state, and learn when to place, derive, or lift state, using context or Redux.  
探索 React 中状态管理的原理，区分本地状态和全局状态，并学习何时放置、派生或提升状态，使用上下文或 Redux。

#### Thinking About State and Lifting State Up  
思考状态和提升状态14:41

Learn to add and manage state with useState, then lift state up to the app component and pass onAddItems to the form and packing list.  
学习如何使用 useState 来添加和管理状态。然后将状态提升到应用组件级别，并将 AddItems 函数传递给表单和清单页面。

#### Reviewing "Lifting Up State"  
回顾"提升状态"6:25

Lift state up to a common parent to share coupons between promotions and total components, then pass a setter to enable updates via inverse data flow in React.  
将状态提升到一个共同的父级，以便在不同的促销活动中共享优惠券以及汇总组件信息。然后通过设置器来启用通过 React 中的反向数据流进行更新功能。

#### Deleting an Item: More Child-to-Parent Communication!  
删除某个物品：更多的亲子沟通机会！8:14

Learn to delete items via child-to-parent communication by passing a delete handler from the app to the packing list and items, using item.id to filter and update state.  
学会通过儿童与父母的沟通方式来删除物品。在应用程序中传递删除处理程序，从而将物品从清单中移除，并使用 item.id 来过滤和更新物品的状态。

#### Updating an Item: Complex Immutable Data Operation  
更新项目：复杂的不可变数据操作7:43

Learn to update items by toggling their packed status with a controlled checkbox, using state, on change, and immutable map-based updates in a packing list.  
学会通过控制复选框切换其打包状态来更新项目，使用状态、变更事件和不可变映射更新打包列表中的项目。

#### Derived State  从属状态3:03

Derive state from the cart to keep data in sync and reduce re-renders by computing the number of items and total price from the cart.  
从购物车中派生状态以保持数据同步并通过计算购物车中的商品数量和总价来减少重新渲染。

#### Calculating Statistics as Derived State  
将统计计算视为衍生状态10:43

Calculate statistics from the items array using derived state in React, including number of items, number packed, and packing percentage, with early returns and conditional rendering.  
使用 React 中的派生状态从 items 数组计算统计数据，包括项目数量、打包数量和打包百分比，并使用提前返回和条件渲染。

#### Sorting Items  整理物品10:48

Sort items by input order, description, and packed status using a select box, implementing a controlled sortBy state and derived sortedItems to reorder the list without mutating the original array.  
可以通过选择框来按照输入顺序、描述顺序以及打包状态对项目进行排序。同时，需要实现一个可控的 sortBy 状态，并通过 derivedSortedItems 来重新排序列表，而无需修改原始数组。

#### Clearing the List  清除列表5:03

Add a clear list button, implement handleClearList to reset items to an empty array, and pass onClearList to the packing list component with a confirmation prompt to prevent accidental deletion.  
添加一个“清除列表”按钮，实现`handleClearList`方法来将列表项重置为空数组，并将`onClearList`事件处理器传递给打包列表组件，同时添加确认提示以防止意外删除列表项。

#### Moving Components Into Separate Files  
将各个组件移动到单独的文件中。8:53

Split your React app into separate component files, exporting default components and importing them correctly to keep App.js organized. Learn refactoring, moving components to a components folder, and using useState.  
将你的 React 应用程序拆分为多个组件文件，导出默认组件，并正确导入这些组件，以使得 App.js 文件保持整洁。了解如何重构代码、将组件移动到组件文件夹中，以及如何使用 useState 函数。

#### EXERCISE #1: Accordion Component (v1)  
练习#1：折纸组件（v1 版）14:15

Build a reusable accordion in React by rendering items from a data array. Each item shows a number, title, and text, and toggles its content with a plus/minus control.  
使用 React 构建一个可重复使用的折叠式组件。通过渲染数据数组中的各项信息，每项信息包含一个数字、标题和文本，并且可以通过加号/减号按钮来切换其显示状态。

#### The "children" Prop: Making a Reusable Button  
"children" 属性：创建可重用按钮16:10

Learn to create a reusable button in React using the children prop to pass arbitrary JSX content, making components generic and highly reusable.  
学习如何使用 children 属性在 React 中创建可重用的按钮，以传递任意的 JSX 内容，使组件更具通用性和高度可重用性。

#### More Reusability With the "children" Prop  
使用 "children" 属性实现更高的可重用性8:12

Learn to build reusable components with the children prop by creating a StepMessage that always displays the current step plus passed-in content, enabling flexible, reusable UI blocks.  
学习如何使用 children prop 构建可重用组件，通过创建一个始终显示当前步骤以及传入内容的 StepMessage，实现灵活、可重用的 UI 块。

#### EXERCISE #2: Accordion Component (v2)  
练习 #2：手风琴组件（v2）10:15

Build a single-open accordion in React by lifting state to the accordion, using curOpen to track the open item, and passing onOpen; embrace the children prop for flexible content.  
通过使用`curOpen`来跟踪展开状态，以及调用`passOnOpen`函数来传递信息，可以用 React 构建一个单开式折叠式组件。同时，利用`children`属性来实现灵活的内容布局。

#### CHALLENGE #1: Tip Calculator  
挑战 #1：小费计算器22:09

Build a dynamic tip calculator in React by composing bill input, select percentage, output, and reset components, lifting state up and deriving the tip from bill and average percentages.  
通过使用 React 构建出一个动态的小费计算器。首先创建“账单输入”组件，然后选择百分比、输出结果以及重置功能。接着，通过更新状态来从账单金额和平均小费百分比中计算出小费金额。


### [可选] 练习项目：吃分  
9 次讲座 • 1 小时 29 分钟

#### Section Overview  部分概述0:55

Finish part one by building the Eat-'N-Split project to reinforce the React fundamentals of state, props, and events. Note this bonus project adds no new concepts, but provides essential practice.  
完成第一部分的工作，通过构建“Eat-'N-Split”项目来巩固 React 中状态、属性和事件的基础概念。请注意，这个附加项目并不包含新的概念，但能提供必要的练习机会。

#### Project Setup  项目设置3:52

Set up a new react project with npx create-react-app version five for the eat-n-split app, then outline ui components to split a bill among friends and prepare static, state-free implementation.  
创建一个新的 React 项目，使用 npx create-react-app 命令来生成版本为 5 的项目。然后设计一些用户界面组件，以便能够在朋友之间分配账单。同时，准备一些静态、无需状态的实现方式。

#### Building the Static App: List of Friends  
构建静态应用程序：好友列表12:01

Learn to build a static React app that renders a friends list, using components, props, and map, with conditional balance styling and a functional UI.  
学习如何构建一个静态的 React 应用程序，该应用程序能够显示好友列表。通过使用组件、属性以及 map 函数来实现这一功能，同时具备条件性的布局样式设计，并且拥有功能性的用户界面。

#### Building the Static App: Forms  
构建静态应用：表单部分9:27

Builds static forms for a bill-splitting app by composing a form add friend and a form split bill, reusing a button component, handling disabled inputs and a simple select.  
为账单分摊应用构建静态表单，通过组合添加朋友表单和分摊账单表单，重用按钮组件，处理禁用输入和简单的选择器。

#### Displaying the New Friend Form  
显示新好友的界面6:33

Use a useState variable to display or hide the new friend form, triggering a re-render and toggling the button text between add friend and close.  
使用`useState`变量来显示或隐藏新朋友的表单，这会触发重新渲染，并让按钮文本在“添加朋友”和“关闭”之间切换。

#### Adding a New Friend  添加新朋友18:38

Implement adding a new friend by wiring controlled inputs in FormAddFriend, generating a unique id with crypto.randomUUID, and lifting state to App to render updates in FriendsList.  
通过在 FormAddFriend 中连接受控输入来添加新朋友，使用 crypto.randomUUID 生成唯一 ID，并将状态提升到 App 中以在 FriendsList 中渲染更新。

#### Selecting a Friend  选择一位朋友15:02

Lift state up to the app, track the selected friend, and pass it via props to the friend list and form to render and update the selection.  
将状态提升到应用程序中，跟踪所选的朋友，然后通过道具将其传递给朋友列表，以便渲染并更新选择结果。

#### Creating Controlled Elements  
创建受控元素9:53

Turn the input fields into controlled elements by wiring bill, paidByUser, and whoIsPaying to React state with useState. Derive paidByFriend from the bill and validate numeric input.  
通过将 input 字段通过代码连接起来，将 bill、paidByUser 和 whoIsPaying 转换为 React 状态中的可控元素。从 bill 中提取 paidByFriend 参数，并验证数字输入的正确性。

#### Splitting a Bill  分摊账单12:47

Finish a bill-splitting app by handling form submission, guarding input, and updating each friend's balance via state. Use a split-bill handler and auto-close the form.  
完成这个账单分割应用程序的开发，包括处理表单提交、验证输入数据，以及通过状态更新来同步每位朋友的账单余额。使用账单分割处理程序，并自动关闭表单。


##   第二部分：中级 React 
[2 个项目]  
2 次讲座 • 2 分钟

#### Introduction to Part 2  第二部分简介1:15

Reinforce part 1 fundamentals through self-led projects, then dive into part 2 to see how React works behind the scenes and build the usePopcorn UIs with hooks.  
通过自主项目来巩固第一部分的基础知识，然后深入第二部分，了解 React 在幕后是如何工作的，并使用 hooks 来构建 usePopcorn 界面。

#### Useful Resources for Part 2  
第二部分的有用资源0:53


### 在 React 中思考：组件、组合以及可重用性  
17 次讲座 • 2 小时 39 分钟

#### Section Overview  部分概述1:00

Think in React by mastering how to split a user interface into components, their composition, and reusability, then implement a simple app layout using component-based design in React.  
通过掌握如何将用户界面拆分为多个组件，了解这些组件的组合方式以及它们的可重用性，从而实现一个简单的应用程序布局。使用基于组件的设计方式，在 React 中实现这一布局。

#### Setting Up the "usePopcorn" Project  
搭建“usePopcorn”项目4:10

Set up the usepopcorn React app, explore static data, and master components and hooks while implementing real data fetching, a rating component, and a watchlist.  
安装 usePopcornReact 应用程序，探索静态数据，并在实现真实数据获取、评分组件以及观看列表功能时，掌握组件和钩子用法。

#### How to Split a UI Into Components  
如何将用户界面拆分为多个组件12:35

Learn to split a UI into well-defined components by balancing size and responsibilities. Apply criteria like logical separation, reusability, and complexity, starting big and refining into smaller, reusable parts.  
学会将用户界面分解为多个明确的组件，同时考虑组件的大小与功能之间的平衡。遵循诸如逻辑分离、可重用性以及复杂度控制等原则，从大型的组件开始，逐步将其拆分为更小、更可重用的部分。

#### Splitting Components in Practice  
在实践中拆分组件23:57

Split a monolithic React component into reusable, focused parts by extracting nav bar, search, and content boxes, lifting state and passing props to create maintainable, modular UI.  
通过将复杂的 React 组件拆分为可复用的部分，比如分离导航栏、搜索框和内容区域，同时提取状态数据并通过传递参数来实现模块化的 UI 设计，从而让应用程序更易于维护。

#### Component Categories  组件分类2:42

Explore the three core React component categories—stateless or presentational, stateful, and structural—and see how props, state, and composition shape scalable applications.  
了解 React 核心组件的三大类别：无状态的呈现组件、有状态的组件以及结构型组件。了解如何通过属性、状态以及组件组合来构建可扩展的应用程序。

#### Prop Drilling  钻探作业7:31

Explore prop drilling by lifting state to the App component and passing it through Main and ListBox to MovieList, illustrating the problem and hinting at component composition as a fix.  
通过提升“状态”组件到“App 组件”中，并将其传递给“Main”和“ListBox”组件，然后再传递给“MovieList”组件来探索 prop drilling 的运作机制。同时，说明存在的问题，并提示可以将组件组合使用作为解决方案。

#### Component Composition  组件组合4:50

Explore component composition to build reusable modal and message components using the children prop, solving prop drilling and enabling flexible, scalable layouts.  
通过利用`children`属性来构建可复用的模态框和消息组件，从而解决组件复用的问题。同时，这种设计方式还能避免属性传递的繁琐过程，并使得布局更加灵活和可扩展。

#### Fixing Prop Drilling With Composition (And Building a Layout)  
通过组合方式修复 prop 的 drilling 问题（并构建相应的布局）8:49

Fix prop drilling through component composition by using children and moving the movies prop into the app component, enabling direct data flow to NumResults and the movies list.  
通过使用`children`元素来修复组件结构中的属性绑定问题，将电影相关的属性移动到应用组件中，从而实现数据与`NumResults`以及电影列表的直接连接。

#### Using Composition to Make a Reusable Box  
通过组合方式制作可重复使用的盒子6:30

Learn to use composition to solve prop drilling and build a reusable box component with a children prop, isOpen state, and conditional rendering for cleaner layouts and clearer app structure.  
学习如何使用组合来解决问题域穿透，并构建一个具有子组件、isOpen 状态和条件渲染的可重用框组件，以实现更清晰的布局和更明确的应用程序结构。

#### Passing Elements as Props (Alternative to children)  
将元素作为道具使用（替代儿童角色）4:44

Compare the children prop with an explicit element prop to pass JSX into a box component, noting React Router uses this pattern while children remains preferred.  
将儿童组件与包含显式元素的组件进行比较，后者可以将 JSX 传递给盒子组件。需要注意的是，React Router 采用了这种模式，而儿童组件则仍然更受青睐。

#### Building a Reusable Star Rating Component  
构建一个可重复使用的星级评分组件11:22

Learn to build a reusable star rating component in React that supports a configurable max rating, hover preview, click to set, and default props via destructuring.  
学习如何在 React 中构建一个可重用的星级评分组件，该组件支持可配置的最大评分、悬停预览、点击设置以及通过解构传递的默认属性。

#### Creating the Stars  创造明星13:04

Build a dynamic star rating component in React, using useState to track rating, handle clicks, and render full or empty stars via an SVG icon and accessibility role.  
在 React 中构建一个动态星级评分组件。使用 useState 函数来管理评分状态，处理点击操作，并通过 SVG 图标以及可访问性功能来显示全星或空星。

#### Handling Hover Events  处理悬停事件7:43

Learn to implement hover-based star rating by tracking a temporary rating on mouse enter and resetting on mouse leave, updating the UI without altering the actual rating.  
学习如何实现基于悬停效果的星级评分功能。当用户点击鼠标进入时记录一个临时评分，在鼠标离开时重置该评分，同时更新用户界面，而不会改变实际的评分数值。

#### Props as a Component API  
作为组件 API 的道具4:28

Think of each component as a creator and consumer, and define its public API by exposing the right props for a simple, flexible weather component.  
将每个组件都视为一个“创造者”和“消费者”，并通过提供适当的属性来定义其公共 API，从而构成一个简单且灵活的天气组件。

#### Improving Reusability With Props  
通过添加道具来提高资源的可重复使用性19:27

Define a flexible public interface for the star rating component with color and size props, plus default values. Enable state updates via onSetRating and allow styling through an optional className.  
为星级评分组件定义一个灵活的公共接口，该接口包含颜色和尺寸属性，以及默认值。通过 onSetRating 方法实现状态更新，并允许通过可选的 className 属性进行样式定制。

#### PropTypes  属性类型7:24

Learn to implement prop-types for type checking in a reusable React star rating component by importing prop-types and using validators such as number, string, array, func, and isRequired with defaults.  
学习如何通过使用 prop-types 来进行类型检查，从而实现一个可复用的 React 星级评分组件。只需导入 prop-types，并使用诸如 number、string、array、func 等验证器，以及具有默认值的 isRequired 等功能。

#### CHALLENGE #1: Text Expander Component  
挑战#1：文本扩展组件18:20

Build a reusable text expander in React that toggles collapsed and expanded text with a button, using props like children, collapsed word count, and button text.  
请使用 React 构建一个可重复使用的文本展开工具。用户可以通过一个按钮来切换文本的折叠和展开状态。该工具需要利用诸如 children、折叠状态下的文本长度以及按钮文本等 props。


### React 是如何在幕后工作的  
18 场讲座 • 2 小时 38 分钟

#### Section Overview  部分概述1:55

Discover how react works internally with a deep dive behind the scenes, boosting your confidence and interview readiness to place you among the top 5% of react developers.  
深入了解 React 内部的运作机制，掌握其背后的原理。这将增强你的信心，让你在面试中脱颖而出，成为最优秀的 React 开发者中的佼佼者。

#### Project Setup and Walkthrough  
项目设置与操作指南11:11

Copy the starter how-react-works project, install dependencies with npm install, and run npm start to explore a prebuilt React app and its tabbed content workflow.  
复制“how-react-works”项目，使用 npm install 安装依赖库，然后运行 npm start 来体验预构建的 React 应用程序及其分标签页的内容工作流程。

#### Components, Instances, and Elements  
组件、实例和元素  预览版5:09

Understand how a React component serves as a blueprint that returns elements via JSX, creates multiple instances, and how each instance maintains state and props to render DOM.  
了解 React 组件如何作为一个蓝图，通过 JSX 返回元素，创建多个实例，每个实例都维护着状态和属性，从而渲染 DOM。

#### Instances and Elements in Practice  
实践中的实例与要素7:53

Inspect how React creates component instances and elements from JSX and props. Avoid calling components directly, as that breaks state and hooks and alters the component tree.  
了解 React 是如何从 JSX 代码和属性中创建组件实例和元素的。尽量避免直接调用组件，因为这会破坏状态管理机制和钩子功能，还会改变组件树的结构。

#### How Rendering Works: Overview  
渲染原理：概述6:55

Explore how React renders applications behind the scenes, from component trees and React.createElement calls to DOM updates during the render and commit phases. Understand initial and re-renders and batching.  
了解 React 如何在幕后渲染应用程序，从组件树到 React.createElement 调用，再到渲染和提交阶段对 DOM 的更新。掌握初次渲染和重新渲染机制以及批量处理的方法。

#### How Rendering Works: The Render Phase  
渲染原理：渲染阶段18:28

Explore how the render phase processes re-renders by building a new virtual dom, reconciling with the fiber tree, and producing a practical diffing workflow to update the dom efficiently.  
了解渲染阶段是如何处理重新渲染的过程的。通过创建新的虚拟 DOM、与 Fiber 树进行同步，以及制定高效的更新 DOM 的工作流程，我们可以有效地实现 DOM 的更新。

#### How Rendering Works: The Commit Phase  
渲染过程的工作原理：提交阶段11:27

Explore how rendering produces a list of DOM updates in the render phase, then commits them synchronously in the commit phase using React DOM, fiber, and diffing.  
了解渲染过程是如何生成一系列 DOM 更新的，然后在提交阶段通过 React DOM、Fiber 技术以及差异分析来同步地将这些更新付诸实施。

#### How Diffing Works  “Diffing”的工作原理是什么5:52

Learn how the React diffing algorithm under reconciliation updates the dom by comparing tree positions, preserving state for identical elements and rebuilding when types change, with keys controlling new instances.  
学习 React 的 diff 算法如何在 reconciliation 过程中通过比较树的位置来更新 DOM，保留相同元素的 state，并在类型变化时重建，同时使用 keys 控制新实例。

#### Diffing Rules in Practice  
实践中的差异规则5:13

Examine how diffing rules decide whether tab content stays in the DOM or is replaced, preserving or resetting state like text visibility and likes.  
了解差分算法是如何决定标签页的内容是保留在 DOM 中还是被替换的，以及如何处理诸如文本可见性、点赞数等状态信息的保留或重置问题。

#### The Key Prop  核心主张7:41

Explore how the key prop guides React's diffing by uniquely identifying list items, keeping stable keys for performance, and changing keys to reset component state.  
了解关键属性如何帮助 React 实现状态切换：通过唯一标识列表项来保持稳定的键值，从而提升性能；同时，通过更改键值来重置组件状态。

#### Resetting State With the Key Prop  
通过“密钥属性”来重置状态4:02

Apply the key prop to tab content to force a new React instance on each render, resetting state when the key changes and ensuring proper behavior across tabs.  
将这个键属性应用到标签内容上，这样在每次渲染时都会创建一个新的 React 实例，当键发生变化时，状态也会重置，从而确保各个标签之间的行为一致。

#### Using the Key Prop to Fix Our Eat-'N-Split App  
利用关键属性来修复我们的“吃并分割”应用程序问题4:07

Fix a state reset issue in the Eat-'N-Split app by assigning a changing key to the FormSplitBill component using selectedfriend.id, ensuring a new instance on each friend change.  
请解决 Eat-'N-Split 应用程序中的状态重置问题。为此，为 FormSplitBill 组件分配一个变量 changeKey，使用 selectedfriend.id 作为该变量的值。这样，每当好友信息发生变化时，就会创建一个新的 FormSplitBill 实例。

#### Rules for Render Logic: Pure Components  
渲染逻辑的规则：纯组件9:24

Explore how render logic and event handlers differ in React components, and why render logic must be pure, avoiding side effects and external mutations, with useEffect handling effects.  
了解在 React 组件中，渲染逻辑与事件处理器的区别。同时了解为什么渲染逻辑必须保持纯净，避免产生副作用和外部影响，以及如何通过 useEffect 函数来处理副作用问题。

#### State Update Batching  状态更新批处理9:10

Explore how React 18 enables automatic batching of multiple setState calls into a single render and why updates are asynchronous with useState.  
了解 React 18 如何实现将多个 setState 调用自动合并为一次渲染操作，以及为什么使用 useState 函数进行状态更新时会产生异步效果。

#### State Update Batching in Practice  
实践中对状态更新的批处理处理15:43

Shows how React batches state updates, uses an undo button to reset showDetails and likes, and explains asynchronous updates and the use of a callback for state changes.  
这段代码展示了 React 如何批量更新状态，如何使用撤销按钮来重置“showDetails”和“likes”功能，以及如何处理异步更新以及如何使用回调函数来执行状态变更。

#### How Events Work in React  
在 React 中，事件是如何处理的？13:42

Explore how events work in React, including propagation, capturing and bubbling, and React's event delegation to the root container with synthetic events. Compare camelCase props and preventDefault usage.  
了解 React 中事件的运作机制，包括事件的传播、捕获机制以及 React 对合成事件的委托处理方式。同时，比较一下 camelCase 属性命名方式和 preventDefault 方法的用法。

#### Libraries vs. Frameworks & The React Ecosystem  
库与框架之争：React 生态系统探析9:52

Understand why React is a library, not a framework, and how its ecosystem of third-party libraries shapes tool choices for routing, styling, and state management.  
理解为什么 React 是一个库而不是一个框架，以及它的第三方库生态系统如何影响路由、样式和状态管理的工具选择。

#### Section Summary: Practical Takeaways  
部分总结：实用要点10:26

Explore practical takeaways on how components serve as blueprints, how rendering and committing work, how diffing and keys optimize updates, and how batching and events power React apps.  
了解有关组件如何充当蓝图、渲染与提交工作、差异比较与键值如何优化更新流程，以及批量处理与事件如何驱动 React 应用程序运行的实用信息。


### 效果与数据获取  
19 次讲座 • 3 小时 13 分钟

#### Section Overview  部分概述0:58

Master data fetching in React apps by using the useEffect hook to manage side effects, load external data, and clean up effects while advancing the usePopcorn project.  
在 React 应用中通过 useEffect 钩子来管理副作用、加载外部数据，并在使用过程中清理资源。这一方法被应用于 Popcorn 项目的开发过程中。

#### The Component Lifecycle  组件的生命周期4:31

Explore the component lifecycle, from mounting and initial render to re-render on prop or state changes and unmounting, and learn how useEffect hooks into these phases.  
探索组件生命周期，从挂载和初始渲染到因属性或状态变化而重新渲染以及卸载，并学习 useEffect 钩子如何融入这些阶段。

#### How NOT to Fetch Data in React  
如何在 React 中避免错误的数据获取方式9:48

Learn why fetching data in render on mount breaks React, using the OMDb API and fetch to illustrate infinite re-renders; recognize that useEffect will manage data loading and side effects.  
了解为什么在挂载过程中获取数据会导致 React 出现崩溃，通过 MDb API 和 fetch 技术来演示无限重新渲染的情况；同时认识到 useEffect 函数可以处理数据加载和副作用问题。

#### useEffect to the Rescue  useEffect 来拯救我们啦！4:40

Learn how the useEffect hook handles side effects in React, including data fetching on mount, using a dependency array to control execution, and preventing infinite loops.  
了解 useEffect 钩子在 React 中的处理方式，包括在组件挂载时进行数据获取、使用依赖数组来控制执行流程，以及如何避免无限循环等问题。

#### A First Look at Effects  
对效果的初步了解6:23

Use the useEffect hook to fetch data when a component mounts and manage side effects, contrasting this with event handlers that respond to user actions and noting the dependency array.  
使用 useEffect 钩子在组件挂载时获取数据，并管理副作用操作。这与那些响应用户操作的事件处理程序有所不同，同时需要注意依赖数组的使用。

#### Using an async Function  使用异步函数5:40

Learn how to convert a React effect to an async function using async/await, handle data fetching and state updates, and understand strict mode's double-invocation in development.  
了解如何使用 async/await 将 React 效果转换为异步函数，处理数据获取和状态更新问题，并理解在开发过程中严格模式下的双重调用方式。

#### Adding a Loading State  添加加载状态3:47

Add a loading state with isLoading to display a Loader while movie data loads, then render the movie list once loading completes, demonstrated under a simulated slow 3G network.  
添加一种加载状态，在电影数据加载过程中显示“加载中”提示，然后一旦加载完成就渲染电影列表。这一效果在模拟的慢速 3G 网络环境下可以观察到。

#### Handling Errors  处理错误13:02

Learn to handle fetch errors in React apps by implementing try-catch, error state, and conditional rendering to show clear messages when offline or no data, ensuring loading states resolve.  
通过实现 try-catch 机制、错误状态处理功能，以及根据是否处于离线状态或数据不足情况来显示相应的提示信息，从而学会如何处理 React 应用程序中的获取错误情况。同时，确保加载状态能够正常解决。

#### The useEffect Dependency Array  
useEffect 依赖数组12:17

Learn how the useEffect dependency array controls when an effect runs, based on state and props, and how mount, update, and empty dependency array cases differ.  
了解 useEffect 依赖数组如何根据状态和属性来决定效果何时执行，以及 mount、update 和 empty 三种依赖数组的情况有何不同。

#### Synchronizing Queries With Movie Data  
利用电影数据同步查询语句15:08

Lift the search query state into the app, synchronize it with movie data through useEffect dependencies, and optimize by guarding empty or short queries.  
将搜索查询状态引入应用程序中，通过 useEffect 依赖项将其与电影数据同步处理，并通过避免处理空查询或短查询来优化性能。

#### Selecting a Movie  选择电影13:19

Select a movie to load its details using the selectedId in the parent app, fetch by id, and display in a movie details view with a back button to close.  
从父应用中通过 selectedId 选择一部电影以加载其详细信息，通过 id 获取，并在带有返回按钮的电影详情视图中显示。

#### Loading Movie Details  加载电影信息中…17:43

Fetch movie details for the selected id when the id changes; show a loading indicator and render title, poster, runtime, IMDB rating, plot, and actors with a star rating.  
当所选的 ID 发生变化时，请获取该电影的详细信息；同时显示加载指示器，并渲染标题、海报、上映时间、IMDB 评分、剧情简介以及演员信息及星级评分。

#### Adding a Watched Movie  添加已观看的电影26:11

Build and manage a dynamic watched movies list in React by adding items with a user rating, preventing duplicates, and removing items, while updating live statistics and derived state.  
在 React 中构建和管理一个动态的已观看电影列表，通过添加带用户评分的项目、防止重复并删除项目，同时更新实时统计数据和派生状态。

#### Adding a New Effect: Changing Page Title  
添加新的效果：更改页面标题8:44

Update the browser tab title in the movie detail component to reflect the currently watched movie using useEffect, handling undefined titles and the dependency array.  
使用 useEffect 函数来更新电影详情组件中的浏览器标签标题，使其反映当前观看的电影名称。同时，需要处理 undefined 标题的情况，以及依赖数组的处理。

#### The useEffect Cleanup Function  
useEffect 清理函数4:20

Explore how to use the useEffect cleanup function to restore the page state on unmount and before re-runs, preventing race conditions and ensuring cleanup of requests, subscriptions, and timers.  
了解如何使用 useEffect 清理函数，在组件卸载后以及再次渲染之前恢复页面状态。这样可以避免竞态条件的问题，并确保请求、订阅和计时器的清理工作能够顺利进行。

#### Cleaning Up the Title  清理标题的混乱状态5:22

Use a cleanup function from an effect to reset document.title when a component unmounts. See how closures remember the title and how cleanup runs between renders as you switch movies.  
在组件卸载时，可以使用效果中的清理函数来重置 document.title 的值。了解闭包如何记住标题的值，以及如何在不同的渲染过程中执行清理操作，从而切换电影时保持数据的连贯性。

#### Cleaning Up Data Fetching  
清理数据获取过程10:16

Learn to clean up data fetching by canceling previous requests with the abort controller, preventing race conditions and ensuring only the latest search results render in the UI.  
学会通过使用“abort”控制器来取消之前的请求，从而清理数据获取过程。这样就能避免竞争条件的问题，并确保只有最新的搜索结果才会显示在用户界面上。

#### One More Effect: Listening to a Keypress  
另一个效果：倾听关键的声音14:06

Implement a global keypress listener to close the movie details with the Escape key, using useEffect, proper cleanup, and dependency handling to prevent multiple event listeners.  
实现一个全局按键监听器，使用 Escape 键关闭电影详情，使用 useEffect、正确的清理和依赖处理来防止多个事件监听器。

#### CHALLENGE #1: Currency Converter  
挑战 1：货币兑换器16:43

Build a real-time currency converter in React with controlled inputs and useEffect, fetching live rates and updating on amount or currency changes, with a loading state that disables inputs.  
使用 React 构建一个实时货币转换器。用户可以通过控制输入来修改数值，利用 useEffect 函数获取实时汇率，并在数值或货币类型发生变化时更新显示结果。同时，系统会有一个加载状态，在加载期间会禁用输入字段的显示。


### 自定义钩子、反射机制以及其他状态设置  
15 次讲座 • 2 小时 2 分钟

#### Section Overview  章节概述0:57

Dive deeper into React hooks and build a solid understanding of their rules and why they exist. Explore useState and useRef, create your own hooks, and continue building the project.  
深入了解一下 React 的 Hook 功能，全面了解它们的规则以及为何存在这样的设计。尝试使用 useState 和 useRef，自己创建 Hook 函数，并继续开发这个项目。

#### React Hooks and Their Rules  
React Hooks 及其使用规则10:55

Explore React hooks like useState, useEffect, useReducer, useContext, and custom hooks, and learn the two rules—top-level calls and calling only from React components—and why call order matters.  
了解 React 的钩子函数，如 useState、useEffect、useReducer、useContext 等，以及自定义钩子。同时掌握两条规则：只能从 React 组件内部调用高阶函数，以及调用顺序的重要性。

#### The Rules of Hooks in Practice  
《钩子法则》在实践中的应用8:36

Demonstrates why hooks must be called at the top level by testing conditional calls and early returns, revealing hook-order errors and the role of ESLint in preventing them.  
通过测试条件式调用的执行情况以及早期返回的情况，展示了为什么必须在最高级别调用钩子函数。这揭示了钩子函数的调用顺序问题，以及 ESLint 在预防这些问题的中的作用。

#### More Details of useState  更多关于 useState 的信息12:30

Explore the useState hook in depth: initial values matter only on the initial render, learn derived state, and manage asynchronous updates with callbacks and useEffect.  
深入探索 useState 钩子：初始值仅在首次渲染时才有意义，了解派生状态的概念，以及如何通过 callback 和 useEffect 来处理异步更新。

#### Initializing State With a Callback (Lazy Initial State)  
通过回调函数来初始化状态（懒加载状态）13:30

store watch list in local storage and update it whenever the list changes. initialize state lazily with a callback that reads from local storage on mount and parses json.  
将列表存储在当地存储中，每当列表发生变化时就会更新该列表。通过回调函数来惰性初始化状态，该回调函数会在读取当地存储的数据时进行解析，并将解析后的数据转换为 JSON 格式。

#### useState Summary  状态使用总结3:14

Master the useState hook to create and update state, including lazy evaluation with a pure no-arg initializer, and update via setter using new objects or arrays instead of mutating.  
掌握 useState 钩子，用于创建和更新状态。包括使用纯无参数初始化的懒加载方式，以及通过设置器来更新状态，可以选用新的对象或数组来进行更新，而无需进行 mutable 操作。

#### How NOT to Select DOM Elements in React  
如何在 React 中错误地选择 DOM 元素3:38

Learn why manual DOM selection contradicts React's declarative approach and discover how refs, via hooks like useEffect, provide a clean way to focus an input on component mount.  
了解为什么手动操作 DOM 选择与 React 的声明式设计理念相矛盾，同时了解如何通过像 useEffect 这样的钩子函数来优雅地让组件在挂载时自动聚焦某个输入框。

#### Introducing Another Hook: useRef  
介绍另一个钩子：useRef5:36

Discover the useRef hook: create a mutable ref with a current property that persists across renders for non-visual data and DOM element references, and compare refs with state.  
了解 useRef 钩子：它可以创建一个可变的引用，该引用中的属性会在不同的渲染过程中保持有效，适用于非视觉数据以及 DOM 元素的引用。同时，也可以比较带有状态的 ref 的使用方式。

#### Refs to Select DOM Elements  
指向特定 DOM 元素的引用10:09

Use the useRef hook to attach a ref to a DOM input via the ref prop, focus in useEffect, and handle Enter to clear the query with proper dependencies.  
使用 useRef 钩子通过 ref 属性将一个 ref 值绑定到 DOM 中的输入框上。在 useEffect 函数中处理焦点事件，并当按下 Enter 键时清除查询结果，同时确保使用正确的依赖项。

#### Refs to Persist Data Between Renders  
在渲染过程中保持数据不变的机制10:15

Use refs to persist data across renders without triggering re-renders. Track rating decisions behind the scenes with a countRef and useEffect, updating countRef.current and saving countRatingDecisions.  
使用 refs 可以在不触发重新渲染的情况下保持数据在多次渲染中的连续性。可以通过 countRef 和 useEffect 在幕后跟踪评分决策，更新 countRef.current 的值，并保存 countRatingDecisions 的数据。

#### What are Custom Hooks? When to Create One?  
什么是自定义钩子？什么时候应该创建自定义钩子呢？4:22

Explore custom hooks to reuse stateful and non-visual logic across components, learn when to create one, name it with use, and leverage libraries on npm.  
探索自定义钩子以在组件间重用有状态和非可视化逻辑，学习何时创建一个，用 use 命名它，并利用 npm 上的库。

#### Creating our First Custom Hook: useMovies  
创建我们的第一个自定义钩函数：useMovies13:02

learn to create a reusable custom hook useMovies by extracting the movie data fetch logic from a component, returning movies, isLoading, and error as a clean public API.  
学习如何创建一个可重用的自定义钩子 useMovies，通过从组件中提取电影数据获取逻辑，将其作为干净的公共 API 返回电影、isLoading 和 error。

#### Creating useLocalStorageState  
创建 useLocalStorageState 的使用方式9:06

Create a reusable useLocalStorageState hook that mirrors useState while persisting state to local storage. It covers a generic key, initial state, and syncing with local storage via useEffect.  
创建一个可复用的`useLocalStorageState`钩子函数，该函数能够模仿`useState`的功能，同时还能将状态持久化到本地存储中。该钩子包含了一个通用的键、初始状态，以及通过`useEffect`函数实现与本地存储的同步操作。

#### Creating useKey  创建 useKey 的使用方式8:19

Create a reusable useKey hook to attach and remove key press handlers, normalize keys to lowercase, and support escape to close a movie and enter to focus the search bar.  
创建一个可重用的 useKey 钩子，用于附加和移除按键处理程序，将按键标准化为小写，并支持使用 Esc 键关闭电影和 Enter 键聚焦搜索栏。

#### CHALLENGE #1: useGeolocate  
挑战#1：使用 Geolocate 功能7:27

Create a reusable useGeolocation hook that encapsulates geolocation logic to fetch the user’s position, exposing isLoading, position, error, and getPosition for any app, with an OpenStreetMap link example.  
创建一个可复用的 geolocation 钩子函数，该函数封装了获取用户位置的地理定位逻辑。该函数会提供 isLoading、position、error 以及 getPosition 这几个方法，方便任何应用程序使用。同时，该钩子函数还会包含一个使用 OpenStreetMap 链接的示例。


### [可选] 在使用 Hooks 之前先使用 React 类组件：基于类的 React 架构  
10 次讲座 • 1 小时 22 分钟

#### Section Overview  章节概述1:16

Explore class-based React in this optional section, including lifecycle methods and legacy patterns, to understand older codebases and how to convert class components to function components.  
在这个可选的部分中，我们将探讨基于类的 React 实现。会涉及生命周期方法以及旧的编程模式，以便更好地理解旧的代码结构，以及如何将基于类的组件转换为基于函数的组件。

#### Our First Class Component  
我们的第一级组件9:32

Build your first React class component by creating a Classy Weather app with Create-React-App, then manage state via constructor and this.state, and compare class components to hooks.  
通过使用 Create-React-App 创建一个 Classy Weather 应用，构建你的第一个 React 类组件，然后通过构造函数和 this.state 管理状态，并与钩子进行比较。

#### Working With Event Handlers  
使用事件处理程序10:24

Demonstrates adding onClick handlers in class components, binding this in the constructor, and updating state with setState via current-state callbacks, contrasting class and function components for state management.  
展示了如何在类组件中添加 onClick 处理程序，如何在构造函数中绑定这些处理程序，以及如何通过当前状态回调使用 setState 来更新状态。同时，还对比了类组件和函数组件在状态管理方面的差异。

#### Class Components vs. Function Components  
类组件与函数组件5:37

Compare function components and class components in React, highlighting hooks, useState, useEffect, props, lifecycle, and custom hooks, and explain why function components offer cleaner code and easier sharing of logic.  
在 React 中，需要比较函数组件和类组件。要重点介绍 hooks、useState、useEffect、props、生命周期以及自定义 hooks 等概念。同时，需要解释为什么函数组件能提供更简洁的代码，并且更容易共享逻辑。

#### Starting the "Classy Weather" App  
启动“经典天气”应用程序7:45

Duplicate and rewrite the Classy Weather app as a React class component, implement a controlled location input, a get weather button, and bind fetchWeather to manage state.  
将 Classy Weather 应用程序进行复制和重写，使用 React 类组件来实现。需要实现一个可控的位置输入功能，以及一个获取天气信息的按钮。同时，将 fetchWeather 函数绑定到状态管理中，以处理天气信息的更新。

#### Fetching Weather Data  获取天气数据9:39

Fetches weather data for a user-entered location by geocoding to coordinates, calls open-meteo, manages an async loading state, and updates the UI with display location and daily weather.  
通过地理编码将用户输入的位置数据转换为具体坐标，调用开放天气服务获取天气信息，实现异步加载功能，并更新用户界面以显示当前位置及每日天气情况。

#### Displaying the Weather  显示天气14:10

Build a weather component and a day subcomponent to render weather data from state as props, conditionally display when data exists, and format day with get weather icon.  
构建一个天气组件和一个日子的子组件，将天气数据作为属性传递；当数据存在时，会条件性地显示这些数据；同时，使用天气图标来格式化日期显示。

#### Removing Boilerplate Code With Class Fields  
使用类字段移除样板代码4:05

Use JavaScript class fields to remove boilerplate in React class components, declaring properties on the class and using arrow functions to preserve this binding, noting linting benefits and functional components.  
使用 JavaScript 的类字段来消除 React 类组件中的冗余代码，在类中声明属性，并利用箭头函数来保持这种绑定关系。同时，要注意代码检查带来的好处，以及函数组件的应用。

#### Child to Parent Communication  
子组件与父组件通信4:19

Explore child-to-parent communication in react by extracting input into its own component and passing a location updater via props to update the parent state.  
在 React 中探索儿童与父母之间的沟通方式。通过将输入数据提取到自己的组件中，并通过 props 传递一个位置更新器，从而更新父组件的状态。

#### Lifecycle Methods  生命周期方法15:36

Employ the three key class lifecycle methods—component did mount, component did update, and component will unmount—to enable weather search as you type and save location in local storage.  
使用这三个关键的类生命周期方法——组件挂载、组件更新和组件卸载——来实现在用户输入地址并保存到本地存储时，能够实时搜索相关结果。


##   第三部分：高级 React 系统与减省设计
[4 个项目]  
2 次讲座 • 2 分钟

#### Introduction to Part 3  第三部分简介1:11

Advance your React skills to expert level by mastering Context API. Explore Redux, useReducer, four projects, a real world spa built with Vite and React Router, plus useMemo and useCallback.  
通过掌握 Context API 将您的 React 技能提升至专家水平。探索 Redux、useReducer，完成四个项目，一个使用 Vite 和 React Router 构建的实时世界 spa，以及 useMemo 和 useCallback。

#### Useful Resources for Part 3  
第三部分的有用资源0:46


### 高级的 useReducer 钩子函数  
17 次讲座 • 3 小时 15 分钟

#### Section Overview  章节概述1:03

Master the advanced useReducer hook to manage state and pave the way to Redux, then apply these skills by building an entire project in this section.  
掌握高级的 useReducer 钩子来管理状态，并为使用 Redux 奠定基础，然后在这一部分通过构建整个项目来应用这些技能。

#### Yet Another Hook: useReducer  
另一个钩子：useReducer17:58

Explore replacing useState with useReducer by introducing a reducer function, actions, and dispatch to manage a counter. Learn to structure actions with type and payload to set, increment, or decrement.  
尝试通过使用 reducer 函数、动作以及调度机制来替代 useState，从而实现对计数器的管理。学习如何根据类型和使用的数据 payload 来构造各种动作，以执行设置、增加或减少计数器的操作。

#### Managing Related Pieces of State  
管理相关的状态片段12:23

Learn to manage related pieces of state with a reducer by defining an initial state, using a switch-based reducer, and dispatching actions to update count and step.  
通过定义初始状态、使用基于开关的 reducer 机制，以及执行更新计数和步长的操作，学会如何管理相关的状态部件。

#### Managing State With useReducer  
使用 useReducer 管理状态14:13

Learn how useReducer centralizes state logic in a reducer, using dispatch actions with types and payloads to produce immutable, pure state updates for complex components.  
了解如何使用 Reducer 来集中管理状态逻辑。通过结合特定类型的动作及其负载，可以实现不可变、纯净的状态更新，从而为复杂的组件提供优化方案。

#### The "React Quiz" App  "React Quiz"应用7:57

Build the React quiz app using a single useReducer-based state to load 50 questions from a fake API, track progress and points, and manage a timer.  
使用基于单一 useReducer 的状态管理结构来构建这个 React 测验应用程序。从虚构的 API 中加载 50 道题目，跟踪用户的进度和得分，同时管理计时器功能。

#### Loading Questions from a Fake API  
正在加载来自伪版权的 API 的问题信息18:31

Set up a fake API with json-server to load quiz questions, fetch data on mount with useEffect, and manage state using useReducer with a status workflow.  
使用 json-server 创建一个假的 API，用于加载测验问题。在组件挂载时使用 useEffect 来获取数据，并通过 useReducer 来管理状态，同时实现状态切换的功能。

#### Handling Loading, Error, and Ready Status  
处理加载、错误以及就绪状态9:05

Manage loading, ready, and error statuses in a React app by destructuring state and rendering loader, error, or start screen components, and derive number of questions from the questions array.  
通过解构状态来管理 React 应用程序中的加载、准备就绪和错误状态。同时，可以渲染加载器、错误提示或主界面组件。此外，还可以从问题数组中得出问题的数量。

#### Starting a New Quiz  开始制作新的测验4:54

Implement a quiz start flow by dispatching a start action to set status to active, which renders the first question and advances the UI through loading, error, and question states.  
通过执行一个启动操作来实现测验的启动流程，该操作会将状态设置为“活动状态”，然后会显示第一个问题，并让用户界面经过加载、出错和问题呈现等多个阶段。

#### Displaying Questions  展示问题7:45

Use an index state to show the current question from a questions array and render its options as buttons by mapping question.options with unique keys in a components folder.  
使用一个索引状态来显示问题数组中的当前问题，并将该问题的选项作为按钮呈现出来。这可以通过在组件文件夹中将 question_options 映射到唯一的键来实现。

#### Handling New Answers  处理新答案16:42

Learn to handle a newAnswer in a React app with the useReducer hook, dispatch newAnswer actions to set the selected option, update points, and reveal the next button.  
学习如何使用 useReducer 钩子来处理 React 应用程序中的新答案。可以触发新的答案操作来设置所选选项、更新积分，并展示下一个按钮。

#### Moving to the Next Question  
进入下一题6:22

Use useReducer to manage quiz state, dispatch nextQuestion to advance index, reset answer, and render a next button after an answer; track points and progress.  
使用 useReducer 来管理测验状态，通过调用 dispatch 函数来推进索引，重置答案，并在用户回答后显示下一个按钮；同时跟踪得分和进度。

#### Displaying Progress  显示进度8:57

Build a reusable Progress component that displays the current question (index), total questions, and points with a progress bar, deriving max points from the questions array.  
构建一个可重用的进度组件，该组件显示当前问题（索引）、总问题数和积分，并带有进度条，从问题数组中派生最大积分。

#### Finishing a Quiz  完成测验15:07

Finish a quiz by moving to a finished status and showing a finish screen with score, max points, and percentage; compute and store a high score on finish.  
完成测验，移动到完成状态，并显示包含分数、最高分和百分比的完成屏幕；在完成时计算并存储高分。

#### Restarting a Quiz  重新启动测验7:05

Build a restart quiz feature by dispatching a restart action to reset points, high score, index, and answer, while preserving questions.  
构建一个重启测验功能，通过执行重启操作来重置分数、最高分、索引值以及答案，同时保留已有的问题。

#### Setting Up a Timer With useEffect  
使用 useEffect 来设置计时器20:56

Build a timer with useEffect that advances seconds remaining every second using setInterval and a reducer, finishing the game when time reaches zero, with cleanup to stop timers on unmount.  
使用一个带有 useEffect 函数的计时器来实现每秒更新剩余时间的功能。通过 setInterval 方法定期更新时间，当时间达到零时结束游戏。在卸载组件时，需要清理计时器以避免资源泄漏。

#### Section Summary: useState vs. useReducer  
章节总结：useState 与 useReducer 的区别5:53

Explore when to use useState versus useReducer by comparing single-piece versus related or complex state, dispatching actions, and centralizing update logic with a reducer, using the startGame action.  
通过对比单个状态与相关或复杂状态的处理方式，探讨何时使用 useState 函数，何时使用 useReducer 函数。同时，了解如何通过 reducer 来集中管理更新逻辑，以使用 startGame 动作为例进行说明。

#### CHALLENGE #1: Creating a Bank Account With useReducer  
挑战 1：使用 useReducer 创建银行账户19:50

Use useReducer to build a bank account with actions open account, deposit, withdraw, request loan, pay loan, and close account, tracking balance of 500, loan, and active state with guards.  
使用 useReducer 来构建一个银行账户组件，包含存款、取款、申请贷款、偿还贷款以及关闭账户等功能。同时，还需要跟踪账户余额、贷款状态，以及是否处于活动状态等信息，这些状态需要通过 guards 来控制。


### React Router：构建单页应用程序（SPA）  
17 次讲座 • 2 小时 59 分钟

#### Section Overview  章节概述1:01

Build a real-world single-page application using React Router to create fluid multi-page experiences, styled with CSS modules, through a hands-on Worldwide project.  
通过实践项目，使用 React Router 构建一个真正的单页应用程序。通过 CSS 模块进行样式设计，打造流畅的多页体验。

#### Creating Our First App With Vite: "WorldWise"  
使用 Vite 创建我们的第一个应用程序：“WorldWise”15:41

Create the WorldWise app with Vite, exploring a single-page React project featuring routing, a map with geolocation, city data, and notes, then configure ESLint for the dev workflow.  
使用 Vite 创建 WorldWise 应用程序。构建一个单页的 React 项目，包含路由功能、基于地理位置的地图、城市数据以及笔记功能。然后配置 ESLint 以优化开发流程。

#### Routing and Single-Page Applications (SPAs)  
路由与单页应用程序（SPAs）6:40

Learn how React Router powers single-page applications by mapping URLs to React components as routes, updating the DOM without full page reloads, and enabling client-side navigation.  
了解 React Router 如何通过将 URL 映射到 React 组件来实现单页应用的功能。它能够在无需完全重新加载页面的情况下更新 DOM，同时支持客户端端的导航操作。

#### Implementing Main Pages and Routes  
实现主页面和路由配置12:15

Implement routing in a React app using React Router v6 to map URLs to home, product, and pricing pages, with a not-found route for seamless navigation.  
在 React 应用中实现路由功能，使用 React Router v6 将 URL 映射到首页、产品和价格页面。同时设置了一个“未找到”的路由，以便用户能够轻松导航到其他页面。

#### Linking Between Routes With <Link /> and <NavLink />  
使用<Link/>和<NavLink/>来连接不同的路线9:57

Create seamless navigation between routes using react router's Link and NavLink, replacing traditional anchors to update the dom without reloads and highlight the active page.  
利用 React Router 中的 Link 和 NavLink 组件，实现路径之间的无缝导航。这样可以替代传统的锚点导航方式，从而在不重新加载页面的情况下更新 DOM 界面，并突出显示当前活跃的页面。

#### Styling Options For React Applications  
React 应用程序的样式选项5:50

Explore the many ways to style React applications, from inline styles and external CSS to CSS modules, CSS in JS, Tailwind, and component libraries.  
探索各种打造 React 应用样式的技巧，包括内联样式、外部 CSS 文件、CSS 模块、JS 中的 CSS、Tailwind 框架，以及各种组件库等。

#### Using CSS Modules  使用 CSS 模块16:30

Learn how css modules work out of the box with create-react-app and vite, creating one css module per component, importing as styles, and using styles.classname to scope styles.  
了解如何使用 create-react-app 和 vite 来实现 CSS 模块的功能。每个组件都可以拥有一个独立的 CSS 模块，也可以将样式导入到相应的模块中。此外，还可以使用 styles.classname 来限定样式的作用范围。

#### Building the Pages  构建页面16:59

Build and wire real application pages in a React course, using React Router and CSS modules to implement the homepage, product, pricing, and login pages, plus a navigable app layout.  
在 React 课程中构建并配置实用的应用程序页面。使用 React Router 和 CSS 模块来实现主页、产品页面、价格页面以及登录页面，同时设计出一个易于导航的应用程序布局。

#### Building the App Layout  构建应用程序的布局8:44

Build the static app layout by organizing starter components into a components folder, creating sidebar and map components with CSS modules, and wiring them into a clean, reusable page structure.  
通过将初始组件组织到 components 文件夹中，使用 CSS 模块创建侧边栏和地图组件，然后将它们整合到一个简洁、可复用的页面结构中，从而构建出静态应用的布局。

#### Nested Routes and Index Route  
嵌套路由和索引路由13:25

Master nested routes in React Router by declaring a parent app route with child routes for cities, countries, and form, rendering via outlet and using an index route for defaults.  
在 React Router 中处理嵌套路由的方法是通过定义一个父应用路由，然后为城市、国家等子路由定义子路由。这些子路由通过 outlet 组件进行渲染，同时还会使用一个默认路由来处理默认情况。

#### Implementing the Cities List  
实施城市列表功能18:42

Load city data from a fake api, render it in the city list, manage loading with a spinner, and display each city item with emoji, date, and an empty-state message.  
从模拟 API 加载城市数据，在列表中渲染，使用加载动画管理加载状态，并显示每个城市项的 emoji、日期和空状态消息。

#### Implementing the Countries List  
实施国家列表11:07

Implement a CountriesList in a React app by deriving unique countries from the visited cities, preventing duplicates with a reducer, and rendering via CountryItem and CountryList components.  
在 React 应用中实现一种`CountriesList`功能，通过从访问过的城市中提取独特的国家信息来生成列表。使用 reducer 来确保没有重复的国家，然后通过`CountryItem`和`CountryList`组件来展示这些国家信息。

#### Storing State in the URL  
在 URL 中存储状态信息5:31

Store ui state in the url using React Router to share, bookmark, and navigate with consistent filters and sorting; use params for city data and query string for global state.  
使用 React Router 将店铺的界面状态存储在 URL 中，以便共享、保存书签，并确保在不同的页面中能够使用相同的过滤和排序功能；可以使用参数来表示城市数据，而查询字符串则用于保存全局状态。

#### Dynamic Routes With URL Parameters  
带有 URL 参数的动态路由10:28

Learn to pass data between pages with React Router params. Create a dynamic city route and read the id with useParams to fetch city data from a fake api.  
学习如何使用 React Router 的参数在页面之间传递数据。创建一个动态的城市路由，并使用 useParams 功能读取 ID 值，从而从虚构的 API 中获取城市数据。

#### Reading and Setting a Query String  
读取并设置查询字符串10:34

Learn to store state in the URL using the query string with React Router's useSearchParams, pass lat and lng via links, and read and share that state across components.  
学习如何使用 React Router 的 useSearchParams 在 URL 中存储状态，通过链接传递 lat 和 lng，并在组件之间读取和共享该状态。

#### Programmatic Navigation with useNavigate  
基于使用的程序化导航功能 useNavigate11:50

Master programmatic navigation with the useNavigate hook to move to a form on map click, navigate back with the navigate function, and build a reusable button component.  
掌握使用 useNavigate 钩子进行程序化导航，在地图点击时跳转到表单，使用 navigate 函数返回，并构建一个可重用的按钮组件。

#### Programmatic Navigation with <Navigate />  
使用<Navigate/>进行程序化导航4:12

Learn how to programmatically navigate with the Navigate component in React Router, including declarative redirects, replacing history entries, and use cases in nested routes.  
了解如何在 React Router 中使用 Navigate 组件进行程序化导航，包括声明式的重定向操作、替换历史记录条目，以及在内嵌路由中的应用实例。


### 高级状态管理：上下文 API  
22 场讲座 • 4 小时 7 分钟

#### Section Overview  章节概述0:59

Explore the context API for advanced state management and apply a powerful pattern to the WorldWise app, including an interactive map.  
探索用于高级状态管理的上下文 API，并将一种强大的模式应用于 WorldWise 应用，包括一个交互式地图。

#### CHALLENGE #1: Understand "The Atomic Blog" App  
挑战 1：了解“The Atomic Blog”应用程序4:22

Explore the atomic blog app by examining starter files, data flow, and prop drilling in a Create React App project, then prepare for context API concepts.  
通过查看 Create React App 项目中的起始文件、数据流以及属性继承机制，来了解原子式博客应用程序的设计原理。接下来，为学习上下文 API 相关概念做好准备。

#### What is the Context API?  
什么是 Context API？5:16

Explore how the context api solves prop drilling by broadcasting a shared value through providers and consumers, enabling deep state access and automatic re-rendering on updates.  
了解上下文 API 如何通过在整个提供者和消费者之间传递共享值来解决属性注入问题，从而实现深度状态访问以及针对更新内容的自动重新渲染。

#### Creating and Providing a Context  
创造并提供背景信息9:26

Refactor the app by replacing prop drilling with the context API, creating a posts context and provider to share posts, search query, and add and clear handlers.  
通过用上下文 API 替代属性传递的方式来重构应用程序。创建一篇帖子的上下文对象以及相关的提供者，以便分享帖子、执行搜索查询，并添加和清除相关处理程序。

#### Consuming the Context  消费上下文10:53

Learn to consume context in React by replacing prop drilling with useContext, reading PostContext across components, and broadcasting posts data through the context provider for cleaner, independent components.  
学会在 React 中利用上下文进行数据传递。通过替换传统的 prop 传递方式，使用 useContext 来管理组件间的上下文关联。同时，可以在不同组件之间共享 PostContext，从而让组件更加简洁且独立地处理数据。

#### Advanced Pattern: A Custom Provider and Hook  
高级模式：一种自定义的提供程序与钩子机制  预览版16:15

Refactor your context usage by introducing a dedicated PostProvider and a usePosts hook to manage and consume context data, ensuring proper provider usage and cleaner components.  
通过引入一个专门的 PostProvider 以及 usePosts 钩子函数来管理上下文数据的使用和获取，从而改进上下文使用的方式。这样能够确保 Provider 的正确使用，并使得组件更加简洁明了。

#### Thinking In React: Advanced State Management  
在 React 中思考：高级状态管理11:38

Explore how the Context API fits into state management, distinguishing local and global, remote and UI state, and comparing useState, useReducer, and Redux for scalable apps.  
了解 Context API 如何在状态管理中发挥作用，区分局部与全局状态、远程与 UI 状态，并比较 useState、useReducer 以及 Redux 在构建可扩展应用程序时的应用。

#### Back to "WorldWise": Creating a CitiesContext  
回到“WorldWise”：创建城市上下文8:32

Create a cities context and provider for the WorldWise app, centralizing cities and isLoading, and expose a custom hook to consume them.  
为 WorldWise 应用程序创建一个城市相关的环境和提供者，将各种城市数据集中起来。同时，提供一个自定义接口来消费这些数据。

#### Consuming the CitiesContext  
消耗城市4:26

Refactor your React app to consume city data via a custom useCities hook and the CitiesContext, moving data access from props to a provider-based context.  
对你的 React 应用程序进行重构，通过自定义的使用 Cities 钩子以及 CitiesContext 来访问城市数据，将数据访问从 props 转移到了基于 provider 的上下文中。

#### Finishing the City View  完成城市景观的绘制工作17:58

Fetch city data by id via http request and render it. Use a cities context to manage current city, show a loading spinner, and highlight the active city.  
通过 http 请求按 id 获取城市数据并渲染。使用 cities 上下文管理当前城市，显示加载动画，并突出显示活动城市。

#### Including a Map With the Leaflet Library  
包括一张带有资料册的地图9:25

Install and configure React Leaflet and Leaflet to build an interactive map in a React app. Use map container, tile layer, marker, and popup with stateful center and scroll-zoom enabled.  
安装并配置 React Leaflet 框架，然后使用它来构建基于 React 应用的互动地图。包括使用地图容器、瓦片层、标记点，以及具有状态管理功能的中心控件和滚动缩放功能。

#### Displaying City Markers on Map  
在地图上显示城市标记5:58

Read cities from the global state with useCities, render one marker per city using its position, and customize popups with city emoji and name.  
使用 useCities 函数从全局状态读取各个城市的信息。为每个城市渲染一个标记，显示其位置信息；同时可以使用城市 emoji 和名称来自定义弹出窗口的样式。

#### Interacting With the Map  与地图进行互动13:38

Center the map on the selected city using url latitude and longitude, and use custom components with the use map hook to sync position and capture clicks for form navigation.  
使用 URL 的纬度和经度将地图居中到选定的城市，并使用自定义组件和地图钩子同步位置并捕获点击以进行表单导航。

#### Setting Map Position With Geolocation  
使用地理定位设置地图位置11:33

Learn to implement geolocation in a React app by rebuilding a reusable useGeolocation hook, syncing geolocation position with a map, and conditionally triggering map movement.  
学习如何在 React 应用中实现地理位置功能。通过重新设计可复用的 useGeolocation 钩子，将地理位置信息同步到地图上，并根据需要触发地图的滚动效果。

#### Fetching City Data in the Form  
以表格形式获取城市数据17:42

Fetch city details by reverse geocoding from url position, read lat and lng with a hook, update city and country, derive emoji from country code, and handle loading and errors.  
通过从 URL 位置进行反向地理编码获取城市详情，使用钩子读取纬度和经度，更新城市和国家，根据国家代码派生表情符号，并处理加载和错误。

#### Creating a New City  创建一座新的城市23:27

Learn to add a city in a React app by handling latitude and longitude, using a date picker, posting to a fake API, and syncing UI state with remote data.  
学习如何在一个 React 应用中添加城市信息，包括通过处理经纬度数据、使用日期选择器、将信息发布到虚拟 API，以及将用户界面状态与远程数据同步。

#### Deleting a City  删除一个城市5:37

Implement a delete city feature by wiring a button to call the api to delete city with the id and remove it from state using filter, while preventing link navigation.  
通过添加一个按钮来实现城市的删除功能。当用户点击该按钮时，系统会调用 API 来删除指定 ID 的城市，并通过过滤器从状态列表中移除该城市。同时，需要防止用户通过链接进行导航操作。

#### Advanced State Management System: Context + useReducer  
高级状态管理系统：上下文处理与 useReducer 的使用22:38

Master advanced state management by combining context API with useReducer to centralize city state, loading, and error handling via a reducer-driven action flow.  
通过结合上下文 API 和 useReducer 函数，可以掌握高级状态管理的技巧。利用 reducer 驱动的动作流程，能够实现对城市状态的集中管理、加载操作以及错误处理。

#### Adding Fake Authentication: Setting Up Context  
添加虚假认证：配置上下文环境11:02

Implement a fake authentication by wiring a hard-coded user through an AuthContext and AuthProvider, using a reducer to manage login/logout state and protect the app.  
通过设置一个硬编码的用户信息，将其通过 AuthContext 和 AuthProvider 进行认证处理。同时，使用 reducer 来管理登录/注销状态，从而保护应用程序的安全。

#### Adding Fake Authentication: Implementing "Login"  
添加虚假认证：实现“登录”功能16:24

Implement login and logout with a fake auth context from AuthProvider, handle the form, and navigate authenticated users to /app while replacing history to protect against unauthorized access.  
使用 AuthProvider 中的假认证上下文实现登录和登出，处理表单，并将已认证用户导航到/app，同时替换历史记录以防止未授权访问。

#### Adding Fake Authentication: Protecting a Route  
添加虚假认证：保护某个路由路径11:21

Build a reusable ProtectedRoute to guard all app routes, using useAuth and useEffect to redirect unauthenticated users to the homepage and wrap the app layout for complete authentication.  
构建一个可重用的 ProtectedRoute 来保护所有应用路由，使用 useAuth 和 useEffect 将未认证用户重定向到主页，并包装应用布局以实现完全认证。

#### CHALLENGE #2: Refactoring "React Quiz" to Context API  
挑战 2：将“React Quiz”重构为使用 Context API 的方式8:22

Refactor the React Quiz using the Context API and useReducer to build an advanced state management system, replacing prop drilling with a global quiz context, provider, and custom hook.  
使用 Context API 和 useReducer 对 React 应用程序中的测验功能进行重构，构建一个高级的状态管理系统。通过创建一个全局的测验上下文、提供者以及自定义钩子，来替代现有的 prop drilling 机制。


### 性能优化与高级的 useEffect 使用方式  
18 场讲座 • 2 小时 57 分钟

#### Section Overview  章节概述1:03

Explore the inner workings of React with practical render analysis, optimize wasted renders and performance with advanced tools, and deepen useEffect insights.  
通过实际的渲染分析来了解 React 的内部运作机制，利用先进的工具优化不必要的渲染和性能问题，并深入探究 useEffect 的工作原理。

#### Performance Optimization and Wasted Renders  
性能优化与不必要的渲染优化6:06

Explore how to prevent wasted renders, boost app speed, and reduce bundle size in React using memo, useMemo, useCallback, and code splitting with lazy loading.  
了解如何在 React 中使用 memo、useMemo、useCallback 技术来避免渲染资源的浪费，提升应用程序的速度，同时减少包的体积。此外，还可以了解如何通过代码分割和懒加载来优化应用程序的性能。

#### The Profiler Developer Tool  
分析器开发工具8:21

Explore the profiler in the React developer tools to analyze renders, view flamegraphs, and identify why components re-render (state updates, context changes, or parent renders) while optimizing performance.  
在 React 开发者工具中查看分析工具，以了解渲染情况、观察火焰图，并找出组件重新渲染的原因（如状态更新、上下文变化或父组件的重新渲染），同时优化性能。

#### A Surprising Optimization Trick With children  
一个令人惊讶的优化技巧，适用于儿童使用11:49

Discover a surprising optimization that uses the children prop to prevent re-rendering a SlowComponent within a counter, showing how pre-created children stay unchanged during state updates.  
发现了一个令人惊讶的优化方法，该方法利用子组件来避免重复渲染同一个 SlowComponent 元素。这样一来，在状态更新过程中，预先创建好的子组件就能保持不变。

#### Understanding memo  理解备忘录5:29

Discover how memoization with memo, useMemo, and useCallback optimizes React apps by memoizing heavy components and caching results to prevent wasted renders when props stay the same.  
了解如何通过使用 memo、useMemo 和 useCallback 来优化 React 应用程序的性能。通过将那些需要频繁调用的组件进行 memo 化，并缓存其结果，可以避免因 props 保持不变而导致的重复渲染，从而节省资源。

#### memo in Practice  实践中的备忘录13:17

Memo in practice demonstrates wrapping a heavy archive component with React.memo to prevent re-renders on unchanged props, then reveals how object props can break memoization and slow typing performance.  
实践中，Memo 展示了如何用 React.memo 包裹一个重型档案组件，以防止在 props 未变化时重新渲染，然后揭示了对象 props 如何破坏 memoization 并降低输入性能。

#### Understanding useMemo and useCallback  
了解 useMemo 和 useCallback 的用法6:39

Learn to keep functions and objects stable with useMemo, useCallback, and memo to prevent wasted renders, memorize values via dependency arrays, and optimize expensive calculations.  
学会如何使用 useMemo、useCallback 和 memo 来保持函数和对象的稳定性，从而避免不必要的重新渲染。通过依赖数组来保存值，并优化那些耗时的计算过程。

#### useMemo in Practice  实践中的 useMemo 使用8:42

Use useMemo to stabilize a prop object for the archive component, fixing memoization and typing lag, and learn to manage dependency arrays to avoid stale closures.  
使用 useMemo 来稳定 archive 组件的 prop 对象，解决 memoization 和类型延迟问题，并学习管理依赖数组以避免过时的闭包。

#### useCallback in Practice  实际中的 useCallback 使用方式10:09

Use useCallback to memorize functions and prevent re-renders, unlike useMemo which memorizes results; profile performance and optimize only slow components.  
使用 useCallback 来记忆函数并防止重新渲染，与 useMemo 记忆结果不同；分析性能并仅优化慢速组件。

#### Optimizing Context Re-Renders  
优化上下文渲染效果14:47

Learn practical strategies to prevent wasted re-renders with the context API by memoizing context values and descendants, and by structuring context per state to improve app performance.  
学习一些实用的策略，通过上下文 API 来避免不必要的重新渲染操作。这可以通过将上下文值及子节点进行缓存来实现，同时根据不同的状态来构建上下文结构，从而提高应用程序的性能。

#### Back to The "WorldWise" App  
回到“WorldWise”应用程序12:37

Revisit the WorldWise app to profile with the profiler, identify slow renders, and fix an infinite loop by stabilizing the getCity function with useCallback for efficient React rendering.  
重新访问 WorldWise 应用程序，与调试工具进行联机调试，找出渲染速度较慢的地方，并通过使用 useCallback 来稳定 getCity 函数，从而解决无限循环的问题，以实现高效的 React 渲染。

#### Optimizing Bundle Size With Code Splitting  
通过代码分割来优化包的大小15:52

Learn to shrink bundle size with code splitting and lazy loading using React Suspense and dynamic imports, splitting the app into route-level chunks and showing a loading spinner during fetches.  
通过利用 React 的 Suspense 功能和动态导入技术，学会如何减少代码的体积。还可以将应用程序拆分为不同路由级别的模块，并在数据加载过程中显示加载 spinner 以提示用户数据正在加载中。

#### Don't Optimize Prematurely!  
不要过早地进行优化！2:59

Avoid premature optimization by profiling to identify real bottlenecks, don't overuse useMemo or useCallback, and limit context optimization to necessary cases, then apply code splitting and lazy loading for routes.  
通过性能分析来识别真正的瓶颈，避免过早进行优化。不要过度使用 useMemo 或 useCallback，将上下文相关的优化限制在必要的情况下。同时，应用代码分割和延迟加载来处理路由相关的优化问题。

#### useEffect Rules and Best Practices  
useEffect 规则和最佳实践10:01

Understand useEffect dependency rules, including reactive values from state, props, and context, to avoid stale closures; apply useCallback, useMemo, and useReducer to optimize behavior.  
理解 useEffect 依赖规则，包括来自状态、属性和上下文的响应式值，以避免陈旧的闭包；应用 useCallback、useMemo 和 useReducer 来优化行为。

#### CHALLENGE #1: Fix Performance Issues in "Workout Timer"  
挑战 #1：修复 "Workout Timer" 的性能问题12:15

Analyze the workout timer project with the profiler tool to identify a performance issue causing frequent re-renders. Then apply useEffect, memo, and useMemo to optimize components and stabilize the UI.  
使用分析工具对锻炼计时器项目进行排查，找出导致频繁重新渲染的性能问题。然后运用 useEffect、memo 和 useMemo 等钩子函数来优化组件逻辑，从而稳定用户界面。

#### Setting State Based on Other State Updates  
基于其他状态更新设置状态11:55

Implement a duration state that updates from four states—number, sets, speed, and duration of the length—via useEffect, with handleInc and handleDec, rounding, and safeguards against negatives.  
实现一个持续时间状态，通过 useEffect 从四个状态——数字、集合、速度和长度的持续时间——更新，并带有 handleInc 和 handleDec，四舍五入以及防止负数的保护措施。

#### Using Helper Functions In Effects  
在副效应中使用辅助函数15:04

Explore how to use useEffect and useCallback with helper functions to synchronize duration changes with sound playback, and manage dependencies for reliable behavior.  
了解如何结合使用 useEffect 和 useCallback，以及辅助函数，以实现对声音播放时长的同步控制，同时管理各种依赖关系，从而确保系统的稳定运行。

#### Closures in Effects  效果中的关闭操作9:52

Explore how stale closures affect use effect, and see why a dependency array ensures the effect re-runs with the latest state and props as snapshots.  
了解过时的闭包如何影响效果，以及为什么依赖数组可以确保效果以最新的状态和属性为基准来重新执行。


### Redux 和现代 Redux 工具包（包含 Thunks 模块）  
19 次讲座 • 2 小时 57 分钟

#### Section Overview  章节概述1:07

Explore Redux fundamentals by relating it to the useReducer hook, then integrate Redux into a small app, migrate to Redux Toolkit, and handle API requests with thunks.  
通过将 Redux 的基础概念与 useReducer 钩子相结合来了解 Redux 的原理，然后将 Redux 集成到一个小型应用中。此外，还可以使用 thunks 来处理 API 请求。

#### Introduction to Redux  Redux 简介12:13

Explore how Redux manages global state with a single store, actions, and reducers, and compare it to useReducer. Learn classic Redux and Redux Toolkit, and when to use it.  
探索 Redux 如何通过单一 store、actions 和 reducers 管理全局状态，并将其与 useReducer 进行比较。学习经典的 Redux 和 Redux Toolkit，以及何时使用它们。

#### Creating a Reducer: Bank Account  
创建 reducer：银行账户10:47

Build a pure redux reducer for a bank account in isolation, with balance, loan, and loanPurpose, handling deposit, withdraw, and loan actions in a new store.js setup.  
请单独创建一个纯的银行账户缩减器，包含余额、贷款以及贷款目的等功能。在 new store.js 配置中，需要处理存款、取款和贷款相关的操作。

#### Creating a Redux Store  创建 Redux 存储库9:46

Create a Redux store with the Redux approach, dispatch deposit and withdraw actions, loan with a payload, log state to see the balance, and introduce action creators to automate actions.  
使用 Redux 方法创建一个存储库，发送存款和取款操作，执行带有数据的贷款操作，将状态记录下来以查看余额，并引入操作创建器来自动化这些操作。

#### Working With Action Creators  
使用动作生成器进行操作6:28

Create action creator functions for deposit, withdraw, request loan, and payLoan that return actions and streamline dispatch, while noting the shift to modern patterns and centralized action type strings.  
创建用于存款、取款、申请贷款和支付贷款的动作创建函数。这些函数能够返回操作结果，并简化操作的调度过程。同时，注意采用现代的模式和集中的动作类型字符串表示方式。

#### Adding More State: Customer  
添加更多州：客户12:30

Create initial customer state and action creators, then build a customer reducer. Combine reducers into a root reducer and wire them to the store for account and customer state.  
创建初始的客户状态以及相应的操作生成器，然后构建一个客户缩减器。将这些缩减器整合到一个根缩减器中，并将其连接到存储层，以便处理账户和客户状态的相关操作。

#### Professional Redux File Structure: State Slices  
专业版 Redux 文件结构：状态切片机制11:47

Organize Redux code into feature slices, with account and customer slices in a features folder, exporting reducers and action creators, then combining into a store and dispatching actions.  
将 Organize Redux 代码划分为不同的功能模块，将用户和客户相关的代码放在相应的文件夹中。同时，导出各种 reducer 和 action Creator，然后将这些组件组合成一个完整的存储库，最后触发相应的动作。

#### Back to React! Connecting our Redux App With React  
回到 React 主题！使用 React 来连接我们的 Redux 应用程序。5:46

Connect the redux store to a react app using the react-redux provider and store prop. Read and display the customer's full name with useSelector, subscribing to store updates.  
通过 react-redux 的 provider 和 store 属性，将 Redux 商店与 React 应用程序连接在一起。使用 useSelector 函数来读取并显示客户的全名，同时订阅商店中的更新信息。

#### Dispatching Actions from Our React App  
从我们的 React 应用中发送操作指令17:21

Use the useDispatch hook to dispatch actions to the Redux store from React components. Import action creators such as createCustomer and use useSelector to render based on the store state.  
使用 useDispatch 钩子将动作发送到 Redux 存储中。导入如 createCustomer 这样的动作创建器，并使用 useSelector 根据存储状态来渲染界面。

#### The Legacy Way of Connecting Components to Redux  
将组件连接起来的传统方式——Legacy Way of Connecting Components to Redux4:50

Discover the legacy way of connecting React components to the Redux store using the connect function and mapStateToProps, shown in the balance display component before hooks existed.  
了解如何使用`connect`函数和`mapStateToProps`将 React 组件与 Redux 存储进行连接的传统方式。在`hooks`出现之前，这种连接方式在`balance display component`中就已经被使用了。

#### Redux Middleware and Thunks  
Redux 中间件和 Thunks4:28

Discover how Redux middleware and thunks enable asynchronous data fetching, while keeping reducers pure and components clean by deferring API calls through thunks.  
了解如何使用 Redux 中间件和 thunks 来实现异步数据获取功能。通过这种方式，我们可以推迟 API 调用的执行，从而保持 reducer 的纯净性，同时让组件代码更加简洁。

#### Making an API Call With Redux Thunks  
通过 Redux Thunks 进行 API 调用17:50

Learn how redux-thunk acts as middleware to convert foreign currency deposits to US dollars via an API call, using three steps: install, applyMiddleware, and apply the middleware in action creators.  
了解 Redux-Thunk 如何作为中间件，通过 API 调用将外币存款转换为美元。这一过程包含三个步骤：安装模块、使用`applyMiddleware`函数，以及在实际的应用代码中调用该中间件。

#### The Redux DevTools  Redux 开发工具10:21

Explore how to install and use Redux dev tools, integrate composeWithDevTools with Redux Toolkit, and time-travel through actions, state transitions, and debugging in a React Redux flow.  
了解如何安装和使用 Redux 开发工具。学习如何将 composeWithDevTools 与 Redux 工具包集成在一起。同时，也能掌握在 React Redux 流程中处理动作、状态转换以及调试问题的技巧。

#### What is Redux Toolkit (RTK)?  
什么是 Redux 工具包（RTK）？3:40

Learn how Redux Toolkit modernizes Redux by reducing boilerplate and enabling mutating-style reducers. Discover automatic action creators and thunk setup that simplify development while remaining compatible with classic Redux.  
了解 Redux Toolkit 如何通过减少样板代码以及引入可动态替换的 reducer 函数来提升 Redux 的功能性。发现自动生成动作创建器以及 Thunk 函数设置功能，这些功能能简化开发过程，同时仍保持与经典 Redux 的兼容性。

#### Creating the Store With RTK  
使用 RTK 技术创建商店4:37

Upgrade to Redux Toolkit by installing @reduxjs/toolkit and migrating gradually. Configure store creates the store with reducers, thunk, and dev tools.  
通过安装@reduxjs/toolkit 并逐步迁移，升级到 Redux Toolkit。配置存储模块后，即可创建包含 reducer、thunk 以及开发工具的存储系统。

#### Creating the Account Slice  
创建账户切片21:43

Learn how to convert a Redux account slice to Redux Toolkit using createSlice, gaining automatic action creators, simplified reducers, and Immer-based mutating logic, with prepare for multi-argument payloads.  
了解如何使用 createSlice 将 Redux 账户切片转换为 Redux Toolkit。这样就能获得自动化的动作创建器、简化的 reducer，以及基于 Immer 的修改逻辑。同时还能处理多参数负载的情况。

#### Back to Thunks  回到“评论”部分5:02

Reuse the existing action creator to implement thunks with redux toolkit for currency conversion, using createAsyncThunk in future projects but with automatic thunk support, no setup, and a loading state.  
使用现有的动作生成器，结合 Redux 工具包来实现货币转换的线程化操作。在后续的项目中，可以使用 createAsyncThunk 来创建异步线程化操作，同时支持自动线程化功能。无需额外设置，且能处理加载状态。

#### Creating the Customer Slice  
创建客户信息切片9:14

Convert the customer slice to Redux Toolkit by using createSlice, preparing payloads for full name, national ID, and createdAt, and implementing create customer and update name reducers.  
通过使用 createSlice 函数将客户数据片段转换为 Redux Toolkit 所需的格式。同时，需要准备包含全名、国家 ID 以及创建时间等信息的有效载荷，并实现 createCustomer 和 updateName 这两个 reducer 函数。

#### Redux vs. Context API  Redux 与 Context API7:43

Compare Redux and Context API, weigh pros and cons, and offer usage guidance for small apps versus large apps, including middleware, DevTools, and performance considerations.  
比较 Redux 和 Context API，分析各自的优缺点，并为小型应用和大型应用提供使用建议，包括关于中间件、开发工具以及性能方面的注意事项。


##   第四部分：专业 React 能力培养
[2 个项目]  
2 次讲座 • 2 分钟

#### Introduction to Part 4  第四部分的介绍1:27

Shift from learning React to leveraging its library ecosystem to build two professional web apps with React Query, Supabase, and Tailwind, featuring authentication, data filtering, charts, dark mode, and modals.  
从学习 React 技术开始，逐渐转向利用 React 的库生态系统来构建两个专业的 Web 应用程序。这些应用程序使用了 React Query、Supabase 和 Tailwind 等库，实现了身份验证、数据过滤、图表展示、深色模式以及模态框等功能。

#### Useful Resources for Part 4  
有用的资源，第 4 部分0:44


### 支持数据加载的 React 路由系统（v6.4 及以上版本）  
12 次讲座 • 2 小时 7 分钟

#### Section Overview  章节概述0:51

Explore the modern data loading features of React Router within a pizza ordering project, and learn to plan a professional React project for building professional React apps.  
在一个披萨点餐项目的实践中，了解 React Router 的现代数据加载功能。同时，学习如何规划一个专业的 React 项目，以构建高效的 React 应用程序。

#### Setting Up a New Project: "Fast React Pizza Co."  
启动一个新项目：“快速反应披萨公司”8:13

Set up a new React project with Vite, install dependencies via npm, configure ESLint, and run npm run dev to start the Fast React Pizza Co. app.  
使用 Vite 创建一个新的 React 项目，通过 npm 安装所需依赖库，配置 ESLint，然后运行`npm run dev`以启动 Fast React Pizza Co.应用。

#### Application Planning  应用规划21:24

Develop a professional React frontend for a pizza delivery client. Plan requirements, pages, features, React Router data fetching with Tailwind and Redux, and data-driven state management.  
为一家披萨配送客户开发专业的 React 前端应用。需要规划页面布局、功能模块，使用 Tailwind 和 Redux 进行 React 路由数据获取，并实现数据驱动的状态管理。

#### Setting Up a Professional File Structure  
建立专业的文件结构8:20

Set up a professional, feature-based file structure for a React app, organizing code into features, UI, services, and utilities, and prepare starter files for scalable development.  
为 React 应用程序设置一个专业的、基于功能的文件结构，将代码组织成功能、UI、服务和工具，并准备可扩展开发的启动文件。

#### A New Way Of Implementing Routes  
一种新的路线实施方式10:30

Learn to implement routes with React Router 6.4 using createBrowserRouter and RouterProvider, enabling data loading and form submissions within routes by defining an array of route objects.  
学习如何使用 React Router 6.4 来实现路由功能。通过使用 createBrowserRouter 和 RouterProvider，可以定义一系列路由对象，从而在各个路由中实现数据加载和表单提交的功能。

#### Building the App Layout  构建应用程序的布局11:18

Build a global app layout with React Router, featuring a header, a persistent cart overview, and a main content area that renders nested routes via outlet for menu and cart.  
使用 React Router 构建一个全球范围内的应用程序布局。该布局包含头部、一个持续的购物车视图，以及主体内容区域。主体内容区域通过 outlet 技术呈现嵌套的路由，用于显示菜单和购物车信息。

#### Fetching Data With React Router "Loaders": Pizza Menu  
使用 React Router 的“加载器”功能来获取数据：披萨菜单示例11:50

Explore React Router loaders to fetch menu data, connect a loader to the route, and use useLoaderData to render a pizza menu with centralized API logic.  
探索 React Router 的加载器功能，以获取菜单数据。将加载器与特定路由关联起来，并使用 useLoaderData 来渲染包含中央 API 逻辑的披萨菜单。

#### Displaying a Loading Indicator  
显示加载指示器6:00

Use the useNavigation hook to access the app's global navigation state, detect when navigation.state equals loading, and render a loader overlay to indicate data loading across pages.  
使用 useNavigation 钩子来访问应用程序的全局导航状态。当 navigation.state 为“loading”时，渲染一个加载器覆盖层，以表示数据正在跨页面加载中。

#### Handling Errors With Error Elements  
使用错误元素来处理错误6:24

Handle errors in React Router by rendering an error element for a loader or an action, access the error with useRouteError, and display it within the layout.  
在 React Router 中处理错误时，可以为加载器或操作过程渲染一个错误元素。通过 useRouteError 函数来获取错误信息，并将其显示在布局中。

#### Fetching Orders  提取订单12:40

Fetch individual orders from the api using the order id from the url, and add a header search field to navigate to /order/{id} with a loader and useLoaderData.  
从 API 中提取单个订单信息，使用来自 URL 的订单 ID 作为参数。同时添加一个头部搜索字段，以便通过 loader 和 useLoaderData 来访问页面路径"/order/{id}"。

#### Writing Data With React Router "Actions"  
使用 React Router 通过“动作”来编写数据19:03

Use React Router actions to write or mutate server data via forms, connect actions to routes, post to create a new order, and redirect to the new order page.  
使用 React Router 的 Action 功能可以通过表单来编写或修改服务器数据。将 Action 与特定路由关联起来，然后提交表单以创建新订单，或者重定向到新订单的页面。

#### Error Handling in Form Actions  
表单操作中的错误处理10:26

Manage form actions with React Router by tracking submitting state, disabling the button, and returning an errors object to validate inputs and display errors via useActionData.  
通过使用 React Router 来管理表单操作，可以通过跟踪提交状态来禁用按钮，并通过 useActionData 返回错误对象来验证输入内容并显示错误。


### [可选] Tailwind CSS 入门课程：打造美观的应用程序样式  
19 次讲座 • 3 小时 15 分钟

#### Section Overview  章节概述0:59

Learn to style a React app from scratch using Tailwind CSS in a focused crash course, setting up Tailwind and applying it to all components.  
参加这个专注的速成课程，学习如何使用 Tailwind CSS 从零开始构建 React 应用。课程将涵盖如何设置 Tailwind CSS，以及如何将其应用于所有组件。

#### What is Tailwind CSS?  什么是 Tailwind CSS？7:56

Explore Tailwind CSS, a utility-first framework with hundreds of classes for markup. Learn how tiny single-purpose classes compose into components, and why Tailwind speeds styling, design system, and responsive design.  
探索 Tailwind CSS，一个以工具优先为特点的框架，拥有数百个用于标记的类。了解微小的单用途类如何组合成组件，以及为什么 Tailwind 加速了样式、设计系统和响应式设计。

#### Setting Up Tailwind CSS  配置 Tailwind CSS 框架12:03

Set up Tailwind CSS with Vite in a modern project, using Tailwind v3, create config files, update index.css, and enable the VS Code extension and prettier integration.  
在现代项目中，使用 Tailwind v3 来配置 Tailwind CSS 框架。创建相应的配置文件，更新 index.css 文件，并启用 VS Code 的扩展功能以及 Prettier 集成。

#### Working With Color  颜色运用8:59

Learn how to work with Tailwind colors using text and background color classes, palettes, and shades to create visual hierarchy.  
了解如何使用文本和背景颜色类、调色板和色调来操作 Tailwind 框架中的颜色效果，以创建视觉层次结构。

#### Styling Text  设置文本样式10:00

Master tailwind text styling by applying classes in HTML and JSX, adjusting font size, weight, case, and tracking, and using escape hatch for arbitrary values.  
通过运用 HTML 和 JSX 中的类来设置文本样式，调整字体大小、字形、大小写以及追踪效果。同时，对于任意值使用特殊字符来表示。

#### The Box Model: Spacing, Borders, and Display  
盒子模型：间距、边框和显示方式11:17

Learn to apply Tailwind's box model concepts by adjusting margin, padding, borders, and display; use spacing and auto margins for centering, and explore responsive design basics.  
学习如何运用 Tailwind 框架中的盒子模型概念，通过调整边距、内边距、边框以及布局方式来实现布局效果；了解如何使用间距和自动边距来进行居中定位；同时掌握响应式设计的基本技巧。

#### Responsive Design  响应式设计10:48

Master responsive design with Tailwind by using mobile-first min-width breakpoints, prefixing classes, and overriding defaults at each breakpoint to tailor layouts.  
通过使用 Tailwind 框架来掌握响应式设计技巧，比如采用以移动设备为优先的最小宽度断点设置、为类名添加前缀，以及在每个断点处覆盖默认布局设置，从而能够灵活调整布局样式。

#### Using Flexbox  使用 Flexbox 布局6:31

Learn to implement layouts with Tailwind’s flexbox utilities by turning a container into a flex container, aligning items center, and applying justify-between across breakpoints for responsive design.  
学习如何使用 Tailwind 框架中的弹性布局工具来实现布局设计。通过将容器转换为弹性容器，将元素居中对齐，并在不同断点上应用“justify-between”属性，以实现响应式设计。

#### Using CSS Grid  使用 CSS 网格布局12:20

Build a responsive three-row layout with CSS grid and Tailwind, centering content with max width and auto margins, and make the middle row fill remaining space while enabling page scroll.  
使用 CSS 网格布局和 Tailwind 框架构建一个响应式的三列布局。将内容居中放置，并设置最大宽度和自动边距。让中间列填充剩余的空间，同时实现页面滚动功能。

#### Styling Buttons: Element States and Transitions  
设计按钮：元素状态与转换12:11

Learn to style links and buttons with Tailwind by applying hover, focus, and disabled states, adding color transitions and focus rings on the cart page.  
通过应用悬停效果、焦点状态以及禁用状态，结合在购物车页面添加颜色渐变和焦点环效果，学习如何使用 Tailwind 框架来设计链接和按钮的样式。

#### Styling Form Elements  整理形状元素10:22

Style form input elements with Tailwind to create pill-shaped fields, adjust padding and text size, apply placeholder styling, and use responsive width and focus rings.  
使用 Tailwind 框架来设计表单输入元素的样式。可以调整内边距、文本大小，设置占位符的样式，并实现响应式宽度和焦点效果。此外，还可以创建药丸形状的输入框。

#### Reusing Styles With @apply  
使用@apply 来复用样式4:28

Learn to reuse styles with Tailwind's apply by composing many Tailwind classes into an input class, and recognize when to prefer a React component for reusable styles.  
通过组合多个 Tailwind 类来创建自定义类，从而学会如何重复使用样式。同时，要懂得何时选择使用 React 组件来复用样式。

#### Reusing Styles With React Components  
在 React 组件中复用样式风格12:27

Learn to reuse Tailwind styles by creating reusable React components like Button and LinkButton, accepting children and props such as disabled and to, and extending styles with @apply.  
学习如何复用 Tailwind 样式表，通过创建可复用的 React 组件来实现这一点，比如 Button 和 LinkButton。这些组件可以接受子节点和各种属性，如 disabled 和 to 等。此外，还可以使用@apply 扩展样式表的功能。

#### Absolute Positioning, z-index, and More  
绝对定位、z 索引以及更多相关概念6:30

Explore absolute positioning and z-index to create a full-page, centered loading overlay with a subtle backdrop blur using Tailwind utilities and theme-based colors.  
利用 Tailwind 工具箱中的功能，通过灵活运用绝对定位和 z-index 属性，可以创建一个全页的居中加载覆盖层，同时加入轻微的背景模糊效果。还可以根据主题需求来设置颜色样式。

#### Configuring Tailwind: Custom Font Family  
配置 Tailwind：自定义字体家族10:53

This lecture shows configuring Tailwind via the tailwind.config file, adding a font family called pizza with Roboto Mono from Google Fonts, and override versus extend for typography including DVH.  
本次讲座介绍了如何通过 tailwind.config 文件来配置 Tailwind 框架，使用了 Google Fonts 中的“pizza”字体家族，并探讨了在排版方面使用覆盖与扩展的方式，包括 DVH 的自定义设置。

#### Styling the Menu  设计菜单18:27

Style the menu page by turning items into flex rows with dividers, padding, and legible typography. Build a reusable primary button with responsive sizing and sold-out styling using grayscale.  
通过使用 divider 分隔符、填充样式以及清晰的字体设计，来打造菜单页面的样式。同时，创建一个可复用的主按钮，该按钮具有响应式尺寸设计，并且采用灰度颜色来表现售罄商品的信息。

#### Styling the Cart  整理购物车11:46

Style a shopping cart page with responsive flexbox layouts, padding, spacing, and dividers, rendering card items and action buttons. Adapt design for small viewports and de-emphasized secondary buttons.  
设计一个响应式购物购物车页面，使用 Flexbox 布局、填充元素、间距以及分隔条来布局页面元素。在卡片项和操作按钮方面进行优化。同时，针对小屏幕尺寸的情况调整设计，使次要按钮不再过于突出。

#### Styling the Order Form  设计订单表单13:21

Style the order form for the fast react pizza co app using mobile-first responsive flexbox, spacing, and typography, including large semi-bold headings, labeled inputs, and error messages.  
为 Fast React Pizza Co 的应用程序中的订单表单设计样式，采用移动优先的响应式布局，使用 Flexbox 布局结构，同时注重间距和字体设计。其中包括较大的半粗体标题、带有标签的输入框以及错误信息的显示方式。

#### Styling the Order Overview  
对订单概览进行样式设计13:53

Style the order overview page using Tailwind, flexbox, and responsive wrapping; display order id, status, priority, delivery estimate, prices, and the list of pizzas with the order item component.  
使用 Tailwind、flexbox 和响应式包裹来样式化订单概览页面；显示订单 ID、状态、优先级、预计送达时间、价格以及使用订单项组件显示披萨列表。


### 添加了 Redux 和高级 React Router  
14 次讲座 • 2 小时 44 分钟

#### Section Overview  章节概述0:39

Implement a shopping cart with Redux Toolkit, and explore advanced React Router data loading features while applying Redux to real-world use cases.  
使用 Redux Toolkit 实现一个购物车功能，并在实际应用中运用 Redux 的高级 React 路由数据加载特性。

#### Modeling the "User" State With Redux Toolkit  
使用 Redux 工具包来建模“用户”状态13:23

Implement global user state with Redux Toolkit to manage the username across the app. Configure the store, provide it through react-redux, and read and update the state with actions.  
使用 Redux Toolkit 实现全局用户状态管理，以跨应用管理用户名。配置 store，通过 react-redux 提供它，并使用 actions 读取和更新状态。

#### Reading and Updating the User State  
读取并更新用户状态10:37

Update the Redux user state by dispatching the update name action on form submit, then navigate to menu, using local input state and reading the username from the Redux store.  
在表单提交时，通过执行“更新用户名”操作来更新 Redux 的用户状态。之后，利用本地存储的数据，从 Redux 存储中读取用户名，然后进入菜单页面。

#### Modeling the "Cart" State  
模拟“购物车”状态14:20

Model the cart state in redux by creating a cart slice with add, delete, increase and decrease quantity, and clear actions, deriving totals from a cart array.  
通过创建一个包含添加、删除、增加、减少数量以及清除功能的购物车组件，来在 Redux 中模拟购物车的状态。同时，从购物车数组中提取总数量。

#### Adding Menu Items to the Cart  
将菜单项添加到购物车中7:06

Learn how to add pizzas to the cart by rendering conditional add buttons, dispatching add item actions with a new item payload, and syncing cart state with redux.  
了解如何通过渲染条件性添加按钮来将披萨添加到购物车中，通过新的物品负载来触发添加物品的操作，以及如何将购物车状态与 Redux 框架进行同步。

#### Building the Cart Overview With Redux Selectors  
使用 Redux 选择器构建购物车界面概览8:59

Build a Redux cart overview by creating selectors in the cart slice to compute total cart quantity and total price from state, using useSelector and null rendering when empty.  
通过在购物车切片中创建选择器，来计算购物车的总数量과总价格。当购物车为空时，使用 null 渲染来表示这种情况。

#### Building the Cart Page  构建购物车页面7:05

Build the cart page by wiring a Redux store with useSelector and a getCart selector, dispatch clearCart, and render the EmptyCart component when empty, styling with Tailwind.  
通过配置 Redux 存储来构建购物车页面，使用 useSelector 和 getCart 选择器来获取购物车数据。当购物车为空时，触发 clearCart 操作，并渲染 EmptyCart 组件。同时使用 Tailwind 框架对页面进行样式设计。

#### Deleting Cart Items  删除购物车物品14:44

Learn to delete cart items in a reusable component by dispatching deleteItem with a pizza ID, while conditionally showing delete or add-to-cart actions based on each item's quantity.  
学会通过派发 deleteItem 并附带披萨 ID 来在可重用组件中删除购物车商品，同时根据每个商品的库存量条件性地显示删除或添加到购物车操作。

#### Updating Cart Quantities  更新商品数量信息13:32

Update cart quantities with a reusable UpdateItemQuantity component, dispatching decrease and increase actions, enforcing deletion at zero via redux reducers, and syncing currentQuantity from the cart state.  
使用可重复使用的 UpdateItemQuantity 组件来更新购物车中的商品数量。同时实现数量的增减操作，并通过 Redux 的 reducer 机制确保当数量达到 0 时才会触发删除操作。此外，还需要将购物车的当前数量与状态同步。

#### Using the Cart for New Orders  
使用购物车进行新订单的购买操作14:30

Utilize the cart data to submit a new order by using getCart, validating a non-empty cart, and calculating a priority price, then display the formatted total on the order button.  
利用购物车的数据，通过调用 getCart 方法提交新的订单订单。确认购物车中至少有商品存在后，计算订单的优先级价格。最后在订单按钮上显示格式化后的总价格。

#### Redux Thunks With createAsyncThunk  
使用 createAsyncThunk 的 Redux Thunks16:04

Explore Redux Toolkit's createAsyncThunk to build a geolocation feature that fetches the user’s GPS position and reverse geocoded address, with pending, fulfilled, and rejected states.  
探索 Redux 工具包中的 createAsyncThunk 功能，用它来构建一个地理定位功能。该功能可以获取用户的 GPS 位置，并将地址进行逆向地理编码处理。同时，该功能还能处理 Pending、Fulfilled 和 Rejected 三种状态。

#### Integrating Geolocation  整合地理定位功能16:04

Integrate geolocation data loaded with Redux into the create order form, fix state.status, handle loading and errors, position data via hidden input, and submit with GPS coordinates using Redux Toolkit.  
将通过 Redux 加载的地理位置数据整合到订单创建表单中。修复状态管理相关的错误，处理加载过程中的问题。通过隐藏输入字段来传递位置数据，并使用 Redux Toolkit 以 GPS 坐标进行提交。

#### Fetching Data Without Navigation: useFetcher  
无需导航即可获取数据：使用 Fetcher 组件12:44

Master using the useFetcher hook in React Router to fetch data from another route without navigation, load menu data for orders, and supply ingredients to each item.  
在 React Router 中利用 useFetcher 钩子，可以从另一个路由中获取数据，无需通过导航菜单即可加载订单信息，并为每个商品提供相关配料。

#### Updating Data Without Navigation  
在不进行导航操作的情况下更新数据14:37

Learn to update data without navigation by using a fetcher form to patch an order's priority, with a make priority button and re-validation to refresh the page.  
学会通过使用一个 fetcher 表单来修补订单的优先级，而无需导航，同时使用一个“设置优先级”按钮和重新验证来刷新页面。


### 搭建我们最大的项目 + 样式组件  
11 次讲座 • 每场 1 小时 50 分钟

#### Section Overview  章节概述0:54

Plan and build a real-world project using Styled Components, React Query, React Hook Forms, and React Router, then set up a Super Base backend for full-stack development.  
使用 Styled Components、React Query、React Hook Forms 和 React Router 来规划和构建一个实际的项目。然后，为全栈开发准备一个基础性的后端框架。

#### Application Planning  应用规划18:29

Plan a hotel management app for The Wild Oasis, outlining requirements, pages, and a shared API with Supabase, and decide the frontend stack including React Router, styled-components, and React Query.  
为《Wild Oasis》项目规划一款酒店管理应用程序，明确需求、页面布局，并选择与 Supabase 合作的共享 API。同时确定前端框架，包括 React Router、styled-components 以及 React Query 等技术。

#### Help Isabelle to Plan an App  
帮助伊莎贝尔策划一款应用程序。

#### Setting Up the Project: "The Wild Oasis"  
项目的搭建：“野生绿洲”8:11

Set up a new 'The Wild Oasis' project with vite version four for a single-page app. Install dependencies and configure eslint with .eslintrc.json and vite plugin.  
使用 Vite 4 版本创建一个新的“The Wild Oasis”项目，目标是一个单页应用。安装所需依赖库，并配置 ESLint 的设置，使用`.eslintrc.json`文件以及 Vite 插件来实现这一目标。

#### Introduction to Styled Components  
styled Components 简介12:22

Learn to style a React app using styled components by writing CSS inside JavaScript, creating reusable components (styled.h1, styled.button, styled.div), and applying styles to the app and its elements.  
学习如何通过编写 JavaScript 中的 CSS 代码来为 React 应用添加样式。了解如何创建可复用的组件，例如 styled.h1、styled.button、styled.div 等。同时，也会学习如何对应用程序和其中的元素应用各种样式。

#### Global Styles With Styled Components  
带有样式组件的全球风格设计14:36

Implement global styles in a React app using styled-components createGlobalStyle to build a GlobalStyles component, apply fonts and a CSS reset, and manage design tokens with CSS variables.  
在 React 应用中使用 styled-components 来实现全局样式。创建 GlobalStyle 组件，用于定义全局字体和 CSS 重置样式，并通过 CSS 变量来管理设计相关的参数。

#### Styled Component Props and the "css" Function  
Styled Component 的属性以及“css”函数10:25

Build a reusable heading component using styled components, props, and the CSS function; render as h1, h2, or h3 with the as prop for semantic, accessible markup.  
使用 styled components、 props 以及 CSS 函数来构建一个可重复使用的标题组件；该组件可以通过 as 属性设置为 h1、h2 或 h3，以实现语义化的标记效果，同时保证可访问性。

#### Building More Reusable Styled Components  
构建更多可复用的样式组件11:02

Design and implement reusable styled components by building a Row component for horizontal and vertical layouts, leveraging props and default props to create a scalable design system.  
设计和实现可复用的样式化组件，通过构建适用于水平和垂直布局的 Row 组件来实现这一目标。利用 props 和默认 prop 来构建可扩展的设计系统。

#### Setting Up Pages and Routes  
配置页面和路由6:27

Install React Router, set up BrowserRouter with declarative routes, configure an index route and navigate redirects, import pages, and apply global styles to finalize the dashboard.  
安装 React Router，配置 BrowserRouter 并使用声明式路由。创建索引路由并实现导航重定向功能。导入各个页面组件，并应用全局样式以完成仪表板的构建。

#### Building the App Layout  构建应用程序的布局14:21

Create AppLayout.jsx, build a left sidebar and header with a main content area using a layout route and outlet; style with styled components and CSS grid for a clean layout.  
创建 AppLayout.jsx 文件，通过布局路由和出口来实现左侧侧边栏、头部区域以及主内容区的布局；使用 styled components 和 CSS 网格来实现简洁的布局风格。

#### Building the Sidebar and Main Navigation  
构建侧边栏和主导航菜单13:09

Build a functional sidebar and main navigation with React Router NavLink, styled-components, and React Icons, including active states, iconography, and responsive styling for smooth navigation.  
使用 React Router 的 NavLink 组件、styled-components 以及 React 图标库，构建一个功能齐全的侧边栏和主导航栏。同时实现活动状态显示、适当的图标选择，以及响应式布局设计，以确保良好的导航体验。


### Supabase 入门课程：构建后端系统！  9 次讲座 • 52 分钟

#### Section Overview  章节概述0:46

Set up a free backend with Supabase, create and relate database tables, and load data into your app via an API to build full-stack apps.  
使用 Supabase 创建一个免费的后端接口，创建并关联数据库表，然后通过 API 将数据加载到应用中，从而构建全栈应用程序。

#### What is Supabase?  Supabase 到底是什么？2:24

Enable front-end developers to quickly create a backend with a hosted Postgres database and an auto-generated API using Supabase. Provide authentication and file storage via a JavaScript library.  
使前端开发者能够快速创建一个带有托管 Postgres 数据库和自动生成 API 的后端，使用 Supabase。通过 JavaScript 库提供身份验证和文件存储。

#### Creating a New Database  创建新的数据库5:27

Sign up for a free Supabase account and create your first database, exploring the table editor, authentication, storage, API docs, and project settings to model your application state.  
注册一个免费的 Supabase 账户，并创建你的第一个数据库。了解表格编辑器、身份验证、存储机制、API 文档以及项目设置等内容，以便更好地构建你的应用程序模型。

#### Modeling Application State  
建模应用程序状态5:34

Model application state by mapping features to data tables in Supabase and linking bookings, guests, and cabins with foreign keys to form a relational data model.  
通过在 Supabase 中将功能映射到数据表来建模模型应用状态，并使用外键将预订、客人和船舱链接起来，形成一个关系数据模型。

#### Creating Tables  创建表格9:31

Implement cabins, guests, and settings tables with row level security, define fields and types, insert sample rows, and prepare bookings as the next step.  
接下来，需要实现包含行级安全性的客舱、客人和设置表格。定义字段和类型，插入示例行，并准备预订相关的数据。

#### Relationships Between Tables  
表与表之间的关系8:29

Create a bookings table with start and end timestamps, nights, guests, cabin price, extras price, and a total price. Establish foreign keys to cabins and guests to model the relationships.  
创建一个预订表格，包含开始和结束的时间戳、预订的夜数、预订的客人、小屋价格、附加服务价格以及总价格。同时设置外键，将小屋号和客人号关联起来，以体现它们之间的关系。

#### Adding Security Policies (RLS)  
添加安全策略（RLS）6:18

Explore how to access Supabase data via a rest API and JavaScript, and implement row-level security policies to control read access for tables like cabins, using anon keys and authentication.  
了解如何通过 REST API 和 JavaScript 访问 Supabase 数据，并实现行级安全策略，以使用匿名密钥和身份验证来控制对如“cabins”等表的读取访问权限。

#### Connecting Supabase With Our React App  
将 Supabase 与我们的 React 应用程序连接起来8:45

Connect your React app to a Supabase backend by installing the Supabase JavaScript library, creating a client with the URL and anon key, and querying cabins data.  
通过安装 Supabase JavaScript 库，创建客户端对象，使用 URL 和匿名密钥来连接你的 React 应用与 Supabase 后端，然后查询小屋的数据。这样就能将你的 React 应用与 Supabase 后端连接起来。

#### Setting Up Storage Buckets  
设置存储桶4:38

Set up avatars and cabin images storage buckets in Supabase, make them public with secure upload policies, and learn to drag-and-drop upload, fetch URLs, and render images.  
在 Supabase 中设置头像和房间图片的存储桶，通过安全的上传政策将其公开可见。同时学习如何使用拖放操作进行上传、获取 URL 以及渲染图片。


### 反应查询：管理远程状态  
16 次讲座 • 3 小时 18 分钟

#### Section Overview  章节概述0:46

Explore how to use React Query to manage the remote state stored on Superbase, taking over fetching and storing remote data to simplify development.  
了解如何使用 React Query 来管理存储在 Superbase 上的远程状态，从而简化数据获取和存储的过程，提高开发效率。

#### What is React Query?  什么是 React Query？6:20

discover how React Query caches remote state to speed data access, automatically handles loading and error states, and supports refetching, prefetching, offline use, and seamless synchronization across components.  
发现 React Query 如何缓存远程状态以加速数据访问，自动处理加载和错误状态，并支持重新获取、预获取、离线使用以及跨组件的无缝同步。

#### Setting Up React Query  配置 React 查询功能7:19

Install and configure tanstack react query in your app by creating a QueryClient and wrapping the tree with QueryClientProvider, set a 60-second stale time, and add react query devtools.  
请在您的应用中安装并配置 tanstack React Query 模块。具体操作包括：创建 QueryClient 实例，使用 QueryClientProvider 包裹树形数据结构；设置 60 秒的缓存有效期；以及添加 React Query 开发工具。

#### Make Sure to Use React Query v4!  
请确保使用 React Query 4 版本！0:16

#### Fetching Cabin Data  获取驾驶室数据21:41

Use React Query to fetch cabin data from Supabase with useQuery and a getCabins function, displayed in CabinTable. Observe caching and how data becomes stale and re-fetches.  
使用 React Query 通过 useQuery 和 getCabins 函数从 Supabase 获取客舱数据，并将结果显示在 CabinTable 组件中。注意缓存机制，以及数据如何变得过时并需要重新获取的情况。

#### Mutations: Deleting a Cabin  
突变：删除一个小屋17:21

Mutate remote cabin data with React Query by deleting a cabin using useMutation, invalidate the cabins query on success, and handle errors with onError for smooth UI updates.  
使用 React Query 通过 useMutation 删除小屋来修改远程小屋数据，在成功时使小屋查询失效，并使用 onError 处理错误以实现平滑的 UI 更新。

#### Displaying Toasts (Notifications)  
显示祝福语（通知）8:41

Implement toasts with the React Hot Toast library, configure a global toaster with position and style options, and replace alerts with toast variants like success and error.  
使用 React 的 Hot Toast 库来实现 toast 对话框的显示。配置一个具有位置和样式选项的全局 toast 组件，并将传统的警报对话框替换为成功提示和错误提示等形式的 toast 对话框。

#### Introducing Another Library: React Hook Form  
又一款库登场：React Hook Form11:17

Use React Hook Form to streamline cabin form submissions and errors in a real-world React app, wiring inputs with useForm, register, and handleSubmit.  
在实际的 React 应用中，可以使用 React Hook Form 来简化舱室表单的提交过程，以及处理表单输入时的错误情况。通过 useForm、register 和 handleSubmit 函数来连接表单的输入与处理环节。

#### Creating a New Cabin  创建新的小屋12:13

Use react hook form and react query to create a cabin via the Supabase API, show a success toast, reset the form, and invalidate cabin queries to refresh the UI.  
使用 react hook form 和 react query 通过 Supabase API 创建一个小屋，显示成功提示，重置表单，并使小屋查询失效以刷新 UI。

#### Handling Form Errors  处理表格错误22:42

Learn handling form errors with react hook form, implement min/max validation and custom rules, build a reusable form row, and manage error display and isCreating loading state.  
学习如何使用 React 的 Hook 函数来处理表单错误，实现最小/最大值的验证以及自定义规则。还可以创建可复用的表单行，并管理错误的显示方式，比如创建加载状态。

#### Uploading Images to Supabase  
将图片上传到 Supabase 平台14:41

Learn how to upload cabin images to a Supabase storage bucket by handling a file input, creating a unique image path, and ensuring safe uploads with error handling.  
了解如何通过处理文件输入、创建唯一的图像路径，以及通过错误处理来确保上传过程的安全，将小屋图片上传到 Supabase 存储桶中。

#### Editing a Cabin  编辑一个小屋30:39

edit cabins by reusing the cabin form, pre-filling data via cabinToEdit and isEditSession, and handling image uploads to update or create cabins with a unified createEdit function.  
通过复用现有的“cabin”表单来编辑小屋；通过`cabinToEdit`和`isEditSession`函数预先填充数据；同时处理图片上传操作，以使用统一的`createEdit`函数来更新或创建小屋。

#### Abstracting React Query Into Custom Hooks  
将 React 查询抽象化为自定义钩子函数17:26

Abstract React query logic into reusable custom hooks—use delete cabin, use create cabin, use edit cabin, and use cabins—to clean components and enable cross-feature data reuse with caching.  
将反应式查询逻辑封装到可复用的自定义钩子中——使用“删除舱”、“创建舱”、“编辑舱”以及“其他舱”这些钩子来优化组件结构，并通过缓存实现跨功能数据复用。

#### Duplicating Cabins  复制小屋6:49

Learn to duplicate a cabin with a dedicated button, icons, and a useCreateCabin hook. Create a new cabin with copied data via React Query's useMutation and provide user feedback.  
学习如何通过一个专门的按钮、图标以及名为 useCreateCabin 的函数来复制一个小屋。通过 React Query 的 useMutation 函数，用复制的数据创建一个新的小屋，并让用户能够给出反馈。

#### Fetching Applications Settings  
获取应用设置信息8:47

Learn to fetch application settings with React Query using a get settings function and a useSettings hook, populate a single settings row in a form, and prep for updates.  
学习如何使用 React Query 来获取应用程序的设置。通过调用 getSettings 函数，可以获取设置信息；同时利用 useSettings 钩子来填充表单中的设置项。此外，还需要为未来的更新做好准备。

#### Updating Application Settings  
更新应用设置11:27

Update application settings individually with a reusable hook and on blur triggers, using React Query to update settings and invalidate the settings query for automatic refresh.  
通过可复用的钩子函数，分别更新应用程序的设置。在模糊处理触发时执行这些操作。使用 React Query 来更新设置，并使设置查询失效，以实现自动刷新。


### 先进的 React 模式  
13 次讲座 • 2 小时 40 分钟

#### Section Overview  章节概述1:13

Explore advanced React patterns used by senior developers, including render props, higher-order components, and the compound component pattern, and build a reusable model window and a context menu.  
探索高级开发者使用的 React 模式，如渲染属性、高阶组件以及复合组件模式。同时，学习如何构建可复用的模型窗口和上下文菜单。

#### An Overview of Reusability in React  
React 中可重用性的概述5:44

Explore advanced React patterns to boost reusability of UI and stateful logic. Learn render props, children and compound components, and when to use custom hooks to share logic across components.  
探索先进的 React 设计模式，以提高 UI 界面和状态逻辑的可复用性。了解如何使用渲染属性、子组件和复合组件，以及何时使用自定义钩子来在不同组件之间共享逻辑。

#### Setting Up an Example  树立一个榜样6:19

Set up a runnable Code Sandbox example to explore the render props pattern, reusing a shared list component for products and companies with custom hooks and render-based item rendering.  
设置一个可运行的 Code Sandbox 示例，用于探索渲染属性模式，重用共享列表组件来展示产品和公司，并使用自定义钩子和基于渲染的项渲染。

#### The Render Props Pattern  渲染属性模式7:42

Explore how the render props pattern uses a render function to describe how to render each item, enabling inversion of control and reusable lists for products and companies.  
了解渲染属性模式是如何使用渲染函数来描述每个项目的渲染方式的。这种模式能够实现控制权的反转，并且使得产品和公司的列表可以重复用于不同的场景。

#### A Look at Higher-Order Components (HOC)  
关于高阶组件的一些介绍8:06

Explore how higher-order components enhance a wrapped component by returning a new component that adds toggles and other functionality.  
了解高阶组件如何通过返回一个新的组件来增强包装组件的功能，这个新组件包含了切换按钮以及其他功能。

#### The Compound Component Pattern  
复合组件模式18:17

Discover the compound component pattern in React by building a flexible counter with a parent and child components like count, label, increase, and decrease, using context for state sharing.  
通过构建一个具有父组件和子组件（如计数、标签、增加、减少等）的灵活计数器，来了解 React 中的复合组件结构。同时，利用上下文进行状态共享，实现组件之间的协作。

#### Building a Modal Window Using a React Portal  
使用 React Portal 构建模态窗口19:54

Build a reusable modal component using a React portal to render content outside the main DOM. Include an overlay and close handling with onClose, and pass the form as children.  
使用 React 的门户组件构建一个可复用的模态组件，以便将内容展示在主 DOM 之外。同时，添加覆盖层以及关闭功能的处理逻辑，并将表单作为子节点传递给该组件。

#### Converting the Modal to a Compound Component  
将模态窗口转换为复合组件21:45

Convert a modal into a compound component that manages its own open state with a context. Define modal.open and modal.window, support multiple named windows, and pass open handlers via cloneElement.  
将模态窗口转换为一个复合组件，该组件能够管理自己的开放状态，并带有特定的上下文。定义 modal.open 和 modal.window 两个方法，支持多个命名窗口，并通过 cloneElement 传递开放处理程序。

#### Detecting a Click Outside the Modal  
检测模态窗口外的点击13:36

Detect clicks outside a modal and close it using a global click listener in useEffect, a ref for containment, and the capturing phase; then create a reusable useOutsideClick hook.  
检测模态框外的点击并使用 useEffect 中的全局点击监听器关闭它，使用 ref 作为边界，并在捕获阶段；然后创建一个可重用的 useOutsideClick 钩子。

#### Confirming Cabin Deletions  
确认舱位的删除操作8:41

Reuse a modal window to edit cabins and confirm cabin deletions, wiring open/close and onConfirm flows with a reusable ConfirmDelete component for safe, user-confirmed actions.  
重用模态窗口编辑客房并确认客房删除，使用可重用的 ConfirmDelete 组件连接打开/关闭和 onConfirm 流程，以确保安全且经过用户确认的操作。

#### Building a Reusable Table  
构建可重复使用的桌子11:45

Learn to build a reusable table using the compound component pattern, pass column definitions via a columns prop, and share them through a table context for header and row components.  
学习如何使用复合组件模式来构建可重复使用的表格组件。通过`columns`属性传递列定义，并通过表格上下文将列定义分享给标题和行组件。

#### Applying the Render Props Pattern  
应用渲染属性模式4:38

Implement a reusable table body with the render props pattern by passing data and render prop, and handle empty data with a 'no data to show at the moment' message.  
通过传递数据和渲染属性，实现一个可重复使用的表格体组件。对于空数据，请使用“当前暂无数据显示”的提示信息来处理。

#### Building a Reusable Context Menu  
构建可重复使用的上下文菜单  预览版32:35

Demonstrate building a reusable context menu via the compound component pattern, enabling one open menu at a time with Menu.Toggle, Menu.List, and Menu.Button, plus dynamic positioning and outside-click support.  
演示如何通过复合组件模式来构建可重复使用的上下文菜单。可以通过 Menu.Toggle、Menu.List 和 Menu.Button 这三个组件来同时打开多个菜单项，并且支持动态定位以及外部点击操作。


### [可选] 添加更多功能：认证系统、深色模式、控制面板等。  
33 次讲座 • 7 小时 51 分钟

#### Section Overview  部分概述1:16

Add real-world features such as filtering, sorting, and paginating data, implement a dark mode, and build a dashboard with multiple charts, while enabling authentication and authorization with Supabase.  
添加现实世界的功能，如数据过滤、排序和分页；实现深色模式；构建包含多种图表的仪表盘。同时，通过 Supabase 实现身份验证和授权功能。

#### Client-Side Filtering: Filtering Cabins  
客户端侧过滤：对舱位进行过滤处理18:49

Implement client-side filtering for cabins using a reusable filter component powered by URL search params, enabling all/no discount/with discount filters and later reusability for bookings.  
为客栈住宿实施客户端过滤功能，使用一种可复用的过滤组件，该组件能够根据 URL 查询参数进行定制。用户可以选择启用全部优惠/无优惠过滤功能，或者仅启用部分过滤功能。此外，这些过滤功能在后续预订操作中仍可复用。

#### Client-Side Sorting: Sorting Cabins  
客户端排序：对集装箱进行排序20:25

Master client-side sorting for a cabins table with reusable sort and filter components, using search params to encode field and direction (name.asc, price.desc) and sort by name, price, or capacity.  
为 cabins 表格实现客户端排序功能，使用可复用的排序和过滤组件。通过搜索参数来指定字段和排序方向（如 name.asc、price.desc），并可以根据名称、价格或容量进行排序。

#### Building the Bookings Table  
构建预订表17:59

Bring bookings into the app with Supabase, create a booking, and render a bookings table showing cabin and guest data via foreign keys, loaded via React Query.  
通过 Supabase 将预订信息导入应用程序，创建预订记录，并展示包含客舱和客人数据的预订表格。这些数据通过 React Query 加载进来。

#### Uploading Sample Data  上传样本数据6:15

Upload sample data to Supabase with the uploader component for bookings, cabins, and guests. Apply authentication policies, then use upload all to populate and review dynamic booking dates and statuses.  
使用预订、客舱和客人相关的上传组件，将样本数据上传到 Supabase 数据库。执行身份验证操作后，通过“全部上传”功能来填充动态预订日期和状态信息，并对其进行审核。

#### API-Side Filtering: Filtering Bookings  
API 端过滤：对预订信息进行筛选17:10

Implement API-side filtering for bookings with a dynamic supabase query, enabling status filters and preparing for server-side sorting, while syncing the URL and leveraging react-query cache.  
为预订操作实施 API 端的过滤功能，使用动态 Supabase 查询来实现状态过滤功能，同时支持服务器端排序操作。同时，需要同步 URL 参数，并利用 react-query 缓存来提升性能。

#### API-Side Sorting: Sorting Bookings  
API 端排序：对预订信息进行排序6:53

Sort bookings on the server side using the Supabase API by deriving sortBy from the URL, passing it to getBookings, and applying ascending or descending order in the query.  
通过 Supabase API 在服务器端对预订进行排序。具体做法是：从 URL 中提取 sortBy 参数，将其传递给 getBookings 函数，然后在查询中设置升序或降序排序。

#### Building a Reusable Pagination Component  
构建可重复使用的分页组件15:52

Learn how to build a reusable pagination component that syncs the current page with the URL using useSearchParams, displaying results, page counts, and next/previous controls.  
学习如何构建一个可重用的分页组件，使用 useSearchParams 同步当前页码与 URL，显示结果、页码总数以及下一页/上一页控制。

#### API-Side Pagination: Paginating Bookings  
API 端分页：对预订信息进行分页处理10:39

demonstrates implementing api-side pagination for bookings using supabase range queries, counting results, and client-side page management with caching and prefetching to improve user experience.  
该示例展示了如何使用 Supabase 的范围查询来实现预约请求中的分页处理，同时利用客户端端的页面管理、缓存和预加载技术来提升用户体验。

#### Prefetching With React Query  
使用 React 查询进行预加载7:58

Learn how to implement prefetching with React Query to preload adjacent pages in pagination, improving UX by caching the next (and previous) page data and avoiding loading spinners.  
了解如何使用 React Query 实现预加载功能，从而在分页情况下预加载相邻页面内容。这种技术可以通过缓存下一页和上一页的数据来改善用户体验，避免不必要的加载过程。

#### Building the Single Booking Page  
构建单一的预订页面15:17

Navigate to the booking detail page from each booking row using the context menu. Fetch data with a custom useBooking hook and render the presentational booking detail component.  
可以通过每个预订行中的上下文菜单访问预订详情页面。使用自定义的 useBooking 钩子函数来获取数据，然后渲染相应的预订详情组件。

#### Checking In a Booking  检查预订信息28:18

Implement a check-in flow for unconfirmed bookings, requiring payment confirmation, and update the booking to checked in and paid via a dedicated route, button, and verification checkbox.  
为未确认预订的订单实现一种登记流程，要求用户确认付款。然后通过一个专门的按钮和验证复选框，将预订状态更新为已登记且已付费状态。

#### Adding Optional Breakfast  
添加可选的早餐服务14:04

Add optional breakfast at check-in by toggling a breakfast checkbox, render it only for guests who didn't have breakfast, and update has breakfast, extras price, total price from settings.  
可以在入住时通过选择“提供早餐”选项来添加额外的早餐服务。只有那些没有享用早餐的客人才会享受到这项服务。同时，可以通过设置来更新“早餐费用”、“附加费用”以及总价格信息。

#### Checking Out a Booking (+ Fixing a Small Bug)  
检查预订情况（+修复一个小错误）9:50

Implement a guest checkout flow by adding a checkout button for checked-in guests, updating booking status to checked-out via a mutation, and reset pagination to page one when filters change.  
通过为已入住的客人添加结账按钮，通过变异更新预订状态为已结账，并在筛选条件更改时将分页重置为第一页来实现客人结账流程。

#### Deleting a Booking  删除预订13:33

Learn to delete a booking via a confirm modal, wire a delete booking mutation with query invalidation, and navigate back after successful deletion.  
学习如何通过确认模态框删除预订，连接删除预订的变异体并实现查询失效，并在成功删除后返回。

#### Authentication: User Login With Supabase  
身份验证：使用 Supabase 进行用户登录22:06

Implement authentication with Supabase, including sign up, login, password management, and avatars, via a login form and React Query mutations; secure JWT sessions and authorize dashboard access.  
通过 Supabase 实现身份验证功能，包括注册、登录、密码管理以及头像显示等功能。所有操作都通过登录表单和 React Query 模块来实现。同时，还需要对 JWT 令牌进行安全处理，并授权仪表板的访问权限。

#### Authorization: Protecting Routes  
授权：保护路由23:23

Wrap the app layout in a protected route to restrict access, load the current user from supabase, show a spinner while authenticating, and redirect to login if unauthenticated.  
将应用程序的界面封装在一个受保护的路由中，以限制访问权限。从 supabase 中获取当前用户的信息，在认证过程中显示旋转指示器。如果用户未通过认证，则重定向到登录页面。

#### User Logout  用户已退出会话9:00

Add a header logout button wired to a Supabase auth sign-out, using a React Query mutation to navigate to the login page and clear the React Query cache.  
在 Supabase 的认证注销功能中添加一个头部按钮，该按钮可以触发 React Query 的突变操作，从而导航到登录页面，并清除 React Query 的缓存。

#### Fixing an Important Bug  正在修复一个重要的漏洞5:35

Fix a critical authentication bug by correcting the cache update in useLogin, switching to setQueryData and using the proper user data to ensure correct isAuthenticated state and protected route access.  
修复了一个严重的认证漏洞，通过修正使用过的 cache 更新机制，切换为 setQueryData 方法，并使用正确的用户数据，以确保用户认证状态正确，并允许对受保护的路由进行访问。

#### Building the Sign Up Form  
构建注册表单10:59

Build a hotel staff sign-up form with react-hook-form, validating required fields, email pattern, password min length, and password match, then submit to create a new user on Supabase.  
使用 react-hook-form 构建一个酒店员工注册表单，验证必填字段、电子邮件格式、密码长度要求以及密码匹配情况，然后提交表单以在 Supabase 上创建新用户。

#### User Sign Up  用户注册15:01

Sign up users via a signUp API with email, password, and info like full name and avatar; manage signup with a useSignup mutation in React Query and enable email verification.  
通过一个 signUp API 注册用户，使用电子邮件、密码以及姓名和头像等信息；在 React Query 中使用 useSignup 变异来管理注册，并启用电子邮件验证。

#### Authorization on Supabase: Protecting Database (RLS)  
在 Supabase 上进行授权：保护数据库（RLS）4:40

enforce server-side authorization on Supabase by updating row-level security policies to require authentication, preventing data exposure for cabins and bookings when not authenticated.  
通过更新行级安全策略来实施服务器端授权功能，要求在使用 Supabase 时必须进行身份验证。这样就能防止在用户未认证的情况下，数据和预订信息被泄露。

#### Building The App Header  构建应用程序头部界面10:18

Build the app header with username and avatar, plus a header menu, using a HeaderMenu component and styled header. Connect navigation to the account page and render user metadata.  
在应用程序的头部区域添加用户名和头像，同时加入一个头部菜单。可以使用 HeaderMenu 组件来实现这一布局，并对其进行样式设计。还将导航栏与账户页面连接在一起，同时显示用户的元数据信息。

#### Updating User Data and Password  
更新用户数据和密码28:26

Update user data, password, and avatar with prebuilt forms, an authentication API, and a use user hook, handling avatar uploads to storage and preparing for dark mode.  
通过预构建的表单、身份验证 API 以及用户钩子函数，更新用户数据、密码和头像。同时处理头像的上传至存储系统的操作，并为深色模式做相应准备。

#### Implementing Dark Mode With CSS Variables  
使用 CSS 变量实现深色模式21:29

Implement dark mode using css variables and light and dark class themes, add a dark mode toggle and context to share state, and update the root class to switch modes.  
通过 CSS 变量实现深色模式，并设置相应的深色和浅色类主题。添加深色模式切换功能，并为其提供相应的上下文信息。同时，更新根类以允许切换模式。

#### Building the Dashboard Layout  
构建仪表板布局4:25

Build the dashboard layout to render today's check-ins and checkouts, display booking statistics, and show sales and stay duration charts with a 7-30-90 day filter.  
请设计好仪表盘的布局，以便能够显示当天的入住和退房信息、预订统计数据，并展示销售情况以及入住时间的图表。同时，可以使用 7 天、30 天和 90 天的筛选功能来查看相关数据。

#### Computing Recent Bookings and Stays  
查询最近的预订记录及住宿信息15:20

Differentiate bookings as sales from stays as actual check-ins, and learn to compute both for the last 7, 30, or 90 days using React Query hooks.  
将预订行为区分于实际入住行为，学会使用 React Query 钩子来计算过去 7 天、30 天或 90 天的这两种情况的统计数据。

#### Displaying Statistics  显示统计数据12:02

Create a reusable stats component to display bookings, check-ins, occupancy rate, and total sales by computing values from bookings and confirmed stays, formatting currency, and styling with icons and colors.  
创建一个可复用的统计组件，用于展示预订信息、入住情况、入住率以及总销售额。通过从预订记录和已确认入住数据中计算数值，对货币数据进行格式化处理，并使用图标和颜色进行视觉设计。

#### Displaying a Line Chart With the Recharts Library  
使用 Recharts 库展示折线图22:55

Build a responsive line chart (area) using Recharts in React, featuring total sales and extra sales data, with tooltips, axes, and dark mode color support.  
使用 React 中的 Recharts 构建一个响应式的折线图（面积型），展示总销售额和额外销售数据。该图表支持工具提示、坐标轴功能，并且可以支持深色模式下的颜色显示。

#### Displaying a Pie Chart  显示饼形图15:52

Build a dynamic pie chart and a stay duration chart in a React dashboard, switching start data for dark and light modes and updating with real data.  
在 React 仪表板上创建一个动态的饼图，以及一个显示停留时长的图表。可以切换深色和浅色模式下的数据显示，并实时更新数据。

#### Displaying Stays for Current Day  
展示当前日期的展览作品20:41

Build the today activity dashboard that lists arrivals and departures with guests, country flags, nights, and check-in/out actions.  
请构建今天的活动仪表盘，该仪表盘能够列出与客人相关的到达和离开信息，同时显示国家标志、住宿天数以及入住/退房操作。

#### Error Boundaries  错误边界9:44

Learn to implement React error boundaries to catch rendering errors by wrapping the app with the React error boundary package, providing a fallback interface and a reset action to recover.  
学习如何使用 React 的错误边界功能来捕获渲染过程中的错误。通过将应用程序包装在 React 的错误边界包中，可以提供一个备用接口，并提供一个重置动作来恢复程序的正常运行。

#### Final Touches + Fixing Bugs  
最后的调整与漏洞修复工作14:38

Finish the final touches by fixing the menu outside-click bug, switching event listening from capturing to bubbling, and improving dark mode defaults for a polished React app.  
完成最后的调整工作，修复菜单中出现的外部点击错误，调整事件监听方式，从捕获模式改为气泡模式，并改进暗模式下的默认设置，从而打造出一个精美的 React 应用程序。


### 使用 Netlify 和 Vercel 进行部署  4 次讲座 • 35 分钟

#### Section Overview  部分概述1:09

Deploy your stunning project by uploading the production build to Netlify, then set up a Git and GitHub repository for continuous deployment with Vercel.  
通过将生产版本的代码上传到 Netlify，你可以顺利部署你的精彩项目。接下来，可以使用 Vercel 来配置一个基于 Git 的 GitHub 仓库，以实现持续部署。

#### Deploying to Netlify  在 Netlify 上部署11:20

Deploy a vite-built react app to Netlify by building the dist bundle and creating a Netlify.toml redirects file.  
通过构建分发包并创建 Netlify.toml 重定向文件，将基于 Vite 的 React 应用程序部署到 Netlify 上。

#### Setting Up a Git and GitHub Repository  
设置 Git 和 GitHub 仓库16:16

Install Git and initialize a local repository, stage and commit changes, create a readme, and push to a remote GitHub repository using origin main and a personal access token.  
安装 Git，创建一个本地仓库。将所做的更改添加到本地仓库中，进行提交操作。编写一份说明文件，然后使用个人访问令牌将代码推送到远程的 GitHub 仓库中，远程仓库的地址为 origin/main。

#### Deploying to Vercel  将项目部署到 Vercel 平台5:54

deploy a react app to vercel with continuous integration from github, run npm run build, deploy automatically, while comparing vercel with netlify and managing domains for Next.js projects.  
将 React 应用部署到 Vercel，并从 GitHub 实现持续集成，运行 npm run build，自动部署，同时比较 Vercel 和 Netlify，并为 Next.js 项目管理域名。

##   第五部分：使用 Next.js 实现全栈 React 应用 
[1 个项目]
2 次讲座 • 2 分钟

#### 第五部分的介绍
Introduction to Part 5
1:23

Master server side rendering with Next.js, learn file-based routing, Suspense, and server components to build a modern, performance-focused customer-facing site for The Wild Oasis.  
掌握使用 Next.js 进行服务器端渲染，学习基于文件的路由、Suspense 和服务器组件，为 The Wild Oasis 构建一个现代化、注重性能的客户端网站。

#### 第 5 部分的实用资源
Useful Resources for Part 5  
0:56


### Next.js 概述：使用“App”路由器  
18 次讲座 • 3 小时 23 分钟

#### Section Overview  部分概述0:46

Explore server-side rendering with Next.js, learn the modern Next.js app router, and dive into React Server Components through diagrams and practical concepts.  
探索 Next.js 的服务器端渲染，学习现代 Next.js 应用路由器，并通过图表和实际概念深入了解 React Server Components。

#### Download Fresh Starter Files + Slides!  
下载新的启动文件及幻灯片！0:11

#### An Overview of Server-Side Rendering (SSR)  
服务器端渲染概述15:45

Explore how server-side rendering with Next.js blends server and client rendering to boost initial page load, SEO, and hydration, while comparing to client-side rendering and static or dynamic rendering.  
了解如何使用 Next.js 的服务器端渲染技术，将服务器端渲染与客户端渲染相结合，从而提升页面的初始加载速度、搜索引擎优化以及数据加载效率。同时，对比一下这种技术与其他服务器端渲染方式或静态/动态渲染方式的差异。

#### Experiment: Manual SSR With React DOM + Node.js  
实验：使用 React DOM 和 Node.js 进行手动的 SSR 操作26:32

Experiment with a bare bones server-side rendering setup using a node.js server, react-dom renderToString, and an html template to render React components on the server, and discuss hydration for interactivity.  
尝试使用 Node.js 服务器、react-dom 的 renderToString 功能，以及一个用于在服务器上渲染 React 组件的 HTML 模板，来构建一个基础级的服务器端渲染架构。同时，探讨一下如何实现交互性，比如通过“hydration”技术来提升用户体验。

#### The Missing Piece: Hydration  
缺失的碎片：水分补充6:33

Learn how hydration restores interactivity after server-side rendering in React apps, by loading the client bundle and matching the server-rendered DOM to reattach event handlers, with common hydration errors.  
了解如何在 React 应用中通过加载客户端代码并匹配服务器渲染的 DOM 结构来恢复交互性。同时，也会讨论常见的数据同步错误及其解决方案。

#### Implementing Hydration  实施保湿措施15:04

Implement hydration by hydrating a server-rendered html on the client with ReactDOM.hydrateRoot, attaching interactivity and state using Babel, React, and a client script.  
通过 ReactDOM.hydrateRoot 在客户端上对服务器渲染的 HTML 进行水合，使用 Babel、React 和客户端脚本附加交互性和状态。

#### What is Next.js?  Next.js 是什么？12:03

Explore Next.js, the meta-framework for React, delivering server-side rendering, routing, data fetching, and performance optimizations with the app router and page router.  
探索 Next.js，这是一个专为 React 设计的元框架，它能够实现服务器端渲染、路由管理、数据获取以及性能优化。同时，它还可以通过应用路由器和页面路由器来实现这些功能。

#### Setting Up a Next.js Project  
配置一个 Next.js 项目10:11

Set up a Next.js 14 project with npx create-next-app, enable the app router, and install Tailwind and ESLint while exploring the project structure and server-side rendering.  
使用 npx create-next-app 工具创建一个新的 Next.js 14 项目。启用应用程序路由功能，同时安装 Tailwind 和 ESLint 工具。在探索项目结构和服务器端渲染功能的同时进行开发。

#### Frequent Next.js Updates + Documentation  
频繁的 Next.js 更新与文档更新0:37

#### Defining Routes and Pages  
定义路由和页面9:38

Learn how Next.js app router creates routes by folders and page.js components, building pages like /cabins, /about, and /account, with server-side rendering and nested routes.  
学习 Next.js 应用路由器如何通过文件夹和 page.js 组件创建路由，构建如 /cabins、/about 和 /account 的页面，并支持服务器端渲染和嵌套路由。

#### Navigating Between Pages  在页面之间切换导航9:25

Learn to navigate Next.js pages with the link component, gaining faster transitions, prefetching, and client-side routing while building a reusable navigation for cabins, about, account pages and discuss server components.  
学习如何使用链接组件在 Next.js 中导航页面，从而实现更快速的过渡效果、预加载功能以及客户端路由功能。同时，还能为小屋页面、关于页面和账户页面构建可复用的导航结构。此外，还可以了解服务器组件的相关内容。

#### Creating a Layout  创建布局12:13

Create a global root layout in a Next.js app to wrap all routes with the children prop and export metadata for the page title, while exploring server components.  
在 Next.js 应用中创建一个全局的根布局，用`children`属性包裹所有路由，并为页面标题导出元数据。同时，了解服务器组件的工作原理。

#### What are React Server Components? (RSC – Part 1)  
什么是 React 服务器组件？（RSC——第一部分）32:04

Discover React server components (RSC) and how server components render on the server, provide data fetching without JavaScript, and pair with client components for interactive user interfaces.  
了解 React 服务器组件（RSC）的工作原理，以及这些组件如何在服务器上进行渲染。它们能够在不依赖 JavaScript 的情况下获取数据，并且可以与客户端组件结合使用，打造出更加互动的用户界面。

#### Fetching Data in a Page  
在页面中获取数据6:36

Fetch data directly in a Next.js server component using fetch and await, render the results as HTML, and observe server-side logging and caching before introducing a client component for interactivity.  
在 Next.js 服务器组件中直接使用 fetch 和 await 获取数据，将结果渲染为 HTML，并在引入客户端组件以实现交互之前观察服务器端日志和缓存。

#### Adding Interactivity With Client Components  
增加与客户端组件的交互性8:02

Build a counter as a client component to add interactivity, learn that useState can't run in server components, and apply the use client directive with server-to-client data bridging and hydration.  
构建一个客户端组件作为计数器以增加交互性，了解 useState 无法在服务器组件中运行，并应用 use client 指令进行服务器到客户端的数据桥接和激活。

#### Displaying a Loading Indicator  
显示加载指示器6:28

Display a global loading indicator in Next.js by adding a loading.js in the app folder that shows LOADING DATA for all pages while data loads, including nested routes.  
在 Next.js 中显示全局加载指示器，只需在 app 文件夹中添加 loading.js 文件，该文件会在数据加载过程中显示所有页面的加载状态，包括嵌套路由。

#### How RSC Works Behind the Scenes (RSC – Part 2)  
RSC 是如何在幕后运作的（RSC——第二部分）18:35

Learn how React server components render on the server and client components render on the client, bridged by the RSC payload and two-step rendering.  
了解 React 服务器组件如何在服务器端渲染，而客户端组件如何在前端渲染。这两部分通过 RSC 负载进行连接，实现了两步渲染的架构。

#### RSC vs. SSR: How are They Related? (RSC – Part 3)  
RSC 与 SSR：它们有什么关系呢？（RSC——第三部分）12:15

Discover how React server components (RSC) relate to server-side rendering (SSR) in Next.js, as server and client components render on the server, produce HTML, and hydrate on the client.  
了解在 Next.js 中，React 服务器组件如何与服务器端渲染机制协同工作。服务器上的组件会生成 HTML 代码，而客户端组件则负责最终的渲染过程。通过这种方式，服务器和客户端可以协同工作，实现更高效的渲染效果。


### 开始构建“野生绿洲”网站  
12 次讲座 • 1 小时 49 分钟

#### Section Overview  部分概述0:51

Create a clean Next.js project structure and build essential pages, then load and optimize fonts and images, add metadata and a favicon, and implement nested layouts with multiple layout.js files.  
创建一个整洁的 Next.js 项目结构，构建必要的页面。然后加载并优化字体和图片，添加元数据及图标，最后实现多层次的布局，使用多个 layout.js 文件来处理复杂的布局需求。

#### Project Planning: "The Wild Oasis" Customer Website  
项目规划："The Wild Oasis" 客户网站13:09

Plan and build a customer website for The Wild Oasis with Next.js, Supabase, and Tailwind, featuring cabin listings, reservations, authentication, and a guest account with profile data.  
为 The Wild Oasis 计划并使用 Next.js、Supabase 和 Tailwind 构建客户网站，该网站将展示小屋列表、预订功能、身份验证以及带有个人资料的访客账户。

#### Project Organization  项目组织11:01

Organize a Next.js project with a dedicated components folder, private underscore-prefixed folders to hide routes, and import aliases for clean, scalable module paths.  
为 Next.js 项目创建一个专门的组件文件夹，使用下划线开头的文件夹来隐藏路由路径，并导入别名以创建简洁、可扩展的模块路径。

#### Styling With Tailwind CSS  
使用 Tailwind CSS 进行样式设计9:24

Set up Tailwind CSS in a Next.js project and apply its utility classes to style the layout. Customize the theme with tailwind.config.js and colors.json to create a branded color system.  
在 Next.js 项目中配置 Tailwind CSS，并利用其工具类来设计布局样式。通过 tailwind.config.js 文件和 colors.json 文件来定制主题，从而创建属于自己的品牌色彩系统。

#### Adding Page Metadata and Favicon  
添加页面元数据和 Favicon10:25

Learn to add metadata and a favicon in Next.js by exporting a layout metadata object to set titles and descriptions, with per-page overrides and an Icon favicon.  
在 Next.js 中，可以通过导出布局元数据对象来添加元数据和图标。这样就能设置标题和描述，并且每个页面都可以进行自定义设置，还可以选择不同的图标图标。

#### Loading and Optimizing Fonts  
加载和优化字体6:24

Use Next.js font optimizations to self-host fonts, reduce layout shifts, and improve privacy. Import Google fonts with next/font/google, set subset and display swap, and apply the generated class name.  
使用 Next.js 的字体优化功能来自定主机字体，减少布局的变动，并提升隐私保护效果。通过 next/font/google 导入 Google 字体；设置子集样式并切换显示模式；同时应用生成的类名称。

#### Improving the Navigation and Root Layout  
改进导航和根布局的设计7:02

Improve navigation and finalize the root layout in a Next.js project by integrating a header with links, applying flex-based centering, and preparing for image performance optimizations.  
改进导航并在 Next.js 项目中最终确定根布局，通过集成带有链接的页眉、应用基于 flex 的居中以及对图像性能优化做准备。

#### Optimizing Images With Next.js <Image /> Component  
使用 Next.js 的 Image 组件来优化图片效果8:39

Optimize images with the Next.js image component, ensuring automatically sized images, lazy loading, and layout stability to improve page speed.  
使用 Next.js 的图片组件来优化图片显示效果，确保图片尺寸能够自动调整、加载过程更加流畅，同时保证布局的稳定性，从而提升页面加载速度。

#### Building the Home Page  构建主页9:47

Build and style a responsive home page in Next.js by replacing the old image usage with the built-in image component, optimizing with fill, object cover, placeholder blur, and static imports.  
使用 Next.js 中的内置图像组件替换旧的图像使用，并通过 fill、object cover、模糊占位符和静态导入来优化，构建和样式化一个响应式主页。

#### Building the About Page With Responsive Images  
构建带有响应式图片的关于页面13:29

Learn to build an about page with responsive images using Next.js image component, including static imports, fill prop, and container-based sizing with aspect ratio and object-fit cover.  
学习如何使用 Next.js 的图片组件来构建带有响应式图片的关于页面。其中包括静态导入、填充属性，以及基于容器大小的布局设计，还会涉及保持特定比例和对象拟合覆盖效果的实现。

#### Adding Nested Routes and Pages  
添加嵌套的路由和页面6:55

Add nested routes and pages for account, reservations, and profile in Next.js using file-system routing and page.js, install date-fns, and set up a nested layout.  
使用文件系统路由和 page.js 在 Next.js 中为账户、预订和个人资料添加嵌套路由和页面，安装 date-fns，并设置嵌套布局。

#### Adding a Nested Layout  添加嵌套布局12:13

Learn to implement nested layouts in Next.js by adding a persistent account-side navigation across routes, using a top-level route layout and a nested account layout with a children prop.  
在 Next.js 中学习如何实现嵌套布局。通过在一个顶级路由布局中添加一个持久性的账户侧导航栏，然后使用一个带有`children`属性的嵌套账户布局来实现这一功能。


### 数据获取、缓存和渲染  
18 场讲座 • 2 小时 49 分钟

#### Section Overview  章节概述1:03

Explore data handling in Next.js by streaming data, implementing Suspense and loading.js, and choosing between static and dynamic rendering. Learn caching strategies to boost performance.  
在 Next.js 中探索数据处理技术，包括数据流式处理、使用 Suspense 和 loading.js 功能，以及选择静态渲染或动态渲染方式。了解缓存策略以提升性能。

#### Setting Up Supabase  配置 Supabase11:43

Set up Supabase for the project, install @supabase/supabase.js, configure environment variables for Supabase URL and key, and explain server vs client access and row level security.  
为项目设置 Supabase，安装 @supabase/supabase.js，配置 Supabase URL 和密钥的环境变量，并解释服务器与客户端访问以及行级安全。

#### Fetching and Displaying Cabin List  
获取并显示车厢列表11:13

Fetch cabin data on the server with getCabins, then render a list of cabin cards using Next.js image optimization and server components, with routing to cabin detail pages.  
使用 getCabins 从服务器获取船舱数据，然后使用 Next.js 的图像优化和服务器组件渲染船舱卡片列表，并路由到船舱详情页面。

#### Streaming Route Segments With loading.js File  
使用 loading.js 文件来流式传输路由段7:15

Explore implementing a global and route-specific loading indicator with loading.js for the cabins route, using a spinner and centered styling, and understand how streaming requires JavaScript.  
尝试在 cabins 路由中使用 loading.js 来实现全局和基于路径的加载指示器功能。同时，注意使用 spinner 动画和居中布局风格。了解流式传输功能是如何依赖 JavaScript 来实现的吧。

#### What is React Suspense?  什么是 React Suspense？12:18

Learn to use React Suspense to declaratively handle asynchronous data fetching and lazy loading with boundaries and fallbacks, enabling cleaner components and library-backed data flows (React Query, Next.js).  
学习如何使用 React Suspense 来动态地处理异步数据获取和延迟加载操作，同时设置适当的边界和回退机制。这样能够使得组件更加简洁，并且利用各种库提供的功能来优化数据流动（如 React Query、Next.js）。

#### Streaming UI With Suspense: Cabin List  
流式传输的界面设计，带来惊喜体验：车厢列表10:29

Learn to stream the cabin list with React Suspense in Next.js by moving data fetching into a CabinList component wrapped in a Suspense boundary with a spinner fallback.  
通过将在 Next.js 中使用 React Suspense 来流式传输数据，可以将数据获取操作封装到一个名为“CabinList”的组件中。同时，将 Suspense 边界应用于该组件，以显示加载 spinner。

#### Dynamic Route Segments: Building the Cabin Page  
动态路由段：构建“Cabin 页面”10:25

Use dynamic route segments to render individual cabin pages. Read the cabin id from the url via params and fetch cabin data for each page.  
使用动态路由段来渲染单个船舱页面。从 URL 的 params 中读取船舱 id，并为每个页面获取船舱数据。

#### Generating Dynamic Metadata  
生成动态元数据3:39

Generate metadata dynamically with an async generateMetadata function that uses current params to fetch the cabin name and build the title like cabin 004.  
通过一个异步的 generateMetadata 函数动态生成元数据。该函数会使用当前的参数来获取小屋的名称，并构建如“小屋 004”这样的标题。

#### Error Handling: Setting Up Error Boundaries  
错误处理：设定错误边界9:03

Set up a global error boundary as a client component to catch rendering errors and show meaningful messages. Explore error.js usage and when to employ global-error.js for root layout issues.  
作为客户端组件，设置一个全局的错误边界，以便捕获渲染过程中的错误并显示有意义的错误信息。了解 error.js 的使用方法，以及何时使用 global-error.js 来解决布局相关问题。

#### Error Handling: "Not Found" Errors  
错误处理：“未找到”错误5:45

Explore handling not found errors in Next.js by using automatic 404s, manually triggering the notFound function, and building custom not-found pages for routes like cabins.  
探索在 Next.js 中处理找不到资源的情况的方法。可以通过利用自动生成的 404 错误页面，或者手动触发 notFound 函数来处理找不到资源的情况。此外，还可以为类似“小木屋”这样的路由创建自定义的找不到资源时的页面。

#### Different Types of SSR: Static vs. Dynamic Rendering  
不同类型的 SSR：静态与动态渲染14:14

Explore static vs dynamic rendering in Next.js by inspecting route-level rendering at build time or on request, with ISR and serverless functions on the edge.  
通过检查构建时或请求时的路由级渲染效果，来了解 Next.js 中的静态与动态渲染差异。同时，可以利用边缘端的 ISR 技术和无服务器函数来实现更高效的渲染处理。

#### Analyzing Rendering in Our App  
分析我们应用程序中的渲染过程4:47

Analyze static versus dynamic rendering in Next.js by examining routes rendered at build time, then run npm run build to see static and dynamic pages, including dynamic cabin IDs.  
通过检查在构建时渲染的路由，来分析 Next.js 中的静态与动态渲染方式。然后运行 npm run build，查看包括动态 cabin ID 在内的静态和动态页面。

#### Making Dynamic Pages Static With generateStaticParams  
通过 generateStaticParams 函数将动态页面转换为静态页面8:33

Learn to use generateStaticParams in Next.js to fetch cabins, extract cabin IDs for a dynamic route, and statically generate pages for a full static site.  
学习如何使用 Next.js 中的 generateStaticParams 来获取小屋信息，提取小屋 ID 以用于动态路由，并为整个静态网站生成静态页面。

#### Static Site Generation (SSG)  
静态网站生成技术（SSG）9:11

Export a Next.js site as static assets via static site generation, producing an out folder for hosting on any static provider, and manage image loading and generate static params.  
通过静态站点生成将 Next.js 站点导出为静态资源，生成一个输出文件夹用于在任何静态提供程序上托管，并管理图像加载和生成静态参数。

#### Partial Pre-Rendering  部分预渲染6:57

Explore partial pre-rendering in Next.js, a middle ground using static shells and suspense boundaries to deliver fast pages from the CDN with 80% static and 20% dynamic rendering.  
了解 Next.js 中的部分预渲染技术。这是一种介于传统渲染和动态渲染之间的中间方式，通过静态外壳和延迟渲染机制，能够在 CDN 上提供快速页面渲染效果，其中 80%的内容是静态的，20%是动态的。

#### How Next.js Caches Data  Next.js 如何缓存数据18:28

Understand how Next.js caches data across server and client with four caches—request memorization, data cache, full route cache, and router cache—and how to revalidate or opt out.  
了解 Next.js 如何通过在服务器和客户端之间使用四种缓存机制来缓存数据——请求记忆、数据缓存、完整路由缓存以及路由器缓存。同时，了解如何重新验证这些缓存，或者选择不使用这些缓存功能。

#### Experimenting With Caching and ISR  
尝试使用缓存和即时响应技术20:39

Explore Next.js caching APIs, focusing on incremental static regeneration and revalidate to refresh static pages with fresh data. Learn to simulate production and balance static and dynamic rendering.  
了解 Next.js 的缓存 API 功能，重点探讨增量静态页面重生成技术，以及如何通过重新验证来用最新数据更新静态页面。学习如何模拟生产环境效果，并平衡静态页面与动态页面的渲染处理。

#### CHALLENGE #1: Fetching the Number of Cabins  
挑战#1：计算出租房间的数量3:46

Practice fetching dynamic data by replacing a hardcoded cabin number with a database-backed count, using async/await in a server component, and configuring revalidate for daily data refetch.  
通过在中端组件中使用异步/等待机制，将硬编码的房间号码替换为基于数据库的计数，从而实践获取动态数据的过程。同时，还需要配置自动重新验证功能，以确保每天都能获取最新的数据。


### 客户端与服务器的交互  
9 次讲座 • 2 小时 22 分钟

#### Section Overview  部分概述1:13

Explore how server and client components render together in Next.js, when to render server components inside client components, and to fetch and stream data via URL or React Context API.  
了解在 Next.js 中，服务器组件和客户端组件是如何协同渲染的，何时应该在客户端组件中渲染服务器组件，以及如何通过 URL 或 React 上下文 API 来获取和流式处理数据。

#### Blurring the Boundary Between Server and Client (RSC – Part 4)  
模糊服务器和客户端之间的边界（RSC #### 第 4 部分）15:05

Explore how React server components blur traditional back-end and front-end boundaries, using knitting and server actions to fetch and mutate data across server and client components.  
了解如何使用 React 服务器组件来打破传统的前端和后端之间的界限。通过服务器操作，我们可以在服务器组件和客户端组件之间获取和修改数据。

#### Client Components in Server Components  
服务器组件中的客户端组件8:14

Embed a client component in a server component to enable interactivity, such as a text expander triggered by a button, using the use client directive and understanding server-client boundaries.  
在服务器组件中嵌入客户端组件以实现交互性，例如通过按钮触发的文本扩展器，使用 use client 指令并理解服务器客户端边界。

#### Highlighting Current Side Navigation Link  
突出当前侧边导航链接7:45

Learn to highlight the active navigation link in Next.js by using the usePathname hook to compare pathnames with link hrefs, and apply conditional Tailwind styling in a client component.  
学习如何在 Next.js 中高亮显示活动导航链接。通过使用 usePathname 钩子来比较路径名与链接的 href 值，从而实现这一目标。同时，在客户端组件中应用有条件的 Tailwind 样式设计。

#### Sharing State Between Client and Server: The URL  
在客户端和服务器之间共享状态：URL34:28

Share state between client and server by implementing a client filter that updates the URL, reads searchParams on the server, and re-renders cabins with Next.js app router.  
通过实现客户端过滤器来共享状态，该过滤器会更新 URL 参数，在服务器端读取搜索参数，然后使用 Next.js 的应用路由机制重新渲染页面。这样就能在客户端和服务器之间实现状态共享了。

#### Advanced: Server Components in Client Components  
高级：服务器组件在客户端组件中的实现10:39

Render server components inside a client component by passing the server component as a prop, enabling server-side data fetching while the client handles state.  
通过将服务器组件作为属性传递，可以在客户端组件中调用服务器端的组件。这样既能实现服务器端的数据获取功能，又能让客户端负责处理状态管理。

#### Data Fetching Strategies for the Reservation Section  
预订模块的数据获取策略27:00

Explore data fetching strategies for the reservation section, balancing server and client components, parallel data retrieval with Promise.all, and streaming with Suspense to improve performance and user experience.  
探索用于预订页面的数据获取策略，平衡服务器和客户端组件的协作，利用 Promise.all 实现并行数据检索，以及通过 Suspense 技术实现流式处理，以提升性能和用户体验。

#### Using the Context API for State Management  
使用上下文 API 进行状态管理25:46

Learn to share date range state across client components in a Next.js app using the context API, including building a reservation provider and a useReservation hook.  
学习如何在 Next.js 应用中使用上下文 API 在客户端组件之间共享日期范围状态，包括构建一个预订提供程序和一个 useReservation 钩子。

#### Creating an API Endpoint With Route Handlers  
通过路由处理程序创建 API 端点11:35

Create a Next.js API endpoint with route handlers in a route.js file, returning JSON data for cabins and booked dates via GET and other HTTP verbs.  
创建一个 Next.js API 端点，在 route.js 文件中使用路由处理程序，通过 GET 和其他 HTTP 请求动词返回小屋和已预订日期的 JSON 数据。


### 通过 NextAuth 进行身份验证（Auth.js）  
8 次讲座 • 1 小时 22 分钟

#### Section Overview  章节概述0:54

Learn to implement authentication in Next.js with Auth.js (formerly NextAuth) using Google, authorize via middleware, and set up Supabase user accounts for a reusable authentication recipe.  
学习如何在 Next.js 中使用 Auth.js（之前的名称是 NextAuth）来实现身份验证功能。利用 Google 的 API 进行授权，并通过中间件来处理身份验证逻辑。同时，为可复用的身份验证方案设置 Supabase 用户账户。

#### Setting Up NextAuth  配置 NextAuth21:38

Set up NextAuth (Auth.js) in a Next.js app by configuring Google as an auth provider, creating environment variables, and wiring API routes for sign-in and session handling.  
在 Next.js 应用中配置 NextAuth (Auth.js)，将 Google 作为认证提供者进行设置，创建环境变量，并配置用于登录和会话处理的 API 路由。

#### Getting the User Session  获取用户会话信息14:11

Render the logged-in user's avatar from the current session, enable conditional UI based on authentication, and secure routes with server-side session data, optional chaining, and Next.js middleware.  
从当前会话中渲染登录用户的头像，根据身份验证启用条件式 UI，并使用服务器端会话数据、可选链和 Next.js 中间件来保护路由。

#### What is Middleware in Next.js?  
在 Next.js 中，中间件到底是什么？4:12

Explore how Next.js middleware sits between incoming requests and responses, running before routes and handling cookies and headers. Apply it to authentication, authorization, and redirects, with analytics and geolocation-based decisions.  
了解 Next.js 中的中间件如何位于传入请求与响应之间，它在路由之前运行，同时处理 cookie 和头部数据。可以将这一机制应用于认证、授权以及重定向操作，还可以根据分析和地理位置信息进行相应的决策。

#### Protecting Routes With NextAuth Middleware  
使用 NextAuth 中间件来保护路由路径10:52

Protect guest routes with NextAuth middleware by exporting a root middleware, using a matcher for /account, and applying an authorized callback to redirect unauthorized users to sign in.  
通过使用根中间件、针对"/account"路径的匹配器，以及应用授权回调函数，可以利用 NextAuth 中间件来保护访客路由。这样就能将未经授权的用户重定向到登录页面进行登录。

#### Building a Custom Sign In Page  
构建自定义登录页面12:17

Build a custom sign in page at /login, replacing the prebuilt auth flow and wiring a server-side sign-in action with a sign-in button for protected routes.  
在 /login 路径下创建一个自定义的登录页面，替换现有的预构建认证流程。同时，为受保护的路由路径配置一个服务器端登录操作，该操作可以通过登录按钮触发。

#### Building a Custom Sign Out Button  
构建自定义的签名退出按钮3:22

Build a custom sign-out button wired to a server action that signs out users and redirects to the homepage, while testing the flow and client-server action behavior.  
构建一个自定义的退出按钮，连接到服务器操作，该操作注销用户并重定向到主页，同时测试流程和客户端-服务器操作行为。

#### Creating a New Guest on First Sign In  
在首次登录时创建新的访客账户14:35

Connect a logged-in user with a guest profile in a Supabase database by creating a new guest on first sign-in and adding the guest ID to the session.  
通过在新用户注册时创建一个访客身份，并将该访客的 ID 添加到会话中，从而将已登录的用户与 Supabase 数据库中的访客资料关联起来。


### 带有服务器操作的突变操作 + 现代 React Hooks  
12 次讲座 • 3 小时 12 分钟

#### Section Overview  部分概述1:07

Explore server actions to mutate data in react server components model, enabling cabin reservations, updates or deletions of reservations, and profile updates with use form status, use transition, and optimistic.  
探索服务器操作，以在 React 服务器组件模型中修改数据。可以实现房间预订、预订信息的更新或删除，以及个人资料的更新。同时还能处理表单状态、过渡效果，以及乐观式的处理方式。

#### What are Server Actions?  什么是服务器操作？12:01

Learn how server actions enable server-side mutations in the React server components architecture, with automatic endpoints, form submissions, and seamless client-server interaction.  
了解如何在 React 服务器组件架构中使用服务器操作来实现服务器端的数据变更。这些操作包括自动生成端点、表单提交功能，以及无缝的客户端与服务器之间的交互。

#### Updating the Profile Using a Server Action  
通过服务器操作更新个人资料30:43

Enable guests to update their profile using a server action linked to a form, with server and client components, session authentication, and national-id validation before updating the database.  
允许访客通过链接到表单的服务器操作来更新他们的个人资料。在更新数据库之前，需要同时使用服务器组件和客户端组件，并采用会话认证以及国家识别码验证机制。

#### Manual Cache Revalidation  
手动重新验证缓存数据5:52

Implement manual cache revalidation with revalidatePath to clear and refetch the current route data, keeping the UI fresh after updates.  
使用 revalidatePath 实现手动缓存失效，清除并重新获取当前路由数据，确保更新后 UI 保持新鲜。

#### Displaying a Loading Indicator: The useFormStatus Hook  
显示加载指示器：useFormStatus 钩子函数7:55

Display a loading indicator for form submissions with the useFormStatus hook in React and Next.js. Learn to render status inside a form, update button text, and disable it during submission.  
使用 React 和 Next.js 中的 useFormStatus 钩子为表单提交显示加载指示器。学习如何在表单内渲染状态，更新按钮文本，并在提交期间禁用它。

#### Building the Guest's Reservations Page  
构建宾客预订页面10:27

Build the guest's reservations page by fetching bookings with the current user's guestId, render past and future reservations, and conditionally hide edit and delete options for past dates.  
构建访客预订页面，通过获取当前用户的 guestId 来获取预订信息，渲染过去的和未来的预订，并对过去的日期条件性地隐藏编辑和删除选项。

#### Deleting a Reservation  删除预订19:30

Delete a reservation via a server action triggered by a button, passing the booking id and validating the user session to revalidate the path and delete only the user's bookings.  
通过按钮触发的服务器操作来删除预订信息。需要传入预订 ID，并验证用户会话状态，以确保路径正确，并且只有用户的预订记录会被删除。

#### Another Loading Indicator: The useTransition Hook  
另一个加载指示器：useTransition 钩子函数7:27

Explore how to use the useTransition hook to render loading indicators for server actions in Next.js, marking updates as transitions to keep the UI responsive and show isPending.  
了解如何在 Next.js 中使用 useTransition 钩子来渲染服务器操作的加载指示器，将更新标记为过渡效果，以确保界面具有响应性，并展示 isPending 状态。

#### CHALLENGE #1: Updating a Reservation  
挑战 1：更新预订信息30:24

Explore how to implement an update reservation feature in React and Next.js, including routing, server actions, form handling, authentication, and data revalidation.  
探索如何在 React 和 Next.js 中实现更新预订功能，包括路由、服务器操作、表单处理、身份验证和数据重新验证。

#### Removing Reservations Immediately: The useOptimistic Hook  
立即取消预订：使用 Optimistic 钩子22:22

Learn to use the useOptimistic hook to remove reservations instantly in the UI, providing snappy feedback while a background server action completes, with rollback if it fails.  
学习如何使用 useOptimistic 钩子函数在用户界面中立即取消预留操作，在后台服务器操作完成时会提供即时反馈；如果操作失败，则可以进行回滚处理。

#### Back to the Cabin Page: Finishing the Date Selector  
返回到“小屋”页面：完成日期选择器设置12:09

Finish the date selector to enable cabin reservations by computing nights and price (discounted) from the selected range, while disabling past and booked dates and sharing range data via context.  
完成日期选择器设置后，系统将根据所选范围内的夜数和价格来计算可预订的日期。同时，系统会禁用已过去或已预订的日期，并通过上下文信息分享相关日期范围数据。

#### Creating a New Reservation  
创建新的预订32:28

Demonstrate creating a new reservation via a form, compute dates and prices, and submit bound data to a server action with revalidation and a thank-you redirect.  
演示如何通过表单创建新的预订，计算日期和价格，然后将绑定后的数据提交到服务器，并经过重新验证后导向感谢页面。


### 在 Vercel 环境下的部署
4 次讲座 • 19 分钟

#### Section Overview  部分概述0:48

Set up a GitHub repository and deploy the Wild Oasis project to production with Vercel. Tackle environment variables and authentication credentials to ensure a secure, hosted deployment.  
创建一个 GitHub 仓库，然后使用 Vercel 将 Wild Oasis 项目部署到生产环境。处理环境变量和认证凭据等问题，以确保部署过程的安全性和可靠性。

#### Setting Up the GitHub Repository  
设置 GitHub 仓库3:24

Create a git repository using Create-Next-App. Add and commit all files as 'initial commit', push to a private GitHub repository, and connect it to vercel.  
使用 Create-Next-App 创建一个 Git 仓库。将所有文件添加到项目中并进行提交，作为“初始提交”。然后将项目推送到私人的 GitHub 仓库中，并将其与 vercel 服务连接起来。

#### Deploying to Vercel  将项目部署到 Vercel 平台6:18

Deploy your Next.js project to vercel using the free tier, import your repo, configure environment variables, and preview live hosting with optimized images.  
使用免费套餐将你的 Next.js 项目部署到 vercel 上。导入你的项目仓库，配置环境变量，然后预览实时托管的效果，同时享受优化后的图片优化效果。

#### Updating Environment Variables and OAuth Credentials  
更新环境变量和 OAuth 凭据8:11

Update environment variables and Google OAuth credentials to restore authentication, redeploy the app, and adjust the NextAuth URL and trailing slash to ensure valid access.  
更新环境变量和 Google OAuth 凭证以恢复身份验证，重新部署应用程序，并调整 NextAuth URL 和尾随斜杠以确保有效访问。


### [可选] 传统版本的 Next.js：名为“Pages”的路由管理器  
11 次讲座 • 1 小时 23 分钟

#### Section Overview  部分概述1:07

Explore the legacy Next.js Pages Router by building a simplified version of the Wild Oasis website to compare it with the App Router and prepare for maintaining older Next.js projects.  
通过构建简化版的 Wild Oasis 网站，来了解 Next.js 页面路由器的功能。这将有助于我们更好地理解应用程序路由器的工作原理，并为维护那些较旧的 Next.js 项目做好准备。

#### Setting Up Another Project  
搭建另一个项目8:52

Set up a Next.js project with the legacy pages router, configure Tailwind and Supabase, organize public and lip folders, and run the dev server to explore pages routing.  
创建一个基于 Next.js 的项目，配置传统的页面路由机制，设置 Tailwind 和 Supabase 的相关设置，整理公共文件夹和私有文件夹的布局，然后运行开发服务器以体验页面路由的功能。

#### Routes, Pages, and Navigation  
路线、页面以及导航功能7:36

Learn how the pages router creates routes by file names and navigate between cabins and about pages with next/link. Explore nested routes via folders and the shift to client components.  
了解页面路由器如何根据文件名来创建路由，以及如何通过“下一个/链接”功能在各个页面之间导航。还可以探索通过文件夹组织的嵌套路由机制，以及客户端组件在路由处理中的应用。

#### Dynamic Routes  动态路线3:33

Create a dynamic cabin route by using square brackets in the file name for cabinId and map to /cabins/{id}, then read the id with useRouter and router.query.cabinId.  
可以通过在文件名中使用方括号来创建动态的船舱路线，例如将船舱 ID 设置为 cabinId，并将映射路径设置为/divisions/{id}。然后，可以使用 useRouter 和 router.query.cabinId 来读取该 ID 的值。

#### Creating a Layout With a Custom _App  
创建具有自定义_App 的布局6:37

Create a custom _app in the Next.js pages router to implement a global layout, load fonts and tailwind, and override the document structure.  
在 Next.js 的 pages 路由器中创建一个自定义 _app，以实现全局布局、加载字体和 Tailwind 样式，并覆盖文档结构。

#### Creating Pages  创建页面2:28

Create pages by copy-pasting starter code into about, index, and contact pages; learn to use next/link, ensure image optimization, and apply use state in client components.  
只需将模板代码复制粘贴到关于页面、索引页面和联系页面上即可创建新页面。同时，要学会使用“下一个/链接”功能，确保图片优化处理，并在客户端组件中应用“使用状态”功能。

#### Defining Page Title and Favicon  
定义页面标题和图标图像6:03

Learn to set a global page title and favicon in Next.js using the next/head component in _app, and override per page with head entries referencing files from the public folder.  
在 Next.js 中，学习如何使用`_app`模块中的`next/head`组件来设置全局页面标题和 Favicon 图标。同时，可以通过在`head`部分添加指向公共文件夹中文件的条目，来实现每页标题的个性化设置。

#### Fetching Data With getStaticProps (SSG)  
使用 getStaticProps 函数获取数据（SSG 模式）8:12

Learn to fetch data on the server with getStaticProps in the Next.js pages router, enabling static site generation and passing cabins data to the cabins page for pre-rendered content.  
在 Next.js 的页面路由器中，使用`getStaticProps`函数来从服务器获取数据。这能够实现静态网站生成，并将数据传递给 cabins 页面，从而实现预渲染的内容。

#### Fetching Data With getServerSideProps (SSR)  
使用 getServerSideProps 函数获取数据（SSR）10:50

Compare getServerSideProps with getStaticProps for dynamic routes, fetching data on each request. Explore getStaticPaths and incremental static regeneration, and how revalidate enables static and dynamic rendering.  
比较 getServerSideProps 与 getStaticProps 在动态路由中的使用，每次请求时获取数据。探索 getStaticPaths 和增量静态再生，以及 revalidate 如何实现静态和动态渲染。

#### API Routes  API 路线11:55

Learn how API routes in the Next.js pages router enable server-side data mutations by creating an API folder, defining a contact API endpoint with a handler, and handling POST requests.  
了解如何在 Next.js 的页面路由中通过 API 路由实现服务器端数据突变。具体步骤包括创建一个 API 文件夹，定义包含处理程序的联系人 API 端点，以及处理 POST 请求。

#### Handling Form Submissions  
处理表单提交15:41

Connect a form to the /api/contact endpoint using the form data API, handle preventDefault, build a formData object with Object.fromEntries, and manage isSubmitting, success, and message states for user feedback.  
使用 form data API 将表单连接到/api/contact 端点。处理 preventDefault 事件，使用 Object.fromEntries 方法创建 FormData 对象，并管理 isSubmitting、成功状态以及用于用户反馈的消息状态。


##   结束！  
1 讲座 • 3 分钟

#### Where to Go from Here  
接下来该去哪里呢3:20

Finish the course and start building complete React applications, practice coding, and subscribe to the monthly newsletter at jonas.io while continuing to learn TypeScript.  
完成这门课程后，你可以开始构建完整的 React 应用程序了。同时，继续练习编程，并订阅 jonas.io 的月度通讯，同时继续学习 TypeScript 编程技术。


# 要求/条件

- NO React experience necessary! I take you from beginner to expert!  
    不需要有任何反应经验！我会带你从初学者水平提升到专家级别！
    
- Basic understanding of JavaScript is required (this course contains a quick JavaScript review section)  
    需要具备基本的 JavaScript 知识（本课程包含一段关于 JavaScript 的快速复习部分）。
    
- Any computer and OS will work — Windows, macOS or Linux  
    任何计算机和操作系统都能胜任这项任务——无论是 Windows、macOS 还是 Linux 系统。
    

# 描述

**_** Just launched 16 BONUS hours of Next.js content in May 2024!  
在 2024 年 5 月，我们刚刚推出了 16 个小时的关于 Next.js 内容的免费学习资源！_**

**_** Take this course after taking my #1 bestselling JavaScript course (900,000+ students)  
在完成我的最受欢迎的 JavaScript 课程之后，再学习这门课程吧（已有 900,000 多名学员参与学习）_**

  

In 2024, React is still the #1 skill to learn if you want to become a successful front-end developer!  
在 2024 年，如果你想成为一名出色的前端开发者，那么学习 React 仍然是最值得投入的技能！

But it can be hard. There are so many moving parts, so many different libraries, so many tutorials out there.  
不过，这可能会很困难。有太多的复杂部分需要处理，有太多不同的库需要使用，还有大量的教程可供参考。

That's why you came here... And you came to the right place! This is **THE** ultimate React course for 2024 and beyond.  
这就是你来到这里的原因……你选对了地方！这是 2024 年及以后最棒的 React 课程。

**_A practice-heavy approach to master React by building polished apps, backed up by diagrams, theory, and looks under the hood of React.  
一种以实践为导向的学习方法，通过构建精美的应用程序来掌握 React 技术。这些方法以图表、理论以及 React 内部的工作原理为基础，帮助学习者逐步提升技能。_**

The all-in-one package that takes you from zero to truly understanding React and building modern, powerful, and professional web applications.  
这个一体化学习套餐能帮助你从零开始，真正掌握 React 技术，并构建出现代、强大且专业的 Web 应用程序。

Real projects. Real explanations. Real React.  
真正的项目。真实的解释。真正的 React 技术。

  

**[01] Why is this the right React course for you?  
[01] 为什么这本关于 React 的教程适合你呢？**

This is the most **epic** React (+ Next.js) course on Udemy, and it's **fundamentally different from all the other ones**.  
这是 Udemy 上最精彩的 React（以及 Next.js）课程之一，它与所有其他课程截然不同。

Here's why:  这是原因：

- Contains **the biggest and most professional project**, where we implement many common app features (see promo video!)  
    这个项目规模庞大且专业性极强，我们在其中实现了许多常见的应用程序功能（请观看宣传视频了解详情！）
    
- **Super detailed explanations** of all important concepts with carefully crafted and animated diagrams  
    对所有重要概念的超详细解释，并配有精心设计的动画示意图来辅助说明
    
- A huge focus on "**how to think in React**" and **modern best practices**  
    对“如何在 React 中思考”和现代最佳实践的高度关注
    
- A long section that explores **how React actually works behind the scenes** will give you the confidence to use React effectively on your own  
    一个详细探讨 React 实际工作原理的部分将让您有信心独立有效地使用 React
    
- **Advanced design patterns** like compound components will make you think like a senior React engineer  
    像复合组件这样的高级设计模式将让您像一名高级 React 工程师一样思考
    
- Many sections (16 hours!) on **Next.js with the modern "app" router** (with React Server Components and Server Actions)  
    关于 Next.js 的许多章节（共 16 个小时！），内容涵盖了现代“应用”路由器技术，以及如何使用 React 服务器组件和服务器动作。
    

And these are just the highlights! Check out the full list below (+ the promo video and curriculum).  
而这些只是其中的一些亮点而已！请查看下面的完整列表（还有宣传视频和课程介绍）。

Also, make no mistake: "React course" actually means "Front-end development with React and modern libraries course".  
另外，请不要误解：“React 课程”实际上指的是“使用 React 和现代库进行前端开发的课程”。

That's right, besides the React library itself, you will learn and master them all: React Router, Redux, Redux Toolkit, React Query, React Hook Form, Styled Components, Tailwind CSS,Next.js, and more.
没错，**除了 React 库本身之外，你还将学习并掌握以下技术：** React Router、Redux、Redux Toolkit、React Query、React Hook Form、Styled Components、Tailwind CSS、Next.js 等等。

This is how we plan, build, and deploy professional projects! We even use industry-standard tools such as Supabase (fully-fledged back-end), GitHub, Netlify, and Vercel.  
这就是我们进行专业项目规划、开发和部署的方式！我们甚至*使用了诸如 Supabase（完整的后端服务）、GitHub、Netlify 和 Vercel 这样的行业标准工具*。

As you expect, this course is 100% project-based. So throughout the course, you'll start building your portfolio of 10+ beautiful and high-quality React apps, where each one will showcase a few of the new skills you will be learning.  
正如你所预料的那样，*这门课程完全采用项目驱动的方式展开教学*。在整个课程中，你将开始构建自己的作品集，其中包括 10 个以上美观且高质量的 React 应用程序。每个应用程序都会展示你所学到的新技能。

**_By the end of the course, you will have all the knowledge and confidence that you need to ace your job interviews and become the professional React developer that companies are looking for.  
在课程结束时，你将拥有所有必要的知识和信心，从而能够在面试中表现出色，成为企业所寻求的专业的 React 开发者。_**

  

**[02] Why am I the right React teacher for you?  
[02] 为什么我是最适合您学习 React 的导师呢？**

My name is Jonas, I'm an experienced web developer and designer, and one of Udemy's top instructors. I've been teaching web development courses since 2015 to over **1,900,000 developers**, so I know exactly how students learn and what is needed to master any subject.  
我的名字是乔纳斯，是一名经验丰富的网页开发专家和设计师，同时也是 Udemy 最优秀的讲师之一。自 2015 年以来，我一直在为超过 190 万开发者教授网页开发相关课程。因此，我非常了解学生的学习方式，也清楚要掌握任何技能所需具备的条件。

With this in mind, I designed the ideal curriculum for this course: **a unique blend of real-world projects, deep explanations, and theory lectures**, to turn you into a confident React and Next.js developer in just a couple of weeks.  
基于这一理念，我设计了*这门课程的理想教学大纲*：*结合实际项目、深入讲解以及理论讲座等多种教学方式*，旨在在短短几周时间内让你成为一名自信的 React 和 Next.js 开发者。

**Ready to become a confident and independent React + Next.js developer, highly capable of building your own apps? Then don't wait any longer, and start your React journey today!  
准备好成为一名自信且独立的 React + Next.js 开发者，能够自主构建自己的应用程序吗？那么请不要再等待了，从今天开始踏上你的 React 开发之旅吧！**

  

**[03] Still not sure? Here are all the nerdy details of what we'll cover:  
[03] 还是不确定？以下是我们将要介绍的所有细节：**

- React fundamentals _[why we even need React, components, JSX, props, events, forms, state, props vs. state]_  
    React 基础知识 [我们为什么需要 React，组件、JSX、属性、事件、表单、状态，以及属性与状态的区别]
    
- How to think about state _[where to place state, guidelines, lifting state up, local vs. global state, UI vs. remote state]_  
    如何思考状态的问题[如何定位状态元素、制定指导方针、如何提升状态的功能、局部状态与全局状态之间的区别、用户界面状态与远程状态之间的差异]
    
- How to think about components _[how to split components, props as API, how to build reusable and composable components, the children prop]_  
    如何思考组件的设计[如何拆分组件、如何将 props 作为 API 使用、如何构建可复用的组合式组件、以及 child prop 的作用]
    
- Deep dive into effects and useEffect _[data fetching, lifecycle vs. synchronization, when to use effects, effects vs. event handlers]_  
    深入探讨效应器以及 useEffect 的使用方法 [数据获取、生命周期管理、同步问题、何时使用效应器，效应器与事件处理器的区别]
    
- Deep dive into hooks _[rules of hooks, how hooks work, useRef, building super-reusable custom hooks]_  
    深入探讨钩子函数[钩子函数的规则、工作原理、useRef、构建可复用的自定义钩子]
    
- Performance optimization _[wasted renders, memoization with memo, useMemo, and useCallback, optimizing Context API, code splitting + Suspense]_  
    性能优化：[避免不必要的渲染操作、使用记忆化技术存储数据、利用 useMemo 和 useCallback 功能、优化 Context API 的使用、实现代码分割以及利用 Suspense 技术处理异步操作]
    
- Advanced state management _[useReducer hook, the Context API, Redux, Redux Toolkit, Thunks, React Query]_  
    高级状态管理[使用 useReducer 钩子、Context API、Redux、Redux 工具包、Thunks、React Query]
    
- Building Single-Page Applications (SPA) _[Vite, routing with React Router, URL parameters and query strings for state management, data loaders and actions (v6.4+)]_  
    构建单页应用程序（SPA）[使用 Vite 框架，通过 React Router 进行路由管理，利用 URL 参数和查询字符串进行状态管理，以及数据加载器和动作处理（版本 6.4 及以上）]
    
- Building real-world features found in many apps _[authentication and authorization, data sorting, filtering and pagination, dark mode, dashboard with charts, etc.]_  
    构建现实世界中许多应用程序中常见的功能 [认证和授权，数据排序，过滤和分页，暗黑模式，带有图表的仪表盘等]
    
- Creating your own back-end with a DB and API using Supabase _[gain the power to build full-stack apps on your own!]_  
    使用 Supabase 通过数据库和 API 构建你自己的后端系统[从而拥有自主构建全栈应用程序的能力！]
    
- How to style React apps _[Tailwind CSS, CSS Modules, and Styled Components]_  
    如何为 React 应用添加样式 [Tailwind CSS, CSS Modules, 和 Styled Components]
    
- Advanced React patterns used by senior developers _[render props, higher-order components, compound components (to build a modal, a context menu, and more)]_  
    资深开发者使用的先进 React 模式 [渲染属性、高阶组件、复合组件（用于构建模态框、上下文菜单等）]
    
- How React works behind the scenes _[rendering, virtual DOM, reconciliation, fiber tree, key prop, events, state batching, etc.]_  
    React 在幕后是如何工作的[渲染、虚拟 DOM、同步更新、Fiber 树结构、key 属性、事件处理、状态管理、批量处理等]
    
- Next.js with the "app" router, React Server Components, and Server Actions _[server-side rendering, static vs. dynamic rendering, state management between server and client, data loading strategies, streaming, caching, image and font optimization, authentication with NextAuth, modern React hooks (useFormState and useOptimistic)]_  
    Next.js 结合“app”路由、React 服务器组件以及服务器操作功能，可以实现服务器端渲染。此外，它还具备静态与动态渲染的切换能力，能够管理服务器与客户端之间的状态交互，同时提供了多种数据加载策略、流处理机制、缓存机制，以及图片和字体资源的优化方案。此外，该框架还支持使用 NextAuth 进行身份验证，并且采用了现代化的 React 钩子函数，如 useFormState 和 useOptimistic 等。
    

  

**[04] By signing up today, you'll also get:  
[04] 如果今天就注册，你还可以获得：**

- Up-to-date HD-quality videos, that are easy to search and reference **(great for Udemy Business learners)**  
    最新的高清视频内容，易于搜索和参考（非常适合 Udemy 商学院的学习者使用）
    
- Downloadable slides PDF for 70+ theory videos (not boring, I promise!)  
    可下载的幻灯片 PDF，包含 70 多段理论视频（保证不无聊！）
    
- Professional English captions (not auto-generated)  
    专业的英语说明文字（并非自动生成的）
    
- Downloadable assets and starter and final code for each project  
    可下载的素材，以及每个项目的初始代码和最终代码
    
- Free and fast support in the course Q&A  
    在课程问答环节提供免费且快速的支持服务
    
- 10+ challenges and exercises to practice your skills (solutions included)  
    包含 10 多个挑战性和练习题，帮助你提升技能（附有解决方案）
    

# 这门课程适合谁


- You just finished learning JavaScript and are looking for your next step!  
    你刚刚学会了 JavaScript，现在正在寻找下一步的学习方向！
- You want to truly master React development, including common libraries needed to build real-world apps: React Router, Redux, React Query, Next..js, and many others  
    你想真正掌握 React 开发技术，包括构建实际应用程序所需的常见库：React Router、Redux、React Query、Next.js 等等。
- You have been learning React, but 1) still don't understand React, or 2) still don't feel confident to build professional React apps  
    你一直在学习 React，但是：1）仍然不清楚 React 的运作原理；或者 2）缺乏信心来构建专业的 React 应用程序。
- You have been building React apps, but are still missing the advanced stuff: useReducer, performance optimization, how React actually works, advanced design patterns, etc.  
    你已经构建过 React 应用，但仍然缺少高级内容：useReducer、性能优化、React 实际工作原理、高级设计模式等。


