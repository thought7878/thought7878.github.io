


# What you'll learn  你将学到什么

- You will learn the whole React WebApp building process, from your pc to the server.  
你将了解到完整的 React Web 应用构建流程，包括从个人电脑到服务器的整个过程。

- Work with nosql databases ( Firebase, MongoDB and Moongoose)  
使用非关系型数据库进行开发，例如 Firebase、MongoDB 和 Moongoose。

- How to deploy applications to services like Firebase and Heroku.  
如何将应用程序部署到像 Firebase 和 Heroku 这样的服务平台上。

- Learn how Redux work and apply it on a real life application.  
了解 Redux 的工作原理，并将其应用于实际的应用场景中。

- Learn what NODE js is and How it works and create your own server,  
了解 NODE 是什么以及它的工作原理，然后创建你自己的服务器。

- Learn the basics of Webpack  
了解 Webpack 的基础知识

# 本课程包含以下内容

- 34.5 hours on-demand video  
34.5 小时的点播视频时间

- 14 downloadable resources  
14 种可下载的资源


# 目录
课程内容

21 sections • 292 lectures • 34h 33m total length  
21 个章节 • 292 次讲座 • 总时长 34 小时 33 分钟

##   引言
4 次讲座 •  4 分钟

### Intro  引言  预览版1:11

Start with React fundamentals, including hooks and components, then advance to forms and Redux. Explore Next.js routing, authentication, deployment, and build Express and Node.js servers with APIs.  
从 React 基础开始学习，包括 hooks 和组件的相关知识，然后进一步学习表单处理和 Redux 状态管理。此外，还会学习 Next.js 的路由机制、身份验证功能，以及部署流程。同时，也会学习如何构建支持 Express 和 Node.js 服务器的 API。

### IDE1:09

Choose VS Code as your IDE for this course, install essential React extensions such as ES7, Redux, and React Native, and use the integrated terminal to install libraries and dependencies.  
请选择 VS Code 作为本课程的集成开发环境。同时，安装必要的 React 扩展程序，如 ES7、Redux 和 React Native 等。此外，利用内置的终端工具来安装所需的库和依赖项。

### GitHub Resources  GitHub 资源  预览版0:53

Access all code for the React 19 course on GitHub and compare your work to video-specific branches; download module branches and the master branch with final code.  
在 GitHub 上获取《React 19》课程的所有代码，并将你的工作成果与视频相关的分支进行比较；下载包含最终代码的各个模块分支以及主分支的代码。

### Installing Node JS  安装 Node JS0:39

Install Node.js as the first step before React; download from nodejs.org, install Node.js, verify with node -v in your terminal, restart if needed, then proceed to React.  
在开始使用 React 之前，首先要安装 Node.js。可以从 nodejs.org 下载 Node.js 安装包，然后在终端中执行 node -v 命令来验证安装是否成功。如果还需要重启服务器，那就重启一下吧。完成后，就可以继续使用 React 了。


##   React 基础  
20 次讲座 • 2 小时 8 分钟

### Creating a React App  创建 React 应用程序4:58

Learn to create a React app with vite, and explore Next.js options for full-stack use; then run npm install and npm run dev to start the server on localhost:5173.  
学习如何使用 Vite 来创建 React 应用程序，同时探讨 Next.js 在全栈开发中的应用前景。之后，使用`npm install`和`npm run dev`命令在本地主机上的 5173 端口启动服务器。

### Project Structure  项目结构5:53

Learn how a react project organizes files from src to dist, manage dependencies in node_modules, and build for production with vite and npm scripts.  
了解一个 React 项目是如何从 src 目录到 dist 目录组织文件的，如何管理 node_modules 中的依赖库，以及如何利用 vite 和 npm 脚本进行生产环境的构建。

### From the ground up  从零开始8:13

Create a src directory with main.js and run npm run dev to view it on localhost 5173. Render the App component with jsx into the route.  
创建一个名为 src 的目录，并在其中放置 main.js 文件。然后运行 npm run dev，即可在本地计算机的 5173 端口上查看该应用程序。将 App 组件通过 jsx 语法渲染到路由中。

### JSX Behind the scenes  JSX 幕后故事9:43

Explain how JSX becomes React elements behind the scenes using createElement, fragments, and single-element returns. Master key JSX rules, including className and modern render patterns in React 19.  
请解释在幕后，如何使用 createElement、片段以及单元素返回来将 JSX 转换为 React 元素。掌握关键的 JSX 规则，包括 className 属性以及 React 19 中的现代渲染模式。

### Planning your App  正在规划你的应用程序1:51

Plan your React app by modularly splitting into components—app wrapper, header, news container, and news items—and shift from single-file code to a dynamic, file-per-component structure.  
通过模块化地将 React 应用拆分成多个组件来规划你的应用程序——比如应用框架、头部区域、新闻容器以及新闻项。这样可以将单一的代码文件结构转变为每个组件对应一个独立文件的动态结构。

### Connecting components  连接组件3:56

Create a components directory, build and export header and news list components, then import and render them in the main JSX to connect the app.  
创建一个“components”目录，构建并导出头部组件和新闻列表组件，然后将这些组件导入到主 JSX 代码中，以便将它们连接起来。

### Single Expressions  单独表达形式3:51

Learn how a React component renders inline dynamic JavaScript using curly braces, returns a single expression, and wraps complex logic in functions for clean, reusable code.  
了解如何使用花括号在 React 组件中内联动态 JavaScript 代码。通过组合简单的表达式，可以将复杂的逻辑封装在函数中，从而打造出简洁、可复用的代码。

### Adding styles  添加样式8:31

Explore styling a React app by embedding Google fonts like Anton, using inline styles or a CSS file, and importing styles into the source for header and logo.  
探索如何为 React 应用程序进行样式设计，可以通过嵌入 Google 字体如 Anton，使用内联样式或 CSS 文件，以及将样式导入到源代码中来处理标题和标志的样式问题。

### Events  活动/事件7:54

Explore how to handle user events in React, using onClick and onChange listeners, camelCase handlers, and synthetic events to access input values and log interactions.  
探索如何在 React 中处理用户事件，使用 onClick 和 onChange 监听器、camelCase 处理程序以及合成事件来访问输入值并记录交互。

### Reusable code  可重复使用的代码13:04

Split code to reuse components and build a clean header and navigation bar in React. Export, import, and map nav items from a data file for dynamic navigation.  
将代码拆分成多个部分以复用组件，并在 React 中构建简洁的头部和导航栏。从数据文件中导出、导入，并映射出导航项，从而实现动态导航功能。

### Component state  组件状态8:55

Learn how component state, via useState, powers a counter with an initial value zero and a setter function to update count on button clicks, including naming conventions and destructuring.  
了解如何使用 useState 函数来获取组件的状态。该状态包含初始值为零的计数器，同时还有一个设置函数，可以在点击按钮时更新计数。此外，还需要了解相关的命名规范以及数据类型的解构操作。

### Prev State  之前的状态5:43

Explore the useState hook and how to access the previous state to update counters, reset to the initial value, and handle increments or decrements with functional updates.  
了解 useState 钩子，以及如何访问之前的状态，以更新计数器、重置到初始值，并通过函数式更新来处理增加或减少的情况。

### Complex State  复杂的状态9:17

Master complex state in React by managing objects with useState, updating from the previous state, and learning when to use useReducer.  
通过利用 useState 函数来管理对象，从之前的状态进行更新，同时了解何时使用 useReducer 函数，从而掌握在 React 中处理复杂状态的方法。

### Header state  头部状态3:10

Use React state to control the header by capturing user input into a keywords state with setKeywords, updating on each change, and sharing data via props across components.  
使用 React 的状态来管理头部内容。通过调用 setKeywords 函数来捕获用户输入的关键词信息，并在每次更改时更新这些数据。同时，也可以通过 props 在各个组件之间共享数据。

### Props  道具/装备7:53

Learn to share data from a parent to a child component using props in a React setup, using data.js and useState to render a news list.  
学习如何使用 React 中的 props 从父组件向子组件传递数据，并利用 data.js 和 useState 模块来渲染新闻列表。

### Props to news item  值得称赞的是这篇新闻报道6:24

Explore how to render dynamic news feeds in React by mapping props data, using keys, and creating reusable NewsList and NewsItem components with prop destructuring.  
了解如何在 React 中渲染动态的新闻资讯。通过映射属性数据、使用键名，以及创建可复用的 NewsList 和 NewsItem 组件来实现这一功能，同时利用属性解构功能来组织这些组件的行为。

### React children  React 子元素3:20

Explore how React children use props.children to pass content into a component, access it in the child, and render it in multiple places, highlighting props and component composition.  
了解 React 子组件如何使用 props.children 来传递内容到组件内部，如何在子组件中访问这些内容，以及如何在多个位置渲染这些内容。同时，也会探讨关于属性和组件组合的相关内容。

### Inline stylings  内联样式设计5:59

Learn to apply dynamic inline styles and CSS classes in React using useState, template strings, and ternary expressions to toggle active states and visualize color changes.  
学习如何在 React 中使用 useState、模板字符串和三元表达式来应用动态内联样式和 CSS 类，从而实现不同的状态切换以及颜色变化的效果。

### Child to Parent  孩子与父母4:48

Share data from child to parent in React by passing a function through props, invoking it from the child, and handling input change events to update the parent state.  
在 React 中，可以通过传递一个函数给子组件，从而将儿童的数据共享给父组件。同时，还需要处理输入变化的事件，以更新父组件的状态。

### Finishing the Basics app  完成基础知识的应用程序4:16

Explore filtering news data with a keyword-driven search, update state, and render filtered results using a JavaScript filter, with plans to fetch from MongoDB and introduce hooks and React Router.  
通过关键词搜索来筛选新闻数据，更新状态，并使用 JavaScript 过滤器来呈现过滤后的结果。同时，有计划从 MongoDB 中获取数据，并引入钩子和 React Router 技术。


##   React hooks
8 次讲座 • 1 小时 1 分钟

### introduction  引言  预览版5:11

Explore core React hooks and fundamentals, set up a project, and build a simple counter to demonstrate use state and the use effect.  
学习 React 的核心钩子和基础概念，创建一个项目，并实现一个简单的计数器，以展示如何使用状态管理和副作用功能。

### UseEffect  使用效果8:54

Learn how to use the React useEffect hook to synchronize a component with an external system, understand dependency arrays, control when effects run, and perform cleanup.  
了解如何使用 React 的 useEffect 钩子来同步组件与外部系统的操作。掌握依赖数组的使用方法，控制效果何时执行，以及如何进行清理工作。

### Cleanup Effect  清理效应10:49

Learn how the cleanup effect works in useEffect by mounting and unmounting a red block component, logging creation and removal, and toggling visibility with state.  
了解在 useEffect 中，通过挂载和卸载红色方块组件、记录组件的创建和移除过程，以及利用状态来切换组件可见性，这些操作是如何实现清理效果的。

### UseRef  使用 Ref5:34

Create a React component that demonstrates the useRef hook to access a dom input and log its current value when a trigger button is pressed.  
创建一个 React 组件，该组件使用`useRef`钩子来访问一个 DOM 输入元素，并在点击触发按钮时记录其当前值。

### Refs as props  作为装饰用的参考标志4:07

Learn how to pass a ref as a prop to a child input component in React 19, and access the input's value through the ref.  
了解如何在 React 19 中将一个引用传递给儿童输入组件，并通过该引用访问输入的值。

### React context part 1  React 上下文部分 16:46

Explore how use context enables sharing data across components to avoid prop drilling, using use state, use effect, and use ref, and contrast it with Redux's centralized store.  
了解如何利用上下文来实现组件间的数据共享，从而避免重复编写相同的代码。同时，了解如何使用状态、效果和引用来传递数据，并与 Redux 的集中式存储机制进行比较。

### React context part 2  React 上下文部分 28:51

Learn to create a React context and provider, wrap the app, share a centralized users state via value, and access it in components with useContext (and the new use API).  
学习如何创建 React 的上下文和提供者，如何包裹应用程序，如何通过值来共享用户的状态，以及如何在组件中通过 useContext（以及新的 use API）访问该状态。

### React context part 3  React 上下文部分 310:27

Explore building a complex React context value with active state, a users array, and setter functions; toggle visibility, map users, and immutably add users from the provider to child components.  
探索如何构建一个复杂的 React 上下文结构，其中包含活动状态、用户数组以及设置函数；如何切换用户的可见性，如何将用户数据映射到其他组件，以及如何在不改变数据的情况下将用户数据添加到子组件中。


##   实践项目一——谁来支付账单？
9 次讲座 • 58 分钟

### Introduction  引言6:45

Learn to build a tiny 'who pays the bill' game in a React app, with more than one player input, validation, two stages, toasts, and context API and Bootstrap integration.  
学习如何在 React 应用中构建一个小型的“谁付账”游戏。该游戏支持多个玩家参与，包含输入验证、两个游戏阶段、祝酒词，同时还集成了对上下文 API 的调用以及 Bootstrap 框架。

### Creating a context  创建上下文5:02

Create a React context by building a context directory, using createContext and provider, managing stage, players, and result with useState, and wrap the app to share state.  
通过创建`context`目录来实现 React 的上下文管理，使用`createContext`和`provider`来承载不同阶段的组件、参与者以及结果信息。同时，利用`useState`来管理状态。最后，将整个应用包装起来以共享状态信息。

### Stage display  舞台展示3:24

Display stage two or stage one by consuming a React context with useContext and conditional rendering. Build an input with validation and refs to populate the players array.  
通过使用 useContext 在 React 上下文中进行条件渲染，可以显示第二阶段或第一阶段的内容。创建一个输入框，并添加验证功能和引用，以填充播放器数组。

### Creating the form input  创建表单输入项4:39

Set up a React Bootstrap form with a text input bound to a useRef, connect to context, and handle submit to add players to a list, with validation planned.  
创建一个使用 React Bootstrap 的表单，包含一个文本输入字段。该字段与 useRef 实例绑定，并连接到上下文环境。表单的提交操作用于将玩家添加到列表中，同时包含了验证功能。

### Add and Show players  添加并展示玩家列表8:29

Add and show players by handling form submission, preventing page reload, updating a context-based players list, rendering the list, and enabling removal with a next-step validation flow.  
通过处理表单提交来添加并显示玩家信息，避免页面重新加载，更新基于上下文的玩家列表，展示该列表，并通过下一步验证流程实现玩家的移除功能。

### Validation  确认/验证6:52

Master React input validation by building a validation function, managing error state, and showing Bootstrap alerts for empty fields or a three-character minimum before advancing.  
通过构建验证函数、管理错误状态，以及在处理前显示针对空字段或至少包含三个字符的字段的 Bootstrap 警告信息，来掌握 React 输入验证的技巧。

### Removing players  移除玩家4:36

Remove a player using a context-based remove handler. Copy the players array, splice the target index, and update state with setPlayers, wiring onClick and toasts for the loser flow.  
通过基于上下文的删除处理程序来移除某个玩家。复制玩家数组，将目标索引位置置为空，然后通过 setPlayers 方法更新状态。同时，为失败者流程处理 onClick 和 toasts 事件。

### Showing toasts  正在展示祝酒词5:00

Show how to display toasts when fewer than two players, including a top-left toast container and two-second auto close, then advance to stage two and reveal the loser.  
展示如何在只有少于两名玩家参与的情况下显示公告信息，包括在屏幕左上角的一个公告容器，以及两秒后自动关闭的功能。然后进入第二阶段，揭示失败者是谁。

### Finishing the App  完成应用程序的构建  预览版13:24

Create a simple React app that generates a random loser from a users list, uses context to display the result, and includes reset and get new loser actions.  
创建一个简单的 React 应用程序，该应用程序可以从用户列表中随机选出一名“失败者”，通过上下文参数显示结果，并包含重置和获取新“失败者”的功能。


##   React 路由系统  10 场讲座 • 每场 46 分钟

### Introduction  引言2:43

Explore how React Router enables multi-view navigation in a single-page app, install the library, set up a React project, and run the dev server with npm commands.  
了解 React Router 如何实现单页应用中的多视图导航功能。安装该库后，创建一个 React 项目，并使用 npm 命令运行开发服务器。

### The router process  路由器进程2:33

Explore how the router process differs between traditional server reloads and a single-page application using React Router, where history changes map URLs to components without full page reloads.  
了解传统服务器重启与单页应用程序使用 React Router 时，路由器的处理方式有何不同。在 React Router 中，历史记录的变化可以将组件与 URL 关联起来，而无需重新加载整个页面。

### Creating routes  创建路线6:56

Set up react router v7 with a high-level browser router, create home, posts, and profile components, and define routes for home, posts, and profile with a persistent header.  
配置 React Router v7，使用高级的浏览器路由机制。创建主页、帖子以及个人资料组件。为主页、帖子和个人资料分别定义路由，并设置一个固定的头部区域。

### Linking  连接6:16

Learn to link in a React Router app using Link instead of the A tag for home, posts, and profile. Use useNavigate for programmatic navigation and useSearchParams for query data.  
学习如何在 React Router 应用中使用 Link 标签来替代 A 标签，用于导航到首页、文章页面和个人资料页面。使用 useNavigate 进行程序化导航，而使用 useSearchParams 来处理查询数据。

### A better header  更好的标题处理方式4:27

learn how to transform a plain header into a cleaner, responsive navigation bar using Bootstrap CSS, a container wrapper, and nav pills for home, posts, and profile.  
学习如何使用 Bootstrap CSS 将普通的头部区域转换为更简洁、响应式的导航栏。同时，了解如何使用导航 pills 来实现首页、帖子页面和个人资料页面的导航功能。

### Dynamic Params  动态参数5:48

Explore building dynamic routes for posts using React Router by turning the post id into a dynamic path parameter, accessing it with useParams, and inspecting route data with useLocation.  
通过使用 React Router 来动态生成帖子路由路径，将帖子 ID 转换为动态路径参数，利用 useParams 函数访问该参数，同时通过 useLocation 函数查看路由数据。

### NavLink and router types  NavLink 和路由器类型4:45

Explore router types in react router, including browser, hash, and memory routers, and learn when to use each. Implement nav link with active state to apply styles.  
了解 React 路由中的各种路由类型，包括浏览器路由、哈希路由和内存路由，并了解何时使用每种路由。实现带有活动状态的导航链接以应用相应的样式。

### Redirections  重定向2:51

Learn how to redirect users in React Router using the navigate component and the useNavigate hook, including replace behavior, redirecting to home, and handling 404 not found routes.  
学习如何在 React Router 中使用 navigate 组件和 useNavigate 钩子重定向用户，包括替换行为、重定向到首页以及处理 404 未找到路由。

### 404 routes  404 条路线3:29

Learn how to implement a 404 not found route with React Router by using the wildcard path, creating a not found component, and rendering a fallback when no routes match.  
学习如何使用 React Router 通过通配符路径、创建未找到组件以及在没有任何路由匹配时渲染回退来实现 404 未找到路由。

### Nested routes  嵌套路由6:39

Explore nested routes by creating a parent route and rendering child routes with an outlet to display users, guests, and admins.  
通过创建一个父路由，并渲染带有出口的子路由，来探索嵌套路由的布局。这样就能显示用户、访客和管理员的信息。


##   React 过渡效果  4 次讲座 • 36 分钟

### Transition Basics  过渡基础10:19

Learn to implement transitions in React using React Transition Group, from installing the library to building a red div with state, ref, and CSS transitions.  
学习如何使用 React Transition Group 在 React 中实现过渡效果。从安装这个库开始，然后演示如何创建一个带有状态的红色 div 元素，并运用 ref 和 CSS 过渡效果来实现各种过渡效果。

### Transition Basics part 2  过渡基础 第二部分5:36

Learn transition basics in React 19 by applying CSS classes to animate enter and exit states, using square elements, state-driven classes, and on enter and on exit callbacks.  
通过应用 CSS 类来实现 React 19 中的过渡效果，例如使用正方形元素、状态驱动的类，以及基于进入和退出事件的回调函数，来学习 React 19 中的过渡机制基础。

### CSS transitions  CSS 过渡效果7:29

Explore how to implement css transitions with react transitions group by wiring state, a node ref, and the in prop to animate a div with enter and exit states.  
了解如何通过 React Transition Group 来实现 CSS 过渡效果。通过使用状态绑定、节点引用以及 in 属性，可以控制一个 div 元素在不同状态下的展示效果，包括进入和退出状态。

### Transitions Group  过渡集团12:33

Learn to implement React transition group for list animations by using an array of objects with numbers and nodeRef, adding and removing items, and applying CSS transitions.  
学习如何使用包含数字的对象数组，结合 nodeRef 来实现 React Transition Group，从而对列表动画进行添加和删除操作，并应用 CSS 过渡效果。


##   React 表单  7 次讲座 • 58 分钟

### Introduction  引言1:42

Learn form handling in React using formic, with components, hooks, and schema validation, then download a three-page form template, install npm packages, and run the dev server at port 5175.  
通过使用 formic 库来学习 React 中的表单处理技术，包括组件、钩子以及模式验证等功能。然后下载一个三页的表单模板，安装所需的 npm 包，并运行开发服务器，端口号为 5175。

### Formic basics  Formic 基础9:09

Learn to build controlled forms with formic in React by wiring initial values, values, handleChange, handleSubmit, onSubmit, and applying basic validation.  
学习如何使用 React 中的 Formic 来构建受控表单。包括配置初始值、表单值、处理提交事件以及应用基本验证功能。

### Validations  验证过程11:47

Apply Formik validation with a validate function, manage errors via an errors object, and show messages using touched for first name, last name, and email. Introduce Yup later.  
使用`validate`函数进行表单验证，通过`errors`对象处理错误，并通过`touched`方法显示与名字、姓氏和电子邮件相关的消息。之后会介绍 Yup 模块。

### Using a validation schema  
使用验证模式7:30

learn to validate forms with a yup validation schema, using formik, applying string, required, max, min, and email rules with custom error messages.  
学习如何使用 formik 库，通过自定义的错误消息来为表单添加“已确认”的验证规则。同时，了解如何应用字符串、必填项、最大值限制、最小值限制以及电子邮件验证规则。

### The Formik hook  福米克钩8:09

Learn to implement the Formik hook with useFormik, define initial values and onSubmit, apply a Yup validation schema, and simplify input handling with getFieldProps, touched, and errors.  
学习如何使用 useFormik 来实现 Formik 钩子功能，定义初始值以及 onSubmit 处理程序。运用 Yup 进行验证规则的定义，并利用 getFieldProps、touched 和 errors 属性来简化输入处理的逻辑。

### Formik components  表单组件9:51

Learn to replace plain forms with Formik components, wrap with a Formik instance, and use Field, ErrorMessage, initial values, validation schema, and onSubmit to manage inputs and errors.  
学习如何使用 Formik 组件来替代传统的表单格式。将表单封装在一个 Formik 实例中，并利用 Field、ErrorMessage、初始值、验证模式以及 onSubmit 等功能来管理输入和错误情况。

### Custom components  定制组件10:08

Create a custom field component and connect it to formic to handle value, placeholder, onChange, onBlur, and errors, enabling a reusable last name input.  
创建一个自定义字段组件，并将其连接到 Formic 系统中。该组件可以处理值、占位符、 onChange 事件、失去焦点时的响应以及错误情况，从而实现一个可复用的姓氏输入功能。


##   处理复杂的状态  
11 次讲座 • 每场 1 小时 24 分钟

### Introduction  引言2:42

Discover how to manage complex React state with Redux or useReducer with context and the global store, and learn when to scale with Redux versus simpler solutions.  
了解如何借助 Redux 或 useReducer 来管理复杂的 React 状态，以及何时选择使用 Redux 与更简单的解决方案来进行扩展。

### Creating a store  创建商店5:52

Create a react app, install redux toolkit and react-redux, configure a store with reducers, wrap the app in the react-redux provider, and create a slice for state management.  
创建一个 React 应用，安装 Redux 工具和 react-redux 库，配置一个包含 reducer 的商店状态，将应用包装在 react-redux 的提供者中，并创建一个用于状态管理的组件。

### Creating a Slice  创建一片切片3:40

Create a redux toolkit slice to manage state, name it meaningfully (movies), define initial state and reducers, export the slice and reducer, then wire them into the store and provider.  
创建一个 Redux 工具包片段，用于管理状态。为这个片段起一个有意义的名字（比如“Movies”）。定义初始状态以及相关 reducer 函数。将片段和 reducer 导出，然后将它们连接到 store 和 provider 中。

### Accessing store  访问商店5:25

Access the centralized Redux store to manage a default movie list, view state with Redux devtools, and render movies using useSelector to read state.movies and map them.  
通过访问集中的 Redux 存储来管理默认的电影列表，使用 Redux 开发工具来查看状态，并通过 useSelector 来读取 state.movies 中的电影数据，然后对其进行映射以显示电影内容。

### Dispatching actions  执行操作13:34

Mutate state with actions in reducers using dispatch and payloads, exporting add movie, and building a users slice with set type to switch between guest and admin.  
在 reducers 中使用 dispatch 和 payloads 来触发状态变更；通过 export add movie 功能添加电影；通过设置 type 来创建用户切片，使得用户能够在这两种角色之间切换。

### Extra Reducers part 1  额外还原器 第一部分12:46

### Extra Reducers part 2  额外还原器 第二部分10:14

Extend extra reducers by mastering the async thunk flow with thunk API, dispatch, and getState; pass arguments, manage loading with pending and fulfilled states, and handle errors.  
通过掌握 thunk API、dispatch 和 getState 的异步 Thunk 流程，扩展额外的简化器;传递参数，管理有待处理和已完成状态的加载，并处理错误。

### Reject and Fulfill  拒绝与满足4:00

Master reject and fulfill using the thunk API, with rejectWithValue and fulfilled payloads, inspect action payloads, and transition from error to success, then compare context and useReducer as Redux alternatives.  
使用 thunk API 进行拒绝操作并完成相关任务。通过 rejectWithValue 传递拒绝信息，通过 fulfilled payloads 表示已完成任务。同时，可以检查操作负载，并从错误状态过渡到成功状态。最后，可以将 useContext 和 useReducer 作为 Redux 的替代方案来使用。

### Promise form dispatch  承诺形式派遣6:10

Explore dispatching thunks as promises, access the response payload via unwrap, handle success and error paths, and fetch users on app load with useEffect.  
尝试将线程调度视为一种承诺，通过 unwrap 函数获取响应数据，处理成功和错误情况，并利用 useEffect 函数在应用加载时获取用户信息。

### Creating a context  创建上下文5:39

Create a React context to replace Redux for state management using useReducer, build a provider, and wire it into the app to share values across components.  
使用`useReducer`创建一个 React 上下文，来替代 Redux 用于状态管理。然后创建一个提供者，并将它连接到应用中，以便在不同组件之间共享值。

### The useReducer hook  useReducer 钩子函数13:39

Explore the useReducer hook to manage state with a reducer, dispatch actions, and handle async data fetching for users, using a context provider and a Redux-like pattern.  
了解如何使用`useReducer`钩子通过 reducer 来管理状态、触发动作，以及处理异步数据获取操作。同时，利用上下文提供器和类似 Redux 的模式来实现这些功能。


##   实践项目二——每日新闻  
16 次讲座 • 2 小时 10 分钟

### introduction  引言2:48

Build a daily news app that loads posts from a local json server on demand with async loading and masonry, plus newsletter signup and a contact form.  
构建一个每日新闻应用，按需从本地 JSON 服务器加载帖子，支持异步加载和瀑布流布局，同时包含新闻简报订阅和联系表单。

### Creating routes  创建路线8:42

Create routes in a React course, set up a routes file, build home and contact components, configure React Router with BrowserRouter, and apply Bootstrap styling.  
在 React 课程中创建路由，设置路由文件，构建主页和联系表单组件。使用 BrowserRouter 配置 React Router，并应用 Bootstrap 样式表进行布局设计。

### Redux config  Redux 配置7:13

learn to configure a redux store with posts and users reducers, create thunks for async actions, wire react-redux provider, and scaffold a header for navigation.  
学习如何配置 Redux 存储系统，包括使用帖子和用户 reducer。了解用于异步操作的 Thunk 函数。掌握 React-Redux 的提供者功能。此外，还要学会如何为导航栏添加元素。

### Header and Layout  标题与布局7:32

### Json server  Json 服务器4:45

### Fetching home posts  获取回家后的帖子13:49

Fetch home posts with a new home posts component, wiring a Redux thunk to request paginated data from the local json-server using Axios, then append six posts on load more.  
将带有“新文章”组件的帖子加载到首页；使用 Axios 向本地 JSON 服务器请求分页数据；在“加载更多”操作时添加六篇帖子。

### Load more posts  加载更多帖子10:35

Fetch the initial six posts and then load more with a thunk that uses get state to merge new data into existing items and manage pages.  
先获取最初的六篇帖子，然后再加载更多内容，其中使用 get 状态机制将新数据合并到现有条目中，同时处理页面显示问题。

### Masonry cards  砖块卡片11:01

Learn to implement responsive masonry cards with the React Masonry library, displaying posts with images, titles, excerpts, read more links, and a loading spinner while data loads.  
学习如何使用 React Masonry 库来实现响应式 masonry 卡片效果。这些卡片可以显示带有图片的帖子，包括标题、摘录以及“阅读更多”链接。在数据加载过程中，还会显示加载 spinner 效果。

### Article route and moment  文章路线与时机3:50

Set up a posts route and post item component, register a dynamic id route, fetch the post by id with a thunk, and format the date using moment.js.  
设置了一个帖子路由和帖子项组件，注册了一个动态 ID 路由，通过 Thunk 函数根据 ID 获取帖子信息，然后使用 moment.js 库对日期进行格式化处理。

### Article view  文章查看11:11

Fetch a post by id with Redux thunk, useEffect, and useParams, then render title, image background, author, date, and content via dangerously set inner HTML; cleanup clears post by id.  
使用 Redux 的 thunk 函数根据 ID 获取帖子信息，通过 useEffect 和 useParams 函数来渲染标题、图片背景、作者、日期以及内容；在渲染完成后，通过 dangerously set 属性在页面内插入这些元素；最后清理该 ID 对应的帖子数据。

### Cleaning post by ID  按照 ID 顺序进行清理工作3:51

Implement a useEffect cleanup that dispatches clear post by ID on unmount to reset post by ID to an empty object via the posts slice.  
实现一个 useEffect 清理函数，在组件卸载时根据帖子 ID 来清除数据，并在组件重新挂载时通过 posts 切片将帖子 ID 对应的数据重置为空对象。

### Contact form  联系表单8:42

Create a React contact form with Formik and Yup, using React Bootstrap alerts and toasts, and dispatch the new contact to the server on submit.  
使用 Formik 和 Yup 库创建一个反应式联系表单，并利用 React Bootstrap 的警报和 toast 功能。在提交表单时，将新的联系信息发送到服务器。

### Contact validation  联系确认3:21

Validate a contact form with yup in a React workflow, defining a schema for email, first name, last name, and message; enforce required fields and a 500-character limit.  
在 React 工作流程中，使用 yup 来验证联系表单的填写内容。需要定义关于电子邮件、名字、姓氏以及消息的字段规范；同时还会强制要求填写必填字段，并且消息长度不得超过 500 个字符。

### Submit contact  提交联系信息8:34

Submit contact explains building a thunk to post contact data to a json server, dispatching actions with unwrap, handling errors, and showing toasts for success or failure.  
提交联系人信息后，会生成一份包含联系人数据的 JSON 格式数据，然后将其发送到服务器。在发送数据的过程中，会处理各种错误，并展示操作的成功或失败情况。

### Newsletter form  电子报订阅表单8:45

Master building a reusable newsletter form in a React, Next.js, and Express app, including a simple input, toast feedback, basic validation, and Redux dispatch to post emails to the database.  
该项目使用 React、Next.js 和 Express 框架来构建可重复使用的新闻通讯表单组件。其中包括简单的输入字段、 toast 式反馈机制、基本验证功能，以及用于向数据库发送电子邮件的 Redux 调度功能。

### Finishing the app  完成应用程序的构建15:19

Finish the app by implementing a redux async thunk to post newsletter signups via axios to a json-server, handle success and failure with toasts, and clear state after each attempt.  
完成这个应用程序的开发工作。需要实现一个 redux 异步 thunk，通过 axios 将订阅注册信息发送到 json-server 服务器。在处理成功和失败的情况时，使用 toast 来提示用户。每次尝试后，清理相关的状态变量。


##   Next.js 基础教程  
14 次讲座 • 1 小时 3 分钟

### Introduction  引言2:29

Explore how Next.js extends React to build SEO friendly, server-rendered pages with a file-based routing system, pre-rendering, and built-in caching, enabling full-stack apps with API and authentication.  
了解 Next.js 如何扩展 React 的功能，从而构建出具有 SEO 友好性的、基于服务器渲染的页面。该框架拥有基于文件的路由系统、预渲染功能以及内置的缓存机制，能够支持带有 API 和身份验证功能的全栈应用开发。

### Creating a Next JS App  
创建一个 Next.js 应用程序  预览版4:19

Create a Next.js app with npx create-next-app, opt into the latest, explore source routes and the app router, then run the dev server on localhost 3000.  
使用 npx create-next-app 创建一个 Next.js 应用。启用“最新功能”模式，探索源路由和应用程序路由功能。然后在本地端口 3000 上运行开发服务器。

### How Routes work  路线是如何工作的3:16

Learn how Next.js uses a file-system routing model with the app router as the current standard, contrasting it with the pages router and directory-based routes like /users and /users/profile.  
了解 Next.js 是如何使用文件系统路由模型的。在这种模型中，应用路由被作为当前的标准方式来处理路由逻辑，这与基于页面的路由方式以及像/users和/users/profile.这样的基于目录的路由方式有所不同。

### Static Routes  静态路由3:31

Learn how to create static routes in Next.js by building pages with page.js, structuring directories for home and contact, and exploring nested routes for simple, fast routing.  
了解如何通过使用 page.js 来创建静态路由，如何规划主页和联系页面的目录结构，以及如何利用嵌套路由实现简单且高效的路由管理。

### Nested Routes  嵌套路线2:54

Explore nested routes in a react and next.js project by using directories, trees, and subtrees, creating users and profile pages with page.js to establish root routes.  
通过使用目录、树结构以及子树结构，在 React 和 Next.js 项目中探索嵌套路由。还可以使用 page.js 来创建用户和个人资料页面，从而建立根路由。

### Dynamic Routes  动态路线3:41

Learn how to implement dynamic routes in Next.js by creating folders with square bracket names to capture id and name params from the URL, using page.js to render them.  
了解如何在 Next.js 中实现动态路由。通过创建带有方括号名称的文件夹，可以从 URL 中获取 ID 和名称参数，然后使用 page.js 来渲染这些参数。

### Params and Searchparams  参数和搜索参数5:46

Access dynamic route values with props.params to get the id and name, then use search params for queries. Await the promise in an async function, noting server routes.  
通过 props.params 获取动态路由的值，从而获取路由的 ID 和名称，然后用于查询操作。在异步函数中等待结果返回，同时需要注意服务器端的路由处理。

### Catch-All routes  万无一失的路线4:50

Discover catch-all routes with app routing and slug parameters, using cars/[...slug] and optional [[...slug]]; decide between single-page slug handling and nested routes.  
通过应用程序的路由功能以及 slug 参数，可以发现各种可能的路由路径。可以使用 cars/[...slug]和 optional[[...slug]]来表示具体的路由路径。你可以选择使用单页 slug 处理方式，或者采用嵌套路由方式来处理不同的路径。

### Layouts  布局设计6:35

Discover how the root layout wraps the entire app, provides html and body, and configures metadata and google fonts via next font, with nested layout.js routes cascading across pages.  
了解根布局如何覆盖整个应用程序，如何提供 HTML 和主体内容，以及如何通过 Next Font 配置元数据和谷歌字体。同时，可以看到嵌套的 layout.js 路由如何在各个页面之间级联传递。

### Linking and Navigation  链接与导航8:32

Learn linking and navigation in Next.js by using the next/link component with hrefs to build a seamless single-page app, including params, useRouter, and client versus server components and redirects.  
通过使用 Next.js 中的 next/link 组件，学习如何构建无缝的单页应用程序。了解参数传递、useRouter 函数，以及客户端与服务器组件之间的协作，还包括重定向机制。

### Standard components  标准组件2:45

Create a standard react component in a dedicated components folder, export it, and import it into a page using next/link, enabling reusable navigation in a Next.js app.  
在专门的 components 文件夹中创建一个标准的 React 组件，将其导出，然后通过 next/link 将其导入到页面中。这样就能在 Next.js 应用中实现可复用的导航功能了。

### Styles and Modules  风格与模块4:06

Explore styling in Next.js by creating a styles directory with a global.css imported in the root layout and using CSS modules for page-specific styles via imports.  
通过创建一个 styles 目录来探索 Next.js 中的样式管理。在根布局文件中导入 global.css 样式表，并利用 CSS 模块实现页面特定的样式设置。

### Parallel Routes  平行路线5:50

Explore parallel routes in Next.js by building a dashboard with analytics and posts, using an @ prefix to render multiple pages within a shared dashboard layout.  
通过构建带有分析和帖子的仪表盘，探索 Next.js 中的平行路由方案。使用@前缀来在共享的仪表盘布局中渲染多个页面。

### Route Groups  路线组3:57

Explore route groups and root groups in Next.js to organize guitars, brands, and models with layouts and direct routes, then learn rendering and pre rendering strategies.  
在 Next.js 中探索路由组和根组的用法，通过布局与直接路由来组织吉他、品牌和型号等信息。同时，了解渲染和预渲染相关的策略。


##   Next JS 渲染技术  
21 次讲座 • 2 小时 10 分钟

### Creating a template  创建模板10:01

Create a Next.js template with app router and bootstrap, set up dynamic employee routes (add/edit) and a header layout, then run the dev server to preview rendering and caching.  
创建一个基于 Next.js 的模板，包含应用程序路由器和 Bootstrap 界面。配置动态员工路由（添加/编辑功能），并设置一个标题布局。然后运行开发服务器，预览渲染效果和缓存情况。

### Strategies  策略  预览版3:07

Explore how Next.js pre-renders pages on the server, delivering HTML with React code to boost SEO and performance, then hydrates on the client with static generation, dynamic rendering, and streaming.  
了解 Next.js 如何在服务器上预先渲染页面，使用 React 代码生成 HTML 以提高 SEO 性能和性能表现。同时，它还能在客户端通过静态生成、动态渲染和流式传输等方式进一步优化性能。

### Setting up a DB  设置数据库4:52

Set up a json server to mock an API, using a db.json to manage employees, install json-server, run both the app and server, and test endpoints with axios.  
设置一个 JSON 服务器来模拟 API 的功能，使用 db.json 来管理员工信息。安装 json-server，同时运行应用程序和服务器，然后使用 axios 测试各个端点。

### Home Employees  家庭员工6:02

Fetches employee data from the server using axios on the home page, maps them into bootstrap cards showing name, position, and age, and links to individual employee pages.  
在主页上使用 axios 从服务器获取员工数据，将它们映射到显示姓名、职位和年龄的 bootstrap 卡片中，并链接到个人员工页面。

### Why can't I use Hooks ? Part 1  
为什么我不能使用 Hooks？第一部分7:04

Explore when React hooks work in Next.js by distinguishing server components from client components, using the use client directive, and observing pre-rendering and client-side behavior.  
通过区分服务器组件和客户端组件，使用`useClient`指令，以及观察预渲染和客户端端行为，来了解 React Hooks 在 Next.js 中的适用情况。

### Why can't I use Hooks ? Part 2  
为什么我不能使用 Hooks？第二部分3:26

explore how to share props or functions between a server component and a client component in Next.js, including marking functions with use server and use client.  
了解如何在 Next.js 中实现服务器组件和客户端组件之间的道具或功能的共享，包括如何通过“useServer”和“useClient”函数来标记这些功能。

### More on Server and Client  
关于服务器和客户端方面的更多信息2:00

Compare client and server components in Next.js, noting server components cannot use hooks or event handlers. Enjoy fetch integration, simple authentication, caching, and full server-side rendering.  
比较 Next.js 中的客户端组件和服务器端组件，注意服务器端组件不能使用钩子或事件处理器。享受 fetch 集成、简单认证、缓存和完整的服务器端渲染。

### Server Action File  服务器操作文件4:43

Explore how server actions attach to server components to run code in client components, using either props passing or importing a separate file with use server at the top.  
了解服务器操作如何与服务器组件相连，从而能够在客户端组件中执行代码。这可以通过传递属性来实现，或者是通过导入一个包含`use server`函数的单独文件来实现。

### Introduction to Server Forms  
服务器表单介绍8:04

Learn to build a simple server form in Next.js to add employees to a JSON server, using form actions and use server or use client to manage submissions.  
学习如何在 Next.js 中构建一个简单的服务器表单，用于将员工信息添加到 JSON 服务器中。可以使用表单操作来添加员工数据，并且可以选择使用服务器端或客户端来处理数据提交。

### Refactor to actions file  将动作文件进行重构4:21

### The "useActionState" hook  
“useActionState”钩子函数13:37

Discover how the useActionState hook simplifies form handling by managing server responses, errors, and loading states in client components with validation and feedback.  
了解 useActionState 钩子函数如何通过管理服务器响应、错误处理以及客户端组件的加载状态，从而简化表单处理的流程，同时还能实现验证和反馈功能。

### Loading UI  加载用户界面4:46

Implement loading user interface at app root with loading.js wrapper, using suspense and a promise to show a loader during data fetches, and add route-specific loaders like employees by id.  
在应用程序的根目录下实现用户界面的加载功能，使用 loading.js 封装库，通过 suspense 和 Promise 技术在数据获取过程中显示加载器。同时，可以添加针对特定路由的加载器，例如根据员工 ID 来加载员工信息。

### Employee by ID  按员工 ID 筛选3:05

Fetch an employee by id using axios, access the id from route params, call the /employees/{id} endpoint, and render the employee’s name and position with loading and 404 error handling.  
使用 axios 根据员工 ID 获取员工信息，从路由参数中提取该 ID，调用/employees/{id}这个端点。同时，需要处理加载时的提示信息以及 404 错误情况，以显示员工的姓名和职位。

### Error UI  错误界面7:38

Explore building error handling in a Next.js app by implementing a client error UI, switching from Axios to fetch, and creating not found and error pages with retry options.  
在 Next.js 应用中实现错误处理功能。包括创建客户端错误界面，将通信方式从 Axios 切换到 fetch，以及创建“未找到”和“错误”页面，并加入重试功能。

### Not Found UI  未找到相关界面/菜单4:29

Implement a not found UI in a Next.js app by replacing errors with a custom not-found page, using server components and next/navigation to redirect and link home.  
在 Next.js 应用中实现一种“未找到”的界面效果，通过将错误页面替换为自定义的页面来应对这种情况。可以使用服务器组件以及 `next()` 函数来实现页面的重定向，并链接到首页。

### Edit Employee page  编辑员工页面7:24

Build an edit employee page by fetching the employee by id, populating a form, validating with formic, and posting changes via a server action using a dynamic id route.  
通过获取员工 ID 来创建编辑员工页面，填充表单信息，使用 Formic 进行验证，然后通过动态的 ID 路由将更改发布到服务器。

### Creating the edit form  创建编辑表单7:30

Create an edit form with formic to pre-populate employee data using enable reinitialize, set initial values for full name, position, and age, and apply yep-based client-side validation with Bootstrap styling.  
创建一个编辑表单，使用 formic 工具预先填充员工数据，通过启用“重新初始化”功能来实现数据的动态更新。为姓名、职位和年龄等字段设置初始值。同时，应用基于 yep 的客户端验证机制，并搭配 Bootstrap 的样式设计。

### Edit employee action part 1  
编辑员工操作部分 15:58

Create and handle a server action to edit an employee, validating age on the server, posting updated data via patch to the employees endpoint, and redirecting back home.  
创建并处理一个服务器操作，以编辑员工信息；在服务器上验证年龄；将更新后的数据通过补丁发送到员工端点；然后返回到初始状态。

### Edit employee action part 2  
编辑员工操作部分 28:23

Learn to connect the edit employee form to a server action using use transition and use state for error handling, with formic validation and redirects on success.  
学习如何将编辑员工信息的表单与服务器操作链接在一起，使用过渡效果以及状态管理来处理错误情况，同时利用表单验证功能在成功情况下实现重定向。

### Deleting employees  删除员工7:30

### Streaming  流式传输5:54

Discover how streaming loads components in chunks with React Suspense, rendering each heavy component as it finishes loading and preventing the whole page from blocking.  
了解如何使用 React Suspense 以块的形式加载组件。这样可以在组件加载完成后再渲染下一个组件，从而避免整个页面出现阻塞的情况。


##   Next JS 缓存机制  7 次讲座 • 43 分钟

### Build and Start  构建并开始3:20

Build and start a production-ready next.js app by running npm run build and npm run start, test the build locally, and understand static versus dynamic routes for production readiness.  
通过运行`npm run build`和`npm run start`来构建并启动一个可投产的 Next.js 应用。同时，请尝试在本地测试一下构建结果。此外，还需要了解静态路由与动态路由的区别，以确保应用在生产环境中能够正常运行。

### Dynamic or Static  动态的或静态的4:21

Next.js uses root, router, and data caches with request memoization to speed builds and control whether pages are static or dynamic via fetch options and dynamic routes.  
Next.js 利用根组件、路由管理器以及数据缓存功能，结合请求记忆技术，来加速构建过程。同时，通过 fetch 选项和动态路由，可以决定页面是静态还是动态展示。

### Segment config options  分段配置选项8:23

Explore segment config options in Next.js to control static and dynamic pages, implement revalidate timing, and optimize data fetching for home, employees, and id routes.  
在 Next.js 中探索片段配置选项，以控制静态和动态页面；实现重新验证计时功能；并优化首页、员工页面以及 ID 路由的数据获取过程。

### Revalidate  重新验证6:47

Learn to revalidate Next.js pages on demand by calling a revalidate function to refresh routes like home and employee pages after add, edit, or delete actions.  
学会根据需求重新验证 Next.js 页面是否有效。在执行添加、编辑或删除操作之后，可以通过调用重新验证函数来更新如首页和员工页面等路由的内容。

### Fetch options  获取选项3:15

Explore fetch options in a Next.js context to control caching, dynamic versus static pages, and revalidation intervals using cache, no-store, and revalidate settings.  
在 Next.js 环境中探索各种获取数据的选项，包括控制缓存、动态页面与静态页面的区分、以及通过缓存、无存储模式和新验证设置来调整重新验证的频率。

### Cookies and Headers  饼干和头部信息9:27

Master headers and cookies in a Next.js server component, learning to read, iterate, and test header values and cookies in a dev environment.  
在 Next.js 的服务器组件中处理头部信息和 cookie 值，学习如何在开发环境中读取、修改和测试这些头部数据以及 cookie 值。

### Static Params  静态参数6:58

Learn how generate static params builds static HTML at build time for dynamic employee routes, creating pre-rendered pages for each id and enabling fast, server-free access via SSG.  
了解如何生成静态参数，从而在构建时为动态员工路由生成静态 HTML 页面。这种方式可以为每个 ID 创建预渲染的页面，并支持通过 SSG 进行快速、无需服务器的访问。


##   下一个 JS API 路由路径  7 次讲座 • 34 分钟

### Introduction  引言1:21

Learn how Next.js API routes power backend tasks by handling get, post, put, patch, and delete requests, returning JSON or HTML to fetch articles and form data.  
了解 Next.js API 的路由功能如何驱动后端任务的处理，它通过处理 GET、POST、PUT、PATCH 和 DELETE 请求，返回 JSON 或 HTML 数据来提取文章和表单数据。

### App route handlers and Postman  
应用程序路由处理程序以及 Postman 工具6:21

Explore the Next.js app router versus the pages router, create API routes with route.js, and test them with Postman using get, post, put, patch, and delete against localhost.  
了解 Next.js 中的应用路由与页面路由的区别，使用 route.js 创建 API 路由，并通过 Postman 进行测试，包括对本地服务器的 GET、POST、PUT、PATCH 和 DELETE 请求测试。

### GET route ### part 1  
GET 路由——第一部分  预览版4:16

Create a get API route under api/data in the complete React 19 course, handling requests and returning JSON responses with query strings x and y.  
在完整的 React 19 课程中，创建一个位于/api/data 下的 get API 路由，该路由能够处理请求，并返回包含查询参数 x 和 y 的 JSON 响应。

### GET route ### part 2  
GET 路由——第二部分7:48

Learn to implement a get route that fetches employees from a JSON server, returns JSON with status 200, and uses cookies and headers such as authorization and content type.  
学习如何实现一个获取路由，该路由从 JSON 服务器获取员工信息，返回包含状态 200 的 JSON 数据。同时，还需要利用 cookie 以及诸如授权和内容类型等头部信息。

### POST employee  POST 员工5:04

Post a new employee by sending a json body to the same endpoint, process the request, post to db.json, and return a 200 ok on success.  
向同一个端点发送一个 JSON 体，用于新员工的注册。处理该请求后，将结果存储到 db.json 文件中，如果成功则返回 200 OK 响应。

### PATCH employee  PATCH 员工6:50

Create and test Next.js API routes with dynamic updates by patching an employee via /api/update/[id], using params and request body, validating responses.  
通过/api/update/[id]接口，通过修改员工数据来实现 Next.js API 路由的动态更新。需要使用参数和请求体来传递数据，并验证响应的结果。

### DELETE employee  删除员工2:32

Learn to implement a delete-by-id endpoint on a json server using a dynamic param, fetch with the delete method, and handle ok or error responses.  
学习如何在 JSON 服务器上实现一个基于 ID 的删除端点。该端点需要使用动态参数进行数据获取，并通过删除方法来完成删除操作。同时，还需要处理成功或失败的响应情况。


##   Next.js 英雄与额外功能  
6 次讲座 • 30 分钟

### Introduction  引言1:56

Explore tailwind and hero ui within a Next.js project, and follow setup steps to create the app, configure the source directory and app router, and install tailwind and hero ui.  
在 Next.js 项目中使用 Tailwind 和 Hero UI，按照安装步骤来创建应用程序，配置源代码目录和应用程序路由器，然后安装 Tailwind 和 Hero UI 组件。

### Installing Tailwind and Hero UI  
正在安装 Tailwind 和 Hero UI 框架9:31

Learn to install tailwind v3 and hero UI for a Next.js project, navigate version mismatches, and configure the provider to run a React 19 course app.  
学习如何为 Next.js 项目安装 Tailwind 3 版本和 Hero UI 组件。了解如何处理版本差异问题，以及如何配置提供商以运行基于 React 19 的课程应用程序。

### Quick Tailwind  快速顺风6:15

Learn the basics of tailwind through a quick introduction, exploring global.css usage, utility classes, and styling components with text centering, blue text, borders, and hover effects.  
通过简短的介绍来学习 Tailwind 框架的基础知识，包括 global.css 的使用方法、实用类组件的应用，以及如何通过文本居中、蓝色文字、边框以及悬停效果等方式来美化组件样式。

### Sass  萨斯3:20

Install sass with npm, create a root styles folder with an scss file defining a Roboto font and a primary color, import it in layout.js, and apply sass classes.  
通过 npm 安装 sass，创建一个名为“rootstyles”的文件夹，并在其中创建一个 scss 文件，用于定义 Roboto 字体和主颜色。将这份文件导入到 layout.js 文件中，然后应用 sass 类。

### Next UI introduction  下一个用户界面介绍4:11

Explore Next UI, a comprehensive React UI library with components like buttons, accordions, and icons; learn to install, import, and copy code from docs to build UI quickly.  
探索 Next UI，一个全面的 React 用户界面库，包含按钮、折叠菜单和图标等组件。了解如何安装、导入该库，并从文档中复制代码以快速构建用户界面。

### Imports and module alias  导入内容及模块别名5:14

Learn how to configure jsconfig.json in Next.js to set a base URL and create path aliases for components and utilities, simplifying imports and speeding up development.  
学习如何在 Next.js 中配置 jsconfig.json 以设置基本 URL 并为组件和实用工具创建路径别名，从而简化导入并加快开发速度。


##   Next JS 认证  
20 次讲座 • 2 小时 40 分钟

### Introduction  引言6:04

Create a Next.js authentication foundation by building a template with Tailwind and Hero UI, configuring the app router, and enabling sign-in with NextAuth.  
通过使用 Tailwind 和 Hero UI 创建一个 Next.js 认证基础框架，配置应用程序的路由，并启用使用 NextAuth 进行登录功能。

### Creating a nav  创建导航菜单11:38

Build a navigation bar by creating a nav component, integrate it into the layout, and add a drop-down menu with links such as register and logout using hero ui.  
通过创建一个导航组件来构建导航栏，将其集成到布局中，并使用 Hero UI 添加包含注册、注销等链接的下拉菜单。

### Configuring Fonts  配置字体5:57

Learn to configure google fonts and local fonts in Next.js by importing local fonts, defining Roboto and Anton with weights like 400 and 700, and applying them via css variables.  
学习如何在 Next.js 中配置 Google 字体和本地字体。通过导入本地字体，定义如 Roboto 和 Anton 等字体样式，并为其指定如 400 和 700 这样的权重值，然后利用 CSS 变量将这些样式应用到具体的元素上。

### Next Auth Credentials  下一个认证凭据11:51

Explore implementing next-auth credentials in a Next.js app, wire an API route, configure credentials and GitHub providers, and simulate sign-in with a dummy user.  
了解如何在 Next.js 应用中实现 next-auth 认证凭证的使用，如何绑定 API 路由，配置认证凭证以及 GitHub 认证服务，并模拟使用虚拟用户进行登录操作。

### Next Auth Secret  下一个认证密钥7:43

Explains configuring next auth with secret and url via dot env, setting environment variables for Vercel or other hosts, using credentials sign-in, and guarding routes with sessions.  
解释如何通过 dot env 配置 secret 和 url 的 next auth，为 Vercel 或其他主机设置环境变量，使用凭证登录，以及用会话保护路由。

### Next Auth Middleware  下一个认证中间件9:50

Implement a next auth middleware in Next.js to guard routes with a matcher, intercept requests, and redirect unauthenticated users to sign in.  
在 Next.js 中实现一个 nextAuth 中间件，使用该中间件可以通过匹配器来保护路由安全，拦截请求，并将未认证的用户重定向到登录页面进行登录。

### User session on the Server  
服务器上的用户会话8:13

Access and guard user sessions on the server with next-auth by using getServerSession in a server component, exporting shared options, and conditionally rendering content based on authentication.  
通过使用服务器组件中的`getServerSession`函数来访问和管理服务器上的用户会话，同时导出共享选项。根据身份验证结果，有条件地渲染不同的内容。

### User session on the Client  
客户端上的用户会话8:49

Wrap the application with a client-side session provider and use the useSession hook to guard routes. Learn to handle authenticated and unauthenticated states with redirects and sign-in flows.  
在应用程序中引入一个客户端会话提供者，并使用 useSession 钩子来管理路由。学会如何通过重定向和登录流程来处理认证状态与非认证状态的情况。

### Github Sign in  Github 登录6:18

Learn to implement GitHub sign in with OAuth by registering an OAuth app, obtaining a client ID and secret, configuring environment variables, and the authorization callback.  
学习如何通过注册 OAuth 应用程序、获取客户端 ID 和密钥、配置环境变量，以及处理授权回调接口，来实现基于 GitHub 的 OAuth 登录功能。

### Register form part 1  注册表格 第一部分13:42

Build a custom register and sign-in form in a Next.js app, manage sessions with server-side checks and redirect to the dashboard, and validate inputs with formic and Yup.  
在 Next.js 应用中构建自定义的注册和登录表单。通过服务器端检查来管理会话，并将用户重定向到仪表板。同时使用 Formic 和 Yup 工具来验证输入数据。

### Register form part 2  注册表格 第二部分8:32

Finish building the register form by wiring email and password inputs to formic, add validation and error handling with a reusable utils helper, and preview error messages.  
请完成注册表的构建工作，将电子邮件和密码信息输入到表单中，使用可复用的工具函数进行验证和错误处理，并预览错误信息。

### Creating a Cluster  创建集群6:08

Create a MongoDB project and a free cluster, set up network access and a database user, then connect your application to the database using mongoose.  
创建一个 MongoDB 项目和一个免费集群，设置网络访问权限和数据库用户，然后使用 mongoose 将应用程序连接到数据库上。

### DB Connect9:08

Learn to connect to MongoDB with mongoose, define models and schemas, and enforce data validation using env var MongoDB URI; implement a robust Next.js aware connection cache to prevent reinitialization.  
学习如何使用 mongoose 与 MongoDB 进行连接操作，定义模型和模式，并通过环境变量 MongoDB URI 来实施数据验证；实现一种高效的 Next.js 兼容的连接缓存机制，以避免需要重复初始化连接。

### Mongoose Schema  猫头鹰架构5:42

Create and export a mongoose user schema and model to enforce email uniqueness, require a password, and add timestamps, then validate and save user data in MongoDB.  
创建并导出 mongoose 用户模式和模型以强制邮件唯一性，要求密码并添加时间戳，然后验证并保存用户数据到 MongoDB。

### Register route  注册路由10:22

Register users by hashing passwords with bcrypt, then saving user with email to database via a register API route. Check existing users, hash with ten rounds, and respond with codes.  
通过 bcrypt 算法对密码进行哈希处理，然后将包含用户信息的数据通过注册 API 接口保存到数据库中。检查现有的用户，对他们的密码进行十轮哈希处理，并返回相应的验证码。

### Register a new user  注册新用户6:11

Register a new user by submitting email and password to the /api/register endpoint with a post request, handling the response to create a user in MongoDB with a hashed password.  
通过发送电子邮件和密码到/api/register 端点来注册新用户。处理返回的响应后，会在 MongoDB 中创建一个用户，密码会被哈希处理。

### Sign in users part 1  
用户登录 第一部分7:49

Learn to sign in users by validating email and password against the database, using bcrypt to compare unhashed input with the stored hash. Issue tokens and cookies with NextAuth.  
通过对比数据库中的电子邮件和密码信息，利用 bcrypt 算法将用户输入的未哈希数据与存储的哈希值进行比对，从而验证用户的身份。使用 NextAuth 模块来生成令牌和 cookie。

### Sign in users part 2  
登录用户 第二部分6:54

Learn to implement sign in with the credentials provider using next-auth react, validating email and password, handling redirects to the dashboard, and signing in users after registration.  
学习如何使用 next-auth React 框架通过认证提供者来实现登录功能。包括验证电子邮件和密码，处理重定向到仪表板的操作，以及用户在注册后自动登录的功能。

### Custom sign in route  个性化的登录流程4:52

Configure next auth to use a custom sign in and register route, disable automatic redirects, and surface errors through console logs and response data.  
配置下一次认证时使用自定义的登录和注册路径，禁用自动重定向功能，并通过控制台日志和响应数据来显示错误信息。

### Logout Users  注销用户4:36

Implement logout by using the use session hook and next-auth sign out, show register and posts links only when authenticated, reinforcing protected routes, and redirect to home after signing out.  
通过使用会话钩子和 next-auth 进行注销操作，仅在用户已认证时显示注册和帖子链接。同时，保护性地使用某些路由路径，并在用户注销后重定向到首页。


##   实践项目 3 ### Event0  
18 场讲座 • 2 小时 47 分钟

### Expanding the nav links  扩展导航链接8:58

Expand the menu by adding admin sections for venues and events, create venue and event forms connected to the database, and guard dashboard routes with session-based middleware to restrict access.  
通过添加针对场馆和活动的管理页面，创建与数据库连接的场馆和活动表单，并使用基于会话的中间件来保护仪表板路由，从而扩展菜单功能。同时，限制对这些页面的访问权限。

### The venue model  场所模式7:43

Create a venue model and form, validate on the server with yup, post to the database via mongoose, and render a client-side form using use action state.  
创建一个表单模型，在服务器上进行验证，然后使用 yup 工具包将结果提交到数据库，最后通过 use action 状态来渲染客户端表单。

### Creating the Venue form  创建场地表单7:27

Create a venue form in a React Next.js app using Hero UI inputs for name and address, and a state select populated from JSON with a submit action.  
在 React Next.js 应用中使用 Hero UI 组件创建一个表单，包含用于输入姓名和地址的输入框。此外，还需要一个从 JSON 数据中填充状态的下拉选择框，并配备一个提交按钮。

### Posting Venues  举办活动的场地8:20

Post venues by creating an action, wiring this action to form data, and posting to a Mongoose database with server-side Yup validations.  
通过创建一个操作，将其链接到特定表单数据，然后将其发布到使用服务器端 Yup 进行验证的 Mongoose 数据库中来实现发布功能。

### Venue Schema Validation  场所结构验证11:40

Learn to validate a venue form with a Yup schema, including name, address, and state, enabling full-field error collection via abortEarly: false, returning simple messages to the client.  
学习如何使用带有 Yup 表单模板的表单验证机制，包括姓名、地址和州信息。通过设置 abortEarly 参数为 false，可以实现全字段错误收集功能。同时，向客户端返回简单的错误信息即可。

### Client Venue errors  客户场地信息出现错误5:21

Handle server responses with use action state to display success or error messages on the venue form, validate on the server, and post venues and events.  
处理服务器响应，利用“操作状态”功能在场地表单上显示成功或错误信息。在服务器上进行验证，然后提交场地和活动的信息。

### The event model  事件模型9:33

build an event model with mongoose, linking to a venue by objectId and slug, and implement a server-side add event flow using formic forms, server components, and validations.  
使用 mongoose 构建事件模型，通过对象 ID 和 slug 将事件链接到特定场地。同时，使用 Formic Forms、服务器组件以及验证功能来实现服务器端的事件添加流程。

### Add event form  添加事件表单13:07

This lecture builds an add event form with formic, covering initial values and validation, on submit logging values, and fetching venues from the server to populate a venue select.  
本次讲座介绍了如何使用 formic 来创建一个添加事件的表单，包括初始值的设置、验证功能的实现、提交时数据的记录，以及从服务器获取场地信息以填充场地选择框的功能。

### Event Datepicker  事件选择日期工具11:08

Learn to integrate react date picker with formik and hero ui, building a custom input button, syncing date with state, validating, styling, and posting an event.  
学习如何将 React 日期选择器与 Formik 和 Hero UI 集成在一起，创建一个自定义的输入框按钮。同时，还需要实现日期与状态的同步管理、输入验证、样式设计以及事件发布功能。

### Posting Events  发布活动信息9:21

Post events with a server action, connect to the database, and save a new event with validation and error handling, then revalidate home and dashboard paths.  
在事件发生后，执行服务器操作，连接到数据库，保存一个新的事件记录，并包含验证和错误处理功能。之后，重新验证主页和仪表板的路径是否正确。

### Displaying toasts  展示祝酒词2:51

learn to implement toasts in a React app using React Toastify, including setting up a toast container in the layout, triggering success toasts, and positioning them bottom-right for events.  
学习如何在 React 应用中使用 React Toastify 来实现 toast 功能的实现，包括如何在布局中设置 toast 容器、触发成功提示，以及将这些提示定位在屏幕的右下角以方便用户查看。

### Getting home events  返回家中的事件记录10:47

Fetch events from the database and populate them on the home page using an action. Use a MongoDB query with populate, sort, skip, and limit for load more posts.  
从数据库中获取事件信息，并通过相应的操作将这些信息展示在首页上。使用 MongoDB 查询语句来实现数据的加载，利用 populate、sort、skip 和 limit 等功能来加载更多的帖子内容。

### Home event cards part 1  
家庭活动卡片 第一部分7:53

Fetch home data, parse MongoDB results, and render them as a masonry layout of hero UI event cards via a new events component and React Masonry.  
获取家庭数据，解析 MongoDB 结果，并通过新的 events 组件和 React Masonry 将其渲染为英雄 UI 事件卡片的马赛克布局。

### Home event cards part 2  
家庭活动卡片 第二部分10:29

Render dynamic home event cards by mapping over events in state, using a masonry layout with hero ui cards, images, venue and artist data, and a load more button.  
通过映射状态中的事件来生成动态的家居活动卡片，使用 Masonry 布局结构，并包含主界面卡片、图片、场地信息以及艺术家数据。此外，还提供了“更多选项”按钮。

### Home load more  主页上的更多内容8:19

Implement a load more feature in a React/Next.js app by wiring a button to a server function that fetches the next posts with skip and limit, updating the UI.  
在 React/Next.js 应用中实现“加载更多”功能。通过将按钮与服务器函数连接，当点击按钮时，服务器会获取下一页的帖子信息，包括跳过某些帖子以及限制显示的数量，然后更新界面以显示这些新信息。

### Event page  活动页面11:27

Build a slug-based event page in a Next.js app by fetching event data by slug, populating venue details, and rendering with the Next/Image component, with 404 handling and a venue card.  
在 Next.js 应用中创建一个基于事件页面的组件。通过获取与事件相关的数据，填充赛事详情，然后使用 Next/Image 组件来渲染页面内容。同时，还需要处理 404 错误情况，并添加一张赛事卡片。

### Event page card  活动页面卡片8:12

Build a reusable event venue card to show venue name, address, state, and date under event posts; fix the event id bug and integrate hero ui cart components.  
请创建一个可重复使用的事件信息卡片，用于在事件帖子中显示场地名称、地址、所在州以及举办日期；修复事件 ID 的错误，并整合英雄 UI 购物车组件。

### Deploying  部署14:24

Deploy the application to Vercel, set up env vars for next auth, build and test locally, and implement revalidate paths to refresh dynamic routes after changes.  
将应用程序部署到 Vercel 平台上。为下一次身份验证设置相关的环境变量。进行本地构建和测试。同时，实现路径的重新验证功能，以便在路径发生变化后自动更新动态路由。


##   Node.js 基础教程  7 次讲座 • 27 分钟

### What is Node ?  什么是 Node？1:30

NodeJS lets you run JavaScript on your local computer and build a custom web server with Express, while supporting desktop apps with Electron and mobile apps with React Native.  
NodeJS 让你能够在本地计算机上运行 JavaScript 代码，并使用 Express 构建自定义的网络服务器。同时，它还能支持使用 Electron 开发的桌面应用程序，以及使用 React Native 开发的移动应用。

### REPL my terminal  这是将我的终端环境转换为 REPL 格式的过程。4:02

Use the node repl to test JavaScript on your local machine, create variables, arrays, and print results. Learn how repl translates code and how node differs from the browser console.  
使用 node-repl 在本地机器上测试 JavaScript 代码，创建变量、数组，并输出测试结果。了解 repl 如何执行代码，以及它与浏览器控制台的区别。

### Blocking VS non-blocking  阻塞式与非阻塞式5:43

Explore blocking vs non-blocking io in Node.js and see how asynchronous methods avoid waiting for io operations. Compare a synchronous timeline with callbacks that run when data arrives.  
了解 Node.js 中阻塞与非阻塞 I/O 的区别。了解异步方法如何避免等待 I/O 操作完成。对比一下同步方式下，数据到达时通过回调进行处理的场景。

### Testing it  对其进行测试5:09

Explore how blocking and non-blocking file operations work in Node.js using the fs module, readFileSync and readFile, callbacks, and the role of threads.  
了解在 Node.js 中，如何通过 fs 模块、readFileSync 和 readFile 函数来实现非阻塞的文件操作，以及回调函数的使用，还有线程在其中的作用。

### Threads  线程1:51

Learn how JavaScript runs on a single thread, executing one timeline at a time. See how non-blocking asynchronous code enables multitasking behind the scenes.  
了解 JavaScript 是如何在单线程上运行的，每次只处理一个任务队列。了解非阻塞的异步代码如何在不显眼的情况下实现多任务处理。

### Threads, Event loop and Call stack part 1  
线程、事件循环和调用栈第一部分3:54

Explore how the call stack manages a single-threaded JavaScript program, tracing function calls in a linear flow and resolving asynchronous, non-blocking operations via the event loop.  
了解调用栈如何管理单线程 JavaScript 程序的运行，追踪函数调用的执行流程，并通过事件循环来处理异步、非阻塞操作。

### Threads, Event loop and Call stack part 2  
线程、事件循环和调用栈第二部分5:09

Trace non-blocking JavaScript in Node.js through setTimeout, the API, the callback queue, and the event loop interacting with the call stack.  
在 Node.js 中，可以通过`setTimeout`函数实现非阻塞的 JavaScript 执行。这个过程涉及到 API、回调函数队列，以及与调用栈交互的事件循环机制。


##   节点服务器与 Express 框架  
17 次讲座 • 1 小时 50 分钟

### Creating a server with Node JS  
使用 Node JS 创建服务器6:00

Create a basic web server with Node.js using the http module's createServer in server.js to handle requests and responses, and listen on port 8181 at localhost.  
使用 Node.js 中的 http 模块在 server.js 文件中创建一个基本的 Web 服务器。利用 createServer 方法来处理请求和响应，并监听本地主机上端口 8181 的端口。

### HTML Response  HTML 响应4:25

Learn to send HTML and JSON responses from a Node.js server by setting content types, reading files with fs, and returning templates or API data.  
学习如何从 Node.js 服务器发送 HTML 和 JSON 响应。这包括设置内容类型、使用 fs 读取文件，以及返回模板或 API 数据。

### JSON Response  JSON 响应5:06

Define an api and an endpoint, and learn how a web api can return json or other formats. Set content type, stringify data, and route requests to api/users.  
定义一个 API 和对应的端点，了解 Web API 如何返回 JSON 或其他格式的数据。设置内容类型，将数据转换为字符串格式，并将请求路由到 api/users 接口。

### Routing  路由选择4:20

Learn to implement routing in a Node.js server by distinguishing home HTML responses from API JSON endpoints, handle 404 errors, and introduce Express for scalable routing.  
学习如何在 Node.js 服务器中实现路由功能，区分处理首页的 HTML 响应与通过 API 提供的 JSON 端点响应。同时，要学会处理 404 错误，并运用 Express 框架来实现可扩展的路由机制。

### Starting with express  从快速开始8:58

Initialize a node project with npm, install express, create server.js, set up routes for html and json responses, use nodemon for auto-restart, and configure port via environment variables.  
使用 npm 初始化节点项目，安装 express，创建 server.js 文件，配置用于 html 和 json 响应的路由，使用 nodemon 实现自动重启，并通过环境变量设置端口号。

### Param & Querystring  参数与查询字符串5:03

Explore building api routes with params, query strings and middleware concepts in Next.js and Express, extracting user id and name from req.params and brand and year from req.query, returning JSON.  
在 Next.js 和 Express 框架下，探索带有参数的 API 路由设计。利用`req.params`从请求参数中提取用户 ID 和姓名，从`req.query`中提取品牌和年份信息，并输出 JSON 格式的数据。

### Middleware part 1  中间件 第一部分4:28

Configure a middleware to serve static css files in express, linking public/css/styles.css in the head and using app.use('/css', express.static(__dirname + '/public/css')) to handle requests.  
在 Express 中配置一个中间件来处理静态 CSS 文件的加载。将公共目录下的 public/css/styles.css 文件复制到项目的头部文件里，然后使用`app.use('/css', express.static(__dirname + '/public/css'))`来处理相关的请求。

### Middleware part 2  中间件第二部分4:04

Learn how middleware runs for every request, using a callback with request, response, and next to log, pass control to endpoints for get or post, and set cookies.  
了解中间件如何为每个请求执行操作。它通过使用回调机制，结合请求、响应以及下一个逻辑，来传递控制给端点函数，以实现 GET 或 POST 操作，同时还能设置 cookies。

### Body Parser  身体解析器8:15

Mastering handling post requests with body parser in Express, set up JSON middleware, and build a post route to add users via API and Axios.  
在 Express 中掌握处理 POST 请求的技巧，使用体解析器来处理数据。配置 JSON 中间件，并创建一条 POST 路由，通过 API 和 Axios 来添加用户。

### Query through post  通过帖子进行查询5:51

Capture post form data in express with the urlencoded parser, creating an api/query/add endpoint and logging first and last names on the server.  
使用 urlencoded 解析器捕获 Express 中的表单数据，创建一个 api/query/add 端点，并在服务器上记录姓名和姓氏信息。

### Express & React  快速与反应14:57

Learn to scaffold an express and react project, link a node server with a vite react app using concurrently, set up a proxy, and expose a /api/users endpoint.  
学习如何构建这样一个快速且响应迅速的项目：将节点服务器与 Vite React 应用连接在一起，使用 Concurrently 功能来实现并行处理；同时设置代理服务器，并暴露 /api/users 端点。

### Creating a Cluster  创建集群2:20

Reuse the existing MongoDB cluster for the React and Express app, set up admin access, generate a password, obtain the connection string, and connect with Mongoose.  
使用现有的 MongoDB 集群来构建 React 和 Express 应用程序。配置管理员访问权限，生成密码，获取连接字符串，然后与 Mongoose 进行连接。

### Connecting to MongoDB  正在连接至 MongoDB 数据库4:00

Install mongoose with npm, keep the logic on server.js, connect to MongoDB via a mongoUri with admin credentials, and define a cars schema and car model.  
使用 npm 安装 mongoose 模块。将相关逻辑放在 server.js 文件中。通过 mongoUri 接口使用管理员凭据连接到 MongoDB 数据库。同时，定义 car 模型以及相关的 car schema。

### Posting Data  上传数据8:07

Post car data from the React app to the server via axios, parse the body with body-parser, save to MongoDB with a mongoose model, and return a 200 response.  
通过 axios 将 React 应用中的车辆数据发送到服务器。使用 body-parser 解析请求体数据，然后用 mongoose 模型将其存储到 MongoDB 数据库中，最后返回 200 的响应码。

### Getting Data  获取数据7:06

Fetch data from MongoDB with find, find one, and find by ID using mongoose, then expose a cars API in Next.js. Use axios and React hooks to render the cars.  
使用 mongoose 通过 `find` 方法根据 ID 查询 MongoDB 中的数据，然后在 Next.js 中暴露一个汽车 API。使用 axios 和 React Hooks 来渲染汽车信息。

### Deleting Data  删除数据8:01

Learn how to delete data in a React app using methods like delete by ID and delete many, and connect front-end delete actions to an Express API via Axios.  
了解如何使用诸如按 ID 删除、批量删除等方法来删除 React 应用程序中的数据，以及如何通过 Axios 将前端删除操作与 Express API 连接起来。

### Update Data  更新数据9:19

Shows how to perform update by id in a full-stack React course, wiring the client button to a backend API, and using MongoDB/Mongoose with findByIdAndUpdate.  
该教程介绍了在全栈 React 项目中通过 ID 进行更新的方法，以及如何将客户端按钮与后端 API 连接，并使用 MongoDB/Mongoose 库通过 findByIdAndUpdate 方法进行更新操作。


##   节点服务器与身份验证  
10 次讲座 • 1 小时 6 分钟

### Introduction  引言8:37

Set up a node express server, install mongoose and body-parser, and a user model for sign in and register flows. Connect to MongoDB and scaffold authentication for testing with postman.  
安装一个 Node.js 节点服务器，下载 mongoose 和 body-parser 库，并创建一个用户模型以处理登录和注册流程。连接 MongoDB 数据库，并使用 Postman 工具来构建测试用的身份验证系统。

### Adding user to the DB  
将用户添加到数据库中5:06

Add a post route at /api/user to capture email and password, save the user to MongoDB with Mongoose, return the created document, and note that passwords must be hashed.  
在/api/user 路径下添加一条 POST 路由，用于接收电子邮件和密码信息。使用 Mongoose 将用户保存到 MongoDB 中，返回创建的文档。需要注意的是，密码必须经过哈希处理。

### Hashing intro  哈希算法简介4:01

Learn how to hash passwords with bcrypt by generating a salt and hashing the password, then store the resulting hash in the database during user registration.  
了解如何使用 bcrypt 算法对密码进行哈希处理。该算法通过生成盐值并将密码进行哈希运算，然后在用户注册时将哈希值存储在数据库中。

### Hashing Password  哈希密码6:10

Hash passwords with a mongoose pre save middleware using bcrypt, salt rounds of ten, and isModified checks to hash only when the password changes, preparing secure login.  
使用 mongoose 的预保存中间件通过 bcrypt 算法对密码进行哈希处理。哈希过程中采用 10 轮盐值生成机制，并且只有在密码发生变化时才进行哈希操作，从而确保登录过程的安全性。

### Sign in user  登录用户4:19

Create a login route that authenticates a user by locating them by email, comparing the password to the hashed version, sending a response with try-catch error handling.  
创建一个登录路由，该路由通过用户邮箱来识别用户，将用户的密码与哈希后的密码进行比对，然后根据结果发送带有异常处理功能的响应。

### Comparing passwords  比较密码5:40

Create a custom comparePassword method on the user schema to compare the candidate password with the hashed password using bcrypt, and handle errors and isMatch via a callback.  
在用户模式中创建一个自定义的 comparePassword 方法，用于使用 bcrypt 算法将候选密码与哈希后的密码进行比对。当发生错误或比较结果不一致时，通过回调函数来处理相关情况。

### JWT Basics  JWT 基础知识6:34

Learn how JSON web tokens secure apps by issuing a signed token on login and validating it on restricted requests using a secret key and the verify method.  
了解如何通过发布经过签名的令牌在登录过程中保护应用程序，并在受限制的请求中使用密钥和验证方法来验证该令牌。

### Sign in user  登录用户8:05

Sign in generates and verifies a json web token from the user, saves it to the user document via mongoose, and returns an auth cookie for session management.  
登录过程会生成并验证一个 JSON Web 令牌，该令牌通过 mongoose 被保存到用户的文档中，同时还会返回一个用于会话管理的认证 Cookie。

### Token Verify  令牌验证10:08

Discover how to protect routes by retrieving the auth cookie, verifying the JWT, decoding the user id, and returning user data or a 401 for invalid tokens using middleware.  
了解如何通过以下方式保护路由：检索认证 cookie、验证 JWT 令牌、解码用户 ID，并根据令牌的有效性返回用户数据，或者返回 401 错误状态码。使用中间件来实现这些功能。

### Auth Middleware  认证中间件7:17

Create a reusable auth middleware that verifies JWTs from cookies, enabling route protection without database access, and demonstrates handling unauthorized responses and passing control with next.  
创建一个可复用的身份验证中间件，该中间件能够从 cookie 中验证 JWT 令牌，从而实现无需访问数据库即可进行路由保护。同时，该中间件还能处理未经授权的请求情况，并通过“next”机制来传递控制权。


##   节点框架 + Express：练习使用 Flickbase（服务器端）  
37 次讲座 • 5 小时 2 分钟

### Starting the project  开始这个项目9:01

Build the flick base full stack project by creating a server with express, mongoose, body-parser, bcrypt, and json web tokens, then develop the React front end.  
通过使用 Express、 mongoose、body-parser、bcrypt 和 json Web Tokens 等技术来构建服务器基础架构，然后开发 React 前端部分。完成整个全栈项目的开发。

### Connecting to MongoDB  正在连接至 MongoDB 数据库4:08

Connect to MongoDB using mongoose and dotenv, configure environment variables, build the Mongo URI with host, user, and password, and begin authenticating users with models.  
使用 mongoose 和 dotenv 连接到 MongoDB 数据库，配置环境变量。然后，通过主机、用户名和密码来构建 MongoDB 的连接字符串，最后开始使用模型进行用户身份验证。

### Creating the models  创建模型9:28

Create and validate user models with mongoose, including email validation, password rules, role management for users and admins, and set up category and article models.  
使用 mongoose 创建并验证用户模型，包括电子邮件验证、密码规则、用户和管理员角色管理，以及类别和文章模型的设置。

### Creating the routes  创建路线8:06

Create a dedicated routes directory with an index file to aggregate user, auth, and articles routes, export the router, and connect it to server.js using app.use('/api', routes).  
创建一个专门的路由目录，并配备一个索引文件来整理用户路由、认证路由以及文章路由等相关内容。然后导出该路由模块，并通过 app.use('/api', routes)将其连接到 server.js 文件中。

### Controllers  控制器4:48

Create and wire auth, user, and articles controllers, map post routes to their callbacks, and export them for use across routes, while introducing services to handle shared logic.  
创建并配置认证、用户和文章相关的控制器，将提交路由映射至它们的回调函数中，然后将这些组件导出以便在不同路由中复用。同时，引入服务来处理共享逻辑。

### Services  服务8:17

Create a modular services layer (auth, user, articles, email) with a central index for easy controller imports, and install http-status, express-xss-sanitizer, and express-mongo-sanitize to safeguard the api.  
创建一个模块化服务层（包括认证、用户、文章、电子邮件等功能），并配备一个中央索引以方便控制器数据的导入。同时，安装 http-status、express-xss-sanitizer 和 express-mongo-sanitizer 等工具来保护 API 的安全。

### Register user part one  注册用户 第一部分7:41

Register users through the auth route by validating email and password, creating the user via the auth service, hashing the password with bcrypt, and returning a token.  
通过认证路由来验证电子邮件和密码，从而注册用户。然后通过认证服务创建用户，使用 bcrypt 算法对密码进行哈希处理，最后返回一个令牌。

### Register user part two  注册用户 第二部分6:12

generate a jwt for a new user via the auth service, sign it with the db secret, and set it as a cookie during registration.  
通过认证服务为新用户生成一个 JWT 令牌，使用数据库中的密钥对其进行签名，然后在注册过程中将其设置为 Cookie 存储。

### Testing the Register route  
测试 Register 路由3:07

Test the register route by downgrading express xss sanitizer to 1.2.0, post credentials to api/auth/register, and verify 201 with token in cookies and database entry; plan sign-in route.  
请通过降级 Express XSS 净化工具到 1.2.0 版本来测试注册路由的功能。将认证信息发送到 api/auth/register 接口，并验证在 cookie 和数据库中的结果是否显示 201 状态码，同时验证令牌是否在 cookie 和数据库中存在。最后，完善登录流程的相关代码。

### Sign in users  为用户登录9:43

Implement sign in with a post route, controller and service, validating email and password, verifying passwords with bcrypt, generating a token, and returning it in the response.  
实现使用`post`路由、控制器和服务进行登录流程。验证用户的电子邮件和密码，使用`bcrypt`算法对密码进行加密验证，生成令牌，并将该令牌返回给客户端。

### Testing sign in route  测试登录流程4:15

Test the sign in route using Postman, save requests in a collection, and verify successful authentication with a token, while handling bad credentials and registering new users.  
使用 PostMan 来测试登录流程，将请求保存到一个集合中，并通过令牌验证成功的身份验证。同时，能够处理无效的凭证，并注册新的用户。

### API Error part One  API 错误，第一部分12:28

Learn to build a custom API error middleware in Node/Express, create an API error class, and return a consistent error object with status, code, and message to React.  
学习如何在 Node/Express 中构建自定义的 API 错误中间件，创建一个 API 错误类，并返回一个符合标准的错误对象，包含状态、代码和消息，以便与 React 进行交互。

### API Error part Two  API 错误，第二部分7:22

Convert mongoose errors into a standardized API error via middleware, adjusting status codes and messages for consistent responses. Guard routes by validating tokens and integrating passport and roles for authorization.  
通过中间件将 mongoose 的错误转换为标准化的 API 错误，调整状态码和错误信息，以确保响应的一致性。通过验证令牌来保护路由，并集成 passport 和角色功能进行授权管理。

### Auth Middleware part one  认证中间件 第一部分12:21

Use passport to authenticate the user token via the is auth route and verify the jwt token with a middleware before the controller returns user data.  
通过`is auth`路由来验证用户令牌，并在控制器返回用户数据之前，使用中间件来验证`jwt`令牌。

### Auth Middleware part Two  认证中间件第二部分11:43

Build an auth middleware with passport using JWT, verify the user, attach req.user, and return API errors for unauthorized access.  
使用 JWT 技术，通过 Passport 构建认证中间件。验证用户身份，将用户信息存储在 req.user 中，并对于未经授权的访问行为，返回相应的 API 错误。

### Creating Roles  创建角色6:58

Learn to implement role-based access with an access control library in a backend app, defining admin and user roles, granting permissions, and enforcing them via an auth verify flow.  
学习如何在后端应用程序中使用访问控制库来实现基于角色的访问权限管理。包括定义管理员角色和用户角色、授予权限，并通过身份验证流程来确保这些权限得到执行。

### Checking Roles  检查角色9:28

Modify the verify function to use defined roles and rights, check permissions with roles.can(action, resource), and enforce access control, returning forbidden when unauthorized.  
修改验证函数，使其使用定义的角色和权限进行权限检查，通过`roles.can.action`和`resource`来判断权限是否足够。同时实施访问控制机制，如果没有授权用户尝试执行某个操作，则返回“禁止”的反馈。

### : Get User Profile  : 获取用户配置文件9:49

Create a profile route under users, fetch profile by token-based user id, protect with auth middleware, and scaffold future updates and role-based access.  
在用户目录下创建个人资料路由，通过基于令牌的用户 ID 获取个人资料信息。使用认证中间件对访问进行保护，并预留出未来进行更新的空间以及基于角色的访问功能。

### Locals Permission  当地许可5:48

Attach a locals permission to responses, apply a filter to expose only allowed fields (no password or id), and centralize user object shaping with a service.  
在回复中附加当地权限的声明，使用过滤器仅显示允许显示的字段（不包括密码或 ID 信息），并通过服务来集中管理用户对象的形态。

### Update Profile  更新个人资料10:18

Demonstrates implementing a user own profile update feature: patch the own profile to update first name, last name, and age using roles, routes, controllers, and a service with findOneAndUpdate.  
展示了如何实现用户自定义个人资料更新功能：通过角色、路由、控制器以及带有 findOneAndUpdate 方法的服务，来更新用户的名字、姓氏和年龄等信息。

### Update user email  更新用户电子邮件地址10:03

Implement update email via a patch route, update the user's email and set verified to false, generate a new token, and send a verification email.  
通过补丁路由实现电子邮件的更新，修改用户的电子邮件地址并将其验证状态设置为“false”，生成一个新的令牌，然后发送一封验证邮件。

### Email intro  电子邮件介绍5:30

Set up email sending from a server using node mailer and Mailgun templates, with Gmail app passwords and environment variables for localhost configuration.  
通过 Node.js 的邮件发送功能，利用 Mailgun 的模板来设置邮件发送流程。同时，需要使用 Gmail 的应用密码以及本地主机配置相关的环境变量来进行设置。

### Email service  电子邮件服务12:49

Implement an email service with node mailer, mailgen, and mailgun to send tokenized verification emails, configure securely with environment variables, and integrate a transporter for user registration flows.  
实现带有 node mailer、mailgen 和 mailgun 的电子邮件服务，发送令牌化验证邮件，安全配置环境变量，并集成传输器用于用户注册流程。

### Testing the email service  
测试电子邮件服务6:24

### Verify email  验证电子邮件地址10:06

Build a React-based email verification flow with a verify route, JWT token validation, and user update from unverified to verified, returning a 201 response.  
构建一个基于 React 的电子邮件验证流程。包含验证路由、JWT 令牌验证功能，以及当用户从未验证状态变为已验证状态时进行更新，并返回 201 响应。

### Add categories  添加分类11:30

Create a mongoose category schema with a unique, trimmed name and a date field defaulting to now; then add admin-only endpoints and a controller to add and fetch categories.  
创建一个名为“mongoose”的类别架构，要求名称具有唯一性且经过修剪，同时设置一个默认值为当前日期的字段；然后添加仅适用于管理员的端点，以及用于添加和获取类别的控制器。

### Get categories  获取分类信息4:00

Build an admin-only get all categories route with a controller and service to fetch all categories and return them in JSON, enforcing authorization.  
构建一个仅适用于管理员的“获取所有分类”路由。该路由需要使用控制器和服务来获取所有分类，并将结果以 JSON 格式返回，同时需要实施授权机制。

### Creating articles Part One  
创作文章 第一部分9:37

Define a mongoose article model with fields and validations, set up article routes and controllers, and outline admin versus user permissions for creating articles.  
定义包含字段和验证规则的文章模型；配置文章相关的路由和控制器；并阐述创建文章时管理员权限与用户权限的区别。

### Creating articles Part Two  
创作文章 第二部分6:07

Learn how to implement adding articles in a full stack setup: create article service, post to MongoDB with validation, and secure with token-based admin access, using Express and Next.js concepts.  
了解如何在全栈环境中实现文章添加功能：创建文章服务，通过验证后将文章存储到 MongoDB 数据库，并使用基于令牌的管理权限进行安全保护。这些操作可以借助 Express 和 Next.js 框架来实现。

### Express validation  快速确认10:11

Learn to add an express validator middleware to post articles, validating title and director with trim, not empty, and length checks, and return structured errors.  
学会在发布文章时添加快速验证中间件，对标题和目录进行修剪、非空性检查以及长度限制验证，并返回结构化的错误信息。

### Get Articles by ID  通过 ID 获取文章11:17

Get articles by id endpoint on the articles route; enforce admin-only access, fetch the article by id via the service, populate the category, and return the article as JSON.  
在 articles 路由上获取指定 ID 的文章；实施仅管理员可访问的权限，通过服务获取该文章的数据，填充相关分类信息，然后以 JSON 格式返回该文章的内容。

### Patch Article by ID  按 ID 筛选文章6:17

Patch Article by ID teaches implementing auth-guarded article updates in a controller and service, updating via patch requests with body and id parameters, and handling admin-only updates before deleting articles.  
通过 ID 进行补丁文章操作，可以指导如何在控制器和服务中实现受保护的文章更新。更新时需要使用带有 body 和 id 参数的补丁请求，并且在删除文章之前，应先处理仅适用于管理员的操作。

### Delete Articles by ID  根据 ID 删除文章5:48

Learn how to delete articles by id in the complete react 19 course, covering controller and service logic and admin versus common-user routing.  
在完整的 React 19 课程中，学习如何根据文章 ID 来删除文章，包括控制器和服务逻辑方面的知识，以及管理员与普通用户路由方面的内容。

### Get Article by ID for users  
为用户获取文章 ID6:27

Learn to create a user-specific article retrieval endpoint using /users/article/{id}, fetch article by id, populate category, enforce no drafts, and handle not found or not allowed errors.  
学习如何创建一个针对特定用户的文章检索端点。该端点能够按文章 ID 提取文章内容，填充分类信息，确保只有授权用户才能访问该端点，同时能够处理文章不存在或访问受限的错误情况。

### Getting all articles  获取所有文章6:17

Learn to fetch all articles from the server using query strings for sort by, order, and limit, showing public articles and implementing a load more flow.  
学习如何使用查询字符串来筛选文章，包括排序、排序顺序、分页限制等操作。同时，了解如何展示公开的文章，并实现“加载更多”的功能。

### Load more  加载更多4:21

Learn to implement load more by posting to the articles endpoint, sending sort by and skip in the body, and returning the next public articles via the controller.  
学会如何在向文章端点发送请求时实现“加载更多”的功能。在请求体中包含排序和跳过相关的参数，然后通过控制器返回下一个公开的文章列表。

### Articles pagination  文章页码14:23

Implement admin article pagination using Mongoose aggregate paginate v2, wiring routes, auth, controllers, and frontend calls for paginated article data.  
采用 Mongoose 的 aggregate paginate v2 功能来实现管理文章的分页功能。需要处理路由、认证、控制器以及前端接口调用等问题，以确保分页后的文章数据能够顺利呈现。


##   节点 + Express：练习使用 Flickbase（前端部分）  
39 次讲座 • 5 小时 37 分钟

### The front end  前端部分8:59

Set up the front end by configuring the proxy to the server on port 3001, installing essential libraries (Formik, Axios, React Redux Toolkit, React Router), and preparing fonts and styles.  
通过配置代理服务器到 3001 端口，安装必要的库（如 Formik、Axios、React Redux Toolkit、React Router），以及准备字体和样式表，来设置前端部分。

### Creating the header  创建标题5:29

Construct the header and side navigation for a react app by creating home and navigation components, wiring routes with browser router and routes, and preparing for future side navigation features.  
通过创建“首页”和“导航”组件，结合使用浏览器路由和路由规则来构建 React 应用程序的头部和侧边导航栏。同时，为未来的侧边导航功能做好准备。

### Side Navigation  侧边导航栏10:52

Create a side navigation drawer with material UI for React, importing components, wiring state for open/close, and linking items with icons like home, mail, and dashboard.  
创建一个带有 Material UI 的 React 侧导航抽屉组件。需要导入相关组件，配置状态以控制页面的打开与关闭功能，并添加诸如首页、邮件和仪表盘等带有图标的菜单项。

### Main Layout  主布局4:03

Create a main layout that wraps the entire app using a high order component, with an admin dashboard layout and integrate react bootstrap container and toast container for notifications.  
创建一个主布局，使用高阶组件来封装整个应用程序；同时配备一个管理面板布局。还需要整合 React Bootstrap 的容器功能，以及用于通知的 Toast 容器。

### Setting up Redux  配置 Redux7:56

Create a Redux store by organizing reducers and actions, build article, user, site, and notification slices with Redux Toolkit, then configure the store and wrap the app with the provider.  
通过组织 reducer 和 actions 来创建 Redux 存储库，使用 Redux Toolkit 构建 article、user、site 和 notification 这几个模块。然后配置存储库，并使用 provider 方式将应用程序封装起来。

### Login Register form part one  
登录 注册 表单 第一部分10:55

Build an auth component for login and register in React, wire routes and navigation, manage state with redux, and validate with formic and yup using Material UI inputs.  
在 React 中创建一个用于登录和注册的身份验证组件。处理路由和导航逻辑，使用 Redux 来管理状态，并利用 Material UI 的输入框功能进行表单验证，包括使用 Formic 和 Yup 库进行验证。

### Login Register form part two  
登录 注册 表单 第二部分7:34

This lecture guides building the login register form two: add submit and toggle buttons, implement form validation with an error helper, and integrate a Mui loader and utils.  
本讲座介绍了如何构建登录注册表单的第二步操作：添加提交按钮和切换按钮，实现表单验证功能，并集成 Mui 加载器和相关工具类。

### Register user  注册用户8:36

Learn to register users using a Redux async thunk with axios to post email and password to /api/auth/register. Manage loading, data, and auth state through reducers and extra reducers.  
学习如何使用 Redux 异步 thunks 和 axios 将电子邮件和密码发布到 /api/auth/register 来注册用户。通过 reducers 和 extra reducers 管理加载状态、数据和认证状态。

### Sign in users  为用户登录4:20

Implement a complete sign-in flow with an async thunk, reducer wiring, and API auth, then persist the token and show toasts while guarding routes.  
实现一个完整的登录流程，包括异步承诺机制、reducer 绑定，以及 API 认证功能。之后将令牌持久化存储，并在保护路由的情况下显示提示信息。

### Notifications  通知/提示13:16

Create a redux-based notifications system with a global reducer for error and success messages. Use useEffect to listen for changes, display toasts, and clear notifications after each.  
创建一个基于 Redux 的通知系统，拥有一个用于处理错误和成功信息的全局 reducer。使用 useEffect 函数来监听状态的变化，在每次变化时显示提示信息，并清除已通知的内容。

### Displaying toasts  展示祝酒词3:59

Create a reusable toast utility in React, using a type-based switch to show success or error messages with bottom-right toasts for a global notification system.  
在 React 中创建一个可重用的 toast 工具，使用基于类型的 switch 来显示成功或错误消息，并使用右下角的 toast 作为全局通知系统。

### Auto sign in part one  
自动登录（第一部分）7:47

Learn how to implement auto sign-in by reading the x axis token cookie with react cookies, validating it with server via a thunk, and updating Redux with the authenticated user.  
了解如何通过以下步骤来实现自动登录功能：使用 React 的 Cookie 来读取 x 轴令牌 Cookie；通过 Thunk 工具在服务器端对令牌进行验证；然后将这些信息更新到 Redux 状态中，以便将已登录的用户信息同步到系统状态。

### Auto sign in part two  
自动登录，第二部分6:58

Learn to implement auto sign-in in a React app using Redux state, token validation, and a loading indicator; guard routes and sidebar options based on auth status.  
学习如何在使用 Redux 状态管理、令牌验证以及加载指示器功能的 React 应用中实现自动登录功能；根据用户认证状态来保护路由和侧边栏选项。

### Prevent sign in  防止用户登录8:51

Implement a prevent sign in flow by building a higher‑order component that redirects authenticated users from the sign in page to the dashboard using react router, location, and redux state.  
通过构建一个高阶组件来实现登录流程的预处理功能。当用户已通过身份验证后，该组件可以将用户从登录页面重定向到仪表板页面，使用 React Router、地理位置功能以及 Redux 状态管理技术来实现这一功能。

### Sign out  注销/退出5:53

Explains how to implement sign-out in a React app by dispatching a logout thunk, removing the token cookie, resetting state, and navigating to home.  
介绍了如何在 React 应用中实现注销操作：通过触发注销事件，删除令牌 cookie，重置状态，然后返回主页。

### Route Guards  路标守卫8:19

Guard the dashboard routes with a Redux-based auth guard, using React Router's outlet and nested routes to protect access and redirect unauthenticated users to the login screen.  
使用基于 Redux 的身份验证保护机制来保护仪表板路由。利用 React Router 的 outlet 功能和嵌套路由，可以限制访问权限，并将未认证的用户重定向到登录页面。

### Admin layout classname  管理员布局 班级名称10:05

Learn to switch between normal and admin layout in a React app by detecting the dashboard route with use location, updating Redux site layout, and applying layout-specific styles.  
学习如何在 React 应用中切换普通布局与管理界面的布局。通过利用 useLocation 功能检测仪表板路由，更新 Redux 网站布局，并应用与特定布局相关的样式。

### Admin layout component  管理界面布局组件9:02

Build an admin layout component using React, Router, and MUI, leveraging useSelector to display admin-only links for the dashboard, articles, and categories, plus a reusable admin title component.  
使用 React、Router 和 MUI 构建一个管理员布局组件。利用 useSelector 钩子来显示仅适用于管理员的链接，比如仪表盘、文章和分类页面的链接。此外，还需要一个可复用的管理员标题组件。

### Add article route  添加文章路由3:48

Set up the dashboard article workflow by adding routes for articles and the add article page, and organize an articles directory with index, add, and edit.  
通过添加文章管理页面以及添加文章页面的链接，来设置仪表板的文章工作流程。同时，还需要设计一个包含索引、添加和编辑功能的文章目录结构。

### Add article common fields part one  
添加文章通用字段，第一部分6:10

Learn to build a complex article form with react, redux, and formik, configuring initial values and yup validation, handling actor arrays and category thunks.  
学习使用 react、redux 和 formik 构建 complex article form，配置初始值和 yup 验证，处理 actor 数组和 category thunks。

### Add article common fields part two  
添加文章通用字段，第二部分10:51

Build a reusable article form with common fields: title, excerpt, score, actors, director, and status, using Formik and Material UI in a React 19 course.  
在使用 Formik 和 Material UI 的 React 19 课程中，创建一个可重复使用的文章表单，包含常见的字段：标题、摘要、评分、演员信息、导演信息以及状态信息。

### Add article categories  添加文章类别9:40

Add article categories in the React course by defining initial article state, fetching categories with an async thunk, and populating a category select in the article form.  
在 React 课程中添加文章分类功能，包括定义文章的初始状态、通过异步 thunk 函数获取分类信息，以及在文章表单中添加分类选择框来显示这些分类。

### Add article actors array  添加文章演员数组12:36

learn to manage an actors array in a React form using Formik field array, with input, add button, and chips to display and delete actors.  
学习如何使用 Formik 字段数组在 React 表单中管理演员数组，包括添加输入、按钮以及用于显示和删除演员的芯片元素。

### WYSIWYG editor  可视化编辑器13:44

Learn how to build a WYSIWYG editor in a React app using Tip Tap, install and configure the starter kit, and connect the editor content to Formik via on update.  
了解如何使用 Tip Tap 在 React 应用中构建 WYSIWYG 编辑器。安装并配置入门套件，然后通过更新机制将编辑器内容与 Formik 连接起来。

### Posting articles  发布文章6:49

Build an async thunk in the articles actions to post new articles with axios to /api/articles, dispatch open and close, handle results with unwrap, and navigate to dashboard/articles on success.  
在文章操作中创建一个异步任务，使用 axios 将新文章发送到/api/articles 接口。处理打开和关闭操作，使用 unwrap 函数解析结果，成功后导航到 dashboard/articles 页面。

### Editing articles part one  
编辑文章 第一部分13:00

Learn to implement edit article functionality in a React app by creating an edit route, fetching an article by ID with async thunk, and populating form data and editor content.  
学习如何在 React 应用中实现编辑文章的功能。包括创建编辑文章的路由路径，通过异步 thunk 函数根据文章 ID 获取文章内容，以及填充表单数据和编辑器中的内容。

### Editing articles part two  
编辑文章第二部分5:42

Edit articles by dispatching an update article thunk that calls the API to patch article data using the article id, values, and auth token, with a status update message.  
通过发送一个更新文章请求来编辑文章。该请求会调用 API，根据文章 ID、数据以及认证令牌来更新文章数据，并附带一个状态更新消息。

### Get paginated articles  获取分页显示的文章8:06

Build a paginated articles view by implementing a getPaginatedArticles thunk with page and limit, and load data into admin articles via Redux.  
通过实现一个名为`getPaginatedArticles`的回调函数，该函数接受页码和页数的参数，然后利用 Redux 将数据加载到管理文章列表中，从而构建出具有分页功能的文章视图。

### Paginate table  表格分页显示13:05

Build a paginated articles table in a react bootstrap admin panel, with add and search controls, and action buttons for edit, remove, and status.  
在 React Bootstrap 的管理面板中创建一个分页式文章表格界面，包含添加和搜索功能，以及用于编辑、删除和切换状态的按钮。

### Paginate button  分页按钮7:50

Build pagination for the articles list with React Bootstrap, showing prev, current, and next pages. Fetch paginated articles on navigation and format dates with Moment.js, enabling status updates and removal.  
使用 React Bootstrap 来实现文章列表的分页功能，能够显示前一页、当前页和下一页的内容。在导航时加载分页后的文章数据，并使用 Moment.js 库来处理日期格式。同时支持状态更新的处理以及文章删除功能的实现。

### Change article status  更改文章状态12:12

Learn to toggle article status with a Redux thunk, patch the server, and update the Redux store with getState, preparing a remove overlay.  
学会使用 Redux 的 thunk 函数来切换文章的状态，对服务器进行更新，并通过 getState 函数更新 Redux 存储，为移除覆盖层做好准备。

### Remove articles by ID part one  
根据 ID 信息删除物品，第一部分8:01

Build a React delete confirmation modal using Bootstrap; manage modal state with useState, pass the article id from the Paginate component, and prepare a delete action for server-side removal.  
使用 Bootstrap 构建一个 React 删除确认模态框；通过 useState 来管理模态框的状态；将文章 ID 从 Paginate 组件中传递过来；同时准备好在服务器端执行删除操作的相关逻辑。

### Remove articles by ID part two  
根据 ID 信息删除物品——第二部分6:04

Implement an async thunk to delete an article by id, sending an Axios delete to the API, then refresh the current paginated articles and close the modal.  
实现一个异步函数，通过向 API 发送一个 DELETE 请求来删除指定 ID 的文章。之后，重新加载当前分页的文章列表，并关闭模态窗口。

### Get categories  获取分类信息8:08

Create an admin route for categories, fetch and display them in a table, and set up a form to add new categories using Redux actions.  
创建一条用于管理分类的路由，获取这些分类并以表格形式显示它们，同时设置一个表单来通过 Redux 操作添加新的分类。

### Post categories  后类别10:57

Create a category form with Formik and Yup validation, dispatch a thunk to post the new category via the API, and update the categories list using the Redux state.  
使用 Formik 和 Yup 进行验证，创建一个分类表单；然后通过 API 发送一个 thunk 来发布新的分类；最后利用 Redux 状态来更新分类列表。

### Home Articles part one  首页文章 第一部分11:29

Fetch and display home articles by wiring a redux thunk to call the server route for articles, loading the first eight and then loading more by updating skip.  
通过调用服务器端的“articles”路由函数，可以获取并展示首页的文章内容。首先加载前八篇文章，然后通过更新跳过功能来继续加载更多文章。

### Home Articles part two  首页文章 第二部分9:34

Learn to display home articles in a responsive grid using material ui, map articles to article components with images and titles, and link to individual articles via a router.  
学习如何使用 Material UI 将首页文章以响应式网格形式展示；将带有图片和标题的文章分配给各个组件；并通过路由系统链接到具体的文章页面。

### Article by ID screen  关于 ID 屏幕的文章9:53

Create an article by id screen in a React Redux app, route to /articles/:id, fetch article by id via a thunk, and display title and HTML content with a loader.  
在 React Redux 应用中，通过 id 筛选机制创建文章。访问路径 /articles/:id 时，通过 thunk 函数获取对应文章的数据。在页面上显示文章的标题和 HTML 内容时，会有一个加载器效果。

### Article by ID scorecard  根据 ID 评分表撰写的文章5:58

Build a React scorecard UI with Material UI components, including list items, avatars, dividers, actor chips, and a director with icons and a star rating.  
使用 Material UI 组件构建一个 React 评分界面，包括列表项、头像、分隔条、角色徽章，以及带有图标和星级评分的主管界面。

# 要求/条件

- Knowing a little bit of HTML, CSS and Javascript, everything else you will learn it as we go.  
    如果你稍微了解一些 HTML、CSS 和 JavaScript 的知识，那么随着课程的进行，你将会学到其他的一切内容。
    

# 描述

Do you want to learn the **whole process of building your App ?.** This is the course for you.  
你想了解开发应用程序的整个过程吗？这就是适合你的课程。

We will start from the very **beginning**, from "_I don't even know how use it..and why would I_“ to actually **understanding how React works and make it communicate with other technologies like Next JS & Node JS.**  
我们将从最基础的地方开始，从“我甚至不知道该如何使用它……为什么要使用它”这种疑问，逐步理解 React 的工作原理，并让它能够与其他技术如 Next.js 和 Node.js 进行协作。  

You will learn all the **logic** and **practice** behind **React** in different modules, and as we advance through the course we will be increasing the difficulty.  
在不同的模块中，你将学习到关于 React 的所有逻辑和实践知识。随着课程的进展，难度也会逐渐增加。

Since I believe that the best way to fully **learn is by coding**, after each important section we will put everything in **practice** with some **course projects**, and after you have a **strong base of React** we will journey into **Next JS**,  **Servers and Databases**, **Redux** and deploying  apps to production.  
因为我认为，学习的最佳方式是通过编程实践。在每节课程结束后，我们会通过一些实际项目来将所学知识应用到实践中。在掌握了扎实的 React 知识之后，我们将继续学习 Next.js、服务器与数据库、Redux 等相关知识，以及如何将应用程序部署到生产环境中。

  

To sum everything up, you will learn in this course:  
总结来说，在本课程中，你将学到以下内容：

- The very **basics of React.** How to install and the logic behind it.  
    React 的最基础内容。如何安装以及其背后的工作原理。
    
- Once you know the basics of **React** we will journey into the confusing stuff.  
    一旦你掌握了 React 的基础知识，我们就可以进入一些较为复杂的领域了。
    
- After you have a **strong base** of React we will create a **full practice project.**  
    在建立起扎实的 React 基础之后，我们将会创建一个完整的实践项目。
    
- Everybody is using **Redux**, so we will learn how to use redux with our React Apps.  
    现在大家都在使用 Redux，所以我们将学习如何在自己的 React 应用中使用 Redux。
    
- We will Deep dive into **Next JS** until we create and deploy a practice app.  
    我们将深入学习 Next.js 技术，直到能够创建并部署一个实用的应用程序。
    
- Since our app will need a database, you will learn how to use **MongoDB and mongoose.**  
    由于我们的应用程序需要数据库支持，因此你将学习如何使用 MongoDB 和 mongoose。
    
- Apply **security, authentication, restricting routes, hashing passwords and JWT’s.**  
    实施安全性措施，如身份验证、限制访问路径、对密码进行哈希处理，以及使用 JSON Web Tokens。
    
      
    

**What about the instructor experience ?  
那么，对于教练来说呢？**

Well I've been around for **15 years now**, built lots of applications for small projects and big companies like **Citi, Fox, Disney and a lot more.**  
嗯，我已经从事这个领域 15 年了。为许多小型项目以及像花旗银行、福克斯公司、迪士尼等大型公司开发了大量应用程序。

Today I work as developer for a major company developing applications in **React or Angular mostly**, but I like to teach in my spare time.  
今天，我在一家主要使用 React 或 Angular 开发应用程序的大型公司担任开发人员。不过，我在业余时间喜欢教课。

**How about the target audience?  
那么，目标受众会如何呢？**

- This course is for almost everyone, a little bit of JavaScript is required. We will use ES6 but you can learn as we go.  
    这门课程适合几乎所有人学习，只需要一些 JavaScript 的基础知识即可。我们会使用 ES6 规范，但你们可以在学习过程中逐步了解相关的内容。
    
- If you want to take your coding to the next level, this is the course for you.   
    如果你希望提升自己的编程能力，那么这门课程非常适合你。
    

  

# 这门课程适合谁

- From beginners in javascript to intermediate.  
    从 JavaScript 的初学者到中级使用者。

