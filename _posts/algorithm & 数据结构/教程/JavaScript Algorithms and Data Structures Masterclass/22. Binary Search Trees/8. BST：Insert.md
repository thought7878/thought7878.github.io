视频讲述了二叉搜索树（BST）*插入方法的原理、具体操作步骤、实现思路*及相关示例，具体如下：

## BST 插入方法核心目标

[00:00]
**通过调用 insert 方法传入数值，树会创建新节点，并根据数值大小将新节点添加到正确位置**。

![[_posts/algorithm & 数据结构/教程/JavaScript Algorithms and Data Structures Masterclass/22. Binary Search Trees/media/3dc1c90a029bcc2a74a3c265cdfc6f6c_MD5.jpeg]]

## BST 插入示例 1（插入 13）

[00:15]
从根节点开始比较，13 大于根节点 10，转向右侧；再与 15 比较，13 小于 15，*检查 15 左侧是否有节点，因无节点*，13 最终插入到 15 左侧。

![[_posts/algorithm & 数据结构/教程/JavaScript Algorithms and Data Structures Masterclass/22. Binary Search Trees/media/cb6a9787f96f85308aac89e28fed8d74_MD5.jpeg]]

## BST 插入示例 2（插入 68）

[00:46]
先检查是否有根节点，存在根节点 41，68 大于 41，转向右侧；与 65 比较，68 大于 65，继续向右；与 91 比较，68 小于 91，转向左侧；再与 72 比较，68 小于 72，检查 72 左侧无节点，68 插入到 72 左侧。

![[_posts/algorithm & 数据结构/教程/JavaScript Algorithms and Data Structures Masterclass/22. Binary Search Trees/media/05365172d00b63b39b8a4444c6930015_MD5.jpeg]]

## BST 插入实现方式

[01:36]
可通过`迭代`或`递归`实现，推荐先尝试迭代方式，*两种方式均可大致实现插入功能*，无需严格遵循固定顺序和代码表述。


## BST 插入迭代实现关键步骤

[01:52]
- 首先创建新节点
- 从根节点开始检查
	- 若不存在节点，树为空（无根节点），新节点直接作为根节点
	- 若存在节点（根结点），*比较新节点与当前节点数值大小（循环起点）*
	- 若新节点值大
		- 检查当前节点右侧是否有节点，
			- 有，则currentNode移动到该节点，重复比较步骤
			- 无，则将新节点插入到当前节点右侧
	- 若新节点值小
		- 检查当前节点的左侧是否有节点
			- 有，则currentNode移动到该节点，重复比较步骤
			- 无，则将新节点插入到当前节点左侧
	- 若新节点值等于当前节点
		- 返回整棵树

此过程需用 while 循环（可考虑 while true）持续处理，直至找到插入位置。

![[_posts/algorithm & 数据结构/教程/JavaScript Algorithms and Data Structures Masterclass/22. Binary Search Trees/media/cd4459d2a713726b67592306633fab15_MD5.jpeg]]

## BST 插入方法返回值建议

[03:35]
建议在插入方法结束时*返回整个树，而非仅返回空或单个节点，便于后续对树进行操作*。

## 后续内容预告

[03:47]
下一视频将提供 BST 插入方法的具体解决方案。
