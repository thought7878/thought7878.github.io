[30分钟弄懂浏览器历史与渲染基本流程 / 浏览器工作原理入门教程](https://www.bilibili.com/video/BV1tc41157Va/?share_source=copy_web&vd_source=9c1e19a73fa7bd23bb37aa8d7467d862)

视频讲述了浏览器发展历史、核心基础概念、网络请求流程及页面渲染原理，以Chrome为例全面讲解浏览器工作机制，具体如下：

## 浏览器起源与基础概念区分
[00:00]
1990年诞生首个浏览器WorldWideWeb；明确互联网是基于TCP/IP的网络，WWW是基于互联网的超链接资源集合，WWW由URI、HTML、HTTP三部分组成，同时区分URI与URL概念。

## 浏览器发展历史
[02:51]
1992年Mosaic浏览器推出，后衍生网景公司；微软推出IE并通过捆绑赢得第一次浏览器大战；网景成立Mozilla基金会开源Gecko引擎，苹果基于KHTML开发WebKit；2008年谷歌发布基于WebKit+V8引擎的Chrome，后改用Blink引擎，如今主流浏览器多基于Chromium开发。

## 进程与线程
[08:02]
`进程`是程序运行实例，`线程`是进程内执行单元；单进程浏览器存在不稳定、卡顿、内存泄露、安全风险问题，Chrome采用多进程架构解决这些问题。

## Chrome多进程组成
[09:56]
包含浏览器进程、渲染进程、插件进程、网络进程、GPU进程，分别负责页面管理、页面渲染、插件运行、资源下载、界面绘制。

## 核心网络协议讲解
[10:52]
IP协议用于设备寻址，DNS负责域名转IP，UDP无连接不可靠，TCP面向连接可靠（三次握手、四次挥手），HTTP定义浏览器与服务器数据传输格式。

## 输入URL后发生了什么？
[14:06]-[17:51]
浏览器判断输入内容→查询缓存→DNS解析→TCP连接→发送HTTP请求→服务器响应→处理重定向/下载/渲染逻辑→准备渲染进程。



### 浏览器判断输入内容
接下来我们开始进入正题，讲讲chrome的工作原理。这个问题其实也可以被理解成从输入URL到页面展示中间发生了什么。
首先就是输入URL或其他内容时，浏览器会根据你输入的内容进行一个判断。因为现在浏览器地址栏也集成了搜索功能。如果你输入的内容不符合URL的规则，它会根据浏览器的默认引擎来合成带有搜索关键字的URL。如果符合URL的规则，则浏览器会自动给你输入的URL加上协议。

那接下来就到了请求的阶段。浏览器在发送请求的时候，会让浏览器的主进程通过进程间通信把URL发送给网络进程。紧接着浏览器的标签页的图标开始转圈，但是当前页面还是老的页面。

![[_posts/browser/渲染/media/9c7665f1583e2847ba69fb31d252c30b_MD5.webp]]

![[_posts/browser/渲染/media/46470e76c05decced193f34e65bc2419_MD5.webp]]


当网络进程接收到服务器返回的HTT响应头时，发现响应类型为HTML类型时，就会让浏览器开启一个渲染进程，并给他发送一个提交文档的命令。同时网络进程传递文档信息，也就是HTGP的响应体给渲染进程传递完毕后，渲染进程会给浏览器进程发送一个接收完毕的命令，那么当前页面就会被白屏所替代。接下来渲染进程联合GPU进程开始渲染，渲染完毕后的页面返回给浏览器主进程进行展示。

上面是简化版的正常情况，其实步骤还是很复杂的，所以接下来会对这个流程进行一个详细的解读。

### 查询缓存

### DNS解析

### 建立TCP连接

### TLS握手

### 发送HTTP请求

### 服务器响应

### 处理重定向/渲染逻辑/下载

### 准备渲染进程


## 渲染流程详解
[17:51]
按**构建DOM树→计算样式→布局→分层→绘制→分块→光栅化→合成**的顺序，将HTML、CSS、JS转化为可视页面。

## 重排、重绘与合成
[24:32]
重排是修改元素布局触发全流程重算，开销最大；重绘是修改样式不改变布局，效率更高；合成是利用分层机制仅做栅格化，如transform，效率最高。