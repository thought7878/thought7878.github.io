


---

### “总结”课的文字记录

Evgenii wraps up the course by summarizing the topics discussed and sharing a few key takeaways. He also answers a question about best practices for updating the UI from a stream of data.
叶夫根尼通过总结所讨论的主题并分享一些关键要点来结束本课程。他还回答了一个关于从数据流更新用户界面的最佳实践的问题。

[00:00:00]  
>> Evgenii Ray: Okay, let's summarize. So we went through the core fundamentals section where you got the knowledge of how the browser renders the element. Then when we brush up the knowledge on our DOM API, on a DOM API usage, and also the intersection observer. So we built a virtualization from scratch utilizing this core fundamentals knowledge.>> 叶夫根尼·雷：好的，我们来总结一下。我们已经学习了核心基础知识部分，在这部分你了解了浏览器是如何渲染元素的。之后我们复习了关于DOM API的知识、DOM API的用法，还有交叉观察器。所以我们利用这些核心基础知识从零开始构建了一个虚拟化（方案）。

[00:00:23]  
So also we went through the application state design, so now you know how you could potentially optimize the way you store the data on the client to minimize the device memory usage and CPU usage. And we also overviewed all network protocols that are available to us as a web developers and how can we fetch the data efficiently.我们也讲解了应用状态设计，所以现在你应该知道如何潜在地优化在客户端存储数据的方式，以最大限度地减少设备内存使用和CPU使用。我们还概述了作为Web开发人员可用的所有网络协议，以及如何高效地获取数据。

[00:00:46]  
And we also looked at the way how can we optimize the JavaScript assets. So I hope that this will be useful to you and you will be able to apply this knowledge in your real life and daily job.我们还研究了如何优化JavaScript资源的方法。所以我希望这对你有所帮助，并且你能够将这些知识应用到现实生活和日常工作中。  
>> Speaker 2: What is the best practice for updating UI when streaming?>> 发言者2：在流式传输时更新用户界面的最佳实践是什么？

[00:01:06]  
>> Evgenii Ray: Updating the UI when?>> 叶夫根尼·雷：什么时候更新用户界面？  
>> Speaker 2: Streaming updates. So, for instance, probably, just thinking like ChatGPT responses.>> 发言者2：流式更新。比如说，可能就像ChatGPT的响应那样。  
>> Evgenii Ray: So first of all, you need to connect to your endpoint that allows us to stream the text data. And the server-sent events is very good way how you can stream the text data.>> 叶夫根尼·雷：首先，你需要连接到你的端点，这个端点能让我们流式传输文本数据。而服务器发送事件是流式传输文本数据的一种非常好的方式。

[00:01:31]  
But one thing about the data streaming is when you see that the ChatGPT streams the text, it's actually already received the chunk of data that it wants to render. So the way you can wait until you get the chunk of data that you want to render. And once this chunk is ready, you'll be able to kind of simulate the streaming of the answer.但关于数据流有一点需要说明，当你看到ChatGPT在流式输出文本时，它实际上已经接收到了想要呈现的数据块。所以，你可以等待，直到获取到想要呈现的数据块。一旦这个数据块准备就绪，你就能模拟出答案的流式输出效果。

[00:01:53]  
This is how I would implement this.这就是我会如何实现它的方法。  
>> Speaker 2: When the final slide says, minimize run time state, does that mean normalizing it or simplifying the data so there's less to consider over time?>> 发言者2：当最后一张幻灯片提到“最小化运行时状态”时，这是否意味着要对其进行标准化或简化数据，以便随着时间的推移需要考虑的内容更少？  
>> Evgenii Ray: It's actually both, so you minimize duplication, so you store less nested objects, and you also store things on the hard drive.>> 叶夫根尼·雷：实际上两者都是，所以你要减少重复，这样就能存储更少的嵌套对象，同时也能把内容存储在硬盘上。

[00:02:19]  
So when you offload the data to the hard drive, you reduce the RAM consumption by the device. Yeah, all right, thanks a lot for watching the course, I hope you liked it. And yeah, feel free to subscribe to my YouTube channel and also share the course with your friends.所以当你将数据卸载到硬盘时，会减少设备的内存占用。好的，非常感谢你观看这门课程，希望你喜欢。另外，欢迎订阅我的YouTube频道，也可以把这门课程分享给你的朋友们。

[00:02:41]  
>> [APPLAUSE] >> [掌声]  
>> Evgenii Ray: Thank you. >> 叶夫根尼·雷：谢谢大家。  
>> [APPLAUSE] >> [掌声]







