- 问：什么是AWS控制台，它的用途是什么？  
答：AWS控制台是一个用户界面，允许用户与各种AWS产品和服务进行交互。  
  
- 问：**应用程序的前端是如何托管的，它使用哪些服务？**  
答：应用程序的前端托管在`Amazon S3`上，这是一种*可扩展的存储服务，用于存储和服务静态资产*，如HTML、CSS和JavaScript文件。 它被配置为托管静态网站，并对所有文件启用了公共读取访问权限。  
  
- 问：从应用程序内部调用两个APIs的目的是什么？  
答：这两个API用于应用程序中的不同功能: 一个用于提交新配方，另一个用于加载现有配方。 它们都映射到相同的端点，但使用不同的方法，一个使用POST来添加食谱，另一个使用GET来检索食谱。 这些端点由AWS API网关管理，以连接前端和后端。  
  
- 问：AWS API网关如何管理应用程序的请求？  
答：AWS API网关处理传入的HTTP请求，并根据请求的端点将它们路由到适当的Lambda函数。 例如，它将GET请求映射到Get Recipes Lambda函数，并将请求发布到Save Recipes Lambda函数。  
  
- 问：Lambda函数在后端的作用是什么？  
答：后端的Lambda函数负责处理配方数据。 Get Recipes Lambda函数从DynamoDB中检索菜谱并将其发送回前端，而Save recipes Lambda函数处理新的菜谱数据并将其添加到DynamoDB中的菜谱表中。  
  
- 问：应用程序如何使用DynamoDB存储食谱数据？  
答：DynamoDB用于以NoSQL格式存储食谱数据。 它包含一个名为Recipes的表，其中每个项目代表一个具有recipe名称、成分和说明属性的recipe。 这允许应用程序即使用户不在网页上也能持久化配方数据。  
  
- 问：日志在应用程序中的重要性是什么？它们是如何管理的？  
答：应用程序中的日志对于故障排除和识别错误至关重要。 它们由AWS Cloudwatch自动捕获，并可以通过单击查看Cloudwatch日志的监视器选项卡进行访问。 日志被组织成日志组，在每个日志组中，都有单独的日志流，代表来自单个源的日志事件序列。  
  
- 问：应用架构如何简化基础架构管理？  
答：应用架构通过利用云服务简化了基础架构管理，这使得应用可扩展、高效且易于管理和监控。 这使开发人员能够专注于提供出色的用户体验，而不必担心基础设施问题。

---

00:00 探索用于可扩展Web应用程序的*AWS服务: S3、API网关、Lambda和DynamoDB*
**概述了托管在AWS上的基于云的应用程序：**
- 利用`S3`进行静态资产存储，
- 利用`API网关`进行请求路由，
- 利用`Lambda`进行服务器端处理，
- 利用`DynamoDB`进行数据持久性。 
该设置可**实现可扩展性、效率和易管理性，专注于用户体验并*最大程度地减少基础架构问题***。

---

# 原文

Let's explore the different components of our cloud based application within Amazon Web Services or AWS. This website is called the AWS Console. It's a user info interface that allows you to interact with various AWS products and services. 

让我们在亚马逊网络服务或AWS中**探索我们基于云的应用程序的不同组件**。这个网站被称为AWS控制台。这是一个用户信息界面，允许您与各种AWS产品和服务进行交互。

Our front end is hosted on Amazon S 3. S 3 is a scalable storage service that allows us to store and serve static assets. Here are HTML, CSS, and JavaScript files. To make our application accessible, we've configured S 3 to host static website inside of the properties. We've enabled static website hosting. This is the Url we've been using to access our application. We've also updated our bucket policy to allow public read access to all of our files. This is necessary for users to view your website at some of the JavaScript code. 

我们的前端托管在亚马逊S3上。`S3`是**一种可扩展的存储服务，使我们能够存储并提供静态资源**。以下是HTML、CSS和JavaScript文件。*为使我们的应用程序可访问*，我们*已在属性中将S3配置为托管静态网站*。我们已启用静态网站托管。这是我们一直用来访问应用程序的网址。我们还更新了存储桶策略，以允许对所有文件进行公开读取访问。这是用户在某些JavaScript代码环境下查看您的网站所必需的。

In here, we call 2 Apis, one for submitting new recipes and another for loading our recipes. They mapped to the same endpoint, but were using a post to add the recipe in a Git to retrieve the recipe. These endpoints are configured with AWS API Gateway to connect our front end and back end, let's take a look. API Gateway handles incoming h.t.t.p. requests and routes them to the appropriate Lambda functions. On the recipes route, we map get requests to the Get Recipes Lambda function. We can see that in the integration requests, if we click the post method, we can see it's mapped to the save recipe function. This means there are two Lambda functions that power our backend. Let's take a look, both of these Lambdas are using a node runtime, which means they're using JavaScript on the server side. 

在这里，我们*使用两个Apis*：一个用于提交新配方，另一个用于加载我们的配方。它们映射到同一个端点，但在Git中却使用POST请求来添加食谱，并通过GET请求获取该食谱。这些端点已通过AWS API Gateway进行配置，*用于连接我们的前端和后端*，我们来看一下。`API网关`*处理传入的HTTP请求，接收请求并将其路由至相应的Lambda函数*。在食谱路由中，我们将GET请求映射到Get Recipes Lambda函数。我们可以看到，在集成请求中，如果单击POST方法，就会发现它已映射到save recipe函数。这意味着有两个Lambda函数为我们的后端提供支持。我们来看一下，这两个Lambda都使用了Node运行时，这意味着它们在服务器端运行的是JavaScript。
![[_posts/architect/_教程/编程基础：应用架构/5 云端应用/media/6cac46cd212a514866aff428d6695183_MD5.webp]]

![[_posts/architect/_教程/编程基础：应用架构/5 云端应用/media/45997811203e61f9f61caab12a90e359_MD5.webp]]

![[_posts/architect/_教程/编程基础：应用架构/5 云端应用/media/a168b8a2397b3cc3cecf4c8169d8e639_MD5.webp]]

Let's take a look at the code for the Git recipes function. This function is responsible for retrieving recipes from our database. It queries DynamoDB, which stores our recipe data in a NoSQL format. When it finds the data, it finds it to API Gateway as JSON, which then sends it back to the front end. 

让我们来看看**Get recipes函数的代码**。*该函数负责从我们的数据库中获取食谱。它会查询DynamoDB，后者以NoSQL格式存储我们的食谱数据。当它找到数据后，会以JSON格式将其返回给API网关，再由API网关将其转发至前端*。

The safe recipe function handles the submission of new recipes. This function processes the recipe data by separating it into different components that can be stored into DynamoDB. Then it makes a request to DynamoDB to add the item to the recipe table. 

**saveRecipeFunction**负责处理新食谱的提交。该函数通过将配方数据拆分为多个可存储至DynamoDB的组件来对其进行处理。随后，它向DynamoDB发出请求，将该条目添加到食谱表中。

When we generate logs within this function, they're automatically captured by AWS Cloudwatch, another AWS service. We can check the logs by going to the Monitor tab and clicking View Cloudwatch Logs. Logs are organized into log groups, which group together all the logs from a specific function. Within each log group, you'll find individual log streams that represent a sequence of log events from a single source. Let's find the logs for our recent recipe edition. Here it is, chocolate dipped strawberries. Logs can be used for troubleshooting and quickly identifying errors. 

当我们在此函数中生成日志时，*这些日志会自动被AWS的另一项服务——`AWS CloudWatch`——捕获*。我们可以通过转到“监控”选项卡并单击“查看CloudWatch日志”来查看日志。日志被组织成日志组，每个日志组汇集了来自特定函数的所有日志。在每个日志组中，您将看到若干个独立的日志流，它们分别对应来自单一来源的一系列日志事件。让我们查找一下最近食谱版本的日志。就是它，巧克力蘸草莓。日志可用于故障排查，并快速定位错误。

The last main part of our application is the database DynamoDB. In Dynamo, we have one table named Recipes where all the recipe data is stored. Each item in the table represents a recipe with attributes for recipe name, ingredients, and instructions. This is how we persist the recipe data even when the user is not on the web page. By leveraging cloud services, we can build applications that are not only scalable and efficient, but are also easy to manage and monitor. This architecture allows us to focus on delivering a great user experience without getting bogged down by infrastructure concerns.

*我们的应用程序的最后一个主要组成部分是`Amazon DynamoDB数据库`*。在Dynamo中，我们有一张名为Recipes的表，用于存储所有食谱数据。表中的每一项都代表一道菜谱，包含菜名、食材和制作步骤等属性。这就是我们在用户不在网页时也能持久化保存食谱数据的方式。

**通过充分利用云服务，我们可以构建出不仅具备高可扩展性和高效性，而且易于管理和监控的应用程序。*这种架构*使我们能够*专注于打造卓越的用户体验*，而*不必为基础设施问题所困扰***。