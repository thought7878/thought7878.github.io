



# 原文
Let's play the guess the number game. To run this application, we'll execute the Python script in the terminal. You can find the terminal on a Mac by using Spotlight and searching Terminal on Windows. You can search for the command prompt in the search bar. 

让我们玩猜数字游戏。要运行此应用程序，我们将在终端中执行Python脚本。您可以通过在Windows上使用聚光灯和搜索终端在Mac上找到终端。您可以在搜索栏中搜索命令提示符。

Before executing the script, we'll navigate to the proper folder using the CD command. In this case, the script is in our desktop, so I'll CD into the desktop. From here, we'll execute the Python script with Python 3 script py, because that's the name of the script. On Windows, the command would just be Python, and then the name of the Python script file script dot py. If you don't have Python already installed, you can install it from python dot org slash downloads dot. Be sure to install a version of Python 3. 

在执行脚本之前，我们将使用CD命令导航到正确的文件夹。在这种情况下，脚本在我们的桌面上，所以我会将CD放入桌面上。从这里开始，我们将使用Python 3脚本py执行Python脚本，因为这是脚本的名称。在Windows上，命令将只是Python，然后是Python脚本文件的名称script点py。如果您还没有安装Python，可以从python dot org slash downloads dot安装它。请确保安装Python 3的版本。

Now let's play the game. We'll start in the middle with a gas of 50. To submit our input to the program, we'll just press enter, looks like we're too high, let's try 25 too low, 33, looks like it's 26. We've guessed the number. Let's play one more time and guess the number. We'll try the number from the previous run again, 26. This time the number was 89 and we got it throughout. Playing the game, we can see how the user interface collects, guesses, and provides feedback to us on whether the guess was right or wrong. 

现在我们来玩这个游戏。我们将从中间开始，气体为50。要提交我们的输入到程序中，我们只需按回车，看起来太高了，让我们尝试25太低，33看起来是26。我们已经猜出了数字。让我们再玩一回，猜数字。我们再次尝试上次的号码，26。这一次的数字是89，我们一直得到它。在玩游戏时，我们可以看到用户界面如何收集、猜测，并向我们提供猜测是对还是错的反馈。

The game logic handles the core functionality of number generation and comparison, all behind the scenes. Each component works together to provide a seamless and engaging gaming experience. 

*游戏逻辑处理数字生成和比较的核心功能，所有这些都在幕后。每个组件协同工作，提供无缝且引人入胜的游戏体验*。