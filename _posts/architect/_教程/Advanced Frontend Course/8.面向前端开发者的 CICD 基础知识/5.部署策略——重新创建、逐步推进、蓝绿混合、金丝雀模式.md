问：NJS应用程序或web应用程序在服务器端呈现或创建静态水疗中心的部署策略是什么？  
答：NJS应用程序或web应用程序的部署策略，具有服务器端渲染或创建静态水疗中心的部署策略，包括通过云解决方案进行简单部署，如没有复杂部署策略的S3存储桶、VPS (虚拟专用服务器) 、VPS上的下午2、VPS上的Docker和VPS上的Kubernetes。  
  
问：什么是VPS，它如何促进NoJS应用程序的部署？  
答：VPS是服务器上的私人空间，与他人共享，但拥有自己的资源，如CPU、内存和存储。 它通过SSH连接提供安全的环境，方便部署NoJS应用程序，应用程序可以直接部署并运行在监听本地主机端口3000的VPS上。  
  
问：在部署NoJS应用程序时，引擎扮演什么角色？  
答：引擎的作用是处理诸如HTTPS的SSL证书之类的事情，有效地提供静态文件，并在用户访问应用程序时充当反向代理。 它将请求从前端转发到在VPS上运行的NoJS应用程序。  
  
问：使用VPS进行部署的优点和缺点是什么？  
答：使用VPS的优点包括最小的设置复杂性，直接访问服务器进行调试，并且没有额外的阻塞层。 缺点包括进程管理问题，其中应用程序崩溃导致停机和需要手动重新启动，以及开发和生产之间的数学环境不一致。  
  
问：什么是下午2，它如何改进基本的VPS部署？  
答：下午2是一个内置负载均衡器的NoJS应用程序生产流程管理器，允许应用程序保持活动状态并在崩溃时自动重启，而不会停机。 它通过提供其他功能，如监控CPU和内存使用情况、零停机时间部署和简化管理，改进了基本的VPS部署。  
  
问：在VPS上使用Docker有哪些权衡和好处？  
答：在VPS上使用Docker的好处包括所有阶段的环境一致性，通过版本控制的Docker镜像轻松回滚到以前的版本，以及简化的部署过程。 权衡是学习Docker的额外复杂性，以及它仍然依赖于具有有限扩展选项的单个服务器的事实。  
  
问：Kubernetes如何促进应用程序的部署和管理？  
答：Kubernetes通过自动处理扩展、故障转移和部署来简化应用程序的部署和管理。 它可以添加更多的容器来处理增加的流量，用新的容器替换旧的容器，监视容器的健康状况，并在它们之间分配流量。  
  
问：使用Kubernetes的优点和缺点是什么？  
答：使用Kubernetes的优点包括自动扩展和负载平衡、自我修复、零停机时间的滚动最新进展和多服务器高可用性。 缺点是复杂的设置和维护要求、运营开销以及管理复杂的分布式系统。  
  
问：什么是AWS弹性计算服务（ECS），它如何简化部署过程？  
答：AWS弹性计算服务（ECS） (亚马逊弹性容器服务) 是一种托管的Docker容器服务，通过处理复杂的设置和维护来简化部署过程。 它允许您专注于更新和部署应用程序，而无需担心底层基础设施。  
  
问：与管理自托管Kubernetes集群相比，在PPS上使用Kubernetes有哪些权衡？  
答：在PPS上使用Kubernetes可以消除管理底层基础设施和编排系统的需要，提供扩展、负载均衡和健康检查等好处，而无需管理开销。 然而，这种便利是有代价的，由于支付AWS服务费用、有限的定制选项以及供应商锁定在AWS基础设施中，可能会产生更高的费用。  
  
问：无服务器计算是如何工作的，它的优点和缺点是什么？  
答：无服务器计算允许运行代码，而无需通过编写部署到AWS等云提供商的小函数来管理服务器。 供应商处理其他一切，包括服务器、扩展和监控。 其优势包括零服务器管理、基于使用情况的自动扩展和按需付费定价。 然而，由于函数执行的时间限制，它也存在诸如供应商锁定和长时间运行的流程限制等缺点。  
  
问：在成本、控制和复杂性方面，平台即服务 (PaaS) (PaaS) 与其他部署选项相比如何？  
答：像Heroku或铁路这样的平台即服务 (PaaS) 提供了一种方便且经济的方式来开发和部署应用程序，内置监控和扩展功能，只需要最少的基础设施知识。 然而，它是最昂贵的选项，并提供对基础设施的最少控制。 权衡表明，虽然它易于使用且非常适合快速原型设计，但它比自我管理选项的成本更高。  
  
问：无服务器计算有哪些用例受益？  
答：无服务器计算使涉及短暂、不可预测流程的使用案例受益，例如文件处理、用户上传、Webhook、计划任务和事件驱动的应用程序。 这些使用案例需要可扩展的解决方案，而不需要长期的服务器承诺。  
  
问：使用平台即服务 (PaaS) (PaaS) 的权衡是什么？  
答：使用PaaS的权衡包括: 与管理VPS相比成本最高，由于在平台的限制内工作而导致的有限定制，以及无法微调基础架构。 它最适合快速原型制作和没有运营专业知识的团队。  
  
问：无服务器计算的权衡是什么？  
答：无服务器计算的权衡包括与其他选项相比最昂贵的定价模式，有限的定制，因为它在平台的限制内运行，以及以应用为中心的开发方法，与平台的方法紧密集成。  
  
问：什么时候使用VPS比Kubernetes等更复杂的解决方案更可取？  
答：对于需要在可扩展性、复杂性和成本之间取得平衡的团队，VPS更受欢迎，以更低的成本提供最多的控制。 它适用于希望专注于开发而没有基础设施问题的小团队或单独开发人员，以及需要以最少的基础设施快速上市的应用程序。  
  
问：无服务器计算什么时候是合适的选择？  
答：无服务器计算适用于需要类似Kubernetes的好处而不需要操作复杂性、持续编排的初创公司和团队，以及需要快速原型设计和快速上市时间且基础设施问题最小的应用程序。

---

# Node.js应用的部署策略: VPS和PM2方法  
00:00
讨论了使用VPS部署Node.js应用程序的简单性和直接访问，重点介绍了PM2在流程管理和零停机部署方面的增强，适用于中小型流量应用程序，强调了设置、优势和限制。  

![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/227bdb6cb640827ae07b54d1f078c791_MD5.png]]

## VPS
![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/0c9559f5e2e75cc600452d6ac8722102_MD5.png]]

部署与运维。VPS

**虚拟私人服务器 (Virtual Private Server)**
`VPS`（虚拟私人服务器）*就像你在服务器上的专属私人空间*。
*即使物理服务器是与其他用户共享的，你分配到的部分，拥有独立的资源（CPU、内存、存储），其他人无法使用*。

架构图流程说明（从左至右）：
1.  **GitHub Actions**：作为自动化部署的起点。
2.  **NodeJS App / SSH Connection**：GitHub Actions 通过 SSH 连接，将 NodeJS 应用部署到服务器。
3.  **VPS (AWS EC2)**：应用被部署到虚拟私人服务器（此处示例为 AWS EC2）。
4.  **node app.js**：服务器上运行着 Node.js 应用程序。
    *   图中有虚线标注 **Trigger**（触发），表示 GitHub Actions 触发了应用的部署或更新。
5.  **my-app.com**：用户访问的域名。
6.  **Nginx**：*域名请求首先到达 Nginx 服务器*。
7.  **localhost:3000**：*Nginx 将请求反向代理转发到本地的 3000 端口*，即 **node app.js** 运行的地方。

### 优点、缺点、使用场景
![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/26e857990f11640960aadcc08a36cafc_MD5.png]]

部署与运维。VPS

**优点 (Pros)**
*   **设置复杂度最低**：配置简单，不需要复杂的架构。
*   **直接访问服务器进行调试**：可以直接登录服务器排查问题。
*   **没有额外的抽象层**：直接运行在操作系统上，没有容器或云平台的中间层。

**缺点 (Cons)**
*   **手动进程管理（应用崩溃 = 停机）**：如果程序崩溃，需要人工介入重启，否则服务就会中断。
*   **没有自动重启或监控**：缺乏自动化的守护进程和监控机制。
*   **开发和生产环境之间的环境不一致**：本地开发环境和服务器环境可能不同，导致“在我电脑上能跑”的问题。

**使用场景 (Use cases)**
*   **快速原型或不需要长期维护的临时应用**：适合做演示或短期项目。
*   **流量极小且无正常运行时间（Uptime）要求的简单应用**：适合个人小项目或测试用途。

## PM2
![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/71e1a7c160ed23be602d5f5b58aa546d_MD5.png]]

部署与运维。在 VPS 上使用 PM2

`PM2` 是 **Node.js 应用程序的生产环境进程管理器，内置了负载均衡器。它允许你让应用程序永远保持运行状态（即守护进程），并在不停机的情况下重新加载应用**。

架构图流程说明（从左至右）：
1.  **GitHub Actions**：作为自动化部署的起点。
2.  **NodeJS App / SSH Connection**：GitHub Actions 通过 SSH 连接，将 NodeJS 应用部署到服务器。
3.  **VPS (AWS EC2)**：应用被部署到虚拟私人服务器（此处示例为 AWS EC2）。
4.  **PM2 (pm2 start app.js)**：
    *   在服务器上，*不再直接运行 `node app.js`，而是使用 PM2 来管理进程*（图中有四个 JS 图标，暗示 PM2 可以运行多个实例/集群模式）。
    *   图中有虚线标注 Trigger（触发），表示 GitHub Actions 触发了 PM2 的启动或重启。
5.  **my-app.com**：用户访问的域名。
6.  **Nginx**：域名请求首先到达 Nginx 服务器。
7.  **localhost:3000**：Nginx 将请求反向代理转发到本地的 3000 端口，即 PM2 管理的 Node.js 应用运行的地方。

### 优点、缺点、使用场景
![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/034279ca66211957124f749e2be4231a_MD5.png]]  
  
部署与运维。在 VPS 上使用 PM2

**优点 (Pros)**
*   **崩溃时自动重启**：如果应用意外停止，PM2 会自动将其重新启动。
*   **内置监控和日志记录**：方便查看应用运行状态和排查问题。
*   **零停机部署**：可以在不中断服务的情况下更新或重新加载应用。

**缺点 (Cons)**
*   **仍然是单台服务器（无高可用性）**：如果这台服务器本身宕机，服务就会中断。
*   **手动服务器维护和更新**：操作系统的补丁和配置仍需人工处理。
*   **扩展选项有限**：主要依赖单机性能，难以像云原生架构那样轻松横向扩展。
*   **需要基本的服务器管理技能**：管理员需要懂得 Linux 命令行和基础运维知识。

**使用场景 (Use cases)**
*   **流量稳定的中小型企业应用**：适合那些不需要极高可用性，但需要比纯手动运行更稳定环境的业务。


# 容器化和编排: Docker、Kubernetes、AWS弹性计算服务（ECS）  
05:14
Docker提供一致的环境和易于回滚，但有手动编排的限制。 Kubernetes提供跨多个服务器的自动扩展和高可用性，是大型应用程序的理想选择。 AWS弹性计算服务（ECS） 管理容器服务，简化可伸缩部署的设置和维护。  

## Docker
![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/89adf9a4e75dce1ec723bf6066792960_MD5.png]]

部署与运维。在 VPS 上使用 Docker

`Docker` ***是一种工具***，**它允许你将应用程序及其所需的一切（代码、库、配置）打包进一个容器，从而确保应用在任何环境（你的电脑、服务器或云环境）中*都能一致运行***。

架构图流程说明（从左至右）：
1.  **GitHub Actions**：作为自动化部署的起点。
2.  **SSH Connection**：*通过 SSH 连接将构建好的 Docker 镜像传输到服务器*。
    *   **Node.js App (Docker image)**：*GitHub Actions 会构建 Docker 镜像*。
    *   **AWS ECR**：*构建好的镜像会被推送到 AWS 弹性容器注册表（ECR）*。
3.  **VPS (AWS EC2)**：应用部署到虚拟私人服务器（此处示例为 AWS EC2）。
4.  **Docker 运行流程**：
    *   **docker pull $IMAGE**：*从 AWS ECR 拉取镜像*（图中虚线箭头标注 **Trigger** 表示此操作由部署流程触发）。
    *   **docker run $IMAGE**：在 VPS 上运行容器化应用。
5.  **my-app.com**：用户访问的域名。
6.  **Nginx**：域名请求首先到达 Nginx 服务器。
7.  **localhost:3000**：Nginx 将请求反向代理转发到本地的 3000 端口，即 Docker 容器中运行的 Node.js 应用。

### 优点、缺点、使用场景
![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/c19e93935da40d5b332e0c5c5e804ba2_MD5.png]]




![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/977084e8193be02b4521f493e2ce4f90_MD5.png]]

![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/243b8a69c1ac9a4a888d2a03229428b2_MD5.png]]

![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/f52149e00b4bfd97015d153af32db977_MD5.png]]  
  
  
# Kubernetes与无服务器: AWS在容器编排和代码执行中的作用  
11:07
对话讨论了使用AWS的托管Kubernetes服务、Aacs和无服务器计算与AWS Lambda的权衡和好处。 它强调了Aacs的托管基础设施、自动扩展和运行状况检查，与无服务器中缺乏基础设施管理的情况进行了对比，后者提供了自动扩展、按使用付费定价和内置高可用性。 这两种方法都有供应商锁定和特定用例，Aacs适合需要Kubernetes优势的初创公司，而无需操作复杂性和无服务器，是不可预测的流量模式和短时间运行任务的理想选择。  


![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/ebc92fafdc11968499664c77726f105c_MD5.png]]

![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/b5f0e0845338d9744e5f557ddb34a0fb_MD5.png]]

![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/b3bafb2b3e1402edb4b7c38417922e1f_MD5.png]]  
  
  
# 平台即服务: 在无服务器应用程序中平衡成本、控制和便利性  
15:00
对话探讨了为事件驱动应用程序使用平台即服务 (PaaS) 的好处和权衡，强调了其便利性、自动监控和可扩展性。 然而，它强调了溢价成本、有限的定制和缺乏基础设施控制等重大缺陷。 作为快速原型设计、MVPs和小型团队的理想选择，PaaS提供了快速部署的路径，无需深入的DevOps知识。 讨论将PaaS与自我管理的VPS和Kubernetes进行了对比，说明了控制与成本、复杂性与可扩展性的范围。


![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/e905a83531e35e09009b71aa6e0ca8aa_MD5.png]]

![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/a1258d1bbb6658d3df6e67773aed1781_MD5.png]]

![[_posts/architect/_教程/Advanced Frontend Course/8.面向前端开发者的 CICD 基础知识/media/89e23a7f7bddf4f577189f4907852aa9_MD5.png]]
