本视频讲述了*使用Webpack进行前端项目构建工具升级的完整实践过程*，涵盖*Webpack 4升级至Webpack 5的依赖更新、配置适配*（如source map移除、merge函数调用方式变更、base.js属性精简）、*构建性能对比测试*（含time脚本精确计时、缓存机制验证），以及*与Rollup构建工具的横向性能 benchmark 分析*，最终*得出Webpack 5开启持久化缓存后构建速度显著优于Webpack 4、但Rollup在纯ESM场景下首构与二次构建综合耗时更优的结论*。


- **Webpack 5 升级准备与依赖安装 [00:02](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=2bd895d791b343148c2e72d311eedd7e#?seek_t=2)**
    
    - 执行 `npm check -u mini` 对 webpack 进行运行时升级。
    - 仅升级开发依赖（如 `webpack`, `webpack-cli`, `webpack-dev-server`, `clean-webpack-plugin`, `html-webpack-plugin`），不升级生产依赖。
- **构建失败排查与配置修复 [00:43](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=2bd895d791b343148c2e72d311eedd7e#?seek_t=43)**
    
    - 执行 `npm run build` 报错：`to the plugin`提示存在非法配置项 `sourceMap: 'prod'`。
    - 删除 `webpack.config.js` 中 `devtool` 或 `optimization.splitChunks` 下冗余的 `sourceMap` 配置项。
- **webpack-merge API 变更适配 [01:29](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=2bd895d791b343148c2e72d311eedd7e#?seek_t=89)**
    
    - 报错 `ipad merge is not a function`（应为 `webpack-merge` 拼写误读）。
    - 查阅 npmjs.com 上 `webpack-merge` v5+ 文档，确认新用法为解构导入：`const { merge } = require('webpack-merge')`。
    - 替换原 `const merge = require('webpack-merge')` 写法，并在多处配置合并处同步更新。
- **base.js 兼容性修正 [02:21](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=2bd895d791b343148c2e72d311eedd7e#?seek_t=141)**
    
    - 构建仍报错，定位到 `base.js` 中存在 Webpack 5 不支持的废弃属性。
    - 仅保留 `globalName` 与 `findName` 两个合法属性，删除其余无效字段。
- **Webpack 5 构建性能基准测试 [02:44](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=2bd895d791b343148c2e72d311eedd7e#?seek_t=164)**
    
    - 成功构建后，Webpack 自身输出耗时 `1.06s`。
    - 使用自定义 `time.js` 脚本实测：`node time.js "npm run build"` 总耗时 `2.6s`（含 Node 启动开销）。
    - 确认 Webpack 5 构建主体时间约 `1s`，显著快于旧版。
- **版本回退至 Webpack 4 验证 [03:25](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=2bd895d791b343148c2e72d311eedd7e#?seek_t=205)**
    
    - 使用 `git reset --hard HEAD~1` 回退提交，使 `webpack` 版本降级至 v4。
    - 删除 `node_modules` 并执行 `npm install`（因无 pnpm，依赖 `package-lock.json`）。
- **Node.js 版本兼容性修复 [04:25](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=2bd895d791b343148c2e72d311eedd7e#?seek_t=265)**
    
    - Webpack 4 构建失败，因当前 Node.js 版本过高。
    - 切换至 Node.js v16，重新安装依赖并执行 `npm run build`，成功构建耗时 `836ms`。
    - `time.js` 测试总耗时 `2s`，未启用持久化缓存。
- **Webpack 5 持久化缓存启用与加速验证 [07:04](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=2bd895d791b343148c2e72d311eedd7e#?seek_t=424)**
    
    - 在 `webpack.config.js` 中启用 `cache: { type: 'filesystem' }`。
    - 首次构建耗时 `239ms`；二次 `npm run build` 降至 `760ms`；三次 `npm run build` 稳定在 `769ms`。
    - `time.js` 实测总耗时 `2s`，但构建阶段仅 `0.24s`，命令总时长 `1.6s`，优于 Webpack 4 的 `1.6s`（无缓存）及 `2s`（有缓存）。
- **Node.js v18 兼容性补充测试 [06:30](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=2bd895d791b343148c2e72d311eedd7e#?seek_t=390)**
    
    - 切换至 Node.js v18，重装依赖后 `npm run build`耗时 `900ms+`，但启用缓存后回落至 `239ms`，验证缓存有效性。
- **Rollup 构建工具引入与基础配置 [07:51](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=2bd895d791b343148c2e72d311eedd7e#?seek_t=471)**
    
    - 安装 `rollup` 作为 devDependency，版本 `3.27.1`。
    - 创建 `rollup.config.js`，基础配置：入口 `src/index.js`，输出 `bundle.js`，格式 `cjs`。
    - `package.json` 中添加 `"type": "module"` 以支持 ES Module 语法。
- **Rollup 插件集成与代码压缩 [10:52](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=2bd895d791b343148c2e72d311eedd7e#?seek_t=652)**
    
    - 安装 `@rollup/plugin-terser` 插件实现代码压缩。
    - 配置中 `import { terser } from '@rollup/plugin-terser'`，并在 `plugins: [terser()]` 中启用。
- **Rollup 性能基准测试与对比 [10:29](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=2bd895d791b343148c2e72d311eedd7e#?seek_t=629)**
    
    - 首次构建耗时 `85ms`（含默认缓存），但输出未压缩，体积 `151.2KB`。
    - 启用 `terser` 后构建耗时 `363ms`，体积同为 `151.2KB`。
    - `time.js` 实测 `npm run rollup-build` 总耗时 `1.1s`（无异常捕获）→ 优化后捕获 `close` 事件，实测 `1.6s`（与 Webpack 5 缓存版持平）。
    - 多次测试取均值：Rollup `1.2s`，Webpack 5 缓存版 `1.6s`，确认 Rollup 在该纯 ESM 场景下整体更快。
- **构建方案综合评估与总结 [16:09](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=2bd895d791b343148c2e72d311eedd7e#?seek_t=969)**
    
    - Webpack 5 升级核心收益：配置现代化、缓存机制增强、长期维护性提升。
    - Rollup 优势场景：轻量级、纯 ESM 项目，首构与增量构建延迟更低。
    - 工程实践关键点：必须使用 `time.js` 等工具分离“构建耗时”与“进程启动耗时”；需严格处理 `package.json` 的 `type` 字段；插件 API 变更须同步文档核查。