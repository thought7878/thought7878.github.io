本视频讲述了使用官方脚手架初始化Vue SSR项目并集成Pinia状态管理的完整实操流程，包括项目结构分析、服务端渲染配置、Pinia store的服务器端初始化与客户端透传、类型定义修正、版本升级解决兼容性问题，以及自动组件导入、图标库和Markdown支持等插件集成验证。

![[_posts/architect/_教程/高级前端工程师（大前端）- Brian/2.阶段二：【全栈开发能力-通识强化】PC端最小闭环打造全栈思维和实战/5.服务端渲染原理、Linux、Docker/2.掌握SSR实战：应用vite-plugin-ssr项目改/media/523da9353205d65ad01d4bb0ac90a48c_MD5.webp]]

## 脚手架初始化
[00:01](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=1)
    
- **初始化方式选择与必要性说明** [00:08](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=8)  
	面临直接改造旧项目或新建脚手架项目的抉择；因Vite + TypeScript示例项目结构、配置及依赖（如Express）发生根本性变化，且官方未提供详细合并指南，故建议直接使用脚手架初始化新项目。
- **初始化与调试启动** [01:03](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=63)  
	执行官方命令快速完成初始化；使用 `npm run dev`启动调试进程；需终止先前占用3000端口的进程后，成功在 `localhost:3000` 启动SSR服务并访问首个SSR页面，验证路由与响应式特性正常。

## 项目结构解析
[01:55](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=115)
    
- **服务端核心逻辑说明** [02:00](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=120)  
	服务端基于Express框架，利用其成熟生态避免重复开发；中间件负责加载 `wet-plugin-server-page`并根据路由响应对应资源。
- **Pages与Render层结构** [02:37](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=157)  
	`pages` 目录实现文件路由系统，对应原项目 `pages`；`index.page.vue` 加载 `counter` 组件；`render` 目录含公共入口 `App.ts`，其中 `createApp` 返回 `App` 实例，并包含 `pageShow`配置项作为全局Layout。
- **初始页面改造与验证** [03:12](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=192)  
	清理 `pageShow` 配置、`link` 标签及冗余样式（公共/局部）；在 `App.vue` 中添加 `hello world`文本并保存，浏览器刷新后成功显示，确认基础渲染链路通畅。

## Pinia状态管理集成
[04:05](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=245)
    
- **集成路径与文档参考** [04:17](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=257)  
	参考官方文档左侧导航栏关于批量集成（如CSS、Sass、PWA）、示例（`examples/vite-pwa`）及 `stores` 创建位置（根目录）的说明。
- **入口文件 `App.ts` 改造** [04:49](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=289)  
	`import { createPinia } from 'pinia'`；在 `createApp` 函数内创建 `const pinia = createPinia()`；修改 `return` 语句为 `return { App, pinia }`。
- **服务端状态透传 `default.page.server.ts` 改造**[07:00](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=420)  
	在 `onBeforeRender` 钩子中，从 `createApp` 返回值解构获取 `pinia`；调用 `pinia.state.value` 获取初始状态；将 `initialStoreState` 属性注入 `pageContext`并 `return`。
- **客户端状态恢复 `client.ts` 改造** [07:41](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=461)  
	解除 `clientRouting: true` 注释；删除冗余 `render` 和 `createApp` 调用；定义 `interface PageContextNew extends PageContextClient`；为 `initialStoreState` 添加 `any` 类型声明；修正 `pageContext` 类型以兼容新属性。

## 集成验证与错误排查 
[09:57](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=597)
    
- **Store创建与页面使用** [10:04](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=604)  
	根目录新建 `stores/useCounter.ts`；在 `index.page.vue` 或 `counter.vue` 中 `import { useCounter } from '@/stores/useCounter'`；调用 `useCounter().increment()` 测试交互。
- **版本兼容性修复** [11:07](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=667)  
	启动报错 `no matching export for houseInjectionContext`；定位 `vue` 版本过低（3.2.33）；执行 `npm update --legacy-peer-deps` 升级 `vue`、`vite`、`@wundergraph/vue` 等至最新版，保留 `@types/node` 与 `typescript` 不升级。
- **初始状态缺失修复** [12:59](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=779)  
	浏览器控制台报错 `initial store state is missing in the passed client list`；在 `default.page.server.ts` 的 `passToClient`对象中显式添加 `initialStoreState: pinia.state.value`。

## 其他插件功能集成验证 
[13:25](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=805)
    
- **自动导入与图标支持** [13:53](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=833)  
	在模板中直接使用 `<mdi-web />` 图标，保存后成功渲染，验证 `@wundergraph/vue` 的自动图标导入功能生效。
- **Markdown支持验证** [14:03](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=843)  
	复制 `test.md` 文件至项目；在 `script` 中 `import MD from '../test.md'`；创建 `md.ts` 声明文件（含 `declare module '*.md'`）；在 `tsconfig.json` 的 `include` 中添加 `md.ts` 及相关路径；保存后 `MD` 模块识别正常，浏览器中 `test.md` 内容正确渲染。
- **组件渲染验证** [15:29](https://b.quark.cn/apps/5AZ7aRopS/routes/mofb35Rkb?debug=0&fid=bb3fc3ec569540d3b6008cdc98e0ef75#?seek_t=929)  
	页面中嵌入的 `image-swiping` 等自定义组件，在Markdown及常规页面中均能正常渲染，确认插件生态集成完整。