

视频讲述了**在容器中导入并调用远程模块（产品列表应用）的导出函数（挂载函数），以实现产品列表的显示**，具体如下：


- **容器中导入并调用产品应用的挂载函数**：[00:00]在容器的bootstrap.js文件中，将产品导入语句更新为从products/index导入挂载函数，以便随时调用该函数在屏幕任意位置显示产品列表应用，可根据用户访问特定URL、点击按钮等场景触发，目前选择立即调用。
- **确保挂载函数能找到显示位置**：[00:49]需要向挂载函数提供一个存在的HTML元素引用，在容器index.html文件中，使用id为my-products的元素，通过选择器获取该元素并传递给挂载函数，使产品列表知道显示位置。
- **修正产品模块的Webpack配置**：[01:23]之前导入的products/index映射到容器Webpack配置中从products模块请求的内容，而产品模块的index.js未导出挂载函数，实际是bootstrap.js导出，因此在产品Webpack配置文件中，当有人请求products/index时，应提供bootstrap文件代替。
- **测试前的准备与测试结果**：[02:24]修改产品Webpack配置后需重启产品应用，之后测试访问localhost:8801确认正常，访问localhost:8800刷新页面能看到产品列表显示。
- **产品模块中导出内容的模式**：[02:53]在产品模块的Bootstrap JS文件中定义挂载函数作为项目真正的启动位置，调用该函数可创建React、Angular、Vue等应用并渲染到指定元素；会检查项目是否独立运行，若是则立即渲染到本地项目HTML文件中存在的元素，若通过容器运行则依赖导出语句。
- **后续计划**：[03:59]接下来将对购物车应用重复上述相同步骤。