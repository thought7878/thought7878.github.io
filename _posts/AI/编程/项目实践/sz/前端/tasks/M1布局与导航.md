# M1 布局与导航模块 - 任务清单

> 模块：M1 布局与导航  
> 状态：待开始  
> 依赖：M0（认证与权限）

## 任务列表

### T1.1 类型定义
- [ ] 创建 M1 类型：`MenuItem`, `BreadcrumbItem`, `LayoutConfig`, `RouteConfig`

### T1.2 Zustand Store
- [ ] 创建 `store/layoutStore.ts`：`sidebarCollapsed`, `breadcrumbItems`, `currentRoutePath` 及 actions
- [ ] 创建 `store/menuStore.ts`：`menuItems`, `flatMenuMap` 及 actions
- [ ] 实现 `loadMenuFromPermissionTree()`：从 M0 权限树生成菜单
- [ ] 实现 `getMenuByPath()` 和 `getParentMenus()`

### T1.3 路由配置
- [ ] 创建 `router/routes.tsx`：定义 `RouteConfig` 配置
- [ ] 配置路由表（含 meta 信息：title, icon, hideInMenu, permission, breadcrumb）
- [ ] 配置 React Router（BrowserRouter + Routes + Route）

### T1.4 主布局组件
- [ ] 创建 `components/Layout/MainLayout.tsx`：侧边栏 + 顶栏 + 内容区布局
- [ ] 创建 `components/Layout/SidebarMenu.tsx`：侧边栏菜单组件
- [ ] 实现菜单展开/收起、选中高亮、hover Tooltip（折叠模式）
- [ ] 创建 `components/Layout/Breadcrumb.tsx`：面包屑组件
- [ ] 实现从 `menuStore.getParentMenus()` 自动生成面包屑
- [ ] 创建 `components/Layout/HeaderBar.tsx`：顶栏组件
- [ ] HeaderBar 内容：折叠按钮 + 通知角标 + 主题切换 + 语言切换 + 用户头像下拉

### T1.5 路由切换联动
- [ ] 实现路由变化时自动更新面包屑
- [ ] 实现路由变化时自动高亮对应菜单项
- [ ] 实现无权限路由时渲染 403 页面

### T1.6 Mock 数据
- [ ] 创建 `mock/menu.ts`：权限树和菜单 Mock 数据

### T1.7 测试
- [ ] 菜单生成测试（不同权限树 → 正确菜单项）
- [ ] 面包屑生成测试（不同路由 → 正确面包屑路径）
- [ ] 侧边栏折叠测试（折叠/展开布局宽度变化）
- [ ] 权限路由测试（无权限路由 → 403 渲染）
