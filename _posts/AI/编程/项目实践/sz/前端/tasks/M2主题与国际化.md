# M2 主题与国际化模块 - 任务清单

> 模块：M2 主题与国际化  
> 状态：待开始

## 任务列表

### T2.1 类型定义
- [ ] 创建 M2 类型：`ThemeMode`, `PresetColor`, `ThemeConfig`, `LocaleConfig`

### T2.2 i18n 配置
- [ ] 安装依赖：`i18next`, `react-i18next`, `i18next-resources-to-backend`
- [ ] 创建 `i18n/index.ts`：i18n 初始化配置
- [ ] 创建 `locales/zh-CN/modules/`：中文语言包目录
- [ ] 创建 `locales/en-US/modules/`：英文语言包目录
- [ ] 编写基础语言包（common.json、dashboard.json 等）

### T2.3 Zustand Store
- [ ] 创建 `store/themeStore.ts`：`mode`, `primaryColor`, `borderRadius`, `fontSize`, `compact` 及 actions
- [ ] 实现 `generateAntdTheme()`：将 ThemeState 转换为 Ant Design 主题配置
- [ ] 创建 `store/localeStore.ts`：`currentLocale`, `supportedLocales` 及 actions
- [ ] 实现持久化（localStorage 读写）

### T2.4 Provider 组件
- [ ] 创建 `providers/ThemeProvider.tsx`：读取 themeStore → 通过 ConfigProvider 注入主题
- [ ] 同步设置 `document.documentElement` 的 `data-theme` 属性
- [ ] 创建 `providers/LocaleProvider.tsx`：读取 localeStore → 切换 i18n + dayjs 语言 → 注入 Ant Design 语言包

### T2.5 切换组件
- [ ] 创建 `components/Theme/ThemeSwitcher.tsx`：亮暗模式切换 + 颜色选择器
- [ ] 创建 `components/Locale/LanguageSwitcher.tsx`：语言下拉切换

### T2.6 全局 Hooks
- [ ] 创建 `hooks/useTheme.ts`：封装主题状态和操作

### T2.7 测试
- [ ] 主题切换测试（`setMode` → `antdTheme.algorithm` 正确切换）
- [ ] 自定义颜色测试（`setPrimaryColor` → token 中 `colorPrimary` 更新）
- [ ] 语言切换测试（Mock i18n → 验证 `changeLanguage` 调用和资源加载）
- [ ] 持久化测试（主题/语言配置 localStorage 读写）
