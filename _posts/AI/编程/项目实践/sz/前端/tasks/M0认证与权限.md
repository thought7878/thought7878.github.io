# M0 认证与权限模块 - 任务清单

> 模块：M0 认证与权限  
> 状态：待开始

## 任务列表

### T0.1 项目基础配置
- [ ] 初始化项目（Vite + React + TypeScript）
- [ ] 安装依赖（antd, zustand, react-router-dom, axios, dayjs, i18next, echarts）
- [ ] 配置路径别名（@ 指向 src/）
- [ ] 创建项目目录结构（api, components, hooks, pages, store, styles, types, utils, router, mock）
- [ ] 配置 ESLint + Prettier

### T0.2 全局类型定义
- [ ] 创建 `types/common.d.ts`：定义 `ApiResponse<T>`, `PaginatedResponse<T>`, `PaginationParams`, `TimeRange`
- [ ] 创建 M0 类型文件：`UserInfo`, `RoleInfo`, `DataScope`, `TokenPair`, `LoginParams`, `RegisterParams`, `CaptchaResponse`, `PermissionTreeNode`, `LoginLog`

### T0.3 API 层 - 认证接口
- [ ] 创建 `api/request.ts`：Axios 实例配置（baseURL, timeout, 拦截器）
- [ ] 创建 `api/auth.ts`：`login`, `logout`, `register`, `refreshToken`, `getCaptcha`, `getCurrentUser`, `getPermissionTree`, `changePassword`, `getLoginLog`
- [ ] 实现 Token 刷新拦截器（401 自动刷新 + 并发队列）

### T0.4 Zustand Store - authStore
- [ ] 创建 `store/authStore.ts`：定义 `AuthState` 接口
- [ ] 实现 `initializeAuth()`：应用启动恢复认证状态
- [ ] 实现 `login()`：登录流程（调用 API → 存储 Token → 获取用户信息）
- [ ] 实现 `logout()`：登出流程（清除 Token → 重置状态 → 跳转登录页）
- [ ] 实现 `refreshAccessToken()`：Token 刷新逻辑
- [ ] 实现 `fetchUserInfo()` 和 `fetchPermissionTree()`
- [ ] 拆分 `store/auth/actions.ts`：独立 action 文件

### T0.5 权限工具函数
- [ ] 创建 `utils/permission.ts`
- [ ] 实现 `hasPermission()`：权限检查
- [ ] 实现 `hasRole()`：角色检查
- [ ] 实现 `filterMenuByPermission()`：菜单权限过滤
- [ ] 实现 `checkDataScope()`：数据权限范围检查

### T0.6 全局 Hooks
- [ ] 创建 `hooks/useAuth.ts`：封装认证状态和操作
- [ ] 创建 `hooks/usePermission.ts`：封装权限校验

### T0.7 登录页面
- [ ] 创建 `pages/Login/LoginForm.tsx`：登录表单组件
- [ ] 实现验证码获取和展示
- [ ] 实现登录提交和错误处理
- [ ] 实现"切换到注册"和"忘记密码"入口
- [ ] 创建 `pages/Login/RegisterForm.tsx`：注册表单组件
- [ ] 创建 `pages/Login/index.tsx`：登录页容器

### T0.8 路由守卫组件
- [ ] 创建 `components/AuthRoute.tsx`：认证路由守卫
- [ ] 实现认证检查 + Token 过期检查 + 静息刷新
- [ ] 创建 `components/PermissionGuard.tsx`：权限守卫组件
- [ ] 实现权限/角色校验逻辑

### T0.9 Mock 数据
- [ ] 创建 `mock/auth.ts`：登录、注册、Token 刷新 Mock
- [ ] 创建 `mock/user.ts`：用户信息、权限树 Mock
- [ ] 配置 MSW 或 Mock.js 拦截

### T0.10 单元测试
- [ ] Token 刷新流程测试（401 响应 + 并发队列）
- [ ] 权限校验测试（`hasPermission`/`hasRole`）
- [ ] 数据权限测试（DataScope 访问控制）
- [ ] 登录流程测试（成功/失败/网络异常）
- [ ] AuthRoute 路由守卫测试
