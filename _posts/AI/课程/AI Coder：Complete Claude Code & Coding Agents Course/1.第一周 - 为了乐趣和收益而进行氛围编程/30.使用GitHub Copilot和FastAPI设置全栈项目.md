# 总结
这段内容主要讲解了从使用 Cursor 切换到 GitHub Copilot 开始新项目，并介绍了*项目初始状态和 AGENTS.md 的配置*。核心要点如下：

1. 工具切换与 Copilot 设置  
   - 今天改用 GitHub Copilot（学员可自选工具）。  
   - 查看 Copilot 设置，了解 Pro 计划的请求配额、使用情况和模型开关。  
   - 提醒注意用量，可设置预算限制。

2. 获取项目代码  
   - 使用 GitHub 上的 [PM 仓库](https://github.com/ed-donner/pm)，通过 Code 按钮复制 HTTPS 地址，在 VS Code 中用 `git clone` 拉取。  
   - 在 VS Code 中打开 PM 目录。

3. **为什么从现有仓库开始**  
   - *从零构建对 agent 来说较容易，而继承已有代码再扩展更有挑战性，也更贴近真实项目场景*。  
   - *项目中已有一个由 Codex 生成的 Kanban 前端 MVP，假装是其他团队交付的，现在要在此基础上完善*。

4. **项目目标**  
   - *将现有前端 MVP 升级为完整应用：* 包含前端、后端、数据库和 API。  
   - *构建一个持久化的项目管理工具，并加入 AI 聊天功能*，用户可以询问项目情况，也可以通过 AI 创建、编辑或移动卡片。

5. **项目文件结构**  
   - `frontend`：已有的 Kanban MVP。  
   - `backend`：暂时为空，后续放入后端代码。  
   - `scripts`：暂时为空。  
   - `.env`：存放 OpenRouter API key。  
   - `.gitignore`：忽略 `.env` 等敏感文件。  
   - `AGENTS.md`：给编码代理的项目说明文件。

6. **AGENTS.md 内容**  
   - **项目定位**：项目管理 MVP Web 应用。  
   - **功能需求**：用户登录、看板展示、列可重命名、卡片可移动、侧边栏 AI 聊天、AI 能操作卡片。  
   - **MVP 限制**：单用户硬编码登录、每个用户一个看板、本地 Docker 运行。  
   - **技术决策**：Next.js 前端、FastAPI 后端、Docker 打包、UV 包管理器、OpenRouter 调用 AI、SQLite 本地数据库。  
   - **起始点**：前端 MVP 已存在但未适配 Docker。  
   - **编码标准**：遇到问题先找根因、用证据证明再修复。  
   - **文档要求**：所有规划文档放在 `docs` 目录，先阅读 `docs/plan.md`。

整体上，*这节课是为后续构建完整项目做铺垫，重点是让学员理解如何基于已有代码和 AGENTS.md 引导代理高效开发*。


## /AGENTS.md
```md
# 项目管理系统 MVP Web 应用

---

## 业务需求

本项目旨在构建一个项目管理应用。核心功能如下：

- 用户可登录系统
- 登录后，用户看到代表其项目的看板
- 看板具有固定列，列名可重命名
- 看板上的卡片支持拖拽移动和编辑
- 侧边栏提供 AI 聊天功能，AI 能够创建、编辑或移动一张或多张卡片

---

## 限制条件

- MVP 阶段，登录用户名和密码采用硬编码（固定为 `user` / `password`），但数据库设计会为未来多用户支持留出扩展。
- MVP 阶段，每位登录用户仅拥有 1 个看板。
- MVP 阶段，应用将在本地运行（使用 Docker 容器）。

---

## 技术决策

- 前端：Next.js
- 后端：Python FastAPI，同时负责在根路径 `/` 下提供 Next.js 静态站点服务
- 整体打包为单个 Docker 容器
- 在 Docker 容器中使用 `uv` 作为 Python 包管理器
- AI 调用使用 OpenRouter，`OPENROUTER_API_KEY` 存放在项目根目录的 `.env` 文件中
- 模型选用 `openrouter/free`
- 数据库使用本地 SQLite，若不存在则自动创建
- 在 `scripts/` 目录下提供适用于 Mac、PC、Linux 的启动和停止脚本

---

## 当前起点

前端工作 MVP 已构建完成，位于 `frontend` 目录中。该版本尚未适配 Docker 环境，仅为纯前端演示。

---

## 配色方案

- 强调色黄色：`#ecad0a` —— 用于强调线、高亮元素
- 主蓝色：`#209dd7` —— 用于链接、关键区域
- 辅助紫色：`#753991` —— 用于提交按钮、重要操作
- 深海军蓝：`#032147` —— 用于主标题
- 灰色文字：`#888888` —— 用于辅助文字、标签

---

## 编码规范

1. 使用当前最新版本的库，并采用截至今日的惯用写法。
2. 保持简洁——绝不过度设计，始终简化，不做任何不必要的防御性编程。不添加多余功能，专注于简洁性。
3. 行文精炼。README 保持最小化。重要：**禁止使用任何 emoji**。
4. 遇到问题时，务必先定位根本原因再尝试修复。不要猜测。用证据证明，然后修复根本原因。

---

## 工作文档

所有用于规划和执行本项目的文档将存放在 `docs/` 目录下。在继续之前，请先查阅 `docs/PLAN.md` 文档。
```

```md
# The Project Management MVP web app

## Business Requirements

This project is building a Project Management App. Key features:
- A user can sign in
- When signed in, the user sees a Kanban board representing their project
- The Kanban board has fixed columns that can be renamed
- The cards on the Kanban board can be moved with drag and drop, and edited
- There is an AI chat feature in a sidebar; the AI is able to create / edit / move one or more cards

## Limitations

For the MVP, there will only be a user sign in (hardcoded to 'user' and 'password') but the database will support multiple users for future.

For the MVP, there will only be 1 Kanban board per signed in user.

For the MVP, this will run locally (in a docker container)

## Technical Decisions

- NextJS frontend
- Python FastAPI backend, including serving the static NextJS site at /
- Everything packaged into a Docker container
- Use "uv" as the package manager for python in the Docker container
- Use OpenRouter for the AI calls. An OPENROUTER_API_KEY is in .env in the project root
- Use `openai/gpt-oss-120b` as the model
- Use SQLLite local database for the database, creating a new db if it doesn't exist
- Start and Stop server scripts for Mac, PC, Linux in scripts/

## Starting Point

A working MVP of the frontend has been built and is already in frontend. This is not yet designed for the Docker setup. It's a pure frontend-only demo.

## Color Scheme

- Accent Yellow: `#ecad0a` - accent lines, highlights
- Blue Primary: `#209dd7` - links, key sections
- Purple Secondary: `#753991` - submit buttons, important actions
- Dark Navy: `#032147` - main headings
- Gray Text: `#888888` - supporting text, labels

## Coding standards

1. Use latest versions of libraries and idiomatic approaches as of today
2. Keep it simple - NEVER over-engineer, ALWAYS simplify, NO unnecessary defensive programming. No extra features - focus on simplicity.
3. Be concise. Keep README minimal. IMPORTANT: no emojis ever
4. When hitting issues, always identify root cause before trying a fix. Do not guess. Prove with evidence, then fix the root cause.

## Working documentation

All documents for planning and executing this project will be in the docs/ directory.
Please review the docs/PLAN.md document before proceeding.
```

## /docs/PLAN.md

```md
# 项目高层次步骤

---

**第 1 部分：计划**

充实本文档，为每个部分详细制定计划，将子步骤列出为可勾选的清单（供智能体逐项完成），并为每项定义测试和成功标准。同时，在 `frontend` 目录内创建 `AGENTS.md` 文件，用于描述现有代码。确保用户审阅并批准该计划。

---

**第 2 部分：脚手架搭建**

设置 Docker 基础设施，在 `backend/` 中搭建 FastAPI 后端，并在 `scripts/` 目录中编写启动和停止脚本。该阶段应能提供静态 HTML 示例，以确认“hello world”示例可在本地运行，并能成功发起 API 调用。

---

**第 3 部分：集成前端**

现在更新项目，使前端被静态构建并提供服务，从而使应用在 `/` 路径下显示演示看板。需编写全面的单元测试和集成测试。

---

**第 4 部分：添加模拟用户登录体验**

现在更新应用，使得首次访问 `/` 时，必须使用硬编码的凭证（`user` / `password`）登录才能看到看板，并支持登出功能。需编写全面的测试。

---

**第 5 部分：数据库建模**

为看板提出数据库模式，以 JSON 格式保存。在 `docs/` 中记录数据库方案，并获取用户签字确认。

---

**第 6 部分：后端开发**

现在添加 API 路由，使后端能够读取和修改指定用户的看板数据；通过后端单元测试进行全面测试。数据库若不存在应自动创建。

---

**第 7 部分：前后端联调**

现在让前端真正使用后端 API，使应用成为具有持久化能力的看板。需进行非常全面的测试。

---

**第 8 部分：AI 连接**

现在允许后端通过 OpenRouter 进行 AI 调用。使用简单的“2+2”测试来验证连通性，确保 AI 调用正常工作。

---

**第 9 部分：扩展后端调用**

现在扩展后端调用，使其始终将看板的 JSON 数据、用户问题（以及对话历史）一并发送给 AI。AI 应通过结构化输出进行响应，其中包含对用户的回复，以及可选的看板更新操作。需进行全面测试。

---

**第 10 部分：UI 侧边栏聊天组件**

现在在 UI 中添加美观的侧边栏组件，支持完整的 AI 聊天功能，并允许 LLM（根据其判断）依据结构化输出来更新看板。如果 AI 更新了看板，UI 应自动刷新。
```


## /backend/AGENTS.md
```md
This file should be updated with a description of the Backend
```

## /scripts/AGENTS.md

```md
This folder will contain start and stop scripts for Mac, PC and Linux
```

# 原文
Here's the corrected version:

So yesterday, we used Cursor to build out our project. Today, we are going to use GitHub Copilot, or at least I am. You can use whichever one you want. Of course, you're probably a dab hand with Cursor now, so you can stick with that if you like it, depending on how far through your quota you are. But here I am in GitHub, and I've gone to my GitHub page. This is just my profile page. I'm going to go up here to my avatar menu and select Copilot settings to look into this.

Now I am on the Copilot Pro plan, and you might be on just a free plan that, you remember, just has a certain quota of requests that you could make. This has a larger quota, and I could pay more and have an even bigger quota. But as I am right now, I have used 1.4% of my premium requests, and it resets monthly. And yeah, so that's going to be what I can consume, and it tells me here that it's enabled to be used in GitHub.com, in a CLI, or in the IDE, which is where we have been using it and where we will again today. And there are also options here to select to turn on and off different models and so on. So it's worth looking at the different settings that you've got here. And most importantly, keep an eye on usage. This is where you can see what you're using according to your plan. So that's one thing I wanted to show you.

I also wanted to show you something that will be linked in the resources, which is the repo that we'll be using. It's a repo called PM. It's private for me right now; that'll be public by the time you see it, I hope.

And it's the usual story. I'm going to ask you to come to this green Code button and just usually select HTTPS and copy that to the clipboard. You don't need to do this because it will also be in the resources, but this address here, which is the address of this repo, that's what we will clone to get started with this project. And so here I am in VS Code.

Remember, similar to Cursor, this is the welcome screen. I'm going to bring up a terminal by pressing Control+backtick, and up it comes. There is a terminal also, of course, on the View menu—you know this now. And I'm going to go into my projects directory: cd projects. Look at where I am—pwd shows I'm in /Users/<yourname>/projects. Yours might look different, but that kind of idea.

And I'm now going to type git clone and then the link to that repo that's called PM.git, or you can just take this entire command from the course resources. That's what you do next. I'm not going to run that because I've already done it. I've already got a PM directory. Look, if I do cd PM, I'm in it right now, and that is where you will hopefully be able to go into as well. Now let's open that project. And so to open the new project, you can go to the File menu, New Window, or you can just press Open right here. And then just find your way to your projects folder and then find your way to PM, which should have been cloned, go into it, press Open. That's the way to do it.

You know you're in the right place because PM appears here. Do you trust the authors? Yes, I trust the authors. Yes, if you get that, then yes, you trust me, right? And now we are in PM. Here we are, we've got everything. So now you may have on your mind that you don't like the fact that I brought you into an existing repo.

You might be thinking I'd rather build everything from scratch. But I have a reason for this. Sometimes we will build everything from scratch, but sometimes we won't, and here's why. The thing is that building things from scratch is actually much easier with vibe coding, with agentic coding, because agents are great at building things from absolutely nothing. They're able to build all the scaffolding themselves. They're able to set up all the right files. They're able to take it through step by step.

It's actually harder when you're starting with something, and one of the hardest is when you're starting with a large legacy project, which we will cover at some point in our journey.

But for now, what I want to do is a little mix-up: there's going to be some code that we're inheriting as part of building this project, and then something new that we're going to be building. Most of it will be new, but there'll be something here already. And what is that thing already?

You're wondering what? You're going to love this. What we've got already is the Kanban front end that Codex made earlier this week—the really cool Kanban view. I'm going to pretend that it wasn't vibe coded, just that we've got that here, that it's been built by somebody else, by another team. That MVP is already being built as a front end. We know it was vibe coded, but we're just going to pretend we've inherited it. We've got that, and our mission is to turn this into an application with a proper front end, back end, database, API, with everything together. So this could actually be a project management tool that would be persistent.

That's what we're going to build. We're going to turn this into the real deal, and we're going to do it with LLMs. And actually, when I say we're going to do it with LLMs, I mean that in two ways. We're going to use, of course, the coding agent to write this, but also we're going to build a project management app that has an AI feature—because doesn't everything have an AI feature these days? It's going to have a "chat with my project" ability, where you'll be able to ask questions about the project setup. You'll also be able to ask for things to be done to the project through an AI chat interface. We're going to build an AI application as well.

Well, I hope you're excited. I certainly am.

Okay, so let me just take you through the file system here on the left. But I do also want to remind you a few things about actually using GitHub Copilot here on the right. If you've forgotten, we've got this agent drop-down here that lets us choose the mode we're working on. We've got the model selector where we can choose which models we want to have, with a Manage Models button that will bring up this whole section with all of the details of the models. This is also where you could choose if you want to be working with free models, to be using Ollama locally and running a model on your local machine if you've got a strong enough local machine and you can run it for free. And remember to look in the course resources for more on that. But if you're looking at using Copilot properly, then also down here, remember this thing here gives us a sense of how much room we've got. You can see that the premium requests—I've not used up many of them, and they reset quite soon for me. You can press this "Manage paid premium requests" button, and it will open you up in GitHub. And this is where you can set your budget limits and things like that, should you wish to go over the allotted free portion that you get automatically.

Okay? So let's just orient you over there on the right. Now I want to orient you on the files on the left. What is set up for you right away? Now, what I've got here is, first of all, a directory structure. Frontend is where we've got the existing Kanban MVP. Backend is empty except for an agents file that is empty—it says something will go here. Script is also empty except for something that says things will go here.

I've got the .env file, which is the same one as before. I just copied it across. You should be able to do that too, or if you want, you can create a new one. Remember, OpenRouter API key in block capitals—paste in the key if you're going to create one from scratch. No mistakes allowed, but you could also just copy it from where it is somewhere else. Be careful with that .env file. The .gitignore file is actually one of the defaults. It's got all the sorts of stuff you typically want to ignore, including the .env file that you never want to check into git. And then last but very much not least, I have prepared an AGENTS.md file to walk you through right now.

That is positioning us for success. That is where we begin, and you shouldn't feel like you have to use what I've got right here. I'm doing right-click and Open Preview to bring it up looking fancy. Don't feel like you have to do this; you can take this in whatever direction you want. I just wrote this right now. So this isn't like I spent ages preparing it, but rather than have you sit there while I type it, I just did it quickly, and this is what I've got.

I've tried to be reasonably scrappy because we want the agent to go with us on this. So we're building a project management MVP web app.

A user should be able to sign in. When they're signed in, they will see a Kanban board representing their project. It has fixed columns that can be renamed, the cards can be moved, there is an AI chat feature in the sidebar, and the AI is able to create, edit, or move one or more cards. Limitations for the MVP: there'll only be a single user sign-in, hard-coded user and password, but the database should support having many users for the future. There will only be one Kanban board per signed-in user; we're not going to have lots of different boards. And it's going to run locally in a Docker container for the MVP. To deploy it out somewhere—that's not to say you can't easily do that. Technical decisions:

So this part I have written based on my opinion, my technical opinion, and you would be absolutely fair to say, "Okay, but how are we to know what to put here?" And the answer is you don't need to know what to put there, because you could just ask an AI. You could just ask the agent—I was going to say the Cursor agent, or GitHub Copilot over here—to do it for you. Now I have a viewpoint. I want a Next.js front end because I want to show that to you and because that's what we've already got. I want the Python backend to be FastAPI. Python is what I do, so I know that stuff well. So I have opinions on this.

I want everything packaged into a Docker container. That makes most sense for us right now.

I want to use UV as the package manager. And honestly, these agents don't like to do that. They like to use simpler things, like the usual pip. I like UV because I love UV, and so I want to use it. You don't need to if you don't want to. I want to use OpenRouter for the AI calls because that makes most sense for us. I'm telling it the OpenRouter API key is in the .env file in the project root.

I'm telling it the model I'd like to use. You can use the free variant—you remember it's just got the word "free" after it, like `:free` I think, `:free`. If you want to use the free variant, which you'll find on the OpenRouter website, or you can use whatever model you want. And I'm saying I want to use a SQLite local database for the database. Of course, there are many other ways of doing it. If you, for example, have a Supabase account where you've got databases running on the cloud, you could instead just put your Supabase keys in the .env, and it will all work great. That works too, but we're going to keep it super simple for this.

And then starting point: a working MVP of the front end has been built and is already in the frontend directory. This is not yet designed for the Docker setup. It's a pure front-end-only demo.

See, currently it's a bit awkward, isn't it? When we call that "starting point," that seems more clear. Otherwise it's only going to get confusing when it sees that that's not the current state later on. So let's say "starting point," okay?

And then I've got the same color scheme as before, the coding standards. I think this is basically the same as before. I may have added the fourth one here: when hitting issues, always identify root cause, do not guess, prove with evidence, then fix the root cause. Or just standard stuff I like to use, but increasingly it's not needed.

And then finally, working documentation: all documents for planning and executing this project will be in the docs directory. Please review docs/plan.md document before proceeding. What is this document, you ask? Let's go look at that now.

