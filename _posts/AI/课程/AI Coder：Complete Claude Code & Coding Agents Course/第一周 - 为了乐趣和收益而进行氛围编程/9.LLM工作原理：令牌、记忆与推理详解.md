If you want to learn:  
中文：如果你想学习以下内容：

- What is a large language model (LLM) and how does it actually generate text?  
中文：- *什么是`大语言模型（LLM）`，它实际上是如何生成文本的？*

- What are tokens and why do LLMs use them instead of words?  
中文：- 什么是`令牌（tokens）`，为什么LLM使用它们而不是单词？

- How does ChatGPT remember your conversation if LLMs are stateless?  
中文：- *既然LLM是无状态的，ChatGPT如何记住你的对话？*

- What is LLM reasoning, and why does "thinking step by step" produce better outputs?  
中文：- *什么是`LLM推理`，为什么“逐步思考”能产生更好的输出？*

- What's the difference between an LLM like GPT and an AI application like ChatGPT?  
中文：- 像GPT这样的LLM与像ChatGPT这样的AI应用之间有什么区别？

- How does the token-by-token inference process work under the hood?  
中文：- 逐令牌推理过程在底层是如何工作的？

Then this lecture is for you!  
中文：那么这门讲座就是为你准备的！

In this foundational theory lecture, you'll build a clear mental model of how large language models work — from the ground up.  
中文：在这节基础理论讲座中，你将*从头开始构建一个关于大语言模型如何工作的清晰心智模型*。

You'll start by understanding what an LLM actually does: taking a sequence of tokens as input and outputting a probability distribution over every possible next token.  
中文：你将**首先理解LLM实际做了什么**：将一串令牌序列作为输入，并输出一个覆盖所有可能下一个令牌的概率分布。

You'll learn what tokens are, how they differ from words, and how LLMs generate output one token at a time through the inference process.  
中文：你将了解**什么是令牌**、它们与单词有何不同，以及*LLM如何通过推理过程一次生成一个令牌的输出*。

From there, you'll explore the critical distinction between an LLM itself (like GPT) and the AI applications built around it (like ChatGPT, Cursor, or Duolingo Max).  
中文：接下来，你将**探索LLM本身（如GPT）与围绕它构建的AI应用**（如ChatGPT、Cursor或Duolingo Max）之间的关键区别。

This distinction is essential for anyone working with agentic AI systems or building AI agent architectures, because the software layer wrapping the LLM is where much of the real functionality lives.  
中文：**这种区分对于任何从事代理AI系统或构建AI代理架构的人来说都至关重要，因为包裹LLM的软件层才是真正功能所在的地方**。

The lecture then breaks down key tricks that make LLMs appear intelligent.  
中文：讲座随后剖析了使LLM显得“智能”的关键技巧。

First, you'll understand the **illusion of memory** — how AI applications pass the entire conversation history into each LLM call to simulate a stateful context window, even though every call to the model is completely stateless.  
中文：首先，你将理解**记忆的幻觉**——*AI应用如何将整个对话历史传递给每次LLM调用，以模拟有状态的上下文窗口，尽管对模型的每次调用都是完全无状态的*。

Second, you'll dive into **LLM reasoning and thinking** — the discovery that prompting a model to output its chain of thought before answering dramatically improves accuracy on complex reasoning tasks.  
中文：其次，你将深入探讨**LLM推理与思考**——*研究发现，在回答前提示模型输出其思维链，能显著提高复杂推理任务的准确性*。

You'll see a concrete probability example where a standard prompt produces the wrong answer, but a reasoning-enabled approach solves it correctly.  
中文：你将看到*一个具体的概率示例*：标准提示会给出错误答案，而启用推理的方法则能正确解决。

This session lays the theoretical groundwork for the practical, hands-on work ahead — giving you the foundational understanding of LLM architecture, token prediction, and prompt design that powers everything from simple chatbots to sophisticated multi-agent AI systems.  
中文：本节为后续的实践动手工作奠定了理论基础——为你提供了*对LLM架构、令牌预测和提示设计的基础理解，这些知识驱动着从简单聊天机器人到复杂多代理AI系统的一切*。

---



## 大语言模型（LLM）是什么？
[01:00]

`大语言模型（LLM）`，本质上是**预测文本的工具，预测输入后应出现的词语，你提供一段输入文本，LLM会预测后续内容**。就像*增强版的自动补全*，实际运作方式是*一个庞大的模式匹配统计引擎*，**已接受大量的海量的训练数据，来自互联网及更广泛的数据，分析了所有数据，学习了数据中的规律，当给定输入文本时，能很好预测后续内容**

## Token

**输入（input）** 实际上是一系列的token（标记/词元），`Token` 是几个字母组成的块，通常是一个单词，但也可能是单词的一部分
LLM的 **输出（output）** 是**每个可能的下一个token的概率**。*例如*，如果输入是“2加2是？”，那么输出很可能是表示“4”的那个词元，但也存在其他词元的概率，比如“香蕉”，不过这种可能性应该非常低。“2加2等于香蕉”，这可不是一句常见的话。它在训练数据中出现的频率并不高，但这正是其用意所在。

输入是这些称为token的序列。输出是下一个token的概率。

## 大语言模型的工作原理（执行过程）

LLM运作的方式之一就是**一次生成一个词元（token）**：
- *比如*，如果输入是“what is the capital of France?”，那么它预测出的第一个词，也就是第一个词元，可能是“the”（中文语境下可理解为“是”）；
- 接着你把“what is the capital of France? The”传回去，下一个词元可能是“capital”；
- 然后是“what is the capital of France? The capital”，再然后是“what is the capital of France? The capital of France is”，最后是“Paris”。
所以 ***LLM 是一次生成一个词元，每次都会把到目前为止的所有词元重新传入，再预测下一个词元***，LLM就是以这种方式运行的，这个过程叫作`推理（inference）`。


## LLM 与AI应用区别
还有一种听起来很像但本质不同的东西，叫作`AI应用`或`AI产品`。这类产品利用LLM来实现商业目标，但它围绕对LLM的调用编写了额外的软件。区分的重点是：
- 一个非常著名的LLM叫`GPT`——*GPT就是一个LLM*，它是一个预测下一个词元的模型；
- 而有一个*AI应用*叫`ChatGPT`，它是*一套软件，封装了对GPT的调用，并提供了其他功能，比如记忆能力、联网搜索功能*等等，*这些功能都是围绕使用GPT这个LLM构建的*。ChatGPT就是一个典型的例子。我们昨天用过的Cursor Agent是另一个例子；如果你用Duolingo学语言，Duolingo Max套餐里有一个高级功能，可以让你和AI对话来学习，这也是一个；还有Atlassian Rovo，它是Atlassian产品套件中的生成式AI功能，诸如此类。

很多地方都会**把生成式AI打包进应用里**。我之所以特别提到这一点，以及众多其他例子，就是为了*区分软件产品（即使用LLM的应用）和LLM本身*。
你心里要记住：*ChatGPT这个产品和GPT这个LLM是不同的，GPT可以被调用来生成给定序列之后最可能的下一个词元，而这个GPT LLM就位于ChatGPT产品背后*。

自从2022年ChatGPT发布以来，我们都有些惊讶——自动补全，也就是预测文本，竟然能展现出这种明显的智能。它能做像Cursor Agent那样的事，也能像ChatGPT那样跟我们进行有见地的对话，这非常非常令人惊讶。而支撑这一切的有几个技巧，尤其是**四个关键技巧**，它们都写在了围绕这个“预测下一个词元”的调用而构建的软件里。现在让我快速带你过一遍这四个技巧。第一个技巧——又是……（此处原文未完整，但翻译应到此为止）


# 原文

好的，我会按照上一轮修复后的英文原文，逐段给出中文翻译。每段英文下方紧跟着对应译文，方便对照。

[00:00]
Well, hello, welcome back. You survived week one, day one, and you're back for week one, day two – thank you. It's a purple day. There's that purple stripe in the middle there, and that means that today is another theory day, our second theory day. And I've got two bits of bad news for you.

嗨，大家好，欢迎回来。你们挺过了第一周的第一天，现在又回来上第一周的第二天了——谢谢你们。今天是“紫色日”，中间有一条紫色的条纹，这意味着今天又是一个理论日，我们的第二个理论日。我有两个坏消息要告诉你们。

[00:18]
One of them is that today is going to be a lot about me talking. I like these sessions to be mostly practical stuff, doing things. Today is going to be a bit of theory and material, but I'll try to get through it quickly, and it paves the way for lots of practical stuff to come. The second bad thing is that today is going to be perhaps somewhat foundational – it's going to be some of the basics. Some of you probably already know this stuff, in which case, please bear with me as I steam through this. There'll still be some interesting bits I'll put in there for everybody, but it is going to be reasonably foundational.

第一个坏消息是，今天大部分时间会是我在讲。我喜欢让这些课程以实操为主，让大家动手做事。但今天会有点理论和材料的内容，不过我会尽量快速过完，而且*这些内容会为后面大量的实操铺路*。第二个坏消息是，今天的内容可能有点基础——会涉及一些基本概念。你们当中有些人可能已经知道这些了，如果是这样，请耐心听我快速讲完。我仍然会穿插一些对大家都有趣的点，但整体上还是比较基础的。

## 大语言模型（LLM）是什么？
[00:51]
If you are new to this, then listen up. There's some important stuff to cover today. Let's get into it, and I am just going to start by level-setting with the sort of basics, like what an LLM is. I imagine everybody knows, but let's just make sure we're on the same page.

如果你是新手，那就要仔细听了。今天有一些重要的内容要讲。我们开始吧，我首先会从基础层面统一一下认知，比如什么是LLM（大型语言模型）。我想大家都知道，但我们还是确保大家理解一致。

[01:06]
So, a large language model such as GPT is, of course, something which is designed to predict what text, what words should come after an input sequence of words. It says, "What will come next?" It's like autocomplete on steroids. And of course, we know that the way it actually works is it's basically this enormous pattern-matching statistical engine. It's been fed tons and tons of training data, example data from the Internet and far beyond. It's looked at all of this data, it's learned patterns in the data, and so it's got really good, when it's given some input text, at predicting what will come next. And of course, I use the word "words" there, but it's really what we call tokens.

那么，像GPT这样的`大型语言模型`，本质上就是***用来预测在给定输入文本序列之后，应该出现什么文本、什么单词的***。它会问：“接下来会是什么？”就像*加强版的自动补全*。当然，我们知道它*实际工作的方式*基本上是*一个巨大的模式匹配统计引擎*。**它被喂入了海量的训练数据，来自互联网**甚至更远地方的示例数据。***它查看了所有这些数据，学习了其中的模式，所以当给定一些输入文本时，它非常擅长预测接下来会出现什么***。当然，我这里用了 *“单词”* 这个词，但实际上我们称之为“`token`”（令牌/标记）。

## Token
[]
The input is actually a sequence of tokens, where a token is a kind of chunk of a few letters that often is a word, but is often also a part of a word. And so there's a sequence of these input tokens.

**输入**实际上是*一个token序列*，`token`可以理解为**由几个字母组成的一个块，它常常是一个单词，但也常常是一个单词的一部分**。所以输入就是*由这些token组成的一个序列*。


The output from an LLM is not necessarily the most likely next token – we often say it that way – but what it actually gives you is the probability of every possible next token. If the input is "2 plus 2 is", then the output has a very high probability that the token is "four", but there might also be a probability for other words, like "bananas". But presumably that would be a very low probability. "Two plus two is bananas" is not a very common thing to say; it won't have appeared very often in the training data. But that's the idea.

**LLM的输出**不一定是概率最高的下一个token——虽然我们经常这么说——但它**实际给出的是每一个可能的下一个token的概率**。*例如*，如果输入是 *“2 plus 2 is”*，那么输出中“four”这个token的概率会非常高，但也可能有其他词的概率，比如“bananas”。不过那个概率大概会非常低，因为“Two plus two is bananas”这种说法不常见，在训练数据中出现的次数很少。但思路就是这样。


The input is a sequence of these things called tokens, and the output is the probability of the next token.

**`输入`就是由这些称为token的东西组成的序列，而`输出`则是下一个token的概率分布。**


## 大语言模型的工作原理（执行过程）

That's what an LLM does. I imagine you're familiar with this, but of course, one of the ways that LLMs work is that they generate the output one token at a time. So if the input is something like "What is the capital of France?", the first token that it predicts to come next might be the word "the". Then you'd pass in "What is the capital of France the", and the next token might be "capital", and then "What is the capital of France the capital", and then "...of France is", and then "Paris". And so it generates things one token at a time. The whole input is passed back in with all the tokens so far, and then it predicts the next token. That is the way that LLMs are run. That process is called inference, and they're run that way to generate the output one token at a time. If you want to know more about that, I have a YouTube series that goes through this – a playlist that I will include in the course resources. I imagine you know this already, but it's good that we're on the same page.

这就是LLM做的事情。我想你们对此都很熟悉，但当然，LLM工作的一种方式是**它们一次生成一个token的输出：**
- *比如，输入是“What is the capital of France?”*，它预测的下一个第一个token可能是“the”；
- 然后你把 *“What is the capital of France? the”* 传入，下一个token可能是“capital”；
- 接着 *“What is the capital of France？ the capital”*，再接着“……of France is”，然后“Paris”。
所以***它一次生成一个token，每次都会把迄今为止所有的token重新传入，然后预测下一个token***。***这就是LLM运行的方式***，这个过程称为“`推理`”（inference），它们就是这样逐token生成输出的。

如果你想了解更多，我有一个讲解这个的YouTube系列播放列表，我会放在课程资源里。我想你们已经知道这些了，但确保大家理解一致还是好的。

## LLM 与AI应用 的区别

Okay, that's an LLM. And then, so we're on the same page, there is something different, but confusingly similar-sounding, called an `AI application` or an `AI product`. This is something that uses an LLM to achieve a business goal, but it's got software written around that call to an LLM. The distinction I'm trying to make here is that there's a very well-known LLM called GPT – I'm sure you're familiar with it. GPT is an LLM; it's a model that predicts next tokens. Then there is an AI application called ChatGPT, which is a piece of software that wraps around calling GPT and has other functionality – it has things like memory, the ability to search the web, and other stuff built around using the GPT LLM. ChatGPT is a classic example. The Cursor agent that we used yesterday is another example. If you use Duolingo to learn a new language, the Duolingo Max plan has a premium feature that lets you chat with an AI in order to learn a language – that's another one. You may be familiar with Atlassian Rovo, which is like Gen AI built into the Atlassian suite of products. There are so many different places where you will find Gen AI being packaged into an application. The reason I draw attention to this and all of the many others is just to draw that distinction between a software product – an application that uses an LLM – from the LLM itself. Keep in your mind, separate ChatGPT the product from GPT, the LLM which can be called to generate the most likely next token to follow a sequence. That GPT LLM sits behind the ChatGPT product.

好了，这就是LLM。接下来，为了保持大家理解一致，还有一个不同但名字听起来容易混淆的东西，叫做“`AI应用程序`”或“`AI产品`”。它是指**利用LLM来实现业务目标的东西**，但**它在调用LLM的周围编写了额外的软件**。

我在这里想区分的点是：有一个*非常著名的LLM叫做GPT*——我相信你们都知道。**GPT是一个LLM**，*它是一个预测下一个token的模型*。然后有一个**AI应用程序叫做ChatGPT，它是一个软件，封装了对GPT的调用，并附加了其他功能——比如记忆能力、搜索网络的能力，以及其他围绕使用GPT LLM构建的功能**。

ChatGPT是一个典型例子。我们昨天使用的*Cursor agent是另一个例子*。如果你用Duolingo学习新语言，*Duolingo Max计划有一个高级功能，让你可以和AI聊天来学习语言*——这也是一个例子。你可能还熟悉Atlassian Rovo，它类似于内置在Atlassian产品套件中的生成式AI。还有很多很多地方都能看到*生成式AI被封装进应用程序*。
我之所以提这些，就是为了**区分清楚：`软件产品`（即使用LLM的应用程序）和`LLM`本身是不同的**。请记住，要把ChatGPT这个产品和GPT这个LLM分开——**GPT是可以被调用来生成序列最可能的下一个token的LLM，而那个GPT LLM就位于ChatGPT产品的背后**。

## 智能背后的四个技巧

Ever since the release of ChatGPT in 2022, we've all been a bit surprised that autocomplete – basically predictive text – is able to show this apparent intelligence. It's able to do things like we saw the Cursor agent do, and it's able to be like ChatGPT – it's able to have an informed conversation with us. It's very, very surprising. There are a few tricks that underlie this, and in particular, there are four tricks that I want to highlight that are written in software that's built around this call to this thing that just predicts likely next tokens. Let me quickly go through four of these tricks with you now.

*自从2022年ChatGPT发布以来，我们都有些惊讶：自动补全——基本上就是预测性文本——竟然能展现出这种明显的智能*。它能做到像Cursor agent那样的事情，也能像ChatGPT那样与我们进行有见地的对话。**这非常非常令人惊讶。这背后有几个“技巧”**，具体来说，我想强调四个技巧，**它们都围绕这个“只预测下一个token”的调用所构建的软件中**。现在让我快速给大家介绍一下这四个技巧。

![[_posts/AI/课程/AI Coder：Complete Claude Code & Coding Agents Course/第一周 - 为了乐趣和收益而进行氛围编程/media/65c745fa43f141c31255500e81ea11cb_MD5.webp]]

### 记忆幻觉

The first trick, which is one again I imagine most of you are super clear on already, but let's get on the same page – I call it the illusion of memory. It's recognizing this very simple point, which is that every time you use an LLM like GPT from OpenAI, it is completely stateless. GPT is a model – a data science model – that takes an input sequence of tokens and predicts what will follow that input sequence. It's completely stateless, and every time you call it with an input sequence, it has no knowledge of previous calls. Every call is a separate call. So if you write some code that just uses the GPT model and you say "I'm Ed", and it replies "Hi Ed", and then you send another call to the LLM – you then use the same LLM to put in a new input sequence, which is "Who am I?" – it's going to respond with something like, "I don't know, I don't have that information." That's the most likely text to come after "Who am I?"

*第一个技巧*，我想大多数人都已经很清楚了，但我们还是统一一下——我称之为“`记忆幻觉`”。*它基于一个非常简单的点：* **每次你使用像OpenAI的GPT这样的LLM时，它完全是*无状态*的**。GPT是一个模型——一个数据科学模型——**它接受一个输入token序列，然后预测该输入序列后面会跟着什么。*它是完全无状态的，每次你用输入序列调用它时，它都不知道之前的调用。每一次调用都是独立的***。*例如*，如果你写一段只使用GPT模型的代码，你说“I'm Ed”，它回复“Hi Ed”，然后你再次调用同一个LLM，输入一个新的序列“Who am I?”，它会回复类似“I don't know, I don't have that information.”这样的内容。这正是在“Who am I?”之后最有可能出现的文本。


But wait, you say that that doesn't match my experience? I use ChatGPT and for sure it keeps the conversation thread going. If I say "Who am I?", it will say "You're Ed, you just told me." Okay, well, you probably know this, but that is because of this trick, this sneaky little trick, which is, of course, that every time you call an LLM, you don't just pass in something like "Who am I?" – you pass in the complete conversation so far. So what actually happens with ChatGPT when you're using the product ChatGPT is that you start by saying "Hi Ed" and it responds "Hello Ed". And then the next message that gets sent to GPT is the whole conversation so far: "I'm Ed", "Hi Ed", "Who am I?" – the next thing I've typed. And when that entire input sequence is sent in, of course, the LLM predicts the most likely next token is something like "You're Ed. You just told me you're Ed." And that gives this illusion that ChatGPT has a memory. It is, in fact, just a byproduct of every single time the LLM is called. The entire conversation so far is passed in. Most of you knew that already, and if you didn't, you're like, "Oh, I see!" And if you do something like my AI Engineering Core track, we actually make these calls and you see it happening just in case you don't believe me.

但等等，你们会说，*这和我体验的不一样啊？我用ChatGPT时它确实能保持对话连贯*。如果我说“Who am I?”，它会说“You're Ed, you just told me.”。好的，你们可能知道原因，但*这是因为这个狡猾的小技巧：* **每次调用LLM时，你不仅传入类似“Who am I?”这样简单的内容，而是传入到目前为止的完整对话**。所以当你在使用ChatGPT这个产品时，***实际发生的是：*** 
- 你先说“I'm Ed”，它回复“Hello Ed”。
- 然后*下一条发给GPT的消息是整个对话历史：* “I'm Ed”、“Hi Ed”、“Who am I?”——以及我接下来输入的内容。
- 当这个完整的输入序列被传入时，LLM预测的下一个最可能的token就是类似“You're Ed. You just told me you're Ed.”这样的内容。
***这就造成了ChatGPT有记忆的假象***。实际上，***这只是每次调用LLM时都传入完整对话历史所带来的副产品***。大多数人都已经知道这一点了，如果不知道，现在就会说“哦，原来如此！”如果你参加我的*AI Engineering Core课程，我们会实际进行这些调用，你会亲眼看到这个过程，以防你不信我*。

### 推理、思考
The second trick that, again, people are pretty familiar with at this point is this trick called reasoning or thinking. This began as this thing we discovered where, when you were putting in this input sequence to GPT, if you put at the end of the input sequence "please think step by step", you tended to get better outcomes, which was sort of surprising. That led over time to this different way of working with LLMs in which they are trained before they give you the answer to output the way in which they're going to come up with the answer. It's very strange, but it turns out that as a side effect of generating tokens to describe what it's going to do, you end up getting better outcomes. That sounds hokey – it sounds too good to be true, and yet it works.

*第二个技巧*，大家现在也比较熟悉了，叫做“`推理`”或“`思考`”。*这个技巧最初源于我们发现的一个现象：* 当你在输入序列的*末尾加上“请一步一步思考”时，往往会得到更好的结果*，这有点令人惊讶。随着时间的推移，**这演变成了一种与LLM交互的不同方式**——它们*在被训练时，会先输出自己打算如何得出答案，然后再给出最终答案*。这很奇怪，但事实证明，*作为生成描述自己计划要做什么的token的副作用，最终结果会更好*。这听起来有点玄乎，好得令人难以置信，但它确实有效。


Here's a classic example – again, I do this on my AI Engineering course. You could have the question: "Hey, you toss two coins. One of them is heads, what's the chance the other one is tails?" I show that when you run this with a model without it outputting tokens to give its reasoning, it will give an answer like a half, 50/50, which is what you might instinctively think would be the right answer. But in fact, it's one of those annoying trick questions – a clever little trick you could ask ChatGPT if you wanted to. But the real answer is two thirds. If you ask it to explain itself – if you want it to output how it will think about this first and then come up with the answer – then it tends to get the answer right. It says two thirds. It first says, "This is probably a trick question. I should think this one through." And it realizes, of course, you're in the situation where you're not saying which one of the coins is heads, and that means that in two thirds of the cases, the other one will be tails. Look it up – it's sneaky. But that is this trick called reasoning or thinking. It's resulted in much stronger models that are trained to generate tokens to think through what they're going to do before they do it.

这里有*一个经典例子*——同样，我在我的AI Engineering课程上也用过。*问题是*：“嗨，你抛两枚硬币。其中一枚是正面，另一枚是反面的概率是多少？”我演示过，*如果让模型在不输出推理过程的情况下直接回答*，它会给出像一半、50%这样的答案，这可能是你直觉上认为正确的答案。但实际上，这是一个烦人的陷阱题——如果你愿意，可以拿这个去考ChatGPT。*真正的答案是三分之二*。**如果你让它先解释自己——即先输出它将如何思考，然后再给出答案——那么它往往能答对**，它会说三分之二。它会先说：“这可能是个陷阱题，我应该仔细想想。”然后它会意识到，问题没有说明哪一枚是正面，这意味着在三分之二的情况下，另一枚会是反面。你看，很狡猾吧。但*这就是所谓的“推理”或“思考”技巧。**这个技巧催生了更强大的模型，它们被训练成在行动之前先输出思考过程**的token*。


