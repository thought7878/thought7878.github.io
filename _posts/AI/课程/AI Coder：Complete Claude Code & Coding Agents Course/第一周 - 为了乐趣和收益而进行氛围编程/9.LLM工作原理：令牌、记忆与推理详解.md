If you want to learn:  
中文：如果你想学习以下内容：

- What is a large language model (LLM) and how does it actually generate text?  
中文：- *什么是`大语言模型（LLM）`，它实际上是如何生成文本的？*

- What are tokens and why do LLMs use them instead of words?  
中文：- 什么是`令牌（tokens）`，为什么LLM使用它们而不是单词？

- How does ChatGPT remember your conversation if LLMs are stateless?  
中文：- *既然LLM是无状态的，ChatGPT如何记住你的对话？*

- What is LLM reasoning, and why does "thinking step by step" produce better outputs?  
中文：- *什么是`LLM推理`，为什么“逐步思考”能产生更好的输出？*

- What's the difference between an LLM like GPT and an AI application like ChatGPT?  
中文：- 像GPT这样的LLM与像ChatGPT这样的AI应用之间有什么区别？

- How does the token-by-token inference process work under the hood?  
中文：- 逐令牌推理过程在底层是如何工作的？

Then this lecture is for you!  
中文：那么这门讲座就是为你准备的！

In this foundational theory lecture, you'll build a clear mental model of how large language models work — from the ground up.  
中文：在这节基础理论讲座中，你将*从头开始构建一个关于大语言模型如何工作的清晰心智模型*。

You'll start by understanding what an LLM actually does: taking a sequence of tokens as input and outputting a probability distribution over every possible next token.  
中文：你将**首先理解LLM实际做了什么**：将一串令牌序列作为输入，并输出一个覆盖所有可能下一个令牌的概率分布。

You'll learn what tokens are, how they differ from words, and how LLMs generate output one token at a time through the inference process.  
中文：你将了解**什么是令牌**、它们与单词有何不同，以及*LLM如何通过推理过程一次生成一个令牌的输出*。

From there, you'll explore the critical distinction between an LLM itself (like GPT) and the AI applications built around it (like ChatGPT, Cursor, or Duolingo Max).  
中文：接下来，你将**探索LLM本身（如GPT）与围绕它构建的AI应用**（如ChatGPT、Cursor或Duolingo Max）之间的关键区别。

This distinction is essential for anyone working with agentic AI systems or building AI agent architectures, because the software layer wrapping the LLM is where much of the real functionality lives.  
中文：**这种区分对于任何从事代理AI系统或构建AI代理架构的人来说都至关重要，因为包裹LLM的软件层才是真正功能所在的地方**。

The lecture then breaks down key tricks that make LLMs appear intelligent.  
中文：讲座随后剖析了使LLM显得“智能”的关键技巧。

![[_posts/AI/课程/AI Coder：Complete Claude Code & Coding Agents Course/第一周 - 为了乐趣和收益而进行氛围编程/media/65c745fa43f141c31255500e81ea11cb_MD5.webp]]

First, you'll understand the **illusion of memory** — how AI applications pass the entire conversation history into each LLM call to simulate a stateful context window, even though every call to the model is completely stateless.  
中文：首先，你将理解**记忆的幻觉**——*AI应用如何将整个对话历史传递给每次LLM调用，以模拟有状态的上下文窗口，尽管对模型的每次调用都是完全无状态的*。

Second, you'll dive into **LLM reasoning and thinking** — the discovery that prompting a model to output its chain of thought before answering dramatically improves accuracy on complex reasoning tasks.  
中文：其次，你将深入探讨**LLM推理与思考**——*研究发现，在回答前提示模型输出其思维链，能显著提高复杂推理任务的准确性*。

You'll see a concrete probability example where a standard prompt produces the wrong answer, but a reasoning-enabled approach solves it correctly.  
中文：你将看到*一个具体的概率示例*：标准提示会给出错误答案，而启用推理的方法则能正确解决。

This session lays the theoretical groundwork for the practical, hands-on work ahead — giving you the foundational understanding of LLM architecture, token prediction, and prompt design that powers everything from simple chatbots to sophisticated multi-agent AI systems.  
中文：本节为后续的实践动手工作奠定了理论基础——为你提供了*对LLM架构、令牌预测和提示设计的基础理解，这些知识驱动着从简单聊天机器人到复杂多代理AI系统的一切*。

---



## 大语言模型（LLM）是什么？
[01:00]

`大语言模型（LLM）`，本质上是**预测文本的工具，预测输入后应出现的词语，你提供一段输入文本，LLM会预测后续内容**。就像*增强版的自动补全*，实际运作方式是*一个庞大的模式匹配统计引擎*，**已接受大量的海量的训练数据，来自互联网及更广泛的数据，分析了所有数据，学习了数据中的规律，当给定输入文本时，能很好预测后续内容**

## Token

**输入（input）** 实际上是一系列的token（标记/词元），`Token` 是几个字母组成的块，通常是一个单词，但也可能是单词的一部分
LLM的 **输出（output）** 是**每个可能的下一个token的概率**。*例如*，如果输入是“2加2是？”，那么输出很可能是表示“4”的那个词元，但也存在其他词元的概率，比如“香蕉”，不过这种可能性应该非常低。“2加2等于香蕉”，这可不是一句常见的话。它在训练数据中出现的频率并不高，但这正是其用意所在。

输入是这些称为token的序列。输出是下一个token的概率。

# 原文

Well, hello, welcome back. You survived week one, day one, and you're back for week 1. Day 2, thank you. It's a purple day. There's that purple stripe in the middle there, and that means that today is another theory day, our second theory day, and I've got 2 b of bad news for you. 

One of them is that today is going to be a lot about me talking. I like these sessions to be mostly practical stuff, doing things. And today is gon na be a bit of like theory and material, but I'll try and get through it quickly and it paves way for lots of practical stuff to come. And the second bad thing is that today is going to be perhaps somewhat, it's going to be foundational. It's going to be some of the basics. And some of you probably already know this stuff, in which case, please put me on two steam through this. There'll still be some interesting bits I'll put in there for everybody, but it is going to be reasonably foundational. 

If you are new to this, then listen up. There's some important stuff to cover today. Let's get into it and I am just gon na start by level setting with the sort of basics like what what is an LlM? I imagine everybody knows, but let's just make sure we're on the on the same page. 

So a large language model such as GPT, of course it is something which is designed to predict what text, what words should come after an input, but sequence of words. And it says what will come next? It's like autocomplete, steroids. And of course, we know that the way it actually works is it's basically this enormous pattern matching statistical engine. It's been fed tons and tons of training data, example data from the Internet and far beyond. It's looked at all of this data. It's learned patterns in the data. And so it's got really good when it's given some input text at predicting what will come next. And of course, I use the word words there, but it's really what we call tokens. 
因此，像GPT这样的`大型语言模型`，其设计目的当然是预测在给定输入之后应当出现哪些文本、哪些词语，也就是对词序列的预测。它告诉我们接下来会发生什么。这就像是带了类固醇的自动补全。当然，我们也知道，它的实际运作方式本质上就是一个庞大的模式匹配统计引擎。它已经接受了海量的训练数据，这些示例数据来自互联网乃至更广阔的领域。它已经查看了所有这些数据。它是数据中的学习到的模式。因此，当输入一段文本时，它在预测接下来的内容方面表现得非常出色。当然，我这里用了“词”这个词，但实际上我们称之为“token”。

The input is actually a sequence of tokens where a token is a kind of chunk of a few letters that often is a word, but is often also a part of a word. And so there's a sequence of these input tokens. 
输入实际上是一个由多个token组成的序列，其中每个`token`（标记/词元）是由几个字母构成的片段，它通常是一个词，但也常常只是某个词的一部分。因此，存在一串这样的输入token。

And the output from an LLMs not necessarily the most likely next token. We often say it that way, but what it actually gives you is the probability of every possible next token. If the input is 2 plus 2 is then the output, there's a very high probability that the output is the token for four, but there might also be a probability of other words, like the word bananas, But presumably that would be a very low probability. Two plus 2 is bananas is not a very common thing to say. It won't have appeared very often in the training data, but that's the idea. 
而LLMs的`输出`不一定是最有可能的下一个令牌。我们常常这么表述，但实际上它给出的是每个可能的下一个token的概率。如果输入是“2加2是”，那么输出很可能是表示“四”的那个词元，但也存在其他词元的概率，比如“香蕉”，不过这种可能性应该非常低。二加二等于香蕉，这可不是一句常见的话。它在训练数据中出现的频率可能并不高，但这正是其用意所在。

The input is a sequence of these things called tokens. And the output is the probability of the next token. 
输入是这些词的序列，称为token。而输出是下一个token的概率。

That's what an llum does, and I imagine you're familiar with this, but of course, one of the ways that llds work is that they generate the output 1 token at a time. So if the input is something like what is the capital of France, then the first word, the first token that it predicts to come next might be the word the, and then you'd pass in what is the capital of France V, and the next token might be capital, and then what is the capital of France the capital, and then what is the capital of France? The capital of France is, and then Paris. And so it generates things one token at a time, and the whole input is passed back in with all the token so far, and then it predicts the next token, and that is the way that llds are run, that process is called inference, and they're run that way to generate the output 1 token at a time. And if you want to know more about that, I have a sort of YouTube series that goes through this, a playlist that I will include in the course resources, I imagine you know this already, but it's good that we're on the same page, okay, that's an llum, and then also, so we're on the same page, there is something different, but confusingly sounding similar, called in like an AI application or an AI product, and this is something that uses an LLD to achieve a business goal, but it's got software written around that call to an LLD and the distinction I'm trying to make here is that something like a, there's an llum that's very well known that's called GPT that I'm sure you're familiar with GPT is an llum, it's something which is a model that predicts next tokens and then there is an AI application that's called ChatGPT, and that is a piece of software that wraps around calling GPT and it has other functionality, it has things like a memory and the ability to search the web and other stuff built around using the GPT LLD and ChatGPT then is a classic example the cursor agent that we used yesterday is another example if you use Duo lingo to learn a new language, the Duolingo max plan has a premium feature that letsgo chat with an AI in order to learn a language that's another one you may be familiar with that Atlassian rovo which is like Gen AI built in to the Atlassian suite of products, there's so many different places where you will find Gen AI being packaged into an application and the reason that I draw attention to this and all of the many others is just to draw that distinction between a software product, an application that uses an llum from the LLD itself keep in your mind, separate ChatGPT the product from GPT, the LLD which can be called to generate the most likely next token to follow a sequence and that GPT llum sits behind the ChatGPT product and ever since the release of ChatGPT in 2022, we've all been a bit surprised that autocomplete basically predictive text on is able to show this apparent intelligence it's able to do things like we saw the cursor agent do and it's able to be like like ChatGPT it's ISS able to have an informed conversation with us, it's very, very surprising and there are a few tricks that underlie this and in particular there are four tricks that I want to highlight are that are written in software that's sort of built around this call to this thing that just predicts likely next tokens and let me quickly go through four of these tricks with you now and the first trick, which is one again. 

I imagine most of you are super clear on already, but let's get on the same page, I call it the illusion of memory. And it's recognizing this very simple point, which is that every time that you use an LLD like GPT from OpenAI, it is completely stateless. 

GPT is a model, a data science model that takes an input sequence of tokens and it predicts what will follow that input sequence and it's completely stateless and that every time you call it with an input sequence, it's got no knowledge of previous call. Every call is a separate call. And so if you write some code that just uses the GPT model and you say I'm Ed, and it replies hi ed, and then you send another call to the llum, you then use the same LLD to put in a new input sequence, which is who am I, it's going to respond with something like, I don't know, I don't have that information. That's the most likely text to come after, who am I, but wait, you say that that doesn't match my experience? I use ChatGPT and for sure sure it keeps the conversation thread going, if I say who am I, it will say you're Ed, You just told me, okay, well, you probably know this, but, but that is because of this trick, this sneaky little trick, which is, of course, that every time you call an LLD, you don't just pass in something like, who am I, you pass in the complete conversation so far. So what actually happens with ChatGPT when you're using the product ChatGPT is that you start by saying hi Ed and it responds hello Ed. And then the next message that gets sent to GPT is the whole conversation so far. 

I'm Ed, hi Ed, who am I the next thing I've typed? And when that entire input sequence is sent in, of course, the llen predicts the most likely next token is something like you're Ed. You just told me you're Ed. And that gives this illusion that ChatGPT has a memory. It is, in fact, just a byproduct of every single time the llum is called. The entire conversation so far is passed in, most of you knew that already, and if you didn't, you're like, oh, I see, and if you do something like my AI engineering core track, we actually make these calls and you see it happening just in case you believe me. 

And the second trick that, again, people are pretty familiar with at this point is this trick called reasoning or thinking. This began out as this, this thing we discovered where when you were putting in this input sequence to GPT, if you put at the end of the input sequence, please think step by step, you tended to get better outcomes, which was sort of surprising. And that led over time to this different way of working with llds in which they were trained before they give you the answer to output the way in which they're going to come up with the answer. And it's very strange. But it turns out that as a side effect of generating tokens to describe what it's going to do, you end up getting better outcomes. And that sounds, it just sounds hokey. It sounds too good to be true, and yet it works. 

And here's a sort of classic example. Again, I do this on my AI engineering course. You could have the question, hey, you toss two coins. One of them is, what's the chances? The other one is tails? And I show that when you run this with a model without it outputting tokens to give its reasoning, it will give an answer like a half, 50, 50, which is what you might instinctively think would be the right answer. But in fact, it's one of those annoying trick questions, which is a clever little trick you could ask ChatGPT if you wanted to tell you why. 

But the real answer is two thirds. And if you ask it to explain itself, you if you want it to output, how it will think about this first and then come up with the answer, then it tends to get the answer right. It says two thirds. 

And it first says this is probably a trick question. I should think this one through. And it realizes, of course, you're in the situation where you're not saying which one of the coins is heads, and that means that in two thirds of the cases, the other one will be tails. Look it up at sneaky. But that is this trick called reasoning or thinking. And it's resulted in much stronger models that are trained to generate tokens to think through what they're going to do before they do it.