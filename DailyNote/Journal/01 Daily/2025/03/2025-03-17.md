---
created: 2025-03-18 15:49
updated: 2025-03-18 15:49
aliases: 
tags:
  - "#daily-note"
log-sleep-hours: 
log-sleep-rating: 
log-healthy-food-rating: 
log-day-rating: 
journal: Daily
journal-date: 2025-03-17
---
# 习惯
- 睡眠时间:: 
- 睡眠分数:: 
- 运动时间:: 
- 睡前冥想:: 


# 任务


## 开始的


## 已完成的
```dataviewjs
const today = new Date("2025-03-18"); // 从 Templater 获取当前日期
//const today = new Date(`= date(now)`); 
const tString = "2025-03-18"
// 查询逻辑
dv.taskList(
    dv.pages().file.tasks
        .where(t => {
            // 过滤没有inline field元数据的任务
            const hasTaskDateField = t.completion;
            if(!hasTaskDateField) return false;
    console.log("t.completion:",t.completion)
    console.log("today:",today)
    console.log("tString:",tString)
            // 今天完成的
            const isTodayCompleted = t.completion?.toDateString() === today?.toDateString();
            
            return t.completed;
        })
)
```


## 未完成的


```dataview
TASK
WHERE start.toDateString() = date(2025-03-17).toDateString() AND !completed
```
(start = date(2025-03-17)) OR (due = date(2025-03-17)) OR (scheduled = date(2025-03-17))

---

```dataviewjs
const today = new Date("2025-03-18"); // 从 Templater 获取当前日期

// 查询逻辑
dv.taskList(
    dv.pages().file.tasks
        .where(t => {
	        // 过滤没有inline field元数据的任务（支持 due:: 或 scheduled::）
            const hasTaskDateField = t.start;
            //t.created || t.start || t.completion || t.due || t.scheduled;
            if(!hasTaskDateField) return false;
            
            //return isIncomplete && taskDateField.toDateString() === today.toDateString();
            return !t.completed;
        })
)
```

## 全部的
```dataviewjs
const today = new Date("2025-03-18"); // 从 Templater 获取当前日期

// 查询逻辑
dv.taskList(
    dv.pages().file.tasks
        .where(t => {
	        // 过滤没有inline field元数据的任务（支持 due:: 或 scheduled::）
            const hasTaskDateField = t.start;
            //t.created || t.start || t.completion || t.due || t.scheduled;
            if(!hasTaskDateField) return false;
            
            //return isIncomplete && taskDateField.toDateString() === today.toDateString();
            return !t.completed;
        })
)
```

# 今日新建的笔记
```dataview
Table file.ctime as "创建日期"
Where date(today) - file.ctime <= dur(0 day)
Sort file.ctime desc
```



# 闪念



# 复盘





